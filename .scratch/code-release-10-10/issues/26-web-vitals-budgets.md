# 26 — Fix the six breached Web Vitals budgets on a production build

**What to build:** A production-mode capture currently breaches six of its own budgets: mobile INP, FCP and TTFB, and desktop LCP, FCP and TTFB. These are the project's own budgets measured the project's own way, so they are real. Close them and re-measure.

**Blocked by:** 25 — route composition drives most of these numbers, so measuring before the thinning lands wastes the run. (25 is done; this is no longer a blocker.)

**Status:** 1 of 7 closed — blocked on infrastructure (no authorized session is obtainable on this machine) and on ticket 28 / the backend for the TTFB that causes four of the six breaches. Report: `reports/26-web-vitals.md`.

- [ ] LCP, INP and CLS meet their targets on production builds for in-scope authenticated routes at the defined reference viewport and device profile.
    BLOCKED: infrastructure. `streamlineos-backend/.env` has no `NEXTAUTH_SECRET`, so `/auth/session-exchange` returns 503 and no local frontend can obtain a backend JWT; with the secret supplied the only available identity is not an active account in the shared Neon database. Every authenticated route therefore paints its access-failure state, and `node scripts/measure-web-vitals.mjs --repeat=12` (exit 1) refuses all 72 samples as evidence. Measured on that floor: CLS 0.000 both profiles (met), desktop LCP p75 1685 ms > 1500 (breach), INP not measured — no interactive control existed to click.
- [ ] The six currently breached metrics are each brought inside budget, or an exception is recorded with a named owner and a concrete reason.
    PARTIAL: all four still-breaching metrics have an owner and a reason recorded in `contracts/route-bundle-manifest.json` `budgetExceptions`, printed by `check:web-vitals-budget` beside each failure; the unmeasured INP entry records that it claims nothing. None is brought inside budget: mobile+desktop TTFB p95 (1804/1761 ms) and the FCP/LCP that follow from them are one backend round trip — `GET /auth/session-data/:userId` measures 588-757 ms and `lib/auth-session.ts:126` issues it twice on a non-2xx, while the same server returns the public landing in 9 ms. Owner: `streamlineos-backend` + ticket 28. Annotation is annotation only — the gate still exits 1 and its self-test asserts an exception never removes a failure.
- [x] The capture is a real production build at the stated repetition count, not a development-mode run.
    `npx next build` exit 0, build id `5KxS0uW9Wrm0BIVYZicTs`, 601 routes; `serverMode` derived as "production" from `.next/BUILD_ID`; 12 repetitions per route per profile (raised from 5). The wall was reproduced first (`next build` exit 1) and is one line: `lib/env.ts:40` throws because `.env` carries a 36-character `NEXTAUTH_SECRET` and the schema requires 44 in production — it is not `/billing/ai-credits`, which does not exist.
- [x] Localhost TTFB variance is acknowledged in the evidence rather than used to dismiss the breach.
    Recorded in the report and in the capture's `serverTtfb` block: loopback removes real network time so these figures understate production; the mobile profile's 150 ms emulated RTT accounts for ~8% of a 1.8 s number, not for the breach; the unthrottled server-side figure (p95 1728 ms, 12 runs, all HTTP 200) is recorded beside the browser figure; host load (3.76 -> 8.06 of 15 CPUs) is stamped into the capture.
- [ ] Navigation, skeleton, optimistic or queued feedback appears within the perceived-responsiveness target of user intent; no action looks unresponsive while work runs.
    PARTIAL: the measurement landed — `measureIntentToFeedback` clicks an in-app nav link and times it to the first DOM mutation on the same clock, recorded as `perceivedResponsiveness` with `target_ms: 100`. It ran on all 6 route/profile pairs and honestly returned "not measured": the access-failure shell renders no in-app navigation to click. Blocked on the same infrastructure as box 1.
- [ ] Route-level JavaScript, CSS, server payload, image/font and third-party budgets are recorded and met.
    PARTIAL: recorded, not met. Coverage 6 -> 12 routes. CSS (58,189 B), fonts (55,206 B), images, third-party (0) and server payload (10-13 kB) are all inside ceiling. JS is not: `check:route-bundle-budget` exits 1 with 12 breaches — 7 on the new `measuredScriptBytes` (over-the-wire, cold cache) and 3 on `measuredFirstLoadJsBytes` (/chat +101 kB, /build/my-work +68 kB, /crm/leads +60 kB) plus /chat's page chunk. Note the gate was ALREADY red on arrival (exit 1, 5 `measuredImageBytes` breaches against a declared ceiling of 0 that measurement contradicts); that is fixed. The JS budget previously passed on a number that is not what users download — /dashboard governs 440,065 B but downloads 640,260 B — so `measuredScriptBytes`/`measuredTotalBytes` were added to the manifest, the gate and both self-tests.
- [x] Public landing visuals and animations remain unchanged; if a frozen landing animation prevents an agreed target, that is escalated rather than worked around.
    Nothing under `app/(public)/**`, `features/marketing/**` or any landing animation was touched. The only public-facing change is a `Cache-Control` header on `public/` assets, which alters no pixel. The landing renders in 9 ms TTFB and is not on the authenticated critical path, so no frozen animation prevents any target. ESCALATED rather than worked around: the landing's animations pin framer-motion (224 kB raw / 71 kB gzip, 278 importers) into every authenticated first load; dropping it is a product decision about the landing, not a refactor.
