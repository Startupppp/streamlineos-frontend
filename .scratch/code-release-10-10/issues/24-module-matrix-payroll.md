# 24 — Payroll module release matrix

**What to build:** The §10 inventory-and-verdict pass for Payroll plus its §10.7 criteria: normalized and immutable financial records, bounded indexed reads, and UI that survives conflict and partial failure.

**Blocked by:** 20.

**Status:** 7 of 8 closed — box 2 CLOSED by the follow-up pass (N+1 batched, filings export moved to a durable job); box 1 remains PARTIAL, blocked on ticket 08's migrations. Report at `reports/24-payroll.md`.

- [ ] Payroll runs, components, assignments, calculations, payslips, taxes, deductions and payment/reconciliation history are normalized, tenant-safe and immutable where financial.
  PARTIAL: normalized and tenant-safe hold (0 payroll failures in `check:tenant-indexes`). Fixed three missing immutability guards (published payslips could be silently overwritten by a re-publish; acknowledged filings could be re-acknowledged; a PAID bank item could be flipped to FAILED) and three writes missing an `orgId` predicate. Still open: `payroll_journal_batches`/`_lines`, `payroll_bank_batches`/`_items`, `payroll_filings` and `payroll_tds_ytd_ledger` have no DB-level immutability trigger — only `payroll_run_employees` and `payroll_line_items` do (migration 0445). Closing it needs a hand-authored migration, which is a schema change outside a verdict pass.
- [x] Run and item reads are bounded with indexed employee/period/status paths and no N+1 calculation; exports are asynchronous.
  Follow-up pass closed both remaining defects. `runs/inputs.service.ts` reimport no longer issues ~4,000 serial round-trips: the per-payee `pullAttendanceInputs` became `pullAttendanceInputsByUser`, which chunks at `PAYROLL_INPUT_PULL_CHUNK = 200`, and the per-row upsert loop became one multi-row `INSERT ... ON CONFLICT DO UPDATE`. Measured, not assumed: `reimport-inputs-call-count.spec.ts` spies on the db handle and asserts 5 calls at 50 payees vs 14 at 1,000 — the whole growth is the four extra chunks. `POST /payroll/filings/export` now returns 202 with a durable `FILING_EXPORT` payroll-job handle and `GET /payroll/filings/export/jobs/:jobId` reports it; the CSV is built by the existing payroll jobs worker, which now also receives `runId`/`month` (it was dropping them, so a queued export resolved no run). No LIMIT was added anywhere — nothing truncates. The three index gaps (`(orgId, runId, status)` on run employees, `(orgId, entityId, month, id)` on runs, `(orgId, userId, status)` on payslip publications) are migrations and belong to ticket 08.
- [x] Caches invalidate correctly after lock, publish and reversal.
- [x] Payroll commits an idempotent Accounting-posting intent through the transactional outbox; Accounting consumes it asynchronously and idempotently. A brief pending state is acceptable; a lost or dangling journal is not.
- [x] Run-state UI covers conflict, retry and partial failure; downloads are permission-checked at request time, not only at list time.
- [x] Calculation, locking and reconciliation are covered end to end.
- [x] Payroll administration stays under its own routes; employee pay remains self-service and is not duplicated into Settings.
- [x] The module's files are inventoried and classified KEEP/REFACTOR/REMOVE with the concrete failure named for each non-KEEP verdict.

**Proof (follow-up pass, 2026-09-02):** backend `jest --runInBand --testPathPattern=payroll` exit 0, **124 suites / 955 tests** (was 121/931). Backend `typecheck` exit 0, **0 errors**. `check:spec-typecheck` exit 0 (it caught a constructor-arity break in `filings-list-pagination.spec.ts` that ts-jest could not). `check:unbounded-reads`, `check:idempotent-commands`, `check:outbox-consumers`, `check:module-gate`, `check:cache-invalidation`, `check:tenant-indexes`, `check:file-sizes` (payroll), `check:module-di`, `check:cycles`, `check:route-classification`, `check:contract-breaking-change`, `check:route-duplicates` all exit 0. `check:db-call-count` exits 1 on other territories; **`/payroll/runs/inputs.service.ts` left its REGRESSED list** (8 regressions at baseline, 7 after) and payroll ACTIONABLE is 0. Frontend `type-check` exit 0; `check:query-scope`, `check:command-catalog`, `check:effect-fetches`, `check:colors`, `check:client-pages`, `check:file-sizes` (payroll), `check:contract-drift`, `check:cycles`, `check:route-access-contract` exit 0. `next build` and the seeded DB e2e suite were **NOT run**.
