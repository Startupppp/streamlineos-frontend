# 40 — Reconcile every PRD checkbox against current source

**What to build:** The PRD's checkboxes and its blocker list have drifted from the tree — at the time these tickets were written, nine listed blockers were already closed and two "proven" claims were false. Reconcile every item against current source, so the final gate is measured against reality.

**Blocked by:** All tickets 01–39.

**Status:** done — 7 of 7 boxes closed. Report: `reports/40-prd-reconciliation.md`. Every gate in both repositories (75 backend + 26 frontend) was executed this pass, plus both typechecks and frontend lint. **One NEW P1 found and routed, not fixed: the frontend does not typecheck in the working tree** (`hooks/api/meetings-ai.ts` is uncommitted and inconsistent with its two consumers), which blocks ticket 41 by construction.

- [x] Every checkbox is classified VERIFIED DONE, REGRESSED, STILL PENDING or NEW against current source, not against the previous reconciliation.
      All **171 immediate** criteria classified: **73 VERIFIED DONE · 14 REGRESSED · 77 STILL PENDING · 6 NOT-VERIFIED · 1 DEFERRED-OPERATOR**. The **34 deferred** production-readiness criteria are untouched and stay deferred. **10 NEW findings** recorded (N1–N10) with evidence; none fixed, all outside this territory. Per-section table and per-item evidence: `reports/40-prd-reconciliation.md` §10. The method is stated in that section, including that PRD boxes and ticket boxes are **not 1:1** — five PRD boxes have no owning ticket box and four ticket boxes have no PRD counterpart.
- [x] A verified fix appears once under DONE and is not reintroduced as pending without current regression evidence.
      Four blockers struck through in the PRD with the measurement that resolved them: cross-tenant isolation declaration coverage (**929/929, exit 0**, was "fails 921/923"), the dead barrel `features/build/inbox/index.ts` (**gone**), the 58 thick route modules (**IN SCOPE thick 0**), and the `kb-rag.service.ts` over-300 regression (**253 lines**). Each of the 14 REGRESSED items carries a gate exit code and number measured **this pass**, not an inherited verdict.
- [x] Each DONE item names its evidence: the command run, the commit, and the number produced.
      Convention stated in the report §8: every DONE item names the **command** and the **number** it produced. The **commit** is the measured head — frontend `feaad0402`, backend `47a68ba2` — stated once and applying to every gate-derived item; specific SHAs are named where the history is itself the evidence (`10c06d01c` and `731d688ab` for the module table's provenance, `e5fed52a8` for the §10.18 flip, `06ed7a642` / `HEAD` for the typecheck attribution).
- [x] Claims are verified against artifacts rather than reports. A delegated fix can land inert, and an agent report can narrate a tick that was never written — count what is on disk.
      101 gates executed, both typechecks, frontend lint, plus direct source and catalog inspection. This is what the box was for: it found a **ticked PRD box whose own gate is red** (`check:routes` on `app/api/media/image/route.ts`, whose evidence line asserts that file does not exist), a **report claim that does not hold** (`check:lifecycle-predicates` recorded as "fixed, rc=0" measures **76/75 exit 1**, and the file it blames appears nowhere in the gate's own list), and a **release census wrong in three ways** (see the next box). It also cleared a recorded release blocker: the journal names **0** missing `.sql` files, not six.
- [x] Gates that were not run are recorded as not run, never as passing. Lint and end-to-end runs are only claimed if explicitly requested and actually executed.
      Lint **was** executed and is reported as **exit 1** (972 problems: 15 errors, 957 warnings), with the characterisation that none of the 15 is in in-scope production code. `next build`, both Jest suites and disposable-database E2E are recorded **NOT RUN**. Five backend gates returned exit 2 for missing infrastructure and are recorded **NOT-VERIFIED**, never as passing; `check:set-null-column-lists` self-reports **INCONCLUSIVE** with 274 constraints unverified and is recorded that way. A clean-`HEAD` frontend typecheck is recorded **not run**, because proving it needs a worktree and `git worktree` is outside this ticket's permitted git verbs.
- [x] The module checklist table and the completion counts are recomputed, not carried forward.
      Table recomputed from disk and **its provenance found**: it was added by `731d688ab` and its rows are a verbatim count of §10 at the pre-collapse commit `10c06d01c`, so every *Proven* number measures a former head. **Nine of eighteen rows — Home, Directory/Me, HRMS, Build/PM, Workflows, Billing/payments, Accounting/finance, Chat, Shared adapters, 60 carried "Proven" boxes — have no §10.x text, no ticket and no report, and are now marked NOT-VERIFIED in the PRD.** Six rows corrected; Payroll is marked **CONTESTED** rather than 100% because `payroll_bank_batch_items` has no UNIQUE in any migration. Counts recomputed: **229 of 290 acceptance boxes (79.0%), 22 of 42 tickets** — the circulated "235 of 293, 23 of 42" counted 6 findings-log checkboxes as acceptance criteria, omitted 3 `- [~]` partial boxes, and therefore also miscounted ticket 28 as fully closed.
- [x] Superseded evidence is marked as covering a former head so it cannot be cited as current.
      Marked in the PRD: the whole 2026-09-02 "Current verification snapshot" (with the specific corrections inline), the five pre-amendment/additive completion bullets, and the ticked §4 migration-baseline evidence (`applied=633`) which covers a 633/634-entry chain against a journal now at **666**. Also recorded in the report §6: the `s02-*` documents (634-entry), the `bootstrap-head-637/` bundle, `scratch_perf_seed_stale_20260902` (**retained, do not drop** — report 22c cites it), and every PRD verification-snapshot count.
