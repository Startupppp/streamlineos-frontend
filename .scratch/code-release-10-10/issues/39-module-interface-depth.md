# 39 — Module interface depth and Home composition boundary

**What to build:** Domain modules expose small stable interfaces and keep implementation local; shallow pass-through layers that add no behaviour are removed. Home composes universal experiences without absorbing the modules it surfaces.

**Blocked by:** 36.

**Status:** done — 8/8 closed. Report: `reports/39-module-interface-depth.md`.

- [x] Pass-through wrappers that add no policy, adaptation or depth are deleted; modules that provide real depth are retained.
      6 shells deleted: backend `build/core/webhook-url-guard.ts` (only consumer was its own spec), `build/execution/iterations.service.ts`, `hr/settings-hub/hr-settings-hub.constants.ts`; frontend `types/{crm,hr,projects}.ts` (directory-shadowing — deletion changes not even the import path; 284 importers untouched, proven by `type-check`). KEEP with reasons: `gdpr-export-worker.service.ts` (its path is a string in `common/slo/slo-queues.ts`, asserted by `slo-catalogue.spec.ts`), `features/hr/shared/hr-ui.tsx` alias block.
- [x] Public interfaces and barrel surfaces are reduced to verified consumers; implementation details stay private to their module.
      Backend `*.module.ts` exported tokens 363 → 312; in-scope tokens with no consumer outside their own Nest module 53 → 0. `check:module-di` Check A bit 3 findings mid-work and 2 exports were restored. Frontend barrels: KEEP — the 56 barrel entries whose callers deep-import are already recorded `RETAINED-BY-CONTRACT — named intentional barrel` by `check:dead-code`; reopening that is barred by the PRD closure protocol.
- [x] Deep imports across module ownership are removed or replaced by the smallest stable interface at the correct seam.
      KEEP with evidence. 847 "deep" imports measured; 811 are `app/**` route files importing their own feature page (ticket 25's convention). Of the 36 real feature→feature edges, the load-bearing one (employee-self-service/Home → hr, 14 imports) already resolves to `/me/*` `self:*` routes with no module gate, pinned by `hooks/api/hr/hr-core-query-access-matrix.test.ts`. The seam exists and is tested; only the path is deep, and adding barrels would create the re-export shells PRD §2.2 forbids.
- [x] Home only composes. Chat, Calendar, Inbox and Notifications keep independent schema, authorization, caching, workers and implementation behind small interfaces.
      `DashboardModule` imports one feature module (`NotificationsModule`) and calls one method through it (`notificationsService.unreadCount`); it touches no notification table and imports no chat/calendar/mail code. `features/dashboard` imports nothing from `features/{chat,calendar,inbox}`.
- [x] Home holds universal work only; module-owned destinations stay in their owning product's navigation.
      `sidebar-home-nav.ts` carries only Overview / Communication / For Me / Company — every entry on CLAUDE.md §8's universal list; `nav-surface-parity.test.ts` proves all three nav surfaces share one predicate. Noted for ticket 26: universal Announcements is served from `/hr/announcements`, a route-ownership question, not a composition one.
- [x] The backend owns the authoritative Home section and access manifest; the frontend consumes a generated contract with no hand-maintained parallel registry.
      Three defects fixed: (a) `generate:home-manifest` crashed with ENOENT on `<frontend>/backend/src/...` and is now marker-based — it reproduces the checked-in contract byte-for-byte; (b) `use-dashboard-access.ts` re-declared six manifest permission keys as literals and now resolves them via `homeSectionPermission(id)`; (c) three fail-closed assertions added to `lib/home/__tests__/home-sections.test.ts`, bite-proven (1 failed / 14 passed when reverted, 15 passed restored).
      PARTIAL: `hooks/api/dashboard.ts` still writes its 8 `useCan` keys as literals. It is ticket 28's territory, so it was pinned by assertion (c) — drift is now a test failure — rather than edited.
- [x] Duplicated constants are replaced by a canonical domain-owned catalog only where at least two real callers share the invariant — no generic dumping grounds, no speculative seams.
      Backend `HR_POLICY_TYPES` (2 definitions → the Zod enum that validates writes). Frontend notification channel catalog (6 byte-identical definitions → `features/notifications/notification-channels.ts`, keyed by `Record<NotificationChannel, string>` so a new union member is a compile error). Deliberately NOT merged: `PRIORITY_STYLES`/`PRIORITY_COLORS` (unrelated domains), `TOOLTIP_STYLE`/`CHART_TOOLTIP_STYLE` (CRM out of scope), `preferences-page.tsx:CHANNELS` (different invariant).
- [x] Deduplication names which implementation is canonical; that decision is the point of the work.
      Six canonical choices named in the report's box 8 table. Seventh recorded but not actioned: `formatRelativeTime` exists three times; canonical is `lib/date-utils.ts` (Intl-based, clock-injectable, NaN-safe, domain-neutral), but unifying changes user-visible strings on Home/Inbox/Notifications — a ticket 30 UX decision in ticket 28's `lib/`.

**Gates:** backend `check:module-di` (+self-test), `check:module-registration` (+self-test), `check:import-direction` (+self-test), `check:kebab-case:self-test`, `check:cycles` — all green. Frontend `generate:home-manifest`, `check:home-manifest` (+self-test), `check:module-manifest` (+self-test), `check:dead-code:self-test`, `check:routes:self-test`, `check:cycles` — all green. Focused jest: frontend 17 suites / 106 tests green; backend 38 suites green.

**Red gates that are NOT this ticket's:** backend `typecheck` (1 error in another lane's uncommitted `modules/finance/ap/payment-run-executor.service.ts`), frontend `type-check` (1 error in `features/billing/components/payments-tab.tsx`), `check:kebab-case` (untracked `src/scripts/.tmp-dcc-lib.mjs`), `check:dead-code` (9 unclassified exports across build/mail/app-api/hooks-api), `check:routes` (`app/api/media/image/route.ts`), `party/legacy-reader-ratchet.spec.ts` (a new untracked spec added a legacy reader). Details in the report.
