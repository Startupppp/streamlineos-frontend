# 25 — Reduce the remaining thick authenticated route modules through domain-owned seams

**What to build:** Authenticated `page.tsx` and `layout.tsx` files are thin route modules: metadata, parameters, server authorization and composition only. State, forms, queries and mutations move behind feature-owned interfaces. The ratchet has already come down substantially; this closes the remainder.

**Blocked by:** None — can start immediately.

**Status:** done

- [x] The thick-route ratchet reaches zero remaining in-scope offenders, and the ceiling is never raised to accommodate one.
      Evidence: `pnpm -s check:route-thinness` — `IN SCOPE thick: 0 (baseline 0)`, down from 58. `BASELINE.inScopeThick` was **lowered** 58 → 0 to hold the gain; it was never raised. `check:route-thinness:self-test` → 9 fixtures pass.
- [x] Implementation moves to feature-owned seams, not into an alternate oversized file that merely relocates the bulk.
      Evidence: `pnpm -s check:over-300` — 510 of 5116 files over 300 lines, down from 518 (baseline 519). None of the 58 extracted `features/**/*-page.tsx` files exceeds 300 lines; the 9 that would have were split into 18 further feature-owned components.
- [x] Converting a page to a thin server component must not drop its authorization. A client gate removed during extraction has to be replaced by the server-side permission the backend actually enforces on the data the page reads — take the key from the route's published permission metadata and confirm it exists verbatim in both catalogs.
      Evidence: no client gate was removed — every `"use client"` body moved intact, and a scripted diff confirms all 47 `useCan("…")` keys across the 22 gated pages survive verbatim (0 missing). Two genuinely ungated pages found in this territory were closed with server gates: `app/(authenticated)/surveys/layout.tsx` (`enforceRouteAccess("/surveys")` → `module:surveys + surveys:view`) and `app/(authenticated)/ai/executive-brief/layout.tsx` (`ai:executive-brief:view`). Both keys are the contract's own `x-permission` on `GET /surveys` and `GET /ai/executive-brief`, and both exist verbatim in `BE/src/modules/rbac/permissions/` and `FE/lib/rbac/permissions/`. `npx jest lib/rbac/route-access/__tests__/page-level-gates.test.ts` → 10/10 pass.
- [x] Platform-core self-service surfaces keep a session gate rather than acquiring a permission key they should not need.
      Evidence: `grep` over all 58 thinned route modules returns **zero** `requirePermission`/`enforceRouteAccess` occurrences — `/chat`, `/notifications/*` and the rest keep only the `(authenticated)` layout's `requireSession()`. `page-level-gates.test.ts` "keeps the session-only allowlist live" and "admits nothing into the allowlist that the runtime registry gates" both pass.
- [x] Public landing visuals and animations are untouched.
      Evidence: no file under `app/(public)/**` was opened for write; the change set is `app/(authenticated)/**`, `features/**` and two `scripts/*.mjs` ratchet lines.
- [x] Route ownership stays canonical: when a page moves, the old route files are deleted with no legacy redirect, and every link is updated.
      Evidence: no route URL changed, so no link needed updating and no redirect was created. The two non-route modules that were living inside `app/` were moved out and their originals deleted — `accounting/settings/accounting-settings-schema.ts` and `accounting/assets/depreciation/depreciation-schema.ts` now sit in their feature folders; `grep` finds no importer of the old paths. `app/(authenticated)/**` now holds route files only, apart from one pre-existing colocated test and one CRM/Inventory helper, both out of scope.
- [x] Any path-keyed or name-keyed gate that referenced an extracted file is re-run — splitting a file silently drops the extract from that gate's coverage.
      Evidence, all run and read: `check:client-pages` 142/600 client pages (was 200; ceiling 304) · `check:route-access-contract` ✔ 203 keys, 627 contract permissions · `check:routes` ✔ · `check:route-thinness` ✔ 0 · `check:over-300` ✔ 510 · `check:properties` (routes + colors + effect-fetches + icon-labels + `madge --circular`) ✔ "No circular dependency found!" over 5130 files · `check:query-signal` ✔ · `check:empty-states` ✔ · `check:module-manifest` ✔ · `check:home-manifest` ✔ · `check:command-catalog` ✔ · `check:seo-metadata` ✔ · `check:contract-drift` ✔ · `tsc --noEmit` clean apart from the 22 known `.next/types/validator.ts` errors.

BLOCKED: none. Three gates are red on pre-existing environment artifacts, not on this work — `check:file-sizes`, `check:query-scope` and `check:formatters` each report only paths under the generated `.next-buildmart/` build directory, and `check:contract-vendor` needs `backend/openapi.json` at a sibling path this machine's layout does not have. `check:dead-code` is red on 3 unused files, all of them `.next-buildmart/dev/types/*` and `.scratch/mint-session.mjs`.
