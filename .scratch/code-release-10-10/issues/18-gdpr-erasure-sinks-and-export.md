# 18 — Finish GDPR erasure sinks and prove subject export is exhaustive and resumable

**What to build:** Database PII erasure and session revocation are already implemented. The remaining sinks are object storage, search and vector indexes, derived projections, caches and supported adapters — plus the export side, which must never silently cap.

**Blocked by:** None — can start immediately.

**Status:** 6 of 7 boxes closed — box 1 open on one cross-territory sink (chat_attachments, chat module)

- [ ] Erasure is idempotent, tenant-scoped in SQL, audited and legal-hold-blocking across every remaining sink; immutable and legal-hold records are preserved rather than deleted.
  - **Session 18c — the support-ticket call site named below is now wired** (`gdpr-subject-erasure.service.ts:338`) and green; the sink audit was re-run against current source rather than trusted from these notes.
  - **P0 found and fixed: the subject's own GDPR export archive survived their erasure.** `gdpr_export_jobs.subject_user_id` is a bare `text` column with no FK to `users`, so `collectSubjectFileKeysWithLegalHold` classified the whole table as org-scoped, and `purgeFromManifest` skips org-scoped keys by design. A complete JSON dump of one person's personal data therefore stayed in object storage after their erasure — and, because `expireOldJobs` had zero callers, still marked `completed` and downloadable. New `gdpr-subject-erasure-export-artifacts.ts` keyset-drains the subject's artifacts BEFORE the transaction, appends them to the manifest as `source: "user-fk"`, and retires the rows inside it (`status=expired`, `expires_at=now`, `file_name=null`) while deliberately KEEPING `file_key` so a failed object delete is retried rather than orphaned.
  - Idempotency, legal-hold blocking and the drain are asserted, not assumed: `nice jest --runInBand --testPathPattern="src/modules/gdpr"` → 15 suites, 219 passed. `gdpr-erasure-export-artifacts.spec.ts` (13 tests) drives a 413-artifact fixture (2 full pages + 13) and asserts 3 select calls; neutering the drain fails with "Expected: 3, Received: 1". A hold blocks before any collection or manifest build (`artifactSelectCalls` 0, `buildManifest` not called, `db.transaction` not called), and the bite proof shows the same fixture DOES collect with the hold released. A second run claims no table and adds no key.
  - Sinks re-audited against current source and found ALREADY CLOSED (the notes above were stale): chat/AI message content (`chat_messages.content`, `ai_chat_messages.content`, `ai_chat_conversations.title` → `[ERASED]`; `kb_chat_messages`/`kb_chat_conversations` hard-deleted), vector indexes (`kb_article_chunks`, `kb_ingestion_checkpoints`, `support_ticket_embeddings` — those are the only three embedding tables in `db/schema`), and full session revocation (`SessionsService.revokeAllForUser` writes the Redis `revoked:session:<id>` tombstone the JwtAuthGuard actually reads, plus the DB flag, plus `bumpPermissionsVersion` and `bustMembershipStatusCache`).
  - PARTIAL: `chat_attachments` is the one sink still open, and it needs one function in the chat module (outside this ticket's territory). Everything else on this box — export archives, chat/AI content, vector indexes, session revocation, idempotency, legal-hold blocking, immutability — is closed and proven. Detail: one sink remains open — `chat_attachments`. The subject's uploaded chat files survive erasure for the same reason the export archive did (no `users` FK; reachable only through `chat_messages.sender_membership_id`). I implemented it, then reverted it: reaching it requires an unpredicated read of `chat_messages`, which pushed `check:lifecycle-predicates` to 76/75 and `check:unbounded-reads` to 1 unclassified. Raising a ratchet the gate says "may only go down" to add one sink is the wrong trade when the natural home for that reach-through is the chat module, which already owns a counted `chatMessages` read (`modules/chat/chat-attachments.service.ts:28`). Prescription is in `reports/18c-gdpr-export-artifact-retention.md`.
  - Proven for every sink the service touches: `nice -n 10 npx jest src/modules/gdpr --maxWorkers=2` → 13 suites, 201 tests, 0 failures. Legal hold blocks before the manifest is built (new bite proof); `audit_logs` and `hr_legal_holds` are never deleted; org-scoped storage keys are now skipped rather than deleted.
  - REMAINING (not fixed, outside the "DB PII already done" boundary I was told not to redo): `support_tickets.requester_email` / `requester_name` for tickets created by the subject's membership are never anonymised, and `support_ticket_embeddings` reproduces those tickets' title+description as a pgvector row. Fix is an `update(supportTickets).set({requesterEmail: null, requesterName: ERASED_NAME}).where(orgId, createdByMembershipId = membership.id)` inside the same transaction.
  - **Session 18b:** the support side of that fix now exists — `src/modules/support/core/support-ticket-erasure.ts` exports `anonymiseSubjectSupportTickets(tx, { orgId, subjectUserId })`, a keyset drain (page 200) that writes the same `erased-<sha256(userId)[0:16]>@erased.invalid` sentinel GDPR already writes to `users.email`, sets `requesterName` to `ERASED`, and deletes the matching `support_ticket_embeddings` rows. `nice -n 10 npx jest src/modules/support/core/support-ticket-erasure.spec.ts --maxWorkers=2` → 6 passed; 437-ticket fixture (2 full pages + 37) asserts 3 select calls and 437 embeddings deleted, and neutering the drain fails with "Expected: 3, Received: 1". Match is on `requester_email`, NOT `created_by_membership_id`: an ingested channel ticket is attributed to the channel owner while `requester_email` holds an external customer, so the membership predicate would erase a third party's address.
  - Still open because the call site is in GDPR territory. Insert in `gdpr-subject-erasure.service.ts` between line 329 (`tablesAnonymised.push("chat_messages")`) and line 331 (`const [otherMembership]`), i.e. BEFORE the `users.email` anonymisation at line 343 — the requester columns are free text with no FK, so the live address is the only link. Exact snippet in `reports/18b-retention-truncation.md`.
- [x] Every drain uses a keyset cursor rather than a fixed limit. A bare limit reports a prolific subject as fully erased while leaving their chunks and embeddings behind — this exact defect has already occurred twice here.
  - Every `.limit(` in `src/modules/gdpr/**` audited (66 occurrences): all are `limit(1)` single-row reads, `limit(BATCH_SIZE)` inside a keyset drain, or the disclosed `SYNC_EXPORT_CAP + 1` probe. The three previously *unbounded* KB id selects (authored articles, owned sources, uploaded attachments) are now keyset drains via `forEachIdPage`. Proof with MORE rows than the page size: `gdpr-erasure-storage-sink.spec.ts` feeds a 200-row full page + a 1-row tail and asserts 3 chunk deletes; neutering `forEachIdPage` to a single page makes it fail with "Expected length: 3, Received length: 2" (run and read).
- [x] Subject export is exhaustive and resumable with no silent caps and no skipped in-scope source.
  - Async worker drains every source in `REQUIRED_GDPR_EXPORT_SOURCES` with `drainExportPages` (keyset, throws if the cursor fails to advance) and is resumable via claim/fail/reclaim/attempt. `nice -n 10 npx jest src/modules/gdpr --maxWorkers=2` → 201 passed, including "resumes after the batch boundary and never truncates an available section" and the source-coverage block.
  - **Session 18c:** "resumable" was inert until now. `GdprExportService.reclaim` — the only thing that moves a job out of `running` — had ZERO callers, so a worker that died between `claim` and `complete` stranded that subject's export for ever (`claim` selects `pending` only). `CronGdprExportRetentionService.reclaimStale` now calls it hourly and returns the count; asserted at 2 reclaimed with a bite proof at 0.
  - The synchronous `/gdpr/export/me` cap is disclosed per section. Its three `exportIncomplete` notes were STALE and told the subject that audit logs, blob keys, chat and mail "are not included / require elevated tooling" when the async export returns all of them; rewritten to name the async endpoint as the exhaustive path.
- [x] Correction is implemented as correction. Export, deletion and anonymization are not substitutes for rectification.
  - `GdprRectificationService` writes the submitted value in place, reads it back, and fails closed with a `ConflictException` if the read-back does not match; before/after SHA-256 hashes go to `audit_logs`. New test asserts `set` receives exactly `{ preferredName: "Anu" }` — a redaction or a delete would fail it. `nice -n 10 npx jest src/modules/gdpr/gdpr-rectification-auth-linked-fields.spec.ts --maxWorkers=2` → 11 passed.
- [x] Authentication-linked fields require a verification challenge rather than a blind update.
  - No blind update exists and it is now enforced: `gdprRectificationBodySchema` is a closed discriminated union that rejects `profile.email` / `auth.email` / `profile.password` / `auth.mfa`, and a new test drives all 9 accepted fields through the service and asserts no `set()` payload ever contains `email`, `emailVerified`, `password`, `passwordHash`, `mfaSecret`, `mfaEnabled` or `backupCodes`. `/me` profile update also has no `email` field (`src/me/dto/me.schemas.ts`).
  - Consequence to record: there is therefore no subject-facing way to rectify `users.email` at all. Adding one requires a challenge store binding (userId, newEmail, tokenHash, expiry); `email_otp_codes` is keyed by userId only and cannot bind a new address, so a schema/migration change would be needed — outside this session's territory.
- [x] Document, payroll, export, purge and retention workflows are each proven never to silently skip or truncate growing work.
  - Export: proven (above). Purge (object storage, org prefix): proven — `storage-purge.service.ts` pages with `ContinuationToken` until `IsTruncated` is false. Retention crons that loop and disclose: kb-chat, kb-chunk, build, ai-usage, hr, outbox, notification, notification-outbox (the capped ones return `truncated: true` and log).
  - NOT proven, two silent truncations found outside this session's territory:
    1. `src/modules/cron/cron-mail-retention.service.ts`, `cron-announcements-retention.service.ts`, `cron-helpdesk-retention.service.ts` each run ONE `LIMIT BATCH_SIZE` delete per org per tick with no loop and no "rows still eligible" signal. A backlog larger than the batch is silently carried forever and the log reports only what was deleted.
    2. `src/modules/organization/core/org-purge.service.ts:69` `listMemberUserIds` uses a bare `.limit(10000)`. On a larger org, members past 10 000 never get session/permission caches busted or org-scoped access revoked during a purge.
  - **Session 18b — both fixed.** (1) All three sweeps now drain until a short batch, capped at `MAX_BATCHES = 100`, returning `truncated: true` and recording it in the `hr_audit_logs` `after` payload. Multi-page proofs: mail 500/500/120 → 3 delete calls / 1120 rows; helpdesk 200/200/37 → 3 / 437; announcements 200/200/15 + 200/6 → 5 calls / 415 + 206. Neutered to one batch each, they fail with "Expected number of calls: 3 / Received number of calls: 1" (mail, helpdesk) and "5 / 2" (announcements). (2) `listMemberUserIds` is a keyset drain on `organization_members.id` (page 500), and `bustMembersMembership` now chunks its fan-out at 50 rather than opening an unbounded `Promise.all`. New `org-purge-member-drain.spec.ts` drives 1 250 members and asserts 1 250 revocations; neutered it fails "Expected 1250 / Received 500". `npx jest src/modules/cron --maxWorkers=2` → 230 passed; `src/modules/organization/core` → 311 passed; `tsc --noEmit` exit 0.
  - **Session 18c — both of 18b's blockers are closed, verified against current source rather than the notes.** (a) `cron-hr-retention.service.ts` now drains with `MAX_BATCHES = 100`, carries `truncated` through `HrRetentionSweepResult` into the `hr_audit_logs` `after` payload, and gives `sweepDocuments` a `scanned` count plus a no-progress guard. (b) The sweeps ARE code-scheduled: `CronRetentionSchedulerService` runs every job in `retention-schedule.ts` in process on its declared cadence, reading the same `cron:heartbeat:<jobKey>` an external POST refreshes so the two compose rather than compete.
  - **P0 found and fixed: the GDPR export lifecycle had no caller at all.** `GdprExportService.expireOldJobs` and `.reclaim` were both implemented, both fully specced by their own module, and neither was invoked anywhere in `src/` — the same zero-caller shape ticket 18 already found in `GdprStoragePurgeService`. Consequences: the declared 72-hour expiry never fired, so every export archive ever produced stayed `completed` and downloadable in object storage indefinitely; and a worker that died between `claim` and `complete` left its job wedged in `running`, which `claim` (pending-only) can never pick up, so that subject's export silently never resumed. New `CronGdprExportRetentionService` + `POST /cron/gdpr-export-artifact-retention`, declared hourly in `RETENTION_JOBS`, wired into the scheduler, monitored by the dead-man alert and documented in the README table.
  - Proof, run and read: `jest --runInBand --testPathPattern="src/modules/cron|src/modules/gdpr"` → 49 suites, 504 passed, 0 failed. The artifact drain is fed 237 rows (2 full pages of 100 + 37) and asserts 3 select calls and 237 object deletes; neutered to one page it fails "Expected: 3, Received: 1" and drops the tail artifact. Ordering bites: a sweep whose every delete fails clears no `file_key` at all, so the next tick retries instead of orphaning.
  - `.limit(` audit re-run over both paths and every occurrence classified. `src/modules/gdpr/**`: every one is a `limit(1)` point read, a `limit(BATCH_SIZE)` page inside `drainExportPages` (which throws if the cursor fails to advance), a `limit(ERASURE_ID_PAGE)` page inside a keyset drain, or the disclosed `SYNC_EXPORT_CAP + 1` probe — no silent cap remains. `src/modules/cron/*retention*`: mail, helpdesk, announcements, hr, outbox, notification-outbox and notification are bounded drains that return `truncated`; kb-chat, kb-chunk, build and ai-usage are UNBOUNDED `for(;;)` drains with no cap at all, so they cannot under-report and need no flag.
  - Retention self-monitoring, each verified in source: **code-scheduled** (`CronRetentionSchedulerService`, jittered boot, `RETENTION_SCHEDULER_ENABLED`) · **bounded/resumable** (`MAX_BATCHES` + keyset cursors; the artifact sweep resumes from `status='expired' AND file_key IS NOT NULL`) · **idempotent** (asserted: a second sweep over an empty predicate expires and deletes nothing) · **audited** (`hr_audit_logs`, `retention_sweep.gdpr_export_artifacts`, carrying `truncated`) · **retryable** (`CronLeaseService.withLease`, compare-and-delete release) · **dead-man signal** (`alert-retention-dead-man.mjs`, 13 monitored sweeps, self-test 23/23) · **durable failure events** (`CronSweepFailureSinkService` writes `cron:last-error:<jobKey>` with `failedOrgIds` on partial per-tenant failure, which a heartbeat check alone cannot see). `retention-schedule-parity.spec.ts` fails if the declaration, the scheduler, the alert and the README drift.
  - **Retention decision for `notification_outbox` and `outbox_events` — made and written down** in `RETENTION_MATRIX` (`src/scripts/check-retention-coverage.mjs`), and asserted by that gate's own self-test (`outboxTablesAreNowCovered`, `decidedTablesAreNoLongerPending`). Both are **RETAIN-BOUNDED at 30 days, terminal states only**: `notification_outbox` PROCESSED/DEAD via `CronNotificationOutboxRetentionService` (forEachOrg, batch 500, per-org lease); `outbox_events` DELIVERED/DEAD/SUPPRESSED via `CronOutboxRetentionService` (batch 1000, plus `inbox_records` past `processed_at`). PENDING and IN_FLIGHT rows are never touched in either — deleting an undelivered event is silent data loss, not retention. `gdpr_export_jobs` is now a third entry in that matrix with the same shape. `pnpm check:retention-coverage` exit 0, `uncovered: 0`.
- [x] Object-storage deletion removes the object, not only the database row — an orphaned object with a public URL survives the record that pointed at it.
  - P0 found and fixed: `GdprStoragePurgeService` was provided and exported by `GdprModule` and had ZERO callers — `POST /gdpr/erasure/:subjectId` anonymised the database and left every avatar, payslip and uploaded document alive in R2. It is now injected into `GdprSubjectErasureService`, the manifest is captured BEFORE the transaction (a nulled `*_key` column no longer names its object), and the objects are deleted after commit with a HEAD re-check.
  - `nice -n 10 npx jest src/modules/gdpr/gdpr-erasure-storage-sink.spec.ts --maxWorkers=2` → 11 passed, including call-order proof that `buildManifest` precedes `db.transaction` and `purgeFromManifest` follows it.
