# 29 — Frontend module matrices for Calendar, Inbox/mail and Knowledge

**What to build:** The §10.13, §10.14 and §10.16 frontend criteria, plus the Knowledge backend query/worker criteria that ride with them.

**Blocked by:** 28.

**Status:** partially closed — **6 of 9 boxes closed, 3 open** (Calendar, Inbox/mail, Knowledge queries/workers). S11 closed the **Knowledge** box: `kb_page_attachments` (migration `1042`) gives KB page media the row it never had, with a composite `(org_id, page_id)` tenant FK, a per-org unique on the storage key and soft delete — verified against a real catalog, not against Drizzle. The remaining three are not defects: Calendar waits on a **product decision** about series-split semantics, Inbox/mail's four remainders are **two NEW REQUIREMENTs** needing a provider-integration design, and Knowledge queries/workers has three items left in `kb/retrieval/**` (one of its four was fixed here). Note for the record: the previous status line said "6 of 9 closed" while only 5 boxes were actually ticked; the count is now true. See `reports/29-calendar-inbox-knowledge.md` §S11.


- [ ] **Calendar:** one `/calendar` serving everyone with module events as toggleable sources; timezone display, series-versus-instance edits, cursor/range keys, and DST, exception, conflict and reminder coverage. No module-specific calendar page exists.
  BLOCKED: **(c) "this and following" needs a product decision, and that is the only thing left in this box that is about calendar code.** (a) is a cross-territory remainder.
  S11 UPDATE — re-verified against current source, because ticket 28 landed in between.
  ✅ **(b) timezone reaches the client — DONE by ticket 28.** `hooks/api/calendar.ts:103` declares `timezone?: string | null`; `event-detail-content.tsx:73-74` passes it to `formatEventDate`/`formatEventTimeRange`, which take an explicit `timeZone` (`lib/date-utils.ts:126-139`) and probe the zone first (`:101`) so an unknown IANA name degrades instead of throwing `RangeError`. Pinned by `lib/date-utils.event-timezone.test.ts`.
  ✅ **(d) source toggles are in the query key — DONE by ticket 28.** `lib/query-keys/platform-hierarchy.ts:6-9` appends the enabled-source set as an optional tail; consumed at `hooks/api/calendar.ts:195`, pinned by `hooks/api/calendar-source-key.test.ts`.
  ❌ **(a) the second calendar surface is STILL THERE, and this session's brief assumed it was gone.** `frontend/features/hr/recruitment/interviews-page.tsx:249` still renders `BigCalendarWrapper` (month/week toggle, prev/next, `eventPropGetter`) at `/hr/recruitment/interviews`. Last touched by `e33873d6c` (route thinning), which did not address it. `hr-interviews` is already a registered aggregate source, so `/calendar` already shows these events; the fix is a list plus a link. **HR / ticket 25 territory — reported, not fixed.**
  BLOCKED: **(c) is a product decision, and five things have to be decided before anything can be built.** Re-verified: the frontend vocabulary is exactly `"occurrence" | "series"` (`event-series-scope-dialog.tsx:15`) and the backend has only `upsertOccurrenceException` / `cancelOccurrence` (`calendar-recurrence.service.ts:32,80`) plus whole-series update. "This and following" mechanically means a **series split**: (1) does it mint a second `calendar_events` row with `UNTIL` stamped on the original RRULE, and therefore a `series_parent_id` and a migration? (2) do exceptions and cancellations after the split point re-parent to the new half — leaving them behind silently resurrects cancelled occurrences; (3) do attendees and RSVPs carry or reset; (4) does the provider sweep truncate-and-create or issue a provider-native edit, given one local event now maps to one `externalEventId`; (5) how do reminders behave across the boundary so an already-sent reminder does not fire twice. Until (1) and (2) are answered there is nothing to build against.
  S10 UPDATE — the S8 fixes were re-verified and hold: `jest --testPathPattern="src/modules/calendar"` → **42 suites / 396 tests pass, exit 0** (41/391 at S8; the delta is this pass's new spec).
  **(b) is now half closed — the backend half.** `calendar_events.timezone` drove occurrence expansion but stopped at the source: `calendar-native-event-source.ts:55` selected it and never put it in `meta`, so `projectionToItem` had nothing to project and `CalendarEventItem` had no field for it. All three now carry it (`null` for aggregate sources with no authored zone, and a non-string zone is rejected rather than passed through as a label). Proof: new `calendar-item-timezone.spec.ts` → **5 pass**, including the two BITE cases and one pinning that the expansion the zone drives is unchanged.
  PARTIAL: (b) the client half — rendering the zone instead of labelling browser-local times with the browser's zone — is `frontend/hooks/api/calendar.ts` + `event-detail-content.tsx`, ticket 28/25 territory.
  BLOCKED: (a) the HR second calendar surface is `frontend/features/hr/recruitment/interviews-page.tsx`, ticket 25 territory.
  BLOCKED: (c) "this and following" is an unimplemented product feature, not a defect — a series split exists nowhere in either repo. It needs a product decision on the split semantics before it can be built.
  BLOCKED: (d) source toggles in the query key is `frontend/lib/query-keys/platform-hierarchy.ts`, ticket 28 territory.
  S8 UPDATE — decision #4 (local-first commit, async provider sync) is now materially closed. Three live defects were found and fixed:
  (i) **The provider-webhook receiver was dead AND unauthenticated.** `CalendarProviderWebhookController` was imported by `calendar.module.ts` but absent from its `controllers: []` array, so `POST /webhooks/calendar/provider` did not exist at runtime; and `handle()` imported `assertCalendarWebhookSecret` without ever calling it, so once registered the `@Public()` route would have accepted any unauthenticated delivery naming any connectionId. `providerWebhookBodySchema` was also a bare `z.object` (a caller-supplied `orgId` was silently stripped, not rejected). All three fixed; the previous session's `calendar-provider-webhook-delivery.spec.ts` had been committed **5 red** and now passes. Proof: `jest calendar-provider-webhook` → **3 suites / 32 pass**.
  (ii) **A re-claimed CREATE minted a duplicate provider event.** `pushCreate` carries no idempotency key and the sweep pushed unconditionally, so a lease expiring after a successful push but before `mark(PROCESSED)` created a second external event and overwrote `externalEventId`, orphaning the first. `calendar-provider-sync-sweep.service.ts` now skips the push when the event already carries an external id.
  (iii) **A failed DELETE was permanent local/external divergence.** `deleteEvent` enqueued its tombstone with `eventId: null`, while `getSyncStatus`/`retrySync` both key on `eventId` and resolve visibility from the (now-deleted) event row — so a delete that exhausted its 5 attempts was invisible in sync status and impossible to retry, leaving the provider copy forever. The tombstone now keeps the deleted event's id (the column has no FK — verified against `migrations/0934`), and status/retry fall back to the tombstone's own author when the local row is gone. A bystander still gets 404.
  Also: `mark()` now carries an explicit `org_id` predicate instead of leaning on RLS alone.
  Proof: new `calendar-provider-divergence.spec.ts` → **7 pass**; reverting each of the three source fixes turns **2, 3 and 2 red** respectively. Full module: `jest src/modules/calendar` → **41 suites / 391 pass** (was 39/371).
  STILL OPEN and unchanged: (a) the HR second calendar surface, (b) timezone never reaching the client, (c) "this and following", (d) source toggles absent from the query key.
  (a) **A second full calendar surface exists.** `frontend/features/hr/recruitment/interviews-page.tsx:249` renders `BigCalendarWrapper` (month/week toggle, prev/next) at `/hr/recruitment/interviews`. Violates root CLAUDE.md §8 ("Never module-specific calendar pages") and §9 (feature→feature import). `hr-interviews` is already a registered aggregate source (`backend/src/modules/hr/hr-calendar-sources.ts`), so the unified calendar already shows these. Fix = replace that view with a list + a link to `/calendar`; file is in ticket 25/HR territory.
  (b) **Timezone never reaches the client.** `calendar_events.timezone` exists and drives expansion (`calendar-occurrence.service.ts:65-85`), but `CalendarEventItem` (`calendar.types.ts:20-38`) and `projectionToItem` never carry it, `frontend/hooks/api/calendar.ts` has no `timezone` field, and `event-detail-content.tsx:74` labels browser-local times with the *browser's* zone. Closing needs a field added in `hooks/api/**` — ticket 28's territory.
  (c) **"This and following" does not exist.** Only `occurrence` | `series` (`event-series-scope-dialog.tsx:15`); no series-split anywhere in either repo.
  (d) **Server-side source toggles are absent from the query key** (`lib/query-keys/platform-hierarchy.ts:5-7`); correctness rests on a blanket `queryKeys.calendar.all` invalidation. `hooks/api/**` — ticket 28's territory.
  DONE within this box: DST (`calendar-dst-edge.spec.ts`), exceptions (`calendar-recurrence.service.ts` + `calendar-series-exception-scope.spec.ts`), conflicts (`calendar-conflict.service.ts` + spec) verified satisfied; reminder coverage fixed — see the silent-truncation note below. `jest src/modules/calendar` → **39 suites / 371 tests pass**.
  P2 fixed here: `CALENDAR_PER_SOURCE_CAP = 400` truncated each source with no signal. `calendar-source.registry.ts` now returns `truncatedKeys` and `calendar-events-aggregate.service.ts` folds it into the existing `truncated` flag the UI already banners.
  P2 fixed here: `calendar-reminder-sweep.service.ts` used three bare `.limit()` reads, so an org with >200 qualifying events in a 20-minute window silently lost reminders. Now keyset-drained. Proof: `calendar-reminder-sweep-event-paging.spec.ts` — 205 candidates across 2 pages; forcing the drain back to one page turns it red.

- [ ] **Inbox/mail:** indexed conversation ordering, search and unread; incremental sync; idempotent send and receive; bounce/retry/DLQ; invalidation of list, thread and count keys.
  PARTIAL: ordering and search were the two real *defects* and both are fixed and measured. The other four are unbuilt features, and S11 decided they are **out of scope for this release and recorded as two NEW REQUIREMENTs** — see below. One more, idempotent send, turned out to be already done.
  S11 UPDATE — every remainder re-verified against current source rather than against these notes.
  ✅ **Idempotent send — DONE, and not where this ticket said it was.** `lib/api-client.ts:172-178` now mints a key only as a last resort and defers to `hooks/common/use-idempotent-operation.ts`, which holds ONE key for the life of a retried operation and releases it on `settle()`. `hooks/api/mail.ts:95,109` use it for `/mail/send` and `/mail/reply`. A compose Retry after a timeout no longer sends the recipient a second real email.
  ❌ **Unread-count invalidation — NOT A DEFECT.** `/notifications/unread-count` is notifications-only and mail writes no notification row, so invalidating it from mail could not change its answer. `queryKeys.inbox.count()` exists (`lib/query-keys/platform-core.ts:144`) but `/me/inbox/unified/count` has **no client hook at all** — there is nothing to invalidate. Struck from this box.
  NEW REQUIREMENT (A) — **unread and incremental sync are one requirement, not two.** `mail_message_metadata.is_read` exists and the keyset index already covers the count's predicate, so the query is trivial; it is not built because **the answer would be wrong**. The mirror holds only what has already been listed, so a fresh account would report "3 unread" for a 400-message mailbox and present it as authoritative. The prerequisite is a real delta sync, and the current checkpoint machinery is not one: `loadPosition` still has **zero production callers**, `clearPositions` has none anywhere, and `mail.service.ts:284` upserts a row on every provider page fetch that nothing ever reads — a **write-only table**. What it stores is the token of the last page *scrolled to*, not a delta position, so wiring `loadPosition` up would resume mid-scroll. Cost: delta-token semantics on `mail_sync_checkpoints`, a `listChanges` method on both providers (Gmail `historyId`, Outlook `deltaLink` — neither wrapper requests one), a background sync worker (there is none; the mirror is populated only as a side effect of a user scrolling), and only then an unread endpoint. The dead write is left in place because `mail-sync-checkpoint-isolation.spec.ts` is 4 of `check:tenant-isolation`'s 924 declarations.
  NEW REQUIREMENT (B) — **idempotent receive and bounce/retry/DLQ are the same missing thing: there is no inbound mail path at all.** Confirmed by scan: zero `webhook`, zero inbound, zero bounce/DLQ references anywhere under `src/modules/mail/`. Mail is outbound-and-poll only. A bounce arrives as an inbound DSN, so bounce handling cannot precede receiving, and "idempotent receive" has nothing to be idempotent about until something receives. Cost: a provider trigger or webhook receiver (`CalendarProviderWebhookController` is the pattern, including `@Public()` + shared secret and a `.strict()` body schema), a dedupe key that includes `account_id` — `mail_message_metadata`'s own unique `(account_id, message_id)` omits `org_id` — and then a DSN classifier feeding the existing `notification-delivery-worker` retry/dead-letter machinery, which has never been applied to mail.
  S10 UPDATE — **ordering is fixed, and it was worse than "not stable": it was a hard dead end.** `mail.service.ts:65-88` returned `nextCursor: null` **unconditionally** on the cached path, so on the ordinary single-account fresh-mirror case the inbox showed one page and "load more" did nothing, forever. `mail_message_metadata` now has its own keyset cursor: `ORDER BY (date DESC, id DESC)` behind a new tiebreak index, `limit + 1` to detect more, and a **separate signed cursor namespace** (`md1` vs `m1`, `providers/mail-metadata-cursor.ts`) so a provider cursor and a metadata cursor can never be decoded as one another — which is what lets the regime be chosen once at page one and held for the whole scroll instead of switching halfway and repeating rows. An exhausted metadata cursor ends the scroll rather than falling through to the provider, where it would decode as "no position" and replay page one.
  S10 UPDATE — **search is fixed, and the ticket's wording above needs correcting.** "That path is unreachable because `listCached`'s `query` argument is never passed" is right; "mail search does not work at all today" is not — provider search works (`mail.controller.ts:81` → `gmail/outlook.listMessages`). What was dead is the **database** search. `listCached`'s `query` and `isFreshForAccount` both had zero callers; both are now live. A trigram index alone would not have helped: `mail_message_metadata` has RLS on and `texticlike` is `proleakproof = false`, so the planner refuses the index outright. Search now goes through `app.search_mail_message_ids` — SECURITY DEFINER, org from `app.current_org_id()` and never a parameter, ids only, `REVOKE`/`GRANT`, `cap + 1` with an ILIKE fallback at the cap — the 0424/0425/0453 pattern. Migration `1022_t29_mail_metadata_search_and_keyset.sql`.
  Measured on `scratch_t29` (258k mail rows, four tenants at 255,000 / 57,000 / 33,600 / 32,250, `VACUUM ANALYZE`d) as the non-owner `streamline_app` with the tenant GUC set, in **buffers**: a selective term drops from **13,595–13,820 to 573–697**, ~20x, consistently across the skew. A term over the cap is deliberately unchanged, because ILIKE under `LIMIT` is already the cheap plan there (92 buffers on the minority tenants). The majority and minority tenants take genuinely different plans — a single-tenant reading would have seen one and missed the other.
  Also here: `idx_mail_metadata_list` is **dropped** as a strict prefix of the new keyset index — measured, not assumed (migration 1007's warning): page one costs 3 more buffers and is still an Index Scan, against 49 MB of index on an 89 MB table maintained on every row of a mirror rewritten on every inbox load.
  Proof: new `mail-metadata-keyset-search.spec.ts` → **9 pass** and `mail-list-paging-regime.spec.ts` → **10 pass**, both with BITE cases (a null-dated cursor that a bare row comparison would drop every dated row for; a cursor minted for another reader; the >cap fallback; and one pinning that with no query the search helper is never called, so the previously-unreachable ILIKE does not become unconditional). Whole module: `jest --testPathPattern="src/modules/mail"` → **13 suites / 103 pass**.
  BLOCKED: **unread** — `unified-inbox.service.ts:395-413` still fans out to the provider for 100 messages and counts in JS, self-declared inexact. Unbuilt feature, not a defect.
  BLOCKED: **incremental sync** — `mail-sync-checkpoint.service.ts::loadPosition` still has zero production callers and the table still stores a Gmail `nextPageToken` rather than a `historyId`/`deltaToken`. Needs a provider-delta design, not a fix.
  BLOCKED: **idempotent send** — the backend decorators are correct; the defect is `frontend/lib/api-client.ts:150-153` minting a fresh UUID per HTTP call. Frontend territory.
  BLOCKED: **idempotent receive** — there is still no inbound mail path at all to make idempotent.
  BLOCKED: **bounce/retry/DLQ** — none for mail; the notification machinery exists and has never been applied to it. Unbuilt feature.
  BLOCKED: **unread-count invalidation** — no mail hook invalidates `notifications.unreadCount()` / `inbox.count()`. `frontend/hooks/api/**`, ticket 28 territory.
  - Ordering: the DB-ordered path matches `idx_mail_metadata_list` (`db/schema/mail/mail-metadata.ts:39-44`) but is only reachable on a first page with no query and a single account (`mail.service.ts:65`); the normal path sorts in JS (`providers/mail-normalizers.ts:428`). Thread ordering never touches the DB, so `idx_mail_metadata_thread` is dead.
  - Search: leading-wildcard `ilike` (`mail-metadata.service.ts:125-134`), no tsvector/GIN on `mail_message_metadata`; and that code path is unreachable because `listCached`'s `query` argument is never passed.
  - Unread: `unified-inbox.service.ts:395-413` fans out to the provider for 100 messages and counts in JS, self-declared inexact.
  - Incremental sync: `mail-sync-checkpoint.service.ts::loadPosition` has **zero production callers** and `clearPositions` has none anywhere; the table is write-only and stores a Gmail `nextPageToken`, not a `historyId`/`deltaToken`. No expiry or invalid-cursor branch.
  - Idempotent send: `@Idempotent("mail.send")` / `("mail.reply")` are present (`mail.controller.ts:112,128`) but `lib/api-client.ts:150-153` mints a **fresh UUID per HTTP call**, so a user retry is a genuine second send.
  - Idempotent receive: there is no inbound mail path. The nearest unique key (`crm/inbound-events.ts:59-63`) omits `account_id`; `mail_message_metadata`'s own unique omits `org_id` (`db/schema/mail/mail-metadata.ts:37`) — schema, off-limits here.
  - Bounce/retry/DLQ: none for mail. The machinery exists for notifications (`notification-delivery-worker.service.ts`) and was never applied.
  - Invalidation: list ✅ and thread ✅ (both under the `queryKeys.mail.all` prefix); **unread count ✗** — no mail hook invalidates `notifications.unreadCount()` or `inbox.count()`.

- [x] **Inbox/mail frontend:** infinite lists, thread hydration, optimistic read/label rollback, compose and send states, offline and reconnect, sanitization, and account-revocation behaviour.
  Closed within this session: **optimistic read** — `useMailAction` already patched `markRead` optimistically with a full `onError` restore, but **nothing in the UI ever called it**: opening a message did not mark it read, so every message stayed unread forever and "Mark unread" was the only read-state control. `features/mail/mail-shell.tsx` now marks read on open, gated on `useCan("mail:messages:manage")`, with a local rollback. Proof: `features/mail/mail-open-marks-read.test.tsx` — 3 tests.
  **Account revocation** now has frontend coverage: `features/mail/mail-account-revocation.test.tsx` — 5 tests over the `needs_reauth` row, the reconnect re-initiation for the account's own toolkit, and the `accountErrors` banner + its route back. Also fixed: the banner read only `pages[0]`, so a revocation surfacing mid-scroll was invisible (`mail-list-pane.tsx`).
  Already satisfied, verified: infinite lists (`hooks/api/mail.ts:38-63`), sanitization (`mail-html-viewer.tsx` DOMPurify + `mail-html-render-boundary.test.ts` pinning it as the only `dangerouslySetInnerHTML` in the feature).
  S8 UPDATE — two of the three remaining items are now closed.
  **Offline/reconnect** now exists: new `features/mail/use-mail-connectivity.ts` subscribes to TanStack's `onlineManager` through `useSyncExternalStore` (the correct primitive for an external store — an effect+setState trips the repo's `react-hooks/set-state-in-effect` rule). Compose and reply refuse to send while offline, save the draft and say so, instead of firing a doomed request.
  **Compose drafts + retry affordance** now exist: new `features/mail/mail-draft-storage.ts` persists body and subject to `localStorage`, keyed per-compose and per-replied-message so two replies never collide. It closes a real data-loss path — `handleClose` unconditionally `reset()` the form, so an Escape or outside-click silently destroyed an unsent body. Escape/outside-click now **preserves** the draft and restores it on reopen; the explicit **Discard** button clears it; a successful send clears it; a failed send persists it and the error toast now carries a **Retry** action. Every read/write is try/caught, so a private window or a quota error degrades to "no draft" rather than breaking compose.
  Proof: new `features/mail/mail-draft-storage.test.ts` → **10 pass**, including 3 BITE cases (unparseable JSON, a non-string body, and a throwing `localStorage`). `jest features/(mail|inbox)` → **9 suites / 54 pass**. `tsc --noEmit` exit 0; `eslint` on all 4 changed files → **0 problems**.
  S9 UPDATE — **this box is now closed.** The three remaining items are done and each is proven by a mounted test, not a source scan.
  **Thread hydration now seeds the detail cache.** New `features/mail/mail-thread-seed.ts` converts the list row into a `MailMessageDetail` and writes it to `queryKeys.mail.thread(accountId, threadId)` — or `queryKeys.mail.message(...)` for a threadless row — from `mail-shell.tsx`'s open handler. Getting the invalidation right was the whole risk: the seed is written with `{ updatedAt: 0 }`, so the entry is **already expired** against `useMailThread`'s `staleTime: 2 * 60_000` and `refetchOnMount` fires the real fetch immediately. It also refuses to overwrite a thread the cache already holds. `MailReadingPane` now derives `isHydrating` from `isFetching` + a body-less message and renders a body skeleton instead of a premature "No content", so the open is subject-first rather than skeleton-first. Proof: `mail-thread-seed.test.tsx` → **6 pass** (incl. a BITE case: seeding without the expired timestamp never refreshes — the stale-data bug) and `mail-thread-hydration.test.tsx` → **5 pass** (MailShell writes the key on click; `dataUpdatedAt === 0`).
  **The offline hook moved to a shared location and both features use it.** Rather than adding a second connectivity hook beside `hooks/common/use-online-status.ts`, `features/mail/use-mail-connectivity.ts` was **deleted** and `useOnlineStatus` was rewritten onto `useSyncExternalStore` over the window `online`/`offline` events. One shared hook, no feature→feature import, one fewer file, and the pre-existing `react-hooks/set-state-in-effect` warning on that hook is gone. `features/inbox` now has real offline handling: the new `features/inbox/use-inbox-actions.ts` holds every mutation handler behind a connectivity guard (archive/unarchive/delete/pin/snooze/approve/reject refuse and say why; the *passive* mark-read on open is skipped silently rather than nagging), and `isOnline` reaches `InboxVirtualList` so "Load more" is disabled instead of silently stalling on a fetch TanStack pauses while offline. Proof: `inbox-offline.test.tsx` → **6 pass**, including the reconnect case and the online baseline that shows the guard is not a blanket block.
  **The compose sheet is now mounted in a test — and it found a real defect.** `MailComposeSheet` had never been rendered under Jest; ticket 29 proved only `mail-draft-storage.ts`. Mounted, the compose draft **was saved on Escape and never restored**: the restore branch was `mode.type === "compose" && (prev.type === "reply" || !open)`, and `!open` is unreachable (the effect returns above it) while `prev.type` is `"compose"` on every reopen — so only a reply→compose transition ever restored. The effect now keys on a closed→open transition or a change of `mailDraftKey(mode)`. Proof: `mail-compose-sheet.test.tsx` → **6 pass** (Escape preserves + restores, Discard clears, offline refuses the send and keeps the draft, reconnect sends it, a failed send keeps it and offers Retry). Restoring the original effect turns **2 red**.
  **Sanitization is now proven on the render path, not just in isolation.** `mail-html-viewer.test.tsx` mounted the viewer directly and `mail-html-render-boundary.test.ts` is a source scan; neither showed that a thread body actually reaches the sanitizer. New `mail-body-sanitization.test.tsx` renders `MailReadingPane` over a thread carrying `<script>`, `onerror`, a `javascript:` href, an `<iframe>` and a tracking pixel: all neutralised in the real DOM, no global set, remote image held back, legit link hardened with `target="_blank" rel="noopener noreferrer"`. **5 pass**, incl. a BITE case asserting the payload is genuinely hostile.
  **Optimistic read/label rollback: audited and satisfied on both sides.** `useMailAction` snapshots every mail-list and unified-inbox page in `onMutate` and restores all of them in `onError`; every optimistic notification mutation in `hooks/api/notifications-inbox.ts` (mark-read, archive, unarchive, delete, pin, unpin, snooze, and the three bulk variants) carries an `onError` that restores both the list snapshots and the unread count. No unrollbacked optimistic update remains in either feature.
  Also fixed here: a **circular import** `mail-compose-sheet.tsx ↔ mail-draft-storage.ts` introduced by the S8 pass (the storage module imported `MailComposeMode` back from the component). `MailComposeMode` moved to `mail-compose-schema.ts` per root §9 rule (1); `madge --circular` is back to **0**. And `features/inbox` + `features/mail` are now **lint-error-free** (12 errors → 0): six `no-raw-visual-values` violations (`text-[13px]`/`text-[11px]`/`text-[9px]` → `text-label`/`text-dense`/`text-micro`), four `react-hooks/use-memo` violations, two unused type imports, one `exhaustive-deps` warning.

- [x] **Knowledge:** spaces, memberships, documents/pages, immutable revisions, attachments, ingestion jobs, chunks/embeddings and deletion/reindex state carry tenant-composite integrity.
  S11 UPDATE — **closed. Attachments were the last gap and they now have a row.** `KbMediaService.upload` uploaded to R2 and returned a key with **zero database writes**: the only tenancy the object carried was the `kb-media/<org_id>/` prefix inside its own key, which is a naming convention, not a constraint. There was nothing to give integrity to, which is why S10 could not close this.
  **Decision: creating the row was in scope, and only the row.** The object's storage *lifecycle* (GC, retention, erasure) is ticket 33's subject and could not be built at all while nothing in the database knew an object existed.
  New `kb_page_attachments` (migration `1042_t29_kb_page_attachments`), the deliberate wiki twin of `kb_article_attachments`: `(org_id, id)` tenant anchor · a **composite** `(org_id, page_id) → kb_pages(org_id, id)` FK, because a single-column `page_id` FK would let a row in org A name a page in org B and still be satisfied · `page_id` nullable on purpose, because `/kb/media` accepts an upload with no page and a composite FK with a NULL member is unenforced under MATCH SIMPLE — that is the intended "not attached" state · a **per-org** unique on `file_key`, so a retried multipart POST cannot mint a second row for one object and one tenant's key value cannot block another's · `deleted_at` with a partial index.
  Also fixed here: **the `pageId` in the request body was never checked.** It now resolves against the caller's org with `deleted_at IS NULL` **before** any bytes are uploaded, and a miss is **404, never 403**.
  Proof, against a real catalog on `scratch_t29` rather than against Drizzle: org A → org B's page **rejected 23503**; duplicate `(org_id, file_key)` **rejected 23505**; org B reusing org A's key *value* inserts (no cross-tenant DoS); NULL `page_id` inserts; a hard page delete cascades the row. Spec: `kb-media.service.spec.ts` → **34 pass** (was 27), incl. 2 BITE cases; removing `assertPageInOrg` turns **3 red**. Whole kb module: **89 suites / 571 tests, exit 0**.
  Remaining, cross-territory and *not* a tenant-integrity gap: `uniq_kb_chunks_article_revision` / `_page_revision` are **live** but undeclared in Drizzle (`schema/support/kb-chunks.ts`). The constraints exist; only the declaration is missing, and `drizzle-kit generate` is unusable in this repo anyway. Reported for whoever owns `schema/support/**`.
  Also reported, for ticket 33: the new FK's `ON DELETE CASCADE` removes the ledger row but **not** the R2 object at `kb-page-tree.service.ts::emptyTrash` and `::purgeExpired`. Those objects are orphaned today as well — there is no row at all — so this is not a regression, but a purge sweep now has something to read.
  S10 UPDATE — schema and migrations were in territory. Every named gap re-verified against `pg_indexes`/`pg_constraint` on the seeded database rather than against these notes, because two of them had already been fixed:
  ✅ **Bare single-column FK on `kb_space_grants` — STALE in SQL, REAL in Drizzle.** `0965_ar02_canonical_tenant_fks_3.sql:392-401` already adds the composite `fk_kb_space_grants_space_id_org (org_id, space_id) → kb_spaces(org_id, id)` and `0972_ar02_drop_superseded_tenant_fks_3.sql:55` already drops the single-column form; both journalled and applied. What was still true is the second half of the sentence: the Drizzle table declared **no FK at all**, so the next `db:generate` would have proposed dropping a live tenant constraint. Declaration fixed under the name the live constraint already carries; **no migration owed**, and 0348's stated reason for keeping it SQL-only (a Drizzle cycle) does not hold at head — `madge --circular` → 0.
  ✅ **`kb_ingestion_checkpoints` has no uniqueness — STALE.** `uniq_kb_ingestion_checkpoint (org_id, content_type, content_id, chunk_index)` exists in `migrations/0701`, in Drizzle, and live. (The table is also under `schema/support/`, not `schema/kb/`, and its only service is in `kb/retrieval/**` — outside this session's territory either way.)
  ✅ **Record links had no uniqueness — REAL, fixed.** `uniq_kb_page_links_source_target` is `(source_page_id, target_page_id)` and a record link leaves `target_page_id` NULL, so it constrained the record grain not at all. Migration `1021` adds the partial unique the service dedupes on, and `add()` now inserts and maps 23505 → 409 instead of racing a read.
  ✅ **`kb_space_members` and `kb_page_templates` composite uniques — REAL, fixed.** Migration `1040`. `kb-members.service.ts:90-105` was the same racing read-then-insert on two independent grains; both now have a partial unique (each on its own column, so a role grant and a membership grant still coexist in one space) and the service reports the right 409 per constraint. `kb_page_templates` gets `(org_id, name)` — `list()` orders by name and shows nothing else, so two same-named templates were indistinguishable — plus a 200-row cap on a list that had no limit at all. Verified by inserting through the real constraints on `scratch_t29`: both duplicates raise 23505; another org's same-named template, a role grant beside a membership grant, and a second member all insert.
  ✅ **Polymorphic pointers cannot be made referential, and here is why rather than a silent gap.** `target_type` spans seven entity types in five modules (`RECORD_LINK_TARGET_TYPES`) with a `text` `target_id`, because those tables share no key type. An exclusive arc would put seven nullable FKs on `kb_page_links` reaching into five other modules' schemas, which backend/CLAUDE.md §1 forbids. It stays what §3 grandfathers — a display/dedupe pointer — and it satisfies the condition §3 attaches: nothing resolves, joins or cascades a record through it (`listByRecord` joins only `kb_page_links → kb_pages` on its own composite tenant FK).
  ❌ **`uniq_kb_pages_public_token` is NOT a defect — this note was wrong.** `getPublicPage(token)` (`kb-pages.service.ts:370-390`) resolves by token alone on a `@Public()` route, so the token *is* the tenant selector and must be globally unique and globally indexed; making it `(org_id, public_token)` would break the only lookup that uses it. It is 24 random bytes, not a tenant-supplied business key, so the cross-tenant DoS §3 guards against does not apply. Left alone deliberately.
  ❌ **`kb_article_restrictions` — no constraint added, deliberately.** There is no writer for that table anywhere in `src/modules/` (only `kb-access.service.ts`, `kb-candidate.service.ts` and `support-ai-triage-data.service.ts`, all reads), so a unique would back no assumption and guess at a natural key nothing asserts.
  BLOCKED: **attachments** — `kb-media.service.ts` still writes to object storage and returns a key with zero DB writes: no row, so no `org_id`, no tenant FK, no soft delete. That is a missing table and service, not an integrity fix, and it is what keeps this box open.
  PARTIAL: **declared-vs-live drift on the chunk revision uniques** — `uniq_kb_chunks_article_revision` / `_page_revision` are live but undeclared in Drizzle. They are on `kb_article_chunks`, declared in `db/schema/support/kb-chunks.ts`, outside this session's `schema/kb/**` territory. (`idx_kb_page_templates_org` from the note below does **not** exist live; that half was stale, and `uniq_kb_page_templates_org_name` is org-leading so no separate index is owed.)
  Audited in full; **immutable revisions is satisfied** (`kb_page_versions` / `kb_article_versions` are append-only — zero non-spec `update`/`delete` against either; restore snapshots the pre-restore state and then writes a *new* version: `kb-page-versions.service.ts:121-144`, `kb-articles.service.ts:342-355`). Remaining gaps, each needing a migration:
  - **Bare single-column FK:** `kb_space_grants.space_id → kb_spaces(id)` (`migrations/0348_drop_polymorphic_tables.sql:69-76`); the Drizzle table declares no FK at all (`db/schema/common/access.ts:404-433`).
  - **Polymorphic pointers with no FK:** `kb_ingestion_checkpoints` (`content_type` + `content_id`), and record links stored as `kb_page_links.target_type`/`target_id` — there is no `kb_page_record_links` table at all, and the polymorphic rows have **no uniqueness**, so `kb-page-record-links.service.ts:34-62` dedupes with a racing read-then-insert.
  - **Missing composite uniques:** `kb_space_members` (same member twice in a space), `kb_article_restrictions` (duplicate rows), `kb_page_templates` (no `(org_id, name)`).
  - **Bare global unique:** `uniq_kb_pages_public_token` (`db/schema/kb/pages.ts:76`) — one tenant's token value blocks every other org.
  - **Declared-vs-live drift:** the two chunk revision uniques (`uniq_kb_chunks_article_revision` / `_page_revision`, `migrations/0510`) and `idx_kb_page_templates_org` exist in SQL but not in Drizzle, so the next `db:generate` proposes dropping them.
  - **No KB media row exists at all:** `kb-media.service.ts` writes to object storage and returns a key with zero DB writes — no `org_id`, no tenant FK, no soft delete.

- [ ] **Knowledge queries/workers:** revision and search plans, ingestion leases with retry and DLQ, chunk dedupe, permission-aware cache keys, purge and reindex.
  PARTIAL: three of the four remaining items are still open and all three live in `modules/kb/retrieval/**`.
  S11 UPDATE — **one of the four is fixed.** `indexArticle` returned unconditionally on an unchanged content hash (`kb-indexing.service.ts:202`), so an article whose ACL moved without its text kept the **old `acl_revision`** on every chunk. The candidate gate joins `acl_revision` with `=`, so those chunks matched nothing and the article dropped out of retrieval **entirely** until somebody edited its body. It fails *closed*, so it lost recall rather than leaking — but it lost it silently, and only space-level changes were repaired out-of-band by `syncAclRevisionForSpace`. `indexPage` already had the branch; the article path is now its twin, moving the ACL columns in place without re-embedding. Proof: new `kb-article-acl-only-reindex.spec.ts` → **5 pass**, including a BITE pinning that the written revision is the article's and not the stale stored one, and one asserting a changed body still takes the full re-embed path; reverting the branch turns **3 red**.
  STILL OPEN (3): `kb.content.delete` is dead code (nothing emits the event, so purging depends entirely on non-outbox-backed in-request `tx.delete`); KB space soft-delete purges nothing; and the search plan is split-brain (`kb-search.service.ts` goes through `app.search_kb_article_ids` over the FTS GIN index, while `kb-article-query.service.ts:67-71` is a leading-wildcard `ilike` on title+excerpt — the pattern BE/CLAUDE.md §3 bans — so `GET /kb/articles?search=` and the search endpoint disagree on the same term).
  Verified satisfied: lease TTL 300s with a Lua compare-and-delete release (`kb-ingestion-lease.service.ts:6,7-8,30,52`); retry with exponential backoff capped at 60s and dead-lettering at 8 attempts (`common/outbox/outbox-envelope.ts:8,38-46`, `outbox-publisher.service.ts:210-245`); chunk dedupe hashes the **source** text, not the rejoined chunks (`kb-indexing.service.ts:180,289` via `kb-chunk-utils.ts:4-6`), skips re-embedding on an unchanged hash, and updates ACL columns in place when only the ACL moved; purge-on-soft-delete, de-index-on-archive and reindex-on-restore all present with covering specs.
  S8 RE-AUDIT — two of these five are now stale and one is fixed here:
  - ~~The lease fails open~~ — **already fixed before this session.** `kb-ingestion-lease.service.ts` returns a discriminated `{status: "acquired"|"contended"|"unavailable"}` with no boolean at all; no-Redis and Redis-error both return `unavailable` and refuse to index. TTL 300s and the Lua compare-and-delete release are both present. Verified against current source.
  - ~~A space-property ACL change never reindexes~~ — **already fixed before this session.** The sync moved inside `KbIndexingService.bumpSpaceAclRevision`, which both spaces and members now call symmetrically.
  - **Lease contention burning the retry budget — FIXED HERE.** `kb-ingestion-consumer.ts` threw `KB_INGESTION_LEASE_CONTENTION` as an ordinary error, and the publisher has no error classification at all (`outbox-envelope.ts` exports only `nextRetryDelayMs`/`shouldDeadLetter`; there is no defer mechanism to reuse), so a heavily-edited page could dead-letter after 8 contentions **without ever failing to index**. Contention now suppresses the delivery instead: the lease is held for the exact same `(org, contentType, contentId)`, the indexer re-reads the live row, and every content mutation emits its own `kb.content.index` — so no write is lost by suppressing a duplicate. Proof: `jest kb-ingestion-consumer` → **20 pass**, including a BITE that drives 7 successive contended deliveries and asserts none consumes a retry.
  STILL not satisfied, for whoever owns retrieval next:
  - **`kb.content.delete` is dead code.** `KbIngestionDeleteConsumer` implements a full four-way durable purge; nothing anywhere emits that event. Purging depends entirely on in-request `tx.delete` calls that are not outbox-backed.
  - **An article whose ACL moved but whose text did not never has its chunk ACL updated.** `kb-indexing.service.ts:202` returns unconditionally on an unchanged content hash. The page twin (`:289-324`) correctly detects an ACL-only change and updates the chunk columns in place without re-embedding; the article path has no such branch and no test. Space-level changes are repaired out-of-band by `syncAclRevisionForSpace`, but an article-level restriction change is not.
  - **KB space soft-delete purges nothing.** `kb-spaces.service.ts` `remove()` sets `deletedAt` and busts the access cache; it neither de-indexes nor purges chunks for the space's pages and articles.
  - **Search plan split-brain.** `kb-search.service.ts` correctly goes through `app.search_kb_article_ids` (SECURITY DEFINER, org from `app.current_org_id()`, ids only, capped) over the `idx_kb_articles_fts` GIN index. But `kb-article-query.service.ts:67-71` is a leading-wildcard `ilike` on title+excerpt only — the pattern BE/CLAUDE.md §3 bans — so `GET /kb/articles?search=` and the search endpoint return different results for the same term.

- [x] KB cache keys include the ACL dimension as a **required** field. An optional ACL version produces an ACL-blind cache key, and the revision gate's null branch skips the check entirely.
  `KbAccessService.getAccessibleSpaceIds` cached the accessible-space list under `(kb:acc-spaces:<orgId>, userId)` — no ACL dimension at all. It is invalidated only by space and member mutations (`kb-spaces.service.ts`, `kb-members.service.ts`), never by `bumpPermissionsVersion`, so a user who lost `kb:spaces:manage` or the role granting space membership kept the admin-wide list for the full 60s TTL — and every KB read path plus `kb-ask` RAG uses it. New `modules/kb/core/kb-acl-cache-key.ts` makes `{ permissionsVersion, membershipId }` a required field and throws on an unresolved version; `computeAccessibleSpaceIds` now takes that same dimension, so a value can never be computed under a dimension the key does not carry.
  Proof: `jest src/modules/kb/core/kb-acl-cache-key.spec.ts` → 6 pass. Reverting the key to `user.userId` turns **2 red**, including the concrete leak (a revoked admin still served the restricted space).
  The revision gate's null branch was checked read-only and is already fail-closed: `kb-candidate.service.ts:95,161` join on `=`, so a NULL `acl_revision` never matches (`kb-acl-revision-gate.spec.ts:79-94`), and the optional `aclRevision` in `kb-ingestion-consumer.ts:27` is inert — the indexer re-reads it from the live row.

- [x] Knowledge comment permissions follow the approved model, rechecking current page and article visibility at the data seam on every action.
  Verified satisfied on both comment surfaces and now pinned: authors edit/delete their own (`kb-comments.service.ts:81-99`, `kb-page-comments.service.ts:97-118`), KB administrators moderate (`kb:spaces:manage` / `kb:pages:manage`), editors resolve (`assertArticleEditable`; route key `kb:pages:update`), and **every** action re-reads current visibility at the seam.
  Proof: new `src/modules/kb/help-centre/kb-comment-permission-model.spec.ts` → 8 pass. Stripping the visibility recheck from the two `update` paths turns **4 red**.
  P2 fixed alongside: `KbPageCommentsService.list` was completely unpaginated (no limit, no cursor) — every comment on a page, every open. Now keyset-paged at 50 with `afterCreatedAt`/`afterId`, matching its article sibling. And `KbCommentsService.list` paged on `(createdAt, id)` while ordering by `createdAt` alone, so rows sharing a timestamp could be skipped at a page boundary; `asc(id)` added.

- [x] A cursor value of undefined vanishes in JSON serialization and replays the first page forever — assert the cursor survives the round trip.
  New `src/modules/mail/mail-cursor-roundtrip.spec.ts` → 5 pass: a Gmail token, an Outlook skip and a `{token, skip}` partial all survive byte-identically; an exhausted account's explicit `null` survives; falsy-but-real positions (`""` token, `0` skip) survive; four successive re-encodings are a fixpoint; and a BITE case pins that an `undefined` position *does* vanish, which is why `mail.service.ts:136-141` must write `null`. No cursor pagination exists in calendar or KB comments, so nothing else is exposed.

## Also closed (from the ticket-14 handover)

- [x] `pnpm -s check:tenant-isolation` reaches 924/924.
  `check:tenant-isolation` → **924 / 924, exit 0**. The declaration existed on arrival but its primary cross-tenant case did not bite: the harness applied an org filter only when the predicate bound one, so *removing* the org clause narrowed the result to nothing instead of exposing the other tenant. `calendar-provider-webhook-tenant-isolation.spec.ts` was rewritten so a predicate that binds no org applies no org filter — the real database behaviour. Actors are built through `humanSessionPrincipal` + `actingMembershipId` and asserted non-owner; the sync-queue and creator-membership lookups are now modelled too.
  Proof: 9 pass. Stripping all three `org_id` predicates from the service turns **4 red**, including the case that previously passed. Two permanent in-suite BITE tests assert the concrete leak (org B's event, connection and creator re-queued under org A; org B's in-flight row discarding an org A webhook).
