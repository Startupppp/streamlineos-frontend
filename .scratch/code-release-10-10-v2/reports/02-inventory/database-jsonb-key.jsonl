{"registry":"database.jsonb-key","item":"(sql-template)->>'occurrenceStart'","owner":"see evidence site","verdict":"REFACTOR","failurePrevented":"PRD-C058: a JSONB property that is filtered or joined in SQL with no expression index forces a full scan and a per-row JSON parse, and it cannot be constrained, so a typo writes a silently unreachable row. Normalise it to a column or index the expression.","evidence":"backend:src/modules/calendar/calendar-provider-sync-sweep.service.ts:386"}
{"registry":"database.jsonb-key","item":"(sql-template)->>'userId'","owner":"see evidence site","verdict":"REFACTOR","failurePrevented":"PRD-C058: a JSONB property that is filtered or joined in SQL with no expression index forces a full scan and a per-row JSON parse, and it cannot be constrained, so a typo writes a silently unreachable row. Normalise it to a column or index the expression.","evidence":"backend:src/modules/calendar/calendar-sync-status.service.ts:101"}
{"registry":"database.jsonb-key","item":"(sql-template)->>'twitter'","owner":"see evidence site","verdict":"REFACTOR","failurePrevented":"PRD-C058: a JSONB property that is filtered or joined in SQL with no expression index forces a full scan and a per-row JSON parse, and it cannot be constrained, so a typo writes a silently unreachable row. Normalise it to a column or index the expression.","evidence":"backend:src/modules/contacts/contact-party-reader.ts:64"}
{"registry":"database.jsonb-key","item":"(sql-template)->>'moduleKey'","owner":"see evidence site","verdict":"KEEP","failurePrevented":"Filtered in SQL and backed by an expression index, so the predicate is a lookup rather than a scan of every row's payload.","evidence":"backend:src/modules/module-access/module-access.service.ts:309"}
{"registry":"database.jsonb-key","item":"(sql-template)->>'email'","owner":"see evidence site","verdict":"REFACTOR","failurePrevented":"PRD-C058: a JSONB property that is filtered or joined in SQL with no expression index forces a full scan and a per-row JSON parse, and it cannot be constrained, so a typo writes a silently unreachable row. Normalise it to a column or index the expression.","evidence":"backend:src/modules/organization/hierarchy/org-hierarchy-branches.service.ts:125"}
{"registry":"database.jsonb-key","item":"(sql-template)->>'address'","owner":"see evidence site","verdict":"REFACTOR","failurePrevented":"PRD-C058: a JSONB property that is filtered or joined in SQL with no expression index forces a full scan and a per-row JSON parse, and it cannot be constrained, so a typo writes a silently unreachable row. Normalise it to a column or index the expression.","evidence":"backend:src/modules/organization/hierarchy/org-hierarchy-locations.service.ts:86"}
{"registry":"database.jsonb-key","item":"business_parties.social_profiles->>'twitter'","owner":"see evidence site","verdict":"REFACTOR","failurePrevented":"PRD-C058: a JSONB property that is filtered or joined in SQL with no expression index forces a full scan and a per-row JSON parse, and it cannot be constrained, so a typo writes a silently unreachable row. Normalise it to a column or index the expression.","evidence":"backend:src/modules/record-layouts/record-layout-catalog.ts:169"}
{"registry":"database.jsonb-key","item":"(sql-template)->>'escalated'","owner":"see evidence site","verdict":"REFACTOR","failurePrevented":"PRD-C058: a JSONB property that is filtered or joined in SQL with no expression index forces a full scan and a per-row JSON parse, and it cannot be constrained, so a typo writes a silently unreachable row. Normalise it to a column or index the expression.","evidence":"backend:src/modules/support/core/support-ai-report.helper.ts:25"}
{"registry":"database.jsonb-key","item":"(sql-template)->>'sources'","owner":"see evidence site","verdict":"REFACTOR","failurePrevented":"PRD-C058: a JSONB property that is filtered or joined in SQL with no expression index forces a full scan and a per-row JSON parse, and it cannot be constrained, so a typo writes a silently unreachable row. Normalise it to a column or index the expression.","evidence":"backend:src/modules/support/core/support-ai-report.helper.ts:26"}
{"registry":"database.jsonb-key","item":"(sql-template)->>'liveSessionId'","owner":"see evidence site","verdict":"KEEP","failurePrevented":"Filtered in SQL and backed by an expression index, so the predicate is a lookup rather than a scan of every row's payload.","evidence":"backend:src/modules/surveys/survey-live-participant.service.ts:24"}
