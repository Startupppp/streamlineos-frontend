{
  "ticket": "18",
  "audited_at": "2026-09-04",
  "backend_head": "e1fd1d475 (main, 4 baseline JSON files modified in tree)",
  "frontend_head": "31c3786a6 (main)",
  "criteria": [
    {
      "id": "PRD-C065",
      "verdict": "OPEN",
      "evidence": "check:tenant-indexes exits 0 with 840/840 tenant tables over 347 schema files (/tmp/beg-check_tenant-indexes.log), so the index clause holds; but /tmp/beg-check_query-projections.log reports 1377 unprojected reads against a ratchet ceiling of 1383, and contracts/benchmark-manifest.json records planAssertionFailures naming 'dashboard-announcements: announcements resolved by Seq Scan' and 'attendance-mine: attendance resolved by Seq Scan' plus a scanRowViolation 'attendance scanned 10188 rows > maxScanRows 200' — 39 of 370 measured slots carry a Seq Scan planSignature and test/perf/benchmark-plans/approved-complex-large.txt retains 13.",
      "blocker": "Explicit projections are a ratchet with 6 slots of headroom, and the repo's own benchmark artifact records two named required Seq Scans plus a 10188-row scan violation.",
      "clauses_failing": "explicit projections; no required full tenant/table scan"
    },
    {
      "id": "PRD-C066",
      "verdict": "CLOSED",
      "evidence": "test/perf/heavy-query-catalog.mjs exports CATEGORIES = exactly reminder|export|fanout|unread|free/busy|recurrence|search/vector|dashboard with 36 named queries; cross-mapping those ids against contracts/benchmark-manifest.json shows every one of the 8 categories has measured slots (reminder 4/4, export 1/1, free-busy 3/3, recurrence 2/2, fanout 2/3, unread 6/8, search/vector 6/8, dashboard 6/7), instrument 'EXPLAIN (ANALYZE, BUFFERS, VERBOSE) as streamline_app' on scratch_perf_seed 1702 MB, and none of the manifest's 6 vacuous 0-row benchmarks is a catalog entry.",
      "blocker": "",
      "clauses_failing": "CAVEAT (does not defeat the criterion): the catalog's reminder-recurring-candidates SQL is 'LIMIT 200 with no ORDER BY' while calendar-reminder-sweep.service.ts now uses orderBy(asc(id)) + gt(id, afterId) keyset, so that one entry measures a shape the service no longer issues; the manifest overall is 366 commits stale but heavy-query-catalog.mjs is NOT one of the two catalogs the gate reports as DRIFTED."
    },
    {
      "id": "PRD-C067",
      "verdict": "OPEN",
      "evidence": "Tenant+subject hold structurally (frontend/lib/query-scope.ts:29-32 hashes every TanStack key as `authenticated:<orgId>:<userId>`; of 137 CACHE_KEYS entries only 7 take no orgId and each is user- or org-derived). The proof does not: I ran `node src/scripts/check-cache-key-shapes.mjs` and `--self-test` — both exit 0 emitting 0 bytes, because the 339-line script has no main/process.argv/process.exit and is a pure library; and src/common/cache/cache-key-dimension-registry.spec.ts:113 asserts `DIMENSION_REGISTRY.length >= 10` over exactly 10 of 137 factories.",
      "blocker": "The gate named for this criterion examines nothing (0 bytes, 0 files) and its own anti-vacuity self-test is equally vacuous; the dimension contract covers 10/137 factories (7.3%) and ~15/143 invalidation-matrix entries with the floor set at the current population (cache-invalidation-matrix.spec.ts:132 `>= 8`). Permission and resource dimensions are unproved for 90%+ of keys.",
      "clauses_failing": "the permission/resource dimension proof; nothing verifies the tenant/subject dimension either"
    },
    {
      "id": "PRD-C068",
      "verdict": "OPEN",
      "evidence": "Stampede (in-process single-flight cache-fill.ts:143-155, nx fill lease :242-254, token-checked Lua CAS release :275-281), TTL jitter applied where the TTL is resolved (:322-325 -> jitterTtl :178-180), no negative caching (:266 never writes null) and a bounded 1s/2000-key outage memo all hold. But CacheService.invalidate (cache.service.ts:165-169) calls fill.drop(key) then redis.del(key), while drop() (cache-fill.ts:109-113) only clears in-process maps — an in-flight fill past its miss still runs redis.set(key, data, {ex}) at cache-fill.ts:271 after the DEL, restoring the pre-mutation value for a full TTL. No epoch/version token guards that write and grep of src/common/cache/*.spec.ts for mid-fill/race/epoch finds no covering spec.",
      "blocker": "Write-after-delete lost-invalidation race: a mutation's DEL landing mid-fill is overwritten by the in-flight fetcher, so a revoked value can survive a full TTL. Unguarded and unspecced.",
      "clauses_failing": "mutation/revocation invalidation never preserving revoked access"
    },
    {
      "id": "PRD-C069",
      "verdict": "OPEN",
      "evidence": "/tmp/beg-check_route-budgets.log (exit 0) states in its own output: '93/3648 operations carry a budget (2.5%)'; 'clause (d) DERIVED from 132 operations on @Controller(\"cron\") classes: 68 scheduled batches — 23 declare a budget (33.8%), 2 out of scope, 43 UNDECLARED'; 'By field: DbCalls 3/93 · BatchSize 0/23 · DurationMs 0/23'. The regression half, test/perf/route-db-call-budget.e2e-spec.ts, covers 2 routes (GET /notifications, GET /notifications/unread-count) with 1 negative control and describe.skip's entirely without APP_DATABASE_URL (:35-44). check:route-budgets-http and check:benchmark-manifest both exit 1 with declared budget breaches.",
      "blocker": "43 scheduled worker batches carry no ceiling of any kind; measuredDbCalls is populated on 3 of 93 budgets and 0 of 23 worker batches; BatchSize and DurationMs are 0/23 measured; the regression assertion covers 2 routes.",
      "clauses_failing": "a maximum for EVERY critical route and worker batch; regression tests failing on unexpected calls beyond 2 routes"
    },
    {
      "id": "PRD-C070",
      "verdict": "OPEN",
      "evidence": "The seam holds (runInTenantTransaction reuse; src/db/borrow-scope.ts statement clock; check:transaction-callbacks exit 0 over 491 doubles / 276 invoking / VOID 2). But nested per-row transactions still exist — src/modules/hr/import/hr-import.service.ts:205 `const ref = await tx.transaction(async (rowTx) => {` inside a growing loop, plus finance/ap/payment-run-executor.service.ts:94->122 and hr/onboarding/flow/module-checklist.service.ts:174 as `handle.transaction` per row in /tmp/beg-check_n1-growing-loops_list.log — and check:placement-bypass exits 0 while excusing exactly 45 [provider-in-transaction] handlers whose reason is literally 'PRE-EXISTING, UNAUDITED' (grep -c on /tmp/beg-check_placement-bypass.log).",
      "blocker": "Nested/per-row transactions remain (hr-import.service.ts:205 is a literal tx.transaction inside a row loop), and 45 handlers hold the request transaction across a provider call with the gate passing over them.",
      "clauses_failing": "never open nested/per-row transactions; minimum correct tenant transaction. NOTE the prior report's F1 (calendar-conflict.service.ts unbounded org-wide drain + LIMIT-less exceptions read) is FIXED at main — the drain is SQL-filtered by mineOrAttending (:50-70) and the exceptions load carries exceptionsInWindow(startDate,endDate) (:171-176)."
    },
    {
      "id": "PRD-C071",
      "verdict": "OPEN",
      "evidence": "The named prohibitions are all at zero (/tmp/beg-check_query-projections.log: unprojected COUNT paths 0, AST existence/COUNT 0 in scope, full-row reads of the 5 vector/tsvector/bytea tables 0; /tmp/beg-check_relation-hydration.log: onto global users 0, onto a credential-bearing table 0). The general rule is not met: 1377 unprojected reads (findMany 287 · findFirst 488 · bare .select() 602) against ceiling 1383, plus 186 unprojected relation hydrations at ratchet 186, and the gate's own closing line 'this is a ratchet, not a clean repository' with 82 colliding const names it admits it cannot resolve.",
      "blocker": "1377 reads still hydrate whole rows and 186 relation hydrations are unprojected; the gate enforces a population ceiling, not the rule.",
      "clauses_failing": "select named columns only / return minimal DTO projections for list paths"
    },
    {
      "id": "PRD-C072",
      "verdict": "OPEN",
      "evidence": "/tmp/beg-check_n1-growing-loops.log exits 0 while reporting 'GROWING (one round trip per row): 100 site(s) across 75 file(s) [ratchet 102]' plus 37 more in excluded crm/inventory, and closes with 'this is a ratchet to drive to zero, not a clean repository'. /tmp/beg-check_db-call-count.log exits 0 while reporting 'ACTIONABLE: 32 file(s) (51 call site(s) to fix)' and naming 3 ACTIONABLE-UNDETECTED N+1s (hr-effective-change-applier, leave-approver, approvals-bulk); 2172 of 5085 loop openers (43%) were skipped uninspected.",
      "blocker": "100 growing-loop database/cache round-trips-per-row remain, 51 more actionable N+1 call sites, 3 the patterns cannot see, and 43% of loop openers are never inspected. Both gates pass as ratchets, not as compliance.",
      "clauses_failing": "forbid database/cache calls inside growing loops"
    },
    {
      "id": "PRD-C073",
      "verdict": "OPEN",
      "evidence": "check:record-access exits 0 over 1193 findFirst calls but proves only the soft-delete half; nothing enforces LIMIT 1. src/modules/accounting/settings/accounting-settings.service.ts:52-58 still answers 'is there at least one POSTED journal entry?' with `.select({ total: count() }).from(journalEntries).where(and(eq(orgId), eq(status,\"POSTED\")))` compared `> 0` — a full tenant count for an existence question; at least 7 further files in src/modules compare a count() result against 0 (leads-reports, ai-feedback, hr-performance-ai, inv-warehouses, hr-interviews, payroll components, payroll checklist).",
      "blocker": "Existence probes still run COUNT(*) over the whole tenant partition instead of a LIMIT 1 indexed probe, and no gate enforces the LIMIT 1 clause.",
      "clauses_failing": "LIMIT 1 on existence/authorization probes; do not fetch counts when only existence is required. NOTE the prior report's additional C073 blocker — 'F1's exceptions read has no LIMIT at all, calendar-conflict.service.ts:117-132' — is untrue at main: that query now carries exceptionsInWindow(startDate,endDate) at :171-176."
    },
    {
      "id": "PRD-C074",
      "verdict": "OPEN",
      "evidence": "src/common/pagination/pagination.ts:8 declares `total: number` non-optional on ListResponse<T>; :21 `buildListResponse<T>(items: T[], total: number, {page,pageSize})` takes total as a required positional; src/common/pagination/list-query.schema.ts:45-50 defines baseListQuerySchema as {page, limit, cursor, sortDir} with no total-opt-in field, so no endpoint in the product can be opt-in. grep for includeTotal|withCount|include_total|with_count over src/ returns hits only at build/core/projects-work-query.service.ts:274,290,319,338, fed a derived isFirstPage heuristic rather than a DTO field.",
      "blocker": "No opt-in field exists anywhere in the shared list-query schema, and total is structurally mandatory on every list response. window-count.ts's totalOverWindow (count(*) OVER ()) has 3 call sites and still computes a total on every request.",
      "clauses_failing": "make exact totals opt-in; independently budgeted; cursor pages must not run an automatic count"
    },
    {
      "id": "PRD-C075",
      "verdict": "OPEN",
      "evidence": "Conflict arbiters hold (/tmp/beg-check_conflict-targets.log exit 0: 367 onConflict, 163 explicit, 161 resolved, 15 partial-index, 2 unresolved and both in specs), and documented chunks exist (INVALIDATE_KEY_CHUNK=256, OUTBOX_INSERT_CHUNK=500). But check:bulk-id-limits reports 'scanned 3411 schema files; no unbounded id-array properties found' while its own header (src/scripts/check-bulk-id-limits.mjs:3) limits it to 'every z.array(...) property whose name is `ids` or ends in `Ids`' — so accounting.schemas.ts:56-70 `lines: z.array(...).min(2)` with no .max() and invoices/dto/invoice-write.schemas.ts:38 `items: z.array(itemSchema).min(1).optional()` are invisible; 335 of 497 z.array( sites in src/modules carry no .max() on the same line. Per-row writes also persist (100 growing-loop sites incl. handle.insert/update/transaction per row).",
      "blocker": "The bulk-input gate is clean only over a name-filtered corpus (`ids`/`*Ids`), so the real uncapped attacker-controlled arrays (journal lines, invoice items) are unmeasured; and one-write-per-row loops remain.",
      "clauses_failing": "bounded bulk insert/update instead of one write per row; transactional batches below documented payload limits"
    },
    {
      "id": "PRD-C076",
      "verdict": "OPEN",
      "evidence": "Six of seven JS-side counter increments are compare-and-swap or sql`col + 1`. The seventh is a lost update: src/modules/hr/config/hr-document-templates.service.ts:140-151 sets `version: existing.version + 1` with `.where(and(eq(orgId), eq(id)))` and no version predicate, and `existing` arrives as a caller-supplied row read outside the transaction (signature at :101 `updateVersion(userId, existing: TemplateRow, input)`). check:idempotent-commands exits 0 over 11 handlers in scope from 547 controllers.",
      "blocker": "hr-document-templates.service.ts:140 is a read-then-write race on a version counter with no optimistic predicate; the idempotency gate's scope is 11 handlers, so the ordering/idempotency clause is essentially unmeasured.",
      "clauses_failing": "counters without read-then-write races; idempotency coverage"
    },
    {
      "id": "PRD-C077",
      "verdict": "OPEN",
      "evidence": "Timeouts hold: resolveTransactionGuards (src/db/pool.config.ts:118) and src/common/tenant/with-tenant.ts:17-30 set statement_timeout / idle_in_transaction_session_timeout / lock_timeout per transaction via set_config(..., true), and pool.config.ts:252-266 refuses a config that disables either or inverts them; cancellation has AbortSignal plumbing with dedicated specs. Resumable jobs do not: src/modules/support/core/support-kb.controller.ts:294 `reindexAll(@CurrentUser() u)` takes no cursor and calls `this.reindex.reindexAll(u.orgId)`, so although kb-article-reindex.service.ts:22-43 is paged (REINDEX_BATCH_SIZE=100, returns nextArticleId) the HTTP surface can never pass afterArticleId and can only ever reindex the first 100 articles; it is also one of the 45 'PRE-EXISTING, UNAUDITED' provider-in-transaction handlers.",
      "blocker": "The whole-corpus KB reindex is still a request-thread call whose resumption cursor is unreachable from HTTP. The sibling kb-page-indexing.controller.ts:78-85 accepts a cursor and is the counter-example done right.",
      "clauses_failing": "move reindexing to resumable jobs"
    },
    {
      "id": "PRD-C078",
      "verdict": "OPEN",
      "evidence": "Measurement exceeds the ask: src/db/pool-telemetry.ts:31-47 records maxWaitMs/averageWaitMs/p95WaitMs, slowAcquires, failedAcquires, saturationEvents, maxHeldMs/averageHeldMs/p95HeldMs, longHolds, and maxIdleInTransactionMs/p95IdleInTransactionMs/idleInTransactionBorrows/statementsPerBorrowMax, exposed at src/health/health.controller.ts:206-226. Release is not done: check:placement-bypass exits 0 while excusing exactly 45 [provider-in-transaction] handlers, each reasoned 'PRE-EXISTING, UNAUDITED' (grep 'provider-in-transaction' /tmp/beg-check_placement-bypass.log | grep -c 'PRE-EXISTING, UNAUDITED' -> 45).",
      "blocker": "45 handlers still hold the pooled connection across an external provider call, and the gate that would say so exits 0 by allowlisting all 45 as unaudited.",
      "clauses_failing": "release connections before external provider calls or long CPU work"
    },
    {
      "id": "PRD-C079",
      "verdict": "OPEN",
      "evidence": "The role/RLS half is genuinely verified: contracts/benchmark-manifest.json records 'role streamline_app (bypassrls=false)' on scratch_perf_seed, test/perf/measure-heavy-query-plans.mjs:90-118 process.exit(1)s if rolbypassrls or rolsuper and asserts a no-GUC read raises 42501 before measuring, and the retained plans carry real authorization predicates (test/perf/benchmark-plans/approved-complex-large.txt shows `One-Time Filter: (current_org_id() = 'aaaaaaaa-1111-...')`, 75 authz/org_id predicate lines). But check:benchmark-manifest EXITS 1: 'STATUS: FAIL — 154/280 (55.0%) of statement ceilings measured', measured 366 commits behind HEAD, 'subject: DRIFTED — 2 measured SQL catalog(s) changed on disk since capture', 5 declared budget breaches; and two instruments record a role they never verified — src/scripts/capture-build-baseline.mjs:8 `process.env.APP_DATABASE_URL || process.env.DATABASE_URL` one line above a message claiming APP_DATABASE_URL is required (and ssl:'require' hardcoded at :16), and src/scripts/measure-route-budgets.mjs:273 writes role: 'streamline_app (rolbypassrls = false, tenant GUC set)' as a string literal from a file containing no postgres(/new Client call at all.",
      "blocker": "The enforcing gate is exit 1 and the evidence is 366 commits stale with two measured SQL catalogs drifted; separately, capture-build-baseline silently falls back to the owner role and measure-route-budgets asserts a role it never connects as.",
      "clauses_failing": "never only as the database owner (owner fallback + unverified role literal); benchmark evidence current enough to describe the code under audit"
    },
    {
      "id": "PRD-C080",
      "verdict": "OPEN",
      "evidence": "Fingerprints and bind-value safety hold: src/db/query-fingerprint.ts:17-64 normalises comments, dollar-quoted, E'', single-quoted, $n and numeric literals to ? before hashing, :71-83 maps 55P03->lockWait, 40P01/40001->deadlock, 57014->timeout, and query-fingerprint-registry.ts:40-54 records calls, rowsReturned, lockWaits, deadlocks, timeouts. But the two halves never meet in one artifact: the registry file states it is 'minus buffers, which needs an EXPLAIN per statement and is measured offline', never records rows READ, and its only importer is src/db/query-telemetry.ts feeding the runtime endpoint health.controller.ts:225; meanwhile contracts/benchmark-manifest.json carries bufferBlocks, scanRows and resultRows but zero occurrences of 'fingerprint', 'lockWait' or 'rows_read' (grep -c = 0 for each).",
      "blocker": "No artifact in the repo carries fingerprint x call-count x rows-read x rows-returned x buffers x lock-waits for a run — the fingerprint registry is a runtime singleton read only by GET /health/db, and the one committed test artifact omits fingerprints and lock waits.",
      "clauses_failing": "buffers and rows read in the fingerprint evidence; fingerprints and lock waits in the test-evidence artifact"
    }
  ]
}
