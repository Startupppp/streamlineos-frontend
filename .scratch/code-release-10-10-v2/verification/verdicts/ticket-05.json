{
  "ticket": "05",
  "verified_at": "2026-09-04",
  "repo_state": {
    "backend_head": "e1fd1d475 (main; working tree dirty: 4 src/scripts/baselines/*.json + migrations/meta/_chain.sha256.json, written by gate runs)",
    "frontend_head": "31c3786a6 (main, clean apart from this verification dir)",
    "note": "GATE-STATUS.tsv records both repos at 31c3786a6; the backend is actually at e1fd1d475."
  },
  "criteria": [
    {
      "id": "PRD-C004",
      "verdict": "OPEN",
      "evidence": "Roll-up of the seven criteria below. Verdicts established here: C111 CLOSED, C114 CLOSED, C116 CLOSED, C081 REFUTED, C112 OPEN, C113 OPEN, C117 OPEN. Two organization-authority verify gates are also non-passing: `verify:membership-revocation` EXIT 1 (/tmp/beg-verify_membership-revocation.log, 'Inventory vs FKs: FAIL', 270 PASS / 12 FAIL / 67 SKIP) and `verify:multi-org-employment` EXIT 1 (/tmp/beg-verify_multi-org-employment.log).",
      "blocker": "Four of the seven sub-criteria are not met. Closing C004 requires: (1) C081 — cross-tenant tests for public/share-token and search/vector surfaces plus an over-the-wire 404 run that is not describe.skip; (2) C112 — an allow/deny/cross-tenant E2E harness (none exists in the frontend); (3) C113 — @Idempotent on module-access.controller.ts:102,115,239,283 paired with a frontend Idempotency-Key change, and 2xx response schemas on the /access/* operations; (4) C117 — a bound on settings.service.ts:89 listApiKeys.",
      "clauses_failing": "organization authority (verify:membership-revocation EXIT 1: invitations/fk_invitations_org_revoked_by_membership is SET NULL without a column list on a composite FK whose org_id is NOT NULL, so revocation will 23502); contract criteria (C113); frontend criteria (C112). NOTE on verify:multi-org-employment: its EXIT 1 is a crash inside findMultiOrgUser (verify-multi-org-employment.ts:18), 'organisation 06161c5f-... has no region. It must be placed before its data can be reached' — it produced no verdict about multi-org employment at all, and additionally ran as neondb_owner with BYPASSRLS. It is not a pass and cannot close anything, but it is a prerequisite failure against the shared Neon database rather than a demonstrated code violation."
    },
    {
      "id": "PRD-C081",
      "verdict": "REFUTED",
      "evidence": "CLAIMED: the ticket Status counts C081 among its '3 closed'. FOUND: three of the nine surfaces the criterion names are not covered by any executed cross-tenant test. (i) public/share-token — all 13 share-token specs assert either an INVALID/absent token (src/modules/public/crm-tenant-isolation.spec.ts:25, public-forms-tenant-isolation.spec.ts:26, public-offers-tenant-isolation.spec.ts:32), a SAME-ORG object scope (test/security/bola/bola-esign-scope-sweeps-and-token.spec.ts:532,538 both pin orgId: CALLER_ORG), or are regex-over-source (test/security/bola/bola-public-org-selector.spec.ts:161-179 string-matches whiteboard-sharing.service.ts). No test uses a valid org-A token against an org-B object. (f) search/vector — src/modules/search/search-tenant-isolation.spec.ts:58 asserts only that the CACHE KEY carries the caller's org; the KB section of test/security/bola-bulk-search-public-token.spec.ts:102-140 is a regex over source and executes no query. (g) realtime — test/security/bola/bola-realtime-grant-time.spec.ts and bola-realtime-channel-scope.spec.ts prove channel/grant disjointness but contain zero status-code assertions. The one harness that measures 'cross-tenant misses return 404' over the wire, test/security/bola/bola-live-cross-tenant.seeded-e2e-spec.ts, is `describeIfSeeded = USABLE ? describe : describe.skip` (:146) behind a five-variable USABLE guard (:128-134), and .github/workflows/ci.yml:1053-1059 supplies only DATABASE_URL/APP_DATABASE_URL/BACKEND_JWT_SECRET while ci.yml:1034 gates the whole job on schedule/dispatch — so it has never run in CI. Its last recorded live run (test/security/bola/live/known-no-404.json, _measured 2026-09-03) still pins 23 no404, 4 serverErrors, 1 inconclusive and 2 LEAKs open: 'POST /crm/consent/contacts/:contactId -> 200' and 'POST /public/intake/:projectId -> 201'. What DOES hold: 306 specs assert a cross-tenant miss as NotFound (286 of them mocked units asserting an exception class, not an HTTP status); surfaces (a) reads, (b) writes, (c) bulk, (d) files, (e) exports, (h) jobs each have a real cross-tenant 404 assertion; and `check:tenant-isolation:run` EXIT 0 executes 452 suites / 1873 tests, all passing.",
      "blocker": "Cross-tenant tests for the public/share-token and search/vector surfaces; a status-code assertion for realtime; and a CI-wired (not describe.skip) run of bola-live-cross-tenant that closes the 2 pinned leaks. Also note the gate cited as coverage evidence, check:tenant-isolation, reports 933/933 using an accept-list whose line 66 is a bare /isolation/i matched against the whole spec file (src/scripts/check-tenant-isolation-coverage.mjs:58-70), and its execution companion collects 452 files of which ZERO are under test/security/ — it runs none of the BOLA suite. check:record-access, also cited, is a soft-delete gate: the strings 403/Forbidden do not appear in src/scripts/check-record-access.mjs at all.",
      "clauses_failing": "'public/share-token paths' — no cross-tenant test exists. 'search/vector' — no executed cross-tenant query. 'realtime' — no 404-vs-403 assertion. 'cross-tenant misses return 404' — proven over the wire only by a suite that is skipped in CI and whose last run left 2 leaks and 113 NO-404 outcomes pinned."
    },
    {
      "id": "PRD-C111",
      "verdict": "CLOSED",
      "evidence": "All three clauses hold. BOUNDED MEMBERSHIP/SESSION READS: every multi-row read in src/modules/access/access-permission.resolver.ts carries an explicit cap — .limit(500) at :152,:170,:209,:269 and .limit(100) at :183; the two membership reads (:100,:359) are findFirst. access-permission-members.resolver.ts caps all 8 reads (:119,:133,:142,:157,:185,:234,:274,:339); denied-modules.resolver.ts:54 .limit(100). The session list read, sessions.service.ts:95, is `limit: SESSION_LIST_CAP + 1` with `orderBy: [desc(lastActive), asc(id)]` and a logged warning on truncation. `check:unbounded-reads` EXIT 0 and no ACTIONABLE entry in src/scripts/baselines/unbounded-reads-classification.json falls in this territory (the 9 ACTIONABLE files are clients, hr, kb, quotes, @common/tenant, @common/relocation). REQUIRED INDEXES: `check:tenant-indexes` EXIT 0 — '347 schema files, 840 tenant tables, 840 leading tenant index'; src/db/schema/common/access.ts declares org_id-leading composites on every RBAC table (:50,:55,:88,:121,:126,:182,:215,:305,:334). IMMEDIATE INVALIDATION: session — 20+ CACHE_KEYS.userSession(...) invalidation call sites (auth.service.ts:229, org-membership.service.ts:216, org-profile.service.ts:186,:310, invitation-acceptance.service.ts:173, delegations.service.ts:76, module-standing-mutations.service.ts:145, entitlements.service.ts:353, user-module-access.service.ts:199 among them); effective-access — the resolved permission set is keyed `${orgId}:${userId}:${version}` (access.service.ts:235) and bumpPermissionsVersion clears the shared access:version:<orgId> key (access.service.ts:134-149); organization — organization-settings.service.ts:65-66 and settings.service.ts:177-178 invalidate org:settings and the org:profile namespace, org-membership.service.ts:214 and org-member-departure.service.ts:321 the same. EXECUTED: `npx jest --testPathPattern=\"access-resolution-cost|cache-invalidation-matrix|cache-multi-instance|cache-key-collision\"` -> EXIT 0, 4 suites, 154 tests passed; cache-invalidation-matrix.spec.ts drives all 143 matrix entries against a stateful in-memory Redis with a negative control that proves the suite bites.",
      "blocker": "",
      "clauses_failing": ""
    },
    {
      "id": "PRD-C112",
      "verdict": "OPEN",
      "evidence": "Four of five clauses now hold under tests I ran myself (the ticket's 'BLOCKED: not run' is stale). `npx jest lib/query-scope-isolation.test.tsx` -> EXIT 0, 3 tests, including a bite-proof control 'proves the guard bites: a plain QueryClient exposes cross-org data'. `npx jest lib/org-scoped-storage.test.tsx lib/query-error-policy.test.ts lib/membership-lifecycle-route.test.ts lib/wizard-gate.test.ts` -> EXIT 0, 4 suites, 33 tests. WORKSPACE/ONBOARDING GATES: frontend/lib/wizard-gate.ts resolveWizardGate is the gate invoked at app/(authenticated)/layout.tsx:31-33; wizard-gate.test.ts covers 12 cases including no-orgId -> /org-setup, suspended -> /access-suspended, and re-trap invariants in both directions. ORGANIZATION SWITCH STATE: membership-lifecycle-route.test.ts (4 cases) plus org-scoped-storage.test.tsx (6 cross-org isolation cases). QUERY-KEY TENANT ISOLATION: query-scope-isolation.test.tsx above, plus `check:query-scope` EXIT 0 over 5411 files. AUTH ERROR STATES: query-error-policy.test.ts, 11 cases covering 401 sign-out, suspended-membership 403 redirect, permission refusal, cancellation and background-refresh.",
      "blocker": "The frontend has NO end-to-end harness at all: no playwright.config.* anywhere in the repo, no e2e/ directory, and no playwright or cypress dependency in frontend/package.json (only jest + @testing-library). The one browser harness, frontend/scripts/browser-journeys.mjs, drives 8 read journeys and 4 write journeys as a SINGLE authenticated user (journeys defined at :88-103 and :225-317) and asserts shell/terminal-state/a11y/contrast/overflow — it makes no allow/deny assertion and has no second tenant. Closing this needs an E2E harness that signs in as at least two principals in two orgs and asserts a permitted action succeeds, a denied action is refused, and a cross-tenant id is not reachable.",
      "clauses_failing": "'allow/deny/cross-tenant E2E' — no harness exists to run it. The ticket's stated blocker ('needs a booted app + Playwright') understates it: Playwright is not merely unconfigured, it is not a dependency."
    },
    {
      "id": "PRD-C113",
      "verdict": "OPEN",
      "evidence": "STRICT ZOD — HOLDS. .strict() is present on every client-boundary schema in the territory, including the five the prior pass tightened: access/entitlements.controller.ts:13 (moduleKeyParamSchema) and :17 (toggleModuleSchema), users/dto/users.schemas.ts:25 (emergencyContactSchema), organization/core/dto/organization.schemas.ts:79 (businessHours value object), organization/setup/dto/announcements.schemas.ts:40 (announcementBodyBase). OWNER/DESCENDANT PROTECTIONS — HOLD on execution. `verify:rbac-integrity` EXIT 0: 19 probes over 9 constraints, each rejection carrying the exact SQLSTATE (23503 on fk_role_assignments_assigner_membership, fk_user_permission_grants_granter_membership, fk_user_permission_grants_permission_module) and each paired with an [ACCEPT] control, so it cannot pass vacuously. `npx jest --testPathPattern=\"authority-matrix|organization-member-status|module-transfer-stale-owner|grant-escalation|standing-mutations\"` -> EXIT 0, 5 suites, 89 tests, covering assertOwnerOnly (org admin cannot bypass), 'rejects removal of the org owner', 'rejects leaving when the user is the org owner' and stale-owner transfer rejection.",
      "blocker": "Two clauses fail. (1) IDEMPOTENT MUTATIONS: across the role/grant/module-access controllers there are 13 POST handlers and 9 carry @Idempotent; the 4 that do not are all in src/modules/module-access/module-access.controller.ts — :102 POST :moduleKey/standing/transfer-owner, :115 POST :moduleKey/standing/:membershipId, :239 POST :moduleKey/groups/:groupId/members, :283 POST :moduleKey/members. (Counts reproduced by grepping @Post(/@Idempotent( across rbac/*.controller.ts, access/*.controller.ts and module-access/*.controller.ts: principal-groups 3/3, rbac.controller 1/1, roles.controller 3/3, module-access 2/6.) The decorator is load-bearing: src/common/idempotency/idempotency.interceptor.ts:91 throws 'An Idempotency-Key header is required for this operation', so adding it needs a paired frontend change. (2) STABLE OPENAPI: `check:openapi-coverage` EXIT 1 — 'response-schemas: 27/3648 ops declare a response body SCHEMA (0.74%) / 3621 uncovered [ceiling 3617]' and 'FAIL — 3621 operation(s) declare no response schema, above the ceiling of 3617'. The log names this ticket's own routes first: GET/POST /access/delegations, DELETE /access/delegations/{delegationId}, GET/PATCH /access/org-modules, GET/PATCH /access/user-module-access/{userId}. The gate that guards stability, `check:contract-breaking-change` (EXIT 0), covers only the 102 'published' operations — and ZERO of them are in this territory (measured against contracts/api-contract-registry.json: 3662 operations, 102 published, 0 matching /access|/rbac|/module-access|/roles|/organization|/settings|/users|/sessions|/mfa|/auth).",
      "clauses_failing": "'idempotent mutations' (4 of 13 grant/standing POSTs) and 'stable OpenAPI' (no 2xx response schema on the /access/* operations; no stability ratchet covers any route in this territory). Separately, 'exhaustive' owner protection is not established by the gates cited for it: check:owner-authority is a 9-row catalog checking itself against 12 shortcut sites, and check:module-gate's walk is driven by the 23 moduleFolder entries in src/common/rbac/module-registry.ts rather than by the directories on disk — 16 of this ticket's 18 controllers are outside its reach and its CONTROLLER_MIN=200 floor can never trip on the gap."
    },
    {
      "id": "PRD-C114",
      "verdict": "CLOSED",
      "evidence": "All three clauses hold. BATCHED/CACHED: src/modules/access/access-resolution-cost.spec.ts pins pooled-connection budgets as explicit constants — FIRST_RESOLUTION_BORROWS 2, WARM_RESOLUTION_BORROWS 0, STEADY_STATE_COLD_BORROWS 1, BORROWS_WITHOUT_SHARED_CACHE 2 (:19-22) — measured through poolTelemetry.borrows; the resolved set is keyed by access version (access.service.ts:235) so a bump is a miss rather than a scan. Executed: `npx jest --testPathPattern=\"access-resolution-cost|cache-invalidation-matrix|cache-multi-instance|cache-key-collision\"` -> EXIT 0, 4 suites, 154 tests passed. SCOPE EXPANSION BOUNDED: every multi-row read in access-permission.resolver.ts carries an explicit cap (:152,:170,:183,:209,:269); src/modules/access/apply-scope.ts takes a materialised `teamIds?: string[]` (:8) and resolves 'team' as `eq(ownerColumn,userId) OR inArray(teamColumn, teamIds)` (:23-25), falling back to 'own' when the array is absent — no correlated org_unit_members subquery remains. INDEXES COVER SUBJECT/ROLE/PERMISSION/MODULE/TENANT: src/db/schema/common/access.ts — role_assignments (org_id, organization_membership_id, role_id) :50 and (org_id, role_id) :55; role_permission_grants (org_id, role_id, permission_key) :88; user_permission_grants (org_id, organization_membership_id, permission_key) :121 and (org_id, module_key) :126; user_module_access (org_id, organization_membership_id, module_key) :182; org_modules (org_id, module_key) :215; principal_group_members (org_id, organization_membership_id) :305; group_role_assignments (org_id, principal_group_id, role_id) :334. `check:tenant-indexes` EXIT 0 over 840/840 tenant tables.",
      "blocker": "",
      "clauses_failing": "None. CAVEAT recorded, not a failing clause: the index evidence is DECLARED (schema + a static gate with a real denominator), which is the right level for a CODE criterion, but it is not confirmed against a live database — check:declaration-column-drift, check:declaration-constraint-drift and check:referential-action-drift are all EXIT 2 (prerequisite absent)."
    },
    {
      "id": "PRD-C116",
      "verdict": "CLOSED",
      "evidence": "The criterion's own text — 'prove global settings contain organization configuration/access governance only while operational and module-owned settings remain with their modules' — holds in both directions, reproduced independently of the prior report. FRONTEND: 23 page.tsx under frontend/app/(authenticated)/settings/** (and zero route.ts there, so that is the whole surface), classifying as 9 organization configuration (/settings/organization plus branches, business-units, chart, cost-centers, departments, locations, structure, teams), 10 access governance (audit-log, delegations, incoming-transfer, modules, roles, roles/[roleId], roles/audit, roles/simulate, users, webhooks), 2 my-account (/settings, /settings/api-tokens) and 2 platform billing (/settings/billing, /settings/billing/ai-credits) — 0 module-owned surfaces and 0 operational work. 58 module settings pages match <module>/settings/** across 12 modules (62 across 13 counting knowledge/wiki/settings, hr/recruitment/settings and the two build project-scoped ones); every module-owned surface the contract names lives there and has no global counterpart — custom-fields x3 (crm, hr, support), automations x4 (crm, hr, support, accounting), integrations x2 (hr, build), import-export x3 (crm, hr, payroll), templates, workflows. All four forbidden platform-billing routes are NOT REACHABLE: app/(authenticated)/billing holds only layout.tsx + invoices/, and billing/ai-credits, settings/subscription and billing/seats do not exist (an app-wide find for directories matching *seat*/*subscription* returns none). BACKEND: src/modules/settings/settings-surface-census.spec.ts walks the whole of src/, finds every controller whose Nest PATH_METADATA prefix is settings or settings/..., reflects real metadata rather than regexing source, and requires each route to carry an explicit verdict in GLOBAL_SETTINGS_SURFACE — written precisely because three settings routes mounted from modules/automation and modules/email had escaped three earlier inventory passes. Executed: `npx jest --testPathPattern=\"settings-surface-census|settings-route-gates|settings-member-role-authority\"` -> EXIT 0, 3 suites, 66 tests.",
      "blocker": "",
      "clauses_failing": "None. NOTE on the ticket's 'PARTIAL': the ticket blocks C116 on 'no legacy redirects', which is a clause of shared CLAUDE.md §8 but is NOT in PRD-C116's text (PRD line 369 is the ownership statement only). Both redirect files do still exist and are pure redirects — frontend/app/(authenticated)/hr/settings/company/page.tsx (7 lines: requirePermission then redirect('/settings/organization')) and frontend/app/(authenticated)/hr/onboarding/my-tasks/page.tsx (5 lines: LegacyMyOnboardingTasksRoute -> redirect('/me/onboarding')) — a real §8 violation, but it belongs to ticket 07 and does not bear on this criterion. /settings/webhooks is compliant: its catalog frontend/features/settings/webhooks/webhook-schema.ts spans 11 events across four owners (CRM lead/deal x4, Support ticket x2, cross-cutting comment x1, HR leave x1, access-governance member x3), so no single module owns it."
    },
    {
      "id": "PRD-C117",
      "verdict": "OPEN",
      "evidence": "Two of three clauses hold. TENANT-LEADING INDEXES: `check:tenant-indexes` EXIT 0 — '347 schema files, 840 tenant tables, 840 leading tenant index. OK — every tenant table declares an index leading with its tenant column'; org_id-leading composites on all RBAC access-path tables at src/db/schema/common/access.ts:50,55,88,121,126,182,215,305,334. INVALIDATION: organization — src/modules/organization/core/organization-settings.service.ts:65-66 invalidates org:settings and the org:profile namespace, src/modules/settings/settings.service.ts:177-178 the same, plus org-membership.service.ts:214 and org-member-departure.service.ts:321. hierarchy — src/common/cache/org-hierarchy-cache.service.ts:35-44 invalidateAfterMutation clears org:hierarchy (and hr:headcount, hr:directory), registered after commit. access — bumpPermissionsVersion clears access:version:<orgId> and drops the local map plus rbac:members and module-access:candidates (src/modules/access/access.service.ts:134-150). entitlement — src/modules/access/entitlements.service.ts:289-290 invalidates entitlements:module:<moduleKey> and entitlements:modules, and :352 busts every active member's userSession. navigation — there is NO backend navigation cache to invalidate (a repo-wide grep for a navigation cache namespace or a /navigation route returns nothing); navigation is a compile-time client tree in frontend/components/layout/sidebar/sidebar-nav-*.ts filtered by the permission set, and the client discards the whole cache on org switch (frontend/hooks/common/auth-hooks.ts:125,170 queryClient.clear()) with the remount proved by lib/query-scope-isolation.test.tsx (EXIT 0, 3 tests, bite-proof control). Executable proof of the invalidation contract: src/common/cache/cache-invalidation-matrix.spec.ts drives all 143 registry entries (108 kind:'write') against a stateful in-memory Redis with an explicit negative control; `npx jest --testPathPattern=\"cache-invalidation-matrix|cache-multi-instance|cache-key-collision|access-resolution-cost\"` -> EXIT 0, 4 suites, 154 tests.",
      "blocker": "BOUNDED SETTINGS READS fails. src/modules/settings/settings.service.ts:89 listApiKeys returns `db.query.apiKeys.findMany({ where: and(eq(apiKeys.orgId, u.orgId), eq(apiKeys.isRevoked, false)), orderBy: desc(createdAt) })` with no .limit(), no cursor and no keyset; its route src/modules/settings/settings.controller.ts:61-64 `@Get(\"api-keys\")` accepts no pagination parameter; and createApiKey (settings.service.ts:96-125) calls no assertWithinLimit, so nothing bounds the row count on either side. The gate that should catch it, `check:unbounded-reads`, is EXIT 0 only because src/scripts/baselines/unbounded-reads-classification.json suppresses '/settings/settings.service.ts' as FALSE-POSITIVE carrying the scanner's own default note 'Unbounded select; needs limit, aggregate exemption, or false-positive review' — a classification with no stated justification. Closing this needs a limit+1 overflow read on listApiKeys (the pattern already used at settings-automations.service.ts:47) or an enforced cap on creation.",
      "clauses_failing": "'bounded settings reads' — GET /settings/api-keys is unbounded on both the read and the write side. SEPARATELY, THE TICKET'S STATED BLOCKER IS REFUTED: the ticket blocks C117 on `verify:membership-revocation` EXIT 1 with '248 PASS / 33 FAIL / 68 SKIP' and names 'org_unit_members declared set-null, actual cascade' as the in-territory defect. The 2026-09-04 run (/tmp/beg-verify_membership-revocation.log) measures 270 PASS / 12 FAIL / 67 SKIP, and org_unit_members appears not among the FAILs but in the leading 'NOTICE — inventory artifacts not checked by this script' block. The one FAIL in this territory is different: 'invitations  SET NULL without column list on composite FK — org_id is NOT NULL, will 23502 (fk_invitations_org_revoked_by_membership)'. The gate is also mislinked — membership-FK referential integrity is not part of PRD-C117's text (PRD line 370), which is bounded settings reads, tenant-leading indexes and cache invalidation."
    }
  ]
}
