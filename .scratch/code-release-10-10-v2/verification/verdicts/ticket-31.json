{
  "ticket": "31",
  "verified_at": "2026-09-04",
  "heads_verified": {
    "backend": "e1fd1d475e64a94f6b9273fc3d06608697375519 (main, --no-ff merge of release/code-10-10-v2, parents ccd31ad23 + ae5333306)",
    "frontend": "31c3786a6621dea105dea11e7676299900fccd06 (main, one commit after merge acf659fc5, parents 94c7fda2c + e5c1ffc2f)",
    "working_trees": "Backend `git status --porcelain` was DIRTY at the start of this audit (4 modified: migrations/meta/_chain.sha256.json resealed to \"sealedAt\": \"2026-09-04\", plus src/scripts/baselines/{db-call-count-classification,query-projection-baseline,unbounded-reads-baseline}.json) and was reverted to clean by another actor mid-audit. Both trees are clean NOW (frontend carries only the untracked verification/ dir this pass created). GATE-STATUS.tsv was captured 08:13, possibly during the dirty window, and its own header misrecords `backend main = 31c3786a6` — that is the FRONTEND sha."
  },
  "criteria": [
    {
      "id": "PRD-C008",
      "verdict": "OPEN",
      "evidence": "REPAIRED AND VERIFIED AT HEAD (refuting the prior 31-audit.md, which said these were open): (a) ACL-aware retrieval — 31-audit.md claimed kb-candidate.service.ts 'takes no orgId at all' and that the fence 'only fires on a zero-row ANN'. Both are FALSE at head: src/modules/kb/retrieval/kb-candidate.service.ts:52 is `async vectorChunkIds(orgId: string, vector: string, cap: number)`, the ANN carries `WHERE org_id = ${orgId}` (:57), and the exact re-run fires on a SHORT pool (`if (annIds.length >= cap) return annIds;` :62) not an empty one. 16-audit F1/F12 are closed. (b) Calendar provider drift/sync — CalendarProviderSyncSweepService is wired (calendar.module.ts:47-53, exported for CronModule) and drift re-queue exists at calendar-provider-webhook.service.ts:161,178,196. STILL OPEN AT HEAD, each verified by grep: (1) bounded read paths — src/modules/mail/mail-metadata.service.ts:165 is still `if (rows.length === 0 || rows.length > SEARCH_ID_CAP) return literal;`, so the definer's definitive EMPTY answer is still discarded into three leading-wildcard ILIKEs (14-audit finding 5, P1, measured 2.2ms/486 buffers -> 184ms/5,006 buffers). (2) sync correctness — `grep -rn loadPosition src --include=*.ts | grep -v spec` returns ONLY the definition at src/modules/mail/mail-sync-checkpoint.service.ts:13, so mail_sync_checkpoints is a write-only table and incremental sync is unimplemented (14-F6). (3) `grep -rn NoTenantTransaction src/modules/mail/` returns ZERO hits while 48 files carry it repo-wide — MailController (src/modules/mail/mail.controller.ts:54) still holds a pooled Postgres transaction across the Composio fetch and LLM completion of POST /mail/ai/draft (:188) against a 60s idle_in_transaction_session_timeout (14-F3). (4) `grep -rn 'markStaleForAccount|clearPositions' src/` returns only the two definitions (mail-metadata.service.ts:335, mail-sync-checkpoint.service.ts:51) — zero callers, so revoking a mailbox purges no mirrored subject/sender/thread data (14-F4). 14-audit.md:354-358 states its own verdict: PRD-C130 'partially met', PRD-C131 'partially met'.",
      "blocker": "Ticket 14 (Inbox/mail) is not complete: 4 P1 findings reproduce verbatim at the release commit, and 14-audit's own criteria verdicts are 'partially met'. No performance evidence was recaptured at the release commit for any of the three modules. Closing needs: split the SEARCH_ID_CAP branches so an empty index answer returns `inArray(id, [])`; wire loadPosition (or delete the checkpoint table and say so); add @NoTenantTransaction to MailController; call markStaleForAccount + clearPositions from IntegrationsService.disconnect; then re-measure buffers/latency at the release SHA.",
      "clauses_failing": "sync correctness (mail incremental sync unimplemented); bounded read paths (mail-metadata ILIKE fallback on the empty case); current performance evidence (not recaptured at head). Clauses HOLDING: provider drift (calendar), ACL-aware retrieval (knowledge)."
    },
    {
      "id": "PRD-C014",
      "verdict": "OPEN",
      "evidence": "Three of the four named clauses now hold on their P0s, verified at head, refuting 31-audit.md's '3 P0 stand': ticket 08 — src/modules/payroll/payout/lib/payout-run-completion.ts now derives coverage from `resolveRunCoverage` returning {paidRunEmployeeIds, payableSubjects, disbursedNet} and emits `net: disbursedNet` to the outbox (not `currentRun.netTotal`), so 08-F2 is fixed; ticket 15 — src/modules/push/push.service.ts:43 calls `SELECT app.claim_push_endpoint(...)` (SECURITY DEFINER, migration 1061, journal idx 817) so 15-F1 is fixed, and src/modules/email/unsubscribe.controller.ts makes GET read-only (`return this.handle(token, req, false)`) with the opt-out written to notification_suppression_rules at token scope, so 15-F2 is fixed; ticket 10 — invoices-update.service.ts now computes `this.posting.gstSplit(totals.taxPool, supplierStateCode, placeOfSupplyStateCode)` in the lineItems branch. THE FAILING CLAUSE IS 'every surviving P0/P1'. Four P1s from the committed audit corpus reproduce verbatim at head (see PRD-C008 evidence: 14-audit findings 3, 4, 5, 6). AND there is no formal disposition anywhere: the only residual-risk register in either repository is architecture-refactor/decisions/release-authority-2026-09-03-UNSIGNED.md §6, whose scope is `production (PRD-C195)` and which records 'P0 residual risks accepted: 0 · P1 residual risks accepted: 0' with the note 'This table is empty because nobody has accepted anything, not because there is nothing to accept.' Its §7 explicitly says 'Unresolved code-level P0/P1 (PRD-C160): not measured by this record — ticket 31 owns it.' The fix wave's own commit message (backend 9534abcf9) accounts for only 178 of 210 claims — '118 confirmed, 60 rejected or materially corrected' — leaving 32 P0/P1 claims with neither a fix nor a recorded rejection.",
      "blocker": "No code-level finding-disposition register exists. 4 verified-open P1s plus 32 claims unaccounted for in the fix wave. Closing needs a per-finding register (fixed | rejected-with-reason | accepted-with-owner-and-deadline) covering all 210 claims, and the 4 mail P1s either fixed or formally accepted.",
      "clauses_failing": "'and every surviving P0/P1 before v2 ticket 31' — neither resolved nor formally dispositioned. Clauses holding on their P0s: ticket 08 payroll, ticket 15 notification/email, ticket 22 security."
    },
    {
      "id": "PRD-C018",
      "verdict": "OPEN",
      "evidence": "`cat jest-e2e-seeded.json` shows testMatch = ['<rootDir>/test/helpers/__tests__/seeded-e2e-app.spec.ts', '<rootDir>/test/**/*.seeded-e2e-spec.ts']. `find . -name '*.seeded-e2e-spec.ts' -not -path '*/node_modules/*' -not -path '*/.claude/*'` returns exactly 11 files: test/billing/ai-credits-reserve-race, test/crm/{crm-import-roundtrip,crm-inbound-ingress,crm-tenant-isolation}, test/db/postgres-error-shape, test/kb/kb-page-visibility, test/perf/route-budget-http, test/security/bola/{bola-live-cross-tenant,t15-own-tenant-500}, test/security/gdpr-export-cross-module-privacy, test/support/support-ticket-follow. Mapping those onto C018's fifteen named areas: Knowledge = 1 spec, Billing = 1 spec (a single credit-reserve race, not the module), and THIRTEEN areas have ZERO disposable-database spec — Organization/RBAC, Home, Settings, HRMS, Payroll, Build, Payments, Accounting, Chat, Calendar, Notifications, Workflows, Inbox/mail. Three of the eleven serve CRM, which PRD line 142 excludes from the release. Separately, no run record exists at the release commit: no seeded-e2e log exists anywhere under /tmp and no results artifact is recorded in .scratch/code-release-10-10-v2/ or architecture-refactor/.",
      "blocker": "The disposable-database suite does not contain specs for 13 of the 15 areas the criterion names, so running it cannot satisfy C018; and it was not run at the release commit in any case. Closing needs seeded-e2e specs authored for the 13 uncovered areas and one recorded run at a named SHA.",
      "clauses_failing": "Coverage (13 of 15 named areas absent) and execution (never run at the release commit)."
    },
    {
      "id": "PRD-C019",
      "verdict": "OPEN",
      "evidence": "No release record exists at the current commit pair. The only release record in either repo is architecture-refactor/decisions/release-authority-2026-09-03-UNSIGNED.md, whose §2 stamps frontend `7469d27895add587f9427e7c50c457f56e0048bf` and backend `2f37e1bb035006e5c03680497298ad62031e79d6` on branch `release/code-10-10-v2` — both superseded by the current heads (frontend 31c3786a6, backend e1fd1d475) — and which says of itself 'The SHAs above are provisional and expected to be superseded.' Its §4.1 states 'The full gate run is not recorded here… Gates not run by ticket 36: 65 of 70' with 'Subset total: 4 passed · 1 failed · 0 inconclusive · 65 not run out of 70.' It records no database identity, no dataset shape and no failure-artifact index. The only artifact that records gate exits at the current head — .scratch/code-release-10-10-v2/verification/GATE-STATUS.tsv — is untracked (frontend `git status --porcelain` shows `?? .scratch/code-release-10-10-v2/verification/`) and its own header misrecords the release SHA: it reads `# backend main = 31c3786a6`, which is the FRONTEND sha; backend main is e1fd1d475.",
      "blocker": "Every one of the six required fields is missing at the release commit: command list, release SHA (the one record that has SHAs has stale ones and the one file with fresh gate exits has the wrong backend SHA), database identity, dataset shape, pass/fail/skip counts, failure artifacts. Closing needs a new record instantiated at backend e1fd1d475 / frontend 31c3786a6 with all six fields populated from actual runs.",
      "clauses_failing": "All six: command, release SHA, database identity, dataset shape, pass/fail/skip counts, failure artifacts."
    },
    {
      "id": "PRD-C020",
      "verdict": "OPEN",
      "evidence": "SBOM: the named family HAS NO GATE. `node -e \"require('./package.json').scripts['check:sbom']\"` is undefined in BOTH repos; the backend offers only `sbom:generate` and `sbom:generate:self-test`, which are generators, and /tmp/beg-check_gate-wiring.log's 100-gate inventory contains no sbom entry. CYCLE: backend check:cycles is exit 1 — /tmp/beg-check_cycles.log: 'Processed 5825 files … ✖ Found 1 circular dependency! 1) modules/notifications/notification-delivery-worker.service.ts > modules/notifications/notification-delivery-preflight.ts'. FILE-SIZE: backend check:over-300 is exit 1 — /tmp/beg-check_over-300.log line 1: 'check-over-300: 402 files exceed 300 lines — 10 above baseline of 392.' OPENAPI FRESHNESS: check:openapi-coverage is exit 1 — 2xx responses declaring no content schema, '... and 3591 more'. RLS: `db:verify-rls` was NOT run at head — it is absent from GATE-STATUS.tsv's 96-gate list and the newest RLS output on disk is /tmp/final_rls.txt mtime 2026-09-02 18:21, two days before the 2026-09-04 04:37 merge. Per the exit contract, not run never counts as a pass. BACKEND BUILD/TYPECHECK and FRONTEND TYPECHECK: not run at head — newest logs are /tmp/be_typecheck.log (2026-09-03 16:48) and /tmp/fe-build.log (2026-09-02 23:23), both predating the merge. Additional reds in scope: check:benchmark-manifest (154/280 = 55.0% of statement ceilings measured), check:route-budgets-http (3 breaches incl. POST /chat/channels/{channelId}/messages p95 5043.126ms > 1000ms), check:test-suppressions (66 runtime-selected suppressions above a ratchet of 29), verify:chat-mentions, verify:membership-revocation, verify:multi-org-employment; plus 11 backend gates at exit 2 (check:alert-ack, audit-log-privileges, declaration-column-drift, declaration-constraint-drift(+:baseline), module-lifecycle, referential-action-drift(+:baseline), replay-ledger, set-null-column-lists, tenant-relationships). Frontend reds: check:route-bundle-budget ('FAIL — 9 route bundle budget breach(es)') and check:web-vitals-budget ('FAIL — this capture is not usable evidence (1 refusal(s), provenance stale)'). GREEN at head and worth recording: spec-typecheck, test-typecheck, dead-code (both repos), tenant-isolation(+:run), permission-keys/verify:permissions, cache-invalidation, cache-key-shapes, outbox-consumers, idempotent-commands, migration-chain/ledger/discipline/immutability/rollback, vulnerabilities, licenses.",
      "blocker": "One of the seventeen named families (SBOM) has no gate to run; three families are RED (cycle, file-size, OpenAPI freshness); the RLS family and both build/typecheck families were never run at the release commit. Closing needs: author and register check:sbom; break the notifications delivery-worker cycle; bring over-300 back to its 392 baseline; give the 3,602 bare 2xx operations content schemas; then run RLS, both builds and both typechecks at one SHA.",
      "clauses_failing": "SBOM (no gate exists), cycle (exit 1), file-size (exit 1), OpenAPI freshness (exit 1), RLS (not run), backend build/typecheck (not run), frontend typecheck (not run)."
    },
    {
      "id": "PRD-C021",
      "verdict": "OPEN",
      "evidence": "Clause 1 ('resolve every code-level P0/P1') fails: 14-audit findings 3, 4, 5 and 6 — all P1 — reproduce verbatim at head, each confirmed by grep (mail.controller.ts carries no @NoTenantTransaction; markStaleForAccount and clearPositions have zero callers; mail-metadata.service.ts:165 still discards the definer's empty answer; loadPosition has zero production callers). Clause 2 ('assign owner/deadline to accepted lower-severity residual risks') fails outright: the only register is architecture-refactor/decisions/release-authority-2026-09-03-UNSIGNED.md §6, whose table body is a single row reading '| — | *(none)* |' with 'P0 residual risks accepted: 0 · P1 residual risks accepted: 0'. Its §7 'Unresolved findings' table has 8 rows, and every single one carries 'Disposition: none'. No finding anywhere in either repository carries a deadline. Backend commit 9534abcf9's message accounts for 178 of 210 P0/P1 claims (118 confirmed + 60 rejected), leaving 32 with no recorded outcome.",
      "blocker": "No accepted-residual-risk register with owners and deadlines exists, and 4 P1s plus 32 unaccounted claims remain. Closing needs the 4 mail P1s fixed, the 32 unaccounted claims dispositioned, and a register giving each accepted lower-severity residual an owner and a review date.",
      "clauses_failing": "Both: 'resolve every code-level P0/P1 finding' and 'assign owner/deadline to accepted lower-severity residual risks'."
    },
    {
      "id": "PRD-C156",
      "verdict": "OPEN",
      "evidence": "The section 'Immediate code-level release candidate' spans lines 167-506 of architecture-refactor/PRD-10-10-CODE-RELEASE-TODO.md (bounded by the next level-2 heading, '## Frontend verification run — 2026-09-02', at line 507). `sed -n '167,506p' … | grep -c '^\\s*- \\[ \\]'` returns 138 unchecked items (37 checked). Independently confirmed by the repo's own gate: /tmp/feg-check_prd-traceability.log reports 'manifest rows 195 · ticket criteria 195 · checked 0 · unchecked 195' and exits 0 — so ZERO of the 195 tracked criteria are checked at the release commit, and the count is trustworthy rather than a parsing artifact. Ticket 31's own completion-evidence requirement ('Update the source PRD checkbox states and the traceability manifest in the same completion commit') is therefore also unmet.",
      "blocker": "138 unchecked items remain in the section, and 0 of 195 tracked criteria are checked. Closing requires each of those items completed with fresh evidence and the PRD/manifest checkbox states updated together.",
      "clauses_failing": "'Every unchecked item … is complete' — 138 remain unchecked; and 'with fresh evidence' — no evidence record exists at the release commit (see PRD-C019)."
    },
    {
      "id": "PRD-C157",
      "verdict": "OPEN",
      "evidence": "CLAUSE 2 HOLDS, MEASURED: `git diff --stat 94c7fda2c21f50e3e20b54ea6c01c85c6d5e8d97..e5c1ffc2fb4a7b482e305992207e09869ca33693 -- frontend/features/landing frontend/app/page.tsx` returns EMPTY across the entire release branch, and `git diff --stat acf659fc5..HEAD -- frontend/features/landing frontend/app/page.tsx` is likewise empty. Zero bytes changed in the 7 landing files (including landing-page-motion.tsx, landing-page.tsx, public-shell.tsx) or the root landing route. Public landing visuals and animations are unchanged. CLAUSE 1 FAILS: the PRD states at line 142 'CRM and Inventory code, migrations and acceptance evidence are excluded' and at line 164 'CRM and Inventory remain excluded.' Measured over the exact release ranges, 14 CRM/Inventory source files were changed: backend `git diff --stat ccd31ad23..ae5333306 -- src/modules/crm src/modules/inventory` = 3 files, +90/-53 (inv-product-catalog.service.ts, inv-warehouses.service.ts, inv-products-warehouses-vendors-isolation.spec.ts); frontend `git diff --stat 94c7fda2c..e5c1ffc2f -- crm/inventory paths` = 11 files, +208/-48, including two NEW files (features/crm/leads/leads-lazy.tsx, leads-lazy-fallbacks.tsx). Concretely harmful: `wc -l src/modules/inventory/warehouses/inv-warehouses.service.ts` = 301 today versus `git show ccd31ad23:…` = 292 — the file crossed the 300-line threshold DURING this release and now appears in backend check:over-300's in-scope 402-file count, which is RED at 10 above its 392 baseline (that gate's corpus includes crm-scoring.service.ts 504, crm-organizations.service.ts 470 and ~20 inventory files, so CRM/Inventory are NOT excluded from it — unlike check:dead-code, whose log correctly tags 12 entries 'CRM/Inventory excluded from PRD scope; not counted'). No separate CRM/Inventory report exists for these changes, which PRD-C023 requires ('CRM/Inventory … must be reported separately').",
      "blocker": "CRM/Inventory code was modified inside the release (14 files), one modified inventory file crossed into a RED in-scope gate, backend check:over-300 does not exclude CRM/Inventory from its corpus at all, and no separate report dispositions those changes. Closing needs either the CRM/Inventory changes reverted/reported separately, or check:over-300 given the same out-of-scope classification the dead-code gates already implement.",
      "clauses_failing": "Clause 1 'CRM/Inventory remain excluded'. Clause 2 'public landing visuals/animations remain unchanged' HOLDS and is measured."
    },
    {
      "id": "PRD-C158",
      "verdict": "OPEN",
      "evidence": "The 'at one commit' half is now satisfiable in the narrow sense and this REFUTES 31-audit.md's central claim ('neither repository has a clean commit', '119 + 155 dirty entries'): both repos are on main and clean — backend `git status --porcelain` returns 0 entries at e1fd1d475, frontend returns only `?? .scratch/code-release-10-10-v2/verification/` (this verification pass's own output) at 31c3786a6. Two caveats recorded honestly: the release landed as two `--no-ff` MERGE commits plus one later frontend commit (31c3786a6 'perf(chat): halve the /chat page bundle'), not one squashed pair; and the backend tree WAS dirty during this audit (4 files, incl. migrations/meta/_chain.sha256.json resealed to 'sealedAt': '2026-09-04') and was reverted mid-audit, so GATE-STATUS.tsv (captured 08:13) may not describe commit e1fd1d475. THE 'PASS' HALF FAILS OUTRIGHT. Architecture gates: 9 backend at exit 1 (benchmark-manifest, cycles, openapi-coverage, over-300, route-budgets-http, test-suppressions, verify:chat-mentions, verify:membership-revocation, verify:multi-org-employment), 11 backend at exit 2 (INCONCLUSIVE, never a pass), and 2 frontend at exit 1 (route-bundle-budget 9 breaches, web-vitals-budget). Builds and typechecks: not run at the release commit (newest artifacts /tmp/be_typecheck.log 2026-09-03 16:48 and /tmp/fe-build.log 2026-09-02 23:23, both predating the 2026-09-04 04:37 merge). Disposable E2E: not run at head and covers only 2 of the 15 areas (see PRD-C018).",
      "blocker": "22 gates are non-green at the release commit (11 red, 11 inconclusive), and builds, typechecks and the disposable E2E were never run there. Closing needs every red gate fixed, every exit-2 prerequisite supplied, and one recorded run of both builds, both typechecks, the focused tests and the disposable E2E at a single SHA pair.",
      "clauses_failing": "'pass' — architecture gates (11 red + 11 inconclusive), builds (not run), typechecks (not run), disposable E2E (not run and 13/15 areas uncovered). The 'at one commit' clause is now technically available (clean pair backend e1fd1d475 / frontend 31c3786a6) but nothing was measured at it."
    },
    {
      "id": "PRD-C159",
      "verdict": "OPEN",
      "evidence": "The only bootstrap/parity bundle in the repository is architecture-refactor/final-refactor/evidence/bootstrap-head-637/. Its manifest.json records `journalAtProofCapture.entries: 637`, releaseIdentity frontend `e33873d6cccca8166fa5dad69aec3dd6679a0b3f` / backend `5ba6bbd7fabb8990d0462bafb10514209a7af90c` on refs/heads/main, and its own `workingTreeCaveat`: 'the proofs were run against the working tree, not against either commit… A checkout of either SHA will not necessarily reproduce the code that produced these numbers.' Its coverage note says plainly 'Head-637 parity is not head-639 parity.' The journal at the current head is 685 entries — `node -e \"console.log(require('./migrations/meta/_journal.json').entries.length)\"` -> 685, `ls migrations/*.sql | wc -l` -> 685, head tag `1061_push_endpoint_cross_tenant_claim` at idx 817. That is 48 migrations beyond the evidence, on a different commit pair. No bootstrap, interrupt/resume or parity run exists at the current head: `find /tmp -maxdepth 1 -name '*.log' -newermt 2026-09-04` returns nothing bootstrap-related, and no evidence directory under architecture-refactor/ references journal 685. The cold-replay half is separately unmeasured: check:replay-ledger is exit 2 (INCONCLUSIVE) in GATE-STATUS.tsv, and per the exit contract exit 2 closes nothing.",
      "blocker": "The C159 evidence is 48 migrations and a different commit pair stale, and nothing was re-run at head. Closing needs three fresh runs at a frozen journal (two independent `node src/scripts/db-bootstrap.mjs` into empty databases, plus one SIGKILL-interrupted-and-resumed run) followed by compare-bootstraps.mjs across all three pairs — and the two known comparator defects (pg_temp_* not in SYSTEM_SCHEMAS; drizzle.__replay vs drizzle.__drizzle_migrations making migrationLedger unpassable between a bootstrap DB and a cold-replay DB) fixed first, or the comparison cannot return differences=0.",
      "clauses_failing": "All three runs (two empty bootstraps, one interrupted-then-resumed) are absent at the new authorized baseline; the catalog-parity comparison at head has not been performed."
    },
    {
      "id": "PRD-C160",
      "verdict": "OPEN",
      "evidence": "P0s: every P0 I re-verified at head is REPAIRED, refuting 31-audit.md's '3 P0 … stand' — 08-F2 (payout-run-completion.ts emits `net: disbursedNet` from resolveRunCoverage), 10-F2 (invoices-update.service.ts writes the gstSplit in the lineItems branch), 15-F1 (push.service.ts:43 `SELECT app.claim_push_endpoint(...)`, migration 1061), 15-F2 (unsubscribe.controller.ts GET is read-only; opt-out written to notification_suppression_rules at token scope), 16-F1 (kb-candidate.service.ts:52 takes orgId, filters on it, and re-runs exactly on a short pool). P1s: the criterion covers P0 AND P1, and at least four P1s from the committed audit corpus reproduce verbatim at head, each confirmed by grep — 14-audit finding 3 (`grep -rn NoTenantTransaction src/modules/mail/` = 0 hits, vs 48 files repo-wide), finding 4 (`grep -rn 'markStaleForAccount|clearPositions' src/` returns only the two definitions, zero callers), finding 5 (mail-metadata.service.ts:165 still `rows.length === 0 || rows.length > SEARCH_ID_CAP`), finding 6 (`grep -rn loadPosition src --include=*.ts | grep -v spec` returns only the definition at mail-sync-checkpoint.service.ts:13). Separately, backend commit 9534abcf9's own message reports '210 P0/P1 claims … 118 confirmed, 60 rejected or materially corrected' — 178 of 210 — leaving 32 claims with no recorded outcome, so the true open count cannot be lower than 4 and is not established.",
      "blocker": "Four P1 findings are open and reproducible at the release commit, and 32 of the 210 audited P0/P1 claims have no recorded fix-or-rejection. Closing needs those four fixed and a per-claim outcome record for all 210.",
      "clauses_failing": "'No unresolved code-level P0/P1 finding remains' — the P1 half. The P0 half holds on every P0 re-verified this pass."
    }
  ]
}
