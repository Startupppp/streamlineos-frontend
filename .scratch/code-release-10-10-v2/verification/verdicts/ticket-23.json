{
  "ticket": "23",
  "heads": {
    "backend": "e1fd1d475e64a94f6b9273fc3d06608697375519 (main; working tree dirty: 4 gate-emitted baseline JSON files)",
    "frontend": "31c3786a6621dea105dea11e7676299900fccd06 (main)"
  },
  "note": "GATE-STATUS.tsv header records both repos at 31c3786a6; the backend is actually at e1fd1d475. All gate re-reads below are from the captured /tmp/beg-*.log at that sweep plus my own re-runs at head.",
  "criteria": [
    {
      "id": "PRD-C082",
      "verdict": "OPEN",
      "evidence": "Seam exists and several limbs hold: check:permission-keys exit 0 (704 backend keys == 704 frontend union); check:tenant-isolation exit 0 AND check:tenant-isolation:run exit 0 with 452 suites / 1873 tests passing (/tmp/beg-check_tenant-isolation_run.log) — this REFUTES the prior 23-audit.md claim that 'check:tenant-isolation:run ... NOT RUN'. check:scope-application now scans src/modules + src/common + src/db (check-scope-application.mjs:34-38 SCAN_DIRS), and check:record-access likewise (check-record-access.mjs:40-44) — this REFUTES prior finding F7 ('five data-access gates scan src/modules only'); check-unbounded-reads.mjs:70-71, check-n1-growing-loops.mjs:91-92 and check-db-call-count.mjs:138-139 all carry @common/@db corpora with minFiles floors at head. What does NOT hold is the word 'every'.",
      "blocker": "Three dimensions are unarbitrated. (1) RECORD: check:record-access is a soft-delete gate only — its own run output is 'OK — every record read excludes soft-deleted rows' over 1193 findFirst calls / 591 record reads; no gate anywhere asserts a per-record authorization decision is made at the right seam. (2) PERMISSION-NOT-INERT: /tmp/beg-check_authz-deny.log — 926 of 3237 authorization-gated handlers (29%) have a declared deny test; 2311 have none and the gate passes because uncoveredRatchet=2441 is a ratchet, not a floor, and the gate's own note says it is static ('proves a deny test EXISTS ... does not run it', attribution per spec FILE not per it()). (3) MODULE ENTITLEMENT: src/scripts/check-module-entitlement.mjs:7 `const PILOT_MODULE = \"timesheets\"` — the run output is one line, 'OK — timesheets: planGated=true ...', covering 1 of 76 module folders. Module reach is also partial: `node src/scripts/check-module-gate.mjs --verbose` at head prints 'Scanned 391 controllers across 22 module folders' against 76 folders under src/modules and 551 controller files. To close: a gate that asserts record-level authorization at the seam, a floor (not a ratchet) under authz-deny coverage, and module-entitlement extended past the one-module pilot.",
      "clauses_failing": "record; DataScope/permission enforcement proof (deny coverage 29%, ratcheted); module entitlement (1 of 76)"
    },
    {
      "id": "PRD-C083",
      "verdict": "OPEN",
      "evidence": "Transactional limb holds (TenantContextInterceptor wraps each request in one withTenant transaction). Concurrent-retry-safe holds for the outbox (for update skip locked + lease). Two prior findings are REFUTED as fixed at head: F3 — src/modules/webhooks/webhooks-dispatch.service.ts:139-145 now reads `if (!registerAfterCommit(deliver)) void deliver();` with a 17-line doc comment naming the exact 42501 defect; and the outbox now bounds consumer work with `withDeliveryDeadline(event.eventType, OUTBOX_DELIVERY_DEADLINE_MS, () => consumer.handle(event), ...)` at src/common/outbox/outbox-publisher.service.ts:166-181, the deadline derived from idle_in_transaction_session_timeout at src/common/outbox/outbox-delivery-deadline.ts:28-32. Notification durability is also repaired: NotificationDispatchService.emit writes the intent into `ambient.tx` at notification-dispatch.service.ts:90-115 and only defers delivery via registerAfterCommit at :118, pinned by notification-dispatch-after-commit.spec.ts.",
      "blocker": "REAL VIOLATION of 'never a dead request transaction', same shape the repo itself documents as forbidden. src/modules/hr/automations/hr-webhooks.service.ts:245-249 — `dispatch(orgId, event, payload): void { void this.run(orgId, event, payload).catch(err => logger.error(...)); }`. `grep -n 'registerAfterCommit\\|runInNewTenantTransaction' src/modules/hr/automations/hr-webhooks.service.ts` returns NOTHING. `run()` issues `await this.db.select()...limit(200)` (:257-271) and then, AFTER that await has resolved — i.e. after the handler has returned and TenantContextInterceptor has committed and released the connection — `await this.db.insert(hrWebhookDeliveries)...returning()` at :275-287, then attemptDelivery() which raw-fetches with retries and writes delivery status. `this.db` is the tenant-aware proxy resolving to context.tx, so those post-await writes land on a committed transaction with the GUC gone: SQLSTATE 42501, swallowed by the .catch that only logs. Request-path callers: src/modules/hr/directory/employee-onboarding.service.ts:345, src/modules/hr/time/leave-decision-effects.service.ts:60, src/modules/hr/automations/hr-automation-engine.service.ts:61. Same shape at :205 (`void this.attemptDelivery(...)` after a returning insert in sendSample) and :234 (redeliver). check:fire-and-forget scores this only as one of 253 TIER-2 entries against ratchet 279 and exits 0 (/tmp/beg-check_fire-and-forget.log), and its own header lists the shapes it cannot see. SECOND failing clause — idempotency: 258 @Idempotent decorators over 2135 mutating handlers (12.1%), and /tmp/beg-check_idempotent-commands.log shows 547 controllers scanned, 11 handlers in scope, all 11 SKIPped, then 'OK — every in-scope mutating handler carries @Idempotent' over an ENFORCED POPULATION OF ZERO. To close: route hr-webhooks dispatch through registerAfterCommit / runInNewTenantTransaction (the pattern already written 20 lines away in webhooks-dispatch.service.ts), and give check:idempotent-commands a non-empty enforced population with an anti-vacuity floor.",
      "clauses_failing": "idempotent; side effects never a dead request transaction"
    },
    {
      "id": "PRD-C084",
      "verdict": "OPEN",
      "evidence": "Two clauses hold. Generic errors / serialization / redaction: one envelope in all-exceptions.filter.ts, check:log-secrets exit 0 over 3677 files ('Redactor 23 substrings + 18 exact names'). Stable HTTP semantics NOW HOLDS — this REFUTES the prior 23-audit.md claim that 'check:envelope-consistency EXITS 1 with 6 violations, of which 4 are in scope (GET /me/inbox, /notification-templates, /notifications, /sign/envelopes)'; at head /tmp/beg-check_envelope-consistency.log ends 'Total violations: 0 / OK — response/error envelopes are consistent', 338 of 339 paginated GETs scanned (99.71%), and GATE-STATUS.tsv records exit 0. check:compression exit 0.",
      "blocker": "MINIMAL RESPONSE PROJECTIONS FAIL as a REAL VIOLATION at head: check:openapi-coverage now exits 1 (GATE-STATUS.tsv line 'check:openapi-coverage' = 1 — itself a refutation of the prior audit's 'exits 0'). /tmp/beg-check_openapi-coverage.log enumerates operations with '2xx response (200) declares no content schema — a bare auto-generated key is not a contract' and truncates with '... and 3591 more'. The response half of the contract is undeclared, so there is nothing against which a projection can be judged minimal. RESOURCE LIMITS partial: my own census over the 353 non-spec *.schemas.ts / *.dto.ts files found 439 `z.array(` properties, 155 carrying a .max()/.length(), 284 uncapped — including the ai/ LLM output schemas, which are shape-validated but size-unbounded. To close: declare 2xx content schemas (or ratchet openapi-coverage back to exit 0 on a real floor) and cap the unbounded arrays.",
      "clauses_failing": "minimal response projections; resource limits"
    },
    {
      "id": "PRD-C102",
      "verdict": "OPEN",
      "evidence": "Backend substrate is real: src/common/observability/ carries redact.ts, tracing.ts, seam-budgets.ts, error-classification.ts, async-hop.ts and trace-boundary-coverage.spec.ts, whose BOUNDARIES table (:11-20) enumerates the eight PRD boundaries with an 'covers all eight named boundaries' assertion at :104. check:log-secrets exits 0 over 3677 files. check:alert-system exits 0 with 14 alert scripts self-testing green, including alert-tenant-ctx-errors (which literally fires on 'permission denied for table outbox_events'), alert-queue-age, alert-job-queue-age and alert-dispatch.",
      "blocker": "Four unmet clauses. (1) FRONTEND CORRELATION GAP — the trace does not start at the browser for four request classes: only frontend/lib/api-client.ts:181-184 mints and sends x-correlation-id (`headers.set(\"x-correlation-id\", correlationId); noteCorrelationId(correlationId)`); `grep -rn 'x-correlation-id' lib/ features/ app/` returns that file and nothing else. frontend/lib/portal-api-client.ts:88-99 (portalFetch builds Headers, sets only Authorization), frontend/lib/server-fetch.ts:14-31, frontend/lib/public-fetch.ts:69 and :88, and frontend/features/notifications/notification-event-stream.ts:34-36 (the SSE stream — an asynchronous boundary C102 names explicitly) all reach the backend with no correlation header. (2) TENANT-UNSAFE LOG — src/modules/ingress/adapters/crm-mailbox.service.ts:341 `this.logger.warn(\\`sweep failed for ${row.mailboxAddress}: ${message}\\`)` puts a customer mailbox address above the production warn threshold; still present at head. (3) EXPECTED-FAILURE MISCLASSIFICATION — src/modules/support/kb-gap/support-kb-gap.service.ts:94 `logger.error(\"support kb-gap draft failed\", { orgId, gapId, kind: gatewayResult.kind })` fires at error for kind 'quota_exceeded', an expected billing outcome, directly against the criterion's 'classify expected domain failures separately from actionable faults'. (4) ALERT DELIVERY UNPROVEN — check:alert-ack exits 2 (INCONCLUSIVE, never a pass): /tmp/beg-check_alert-ack.log reads 'PREREQUISITE MISSING: ALERT_WEBHOOK_URL is not set. Alert acknowledgement cannot be confirmed without a configured webhook destination.' To close: set ALERT_WEBHOOK_URL and run node src/scripts/drill-alert-system.mjs to completion, add x-correlation-id to the four frontend request paths, drop the mailbox address from the warn line, and demote quota_exceeded below error.",
      "clauses_failing": "correlate one user intent through asynchronous work (frontend entry classes); tenant-safe logs (mailbox address); classify expected domain failures separately from actionable faults; alert acknowledgement (exit 2 prerequisite)"
    },
    {
      "id": "PRD-C136",
      "verdict": "OPEN",
      "evidence": "Two prior findings are REFUTED as fixed at head. F5 — src/common/outbound/call-provider.ts:152 now reads `if (cls === \"retryable\") breaker.recordFailure(...)`, with a 14-line comment explaining that counting a terminal 4xx opened `razorpay-orders` for every other tenant; a terminal failure no longer touches the counter. F6 — src/health/health.controller.ts:79-96 no longer reads sharedProviderBreaker; it calls `providerCheck(this.config.requiredProviders, (now) => openProvidersAcrossBreakers(now), ...)` with a comment naming the exact 'permanently empty map' defect. Bounded retries (attempt <= maxAttempts, full-jitter backoff at :38-47), per-attempt timeouts (wrapWithTimeout at :53-64) and per-attempt spans with traceparent + x-correlation-id injection all hold; razorpay does razorpayOrderResponseSchema.parse.",
      "blocker": "The named clause 'removal of duplicate provider-specific policy from product modules' is NOT DONE. `grep -rn 'callProvider(' src --include='*.ts' | grep -v spec` returns exactly THREE call sites tree-wide: build/core/projects-webhooks-dispatch.service.ts:260, webhooks/webhooks-dispatch.service.ts:200, billing/payments/adapters/razorpay.adapter.ts:114. Against that, product modules still run their own policy against raw fetch: src/modules/hr/automations/hr-webhooks.service.ts:33 `function backoffMs(attempt)` with its own raw `await fetch(url, ...)` at :316; src/modules/inventory/webhooks/webhook-emitter.service.ts:93-96 its own AbortController + 10_000 ms timeout; src/modules/automation/automation-webhook.service.ts:81; src/modules/crm/automation-studio/crm-automation-runner.service.ts:234; src/common/security/turnstile.service.ts:41. None of these is circuit-broken, span-traced or observable through the shared seam. Two further limbs fail: idempotency (12.1% of mutating handlers, gate enforces over 0 — see C083) and backpressure at the provider seam (45 provider-in-transaction handlers, see C147). Cache/credential isolation remains partial: razorpay.adapter.ts:57 `const razorpayBreaker = new ProviderCircuitBreaker()` keyed on the process-global string 'razorpay-orders' at :116 while credentials are per-tenant, so a retryable outage for one tenant still opens the circuit for all. To close: migrate the eight raw-fetch provider call sites onto callProvider and tenant-scope the razorpay breaker key.",
      "clauses_failing": "removal of duplicate provider-specific policy from product modules; idempotency; backpressure; credential/cache isolation of the breaker key"
    },
    {
      "id": "PRD-C146",
      "verdict": "OPEN",
      "evidence": "The reference implementation is real and excellent: src/modules/storage/storage.controller.ts:142 checks `this.transforms.hasCapacity()` BEFORE any expensive work, writes a durable quarantine row, defers with registerAfterCommit and returns `{quarantineId, status:'pending_scan', ...}` promptly; MediaTransformRunner (src/modules/storage/media-transform.runner.ts) bounds it at MAX_CONCURRENT=2 / MAX_QUEUED=32 (refused past that) / JOB_TIMEOUT_MS=60_000 with drain() on shutdown. FinanceReportExportRequestedConsumer only calls worker.wake() and returns.",
      "blocker": "Adoption is partial and the heavy work is still on request threads. `grep -rn 'MediaTransformRunner|transforms.hasCapacity' src --include=*.ts | grep -v spec` shows the runner injected into exactly THREE controllers — storage.controller.ts:109/:142, storage-onboarding.controller.ts:52/:98, feedbucket-public.controller.ts:131/:219 — of 76 module folders. /tmp/beg-check_placement-bypass.log still records 45 `SKIP [provider-in-transaction]` entries at head (`grep -c 'SKIP  \\[provider-in-transaction\\]'` = 45), every one annotated 'PRE-EXISTING, UNAUDITED', i.e. a provider round trip on a request thread holding a pooled connection: e.g. support/core/support-kb.controller.ts:294 reindexAll (re-indexes an entire corpus through AiGatewayService.embedBatchWithCredit inline), support-kb.controller.ts:258 askQuestion, payroll/insights/payroll-ai-explain.controller.ts:43, and 10 handlers on support/core/support-ai.controller.ts. The gate exits 0 because they are allowlisted with a reason, not because they were moved. There is also no shared durable job/status contract: src/scripts/alert-job-queue-age.mjs:32 declares JOB_QUEUES with `JOB_QUEUES.length === 8` asserted at :107, while the tree carries 13+ per-module job tables each re-implementing claim/progress/complete/fail. To close: move the 45 allowlisted provider-in-transaction handlers behind a durable job, or shrink that list with real migrations rather than allowlist entries.",
      "clauses_failing": "move CPU/IO-heavy work off request threads (45 provider-in-transaction handlers); single durable job/status contract"
    },
    {
      "id": "PRD-C147",
      "verdict": "OPEN",
      "evidence": "Mechanisms exist and are well reasoned: src/db/pool-admission.ts is a gate in front of postgres-js (which has no acquire timeout), with DEFAULT_ACQUIRE_TIMEOUT_MS=5_000 (:33) and DEFAULT_QUEUE_DEPTH_FACTOR=4 (:34), wired at pool.config.ts:221-222; AdmissionGuard is an APP_GUARD; MediaTransformRunner bounds media work at 2/32/60s.",
      "blocker": "The numbers are not reconciled, so nothing bounds the process. Verified at head: pool max = 10 on a direct Neon endpoint (pool.config.ts:15 DIRECT_ENDPOINT_SAFE_MAX=10, :176-177 `tuning.DB_POOL_MAX ?? (isDevelopment ? 5 : isPooled || !isNeon ? 20 : 10)`), queue = max*4 = 40, acquire timeout 5s → 50 total capacity. admission.config.ts:58-65 defaults: maxConcurrent 200, maxQueueDepth 400, orgMaxConcurrent 50, reservedFraction 0.2. One tenant at its own admission ceiling (50) therefore consumes 100% of the process's database capacity, and the reported ceiling is neither number — AdmissionService.snapshot() at admission.service.ts:59-65 returns `maxConcurrent: this.config.maxConcurrent` = 200, while the real shed line for ordinary writes is floor(200*0.8)=160 and reserved classes face only maxQueueDepth=400. NO TENANT DIMENSION in the rate limiter: rate-limit.guard.ts:30 `const identifier = req.user?.userId ?? extractClientIp(req)` and rate-limit.service.ts builds `rl:${tier}:${identifier}` — no orgId anywhere. PROVIDER LIMB FAILS: 45 handlers hold a pooled connection across a provider round trip (/tmp/beg-check_placement-bypass.log), so ~11 concurrent AI requests exhaust a 10-connection direct pool and every unrelated route is shed after the 5s acquire timeout; a standard-tier LLM call at 60s sits exactly on idle_in_transaction_session_timeout (pool.config.ts:122, 60_000). BUDGETS UNMEASURED: check:route-budgets prints 'PARTIAL — 387 of 648 declared ceilings are measured ... 93/3648 operations carry a budget at all' and exits 0; check:route-budgets-http EXITS 1 with real breaches (POST /chat/channels/{channelId}/messages measuredLatencyP95Ms=5043 > 1000; measuredDownstreamCalls=3 > 0; GET /cron/storage-sweep measuredDownstreamCalls=1 > 0) plus 'contracts/route-budgets.json is out of date with the capture'. To close: reconcile ADMISSION_ORG_MAX_CONCURRENT against DB_POOL_MAX+queue, add an org dimension to the rate limiter, and release the connection on the 45 provider-in-transaction handlers.",
      "clauses_failing": "connection-pool vs per-tenant limit reconciliation; per-tenant limit (rate limiter has no org dimension); provider limit (45 handlers hold a connection across the call); queue/worker ceilings unmeasured (check:route-budgets-http exits 1)"
    }
  ]
}
