{
 "ticket": "30",
 "heads_verified": "backend release/v2-closeout e1fd1d475e64a94f6b9273fc3d06608697375519 (identical to main); frontend release/v2-closeout 31c3786a6621dea105dea11e7676299900fccd06; DB gates measured against local postgres scratch_head_1010 as role streamline_app (rolbypassrls=f, 944 public tables), read-only",
 "criteria": [
  {
   "id": "PRD-C011",
   "verdict": "REFUTED",
   "evidence": "REFUTED CLOSURE CLAIM: the ticket Status reads 'PRD-C011/C015/C104 closed'. The criterion demands 'bite-proven architecture/release gates AND a portable verification harness'. The harness half holds (see PRD-C015). The bite-proven half does not.\n\nWHAT I VERIFIED AS TRUE (all of the ticket's own sub-claims about check-gate-wiring hold):\n* `node src/scripts/check-gate-wiring.mjs --self-test` -> 'check-gate-wiring self-tests: 28 passed', exit 0, in BOTH repos (frontend: `node scripts/check-gate-wiring.mjs --self-test` -> same line). The gate that guards the gates now has a self-test.\n* I bite-proved the new continue-on-error rule MYSELF, in a mkdtemp copy of the frontend repo (.github/workflows + frontend/scripts + frontend/package.json, node_modules symlinked), never in the working tree. CONTROL: exit 0, 'check-gate-wiring: 36 gates, 33 of them able to FAIL the job (3 registered non-blocking)'. MUTATION A (step-level `continue-on-error: true` inserted after .github/workflows/frontend.yml:645 `run: pnpm run check:gated-reads:self-test && pnpm run check:gated-reads`): exit 1, 'CANNOT FAIL: check:gated-reads - every step invoking it carries `continue-on-error`, so CI reports success whatever it finds.' MUTATION B (job-level `continue-on-error: true` on the `gates` job at frontend.yml:262): exit 1, 33 CANNOT FAIL lines. NO FALSE POSITIVE: the same insertion under a non-job key produced exit 0.\n* '12 of 18 flags removed; 6 remain with named owners' HOLDS. `grep -c '^ *continue-on-error: true' .github/workflows/*.yml` = 3 backend (all in ci.yml: :71 knip Dead-code report, :1124 Route-budget HTTP capture, :1145 Upload the perf capture) + 3 frontend (check:dead-code, check:route-bundle-budget, check:web-vitals-budget) = 6, and each of the 3 frontend ones is registered in NON_BLOCKING_BY_DESIGN in frontend/scripts/check-gate-wiring.mjs:97-136 with a measured reason and a named owner; the 3 backend ones carry owner comments in ci.yml.\n\nWHY IT IS STILL NOT MET - 'bite-proven ... gates' is a statement about the gate SET, and 11 gate commands have no bite proof of any kind:\n* BACKEND, enumerated from package.json (96 non-helper check:/verify: commands, 88 with a ':self-test' sibling): EIGHT have none - check:s05-artifact-contract, check:alert-system, check:body-binding, check:cycles, verify:permissions, verify:chat-mentions, verify:membership-revocation, verify:multi-org-employment. The ticket's own report named only three. Three of the eight (verify:chat-mentions, verify:membership-revocation, verify:multi-org-employment) are exit 1 at head per GATE-STATUS.tsv, so they are un-bite-proved AND red.\n* FRONTEND (37 non-helper commands, 34 with a sibling): THREE have none - verify:server-data-seam, check:cycles, check:properties.\n* Note check-s05-artifact-contract.mjs and check-alert-system.mjs do contain a --self-test string internally, but no package.json entry runs it and ci.yml:682 runs `pnpm check:alert-ack:self-test` only - `check:alert-system` itself has no self-test step.\n\nAND SIX GATES EXIT 0 WHILE ENFORCING NOTHING (a green that is not a bite), each read from its own output at head:\n* check:idempotent-commands - 'Controllers scanned 547 / Handlers in scope 11', and all 11 are SKIP lines. Enforced population = ZERO, prints 'OK - every in-scope mutating handler carries @Idempotent.'\n* check:multipart-contracts - prints 'scanned 551 controller file(s) ... OK' and never names the governed count. I counted it: only 8 files in src/ reference FileInterceptor/FilesInterceptor outside specs, and the REAL upload path - src/modules/storage/storage-multipart.controller.ts initiate/complete/abort (:68,:133,:204) - carries no interceptor at all, so the gate governs ZERO of the multipart surface it is named for.\n* check:public-object-urls - 'public-base references 9 (9 declared)', every one SKIPped. Zero enforceable sites.\n* FE check:response-contracts - 'Carrying a contract: 86 (3.2%)' of 2666 seam calls; the gate's own last line: 'NOTE: 97.8% of the seam is still an unchecked cast. This gate freezes that debt; it does not retire it.' PASS.\n* FE check:contract-drift - 'Timesheets calls extracted: 49'. One module of the whole app.\n* src/scripts/check-module-entitlement.mjs:7 - `const PILOT_MODULE = \"timesheets\";` - 1 module.\n* check:fire-and-forget is green (tier 1 = 0, tier 2 = 253 vs ratchet 279) while src/modules/hr/automations/hr-webhooks.service.ts:246 does `void this.run(orgId, event, payload).catch(...)` with no registerAfterCommit - I read the file; the shape the gate is named for is live and unflagged.",
   "blocker": "11 gate commands (8 backend + 3 frontend) have no self-test / known-bad fixture of any kind, so they cannot be shown to fail for the intended reason; and at least six more exit 0 over an enforced population that is empty or a single pilot module. Closing needs a self-test per un-proved gate and a governed-count (not corpus-count) line per vacuous gate - the primitive for the latter, src/scripts/gate-corpus.mjs, exists and is adopted in only 2 gates.",
   "clauses_failing": "'bite-proven architecture/release gates' FAILS. 'portable verification harness' HOLDS (see PRD-C015). Every stated sub-claim of the ticket body (28-assertion self-test, step- and job-level continue-on-error detection, 12-of-18 flags removed, 6 remaining with named owners) is TRUE and independently reproduced."
  },
  {
   "id": "PRD-C015",
   "verdict": "CLOSED",
   "evidence": "All three clauses verified by me at backend e1fd1d475 / frontend 31c3786a6.\n\n(1) NO ABSOLUTE WORKSTATION PATHS. `grep -rn '/Users/|/home/[a-z]|C:\\\\Users' src/scripts/ scripts/ .github/workflows/ package.json` in the backend -> ZERO hits. Same sweep over frontend/scripts/, .github/workflows/, frontend/package.json, package.json -> 3 hits, all false positives on the substring 'lib/home/' (generate-home-manifest.mjs:4, check-home-manifest.mjs:4, assertion-ceiling-ledger.json:1972). The four `C:\\Program Files\\...` literals in the browser drivers are Chrome/Edge discovery candidates, which is correct.\n\n(2) BOTH REPOSITORIES RESOLVE FROM THE WORKSPACE OR AN EXPLICIT VALIDATED ARGUMENT - and the validation bites. frontend/scripts/check-contract-vendor.mjs:6 now imports `{ BACKEND_ROOT, backendAvailable, backendUnreachableReason }` from the shared ./check-repo-paths.mjs. MEASURED MYSELF: control `node scripts/check-contract-vendor.mjs` -> exit 0. `STREAMLINE_BACKEND_ROOT=/nonexistent-xyz node scripts/check-contract-vendor.mjs` -> exit 2 (INCONCLUSIVE, per the exit contract) printing 'STREAMLINE_BACKEND_ROOT is set to \"/nonexistent-xyz\" but that directory does not contain src/modules/rbac/permissions.' This is the exact defect the ticket claimed to fix (the gate previously read a private STREAMLINEOS_BACKEND_ROOT one letter different from the documented variable and exited 0 against a checkout the operator had overridden away from). The legacy variable survives only as a fallback at :30, after the shared resolver.\nThe two backend depth-guess fixes are also at head: src/scripts/production-ops-evidence.mjs:24 and src/scripts/collect-s7-evidence.mjs:6 both import WORKSPACE_ROOT from check-repo-paths.mjs, and each retains a comment recording the old `resolve(scriptDir, '../../..')` / `resolve(backendDir, '..')` guess it replaced.\n\n(3) WINDOWS/macOS/LINUX. run-gate.mjs exists in both repos (frontend/scripts/run-gate.mjs, src/scripts/run-gate.mjs), is `#!/usr/bin/env node` - not a bash `set +e` snippet, so it does not depend on `$?` semantics that differ under PowerShell - and it splits the exit contract correctly: exit 2 becomes '::warning title=INCONCLUSIVE::...' and a passing step, every other non-zero fails (scripts/run-gate.mjs:41, :157). Path construction in the fixed scripts uses node:path segment arguments rather than 'a/b/c' string literals.",
   "blocker": "",
   "clauses_failing": "None. EVIDENCE CAVEAT, not a failing clause: no workflow run was ever triggered on a Windows or Linux runner - the ticket's own report says 'CI behaviour is inferred, not observed' and the single-repository case was simulated by pointing the resolver at a nonexistent directory rather than by a real one-repo clone. I verified the mechanism is OS-agnostic by construction and verified the validated-argument path bites on macOS; the Windows/Linux executions are unobserved."
  },
  {
   "id": "PRD-C064",
   "verdict": "OPEN",
   "evidence": "The ticket self-declares PARTIAL. Confirmed still PARTIAL at head, but the ledger has moved in BOTH directions and two of its named blockers are refuted.\n\nSUB-PROOFS THAT HOLD (exit 0 at head, GATE-STATUS.tsv + gate logs): migration chain (check:migration-chain, :migration-discipline, :migration-ledger, :migration-rollback, :migration-immutability), snapshot-chain guard (check:db-generate-guard - '672 journal entries, latest snapshot 0464, real drift 207'), catalog parity (check:permission-keys), tenant indexes (check:tenant-indexes), cache invalidation (check:cache-invalidation exit 0 - the report recorded this as NOT RUN), contract-breaking-change, contract-registry, operation-ids.\n\nTWO OF THE TICKET'S NAMED BLOCKERS ARE REFUTED AT HEAD:\n* 'tenant relationships - INCONCLUSIVE, exit 2, target mid-bootstrap at 573/672' is NO LONGER TRUE. MEASURED MYSELF: `TENANT_RELATIONSHIPS_GATE_DATABASE_URL=postgres://streamline_app:ci@127.0.0.1:5432/scratch_head_1010 DATABASE_URL=... APP_DATABASE_URL=... PGSSLMODE=disable pnpm -s check:tenant-relationships` -> exit 0, 'Actionable 0 / OK - zero actionable single-column tenant FKs.' The exit 2 in GATE-STATUS.tsv is an unset environment variable on this machine, not a defect.\n* '`check:contract-vendor` exit 1: the vendored OpenAPI copy is STALE' is NO LONGER TRUE: `node scripts/check-contract-vendor.mjs` -> exit 0 at frontend 31c3786a6.\nFour more DB gates flip 2 -> 0 the same way, which I ran read-only against scratch_head_1010 as streamline_app: check:declaration-column-drift exit 0 ('Catalog half OK - no write-blocking or read-blocking column drift'), check:referential-action-drift exit 0, check:set-null-column-lists exit 0 ('Catalog SET NULL constraints 805 ... Catalog half OK'), check:audit-log-privileges exit 0 ('{\"role\":\"streamline_app\",\"isAppRole\":true,\"updateRevoked\":true,\"deleteRevoked\":true,\"triggerPresent\":true}').\n\nWHY IT IS STILL OPEN - four named clauses of the criterion are unmet or unproven:\n* 'OpenAPI/contract compatibility' NOW FAILS. check:openapi-coverage is exit 1 at head: 'response-schemas: 27/3648 ops declare a response body SCHEMA (0.74%) / 3621 uncovered [ceiling 3617, ratchet] ... FAIL - 3621 operation(s) declare no response schema, above the ceiling of 3617.' The ticket recorded this gate as exit 0 at its ceiling; the debt has since GROWN past its own ratchet. That refutes the ticket's C064 line 'check:openapi-coverage exit 0 (3,642 operations stamped)'.\n* 'two clean bootstraps' STILL NOT RUN. check:replay-ledger is exit 2 - its log is one line, 'COLD_DATABASE_URL is required' - and the database it needs is one built by `replay:chain-cold` (which writes drizzle.__replay); db:bootstrap does not produce it. This is one of only two backend gates that stay honestly inconclusive after supplying every available URL.\n* 'query plans' NOT RUN. No gate at head emits one; check:route-budgets-http is exit 1 and its CI step (ci.yml:1123) needs five CI_PERF_SEED_* repository secrets that the ticket's own report records as nonexistent.\n* FINAL ACCEPTANCE - 'zero unclassified unnecessary keys and no orphaned schema/code reference' - UNPROVEN. The knip reach fix was NOT landed, exactly as the ticket says. I read both configs at head: backend knip.json `entry` still contains 'src/**/*.spec.ts', 'src/**/*.e2e-spec.ts', 'test/**/*.ts', 'evals/**/*.spec.ts' and has no `\"jest\": false`, so the jest plugin is on; frontend knip.json `entry` still contains '**/*.test.{ts,tsx}', '**/*.spec.{ts,tsx}', '**/__tests__/**' with no `\"jest\": false`. Production code kept alive only by its own test is structurally unreportable, and the BLOCKING backend check:dead-code is green over that input: 'knip: 0 unused file(s) ... PASS: every dead-code finding is classified'. Root CLAUDE.md section 10 makes knip the required proof for any dead-code claim, so the orphaned-reference clause rests on a tool configured not to find it.\n\nHARNESS-LEVEL NOTE bearing on this criterion: even where the rule holds, the harness does not always run it. ci.yml:419 runs `pnpm check:module-lifecycle:self-test` only - the live half is never run in CI. ci.yml:682 runs `pnpm check:alert-ack:self-test` only. ci.yml:1070 runs check:audit-log-privileges under `APP_DATABASE_URL: ${{ secrets.CI_APP_DATABASE_URL }}` and its own step comment says 'Until this job runs, the privilege boundary is UNPROVEN and must not be cited as evidence.' No CI run has been observed for any of it.",
   "blocker": "check:openapi-coverage exit 1 (3621 uncovered vs ceiling 3617) breaks the OpenAPI/contract clause; check:replay-ledger exit 2 (no COLD_DATABASE_URL, and no database built by replay:chain-cold exists) leaves 'two clean bootstraps' unrun; no query-plan proof exists (needs the five absent CI_PERF_SEED_* secrets); and 'no orphaned schema/code reference' rests on a knip config in BOTH repos that still makes every spec an entry point, so backend check:dead-code is green over a blind input.",
   "clauses_failing": "'OpenAPI/contract compatibility'; 'two clean bootstraps'; 'query plans'; final acceptance 'zero unclassified unnecessary keys and no orphaned schema/code reference'. Clauses that DO hold: migration chain/ledger, catalog parity, tenant relationships (refuting the ticket's blocker), tenant indexes, cache invalidation."
  },
  {
   "id": "PRD-C104",
   "verdict": "REFUTED",
   "evidence": "REFUTED CLOSURE CLAIM: the ticket Status reads 'PRD-C011/C015/C104 closed'. Two of the criterion's own clauses fail at head, each measured by me.\n\nCLAUSE 'zero silently skipped/quarantined tests' - FAILS OUTRIGHT. check:test-suppressions is exit 1 at backend e1fd1d475, and its full output is:\n  'Spec files 2201 - suppression sites 20 - conditional aliases 65\\n   conditional 66 - placeholder 13 - quarantine 6\\n   FAIL - 66 runtime-selected suppressions, above the ratchet of 29. This class is honest but it is also the easiest place to launder a hand-written skip, so it is capped.'\nSix quarantined tests and 66 runtime-selected suppressions is not zero, and the gate is RED against its own cap.\n\nCLAUSE 'make EVERY architecture/release gate bite-proven with a known-bad fixture or mutation' - FAILS. Enumerated from package.json: 8 backend gate commands have no ':self-test' sibling (check:s05-artifact-contract, check:alert-system, check:body-binding, check:cycles, verify:permissions, verify:chat-mentions, verify:membership-revocation, verify:multi-org-employment) and 3 frontend (verify:server-data-seam, check:cycles, check:properties). 11 of 133 gate commands have no bite proof of any kind. The ticket's own report claimed three.\n\nCLAUSE 'critical tests must exercise ... authorization deny/cross-tenant paths' - NOT MET AS A FACT. check:authz-deny is exit 0 but its own output reads 'Gated handlers with a DECLARED deny test 926 / 3237 (29%)' and 'Uncovered 2311 is BELOW the ratchet of 2441', plus 'NOTE: this gate is static. It proves a deny test EXISTS and is attributable to the handler; it does not run it.' 2,311 of 3,237 authorization-gated handlers have no deny test, and that hole is held by a RATCHET rather than a floor - i.e. the number is licensed to stay, only not to grow.\n\nCLAUSE 'swallowed promise failures' - PARTIALLY. check:fire-and-forget exit 0 with tier 1 = 0, but tier 2 = 253 floating promises + swallowed rejections held by ratchet 279, and I read a live instance of the named shape it does not flag: src/modules/hr/automations/hr-webhooks.service.ts:246 `void this.run(orgId, event, payload).catch(...)` with no registerAfterCommit, in a service whose run() writes hr_webhook_deliveries after its first await.\n\nCLAUSES THAT DO HOLD: 'vacuous mocks' - check:vacuous-assertions exit 0 ('Spec files 2066 - test callbacks 15770 - expect() calls 59842 ... every test that runs can fail, except 7 registered site(s)') and check:mock-surface exit 0 ('Doubles scanned 4257 - Genuine defects 0'). 'transaction callbacks' - check:transaction-callbacks exit 0. 'no baseline raised merely to turn a regression green' - check:baseline-integrity exit 0, 'unregistered 0 - stale 0 ... none moved in its unsafe direction', and it currently reports one IMPROVEMENT available (bare-throw 2 -> 1), which is the safe direction. The openapi-coverage ceiling of 3617 was recorded at the honest figure rather than raised to green - and it is now RED at 3621, which is the ratchet working.",
   "blocker": "check:test-suppressions is exit 1 with 6 quarantined tests and 66 runtime-selected suppressions against a ratchet of 29; and 11 gate commands (8 backend, 3 frontend) have no known-bad fixture or mutation at all. Closing needs the suppression count driven to the ratchet or below with the 6 quarantines removed, and a self-test authored for each of the 11 un-proved gates.",
   "clauses_failing": "'zero silently skipped/quarantined tests'; 'every architecture/release gate bite-proven with a known-bad fixture or mutation'; and as a fact 'critical tests must exercise authorization deny/cross-tenant paths' (29% coverage, held by a ratchet). Holding: vacuous mocks, transaction callbacks, no baseline raised to turn a regression green."
  }
 ]
}