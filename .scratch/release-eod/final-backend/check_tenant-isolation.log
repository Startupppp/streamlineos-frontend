Service files with db handle   998
  — tenant-owned               934
  — global/platform            64
Isolation test files found     715
Services with a DECLARED test  933 / 934  (100%)

NOTE: this gate is static. It matches a spec file that names the service and does not
      run it, so a spec that throws before its first expectation still counts here.
      Execution proof is `pnpm check:tenant-isolation:run`; both are required.

UNCOVERED — tenant-owned services with no cross-tenant negative test:
  MISSING  src/modules/hr/governance/positions/positions-transitions.service.ts

FAIL — 1 tenant-owned service(s) have no cross-tenant negative test (100% covered).
