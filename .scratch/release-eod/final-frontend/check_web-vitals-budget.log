Core Web Vitals budgets:
  [mobile]
    LCP p75   ≤ 2500ms
    INP p75   ≤ 200ms
    CLS p75   ≤ 0.1
    FCP p75   ≤ 1800ms
    TTFB p95  ≤ 600ms
  [desktop]
    LCP p75   ≤ 1500ms
    INP p75   ≤ 200ms
    CLS p75   ≤ 0.1
    FCP p75   ≤ 1200ms
    TTFB p95  ≤ 400ms

Commits since capture: UNKNOWN — the capture records no release SHA

check-web-vitals-budget: PROVENANCE — the capture measured build HLbxAqjmWOjuFHatusviu, but there is no .next/BUILD_ID to compare it against, so its freshness could NOT be checked. Build the app before this gate, or treat this run as proving nothing about whether the numbers describe the checked-out code.

  CAPTURE REFUSED — contentAssertion.unusableSamples: 16 sample(s) did not render real page content (branded loader, error boundary, or under 10 words)
      route(s): /crm/leads
      an error page paints almost nothing, so its LCP, FCP and CLS flatter the product
      the capture's own verdict: "capture is NOT usable evidence"

check-web-vitals-budget: FAIL — this capture is not usable evidence (1 refusal(s), provenance no-build-on-disk).
  No budget verdict is reported from it. Fix the capture and re-run measure:web-vitals;
  the producer refuses these runs itself, and this gate now refuses to publish them.
