Declared tables 873  ·  declared columns 10611
INCONCLUSIVE — the catalog half did not run. Whether a live column is missing from a declaration is only in pg_attribute; nothing static can see it, so all 873 declared tables are UNVERIFIED.
  Set COLUMN_DRIFT_GATE_DATABASE_URL to a database bootstrapped to head to run it.
  Or set STREAMLINE_ALLOW_PARTIAL_GATES=1 to accept a declaration-only run.
