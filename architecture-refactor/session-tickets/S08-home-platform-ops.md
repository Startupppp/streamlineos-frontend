# S08 — Home, Platform Operations, Contracts, Cache & Operator Evidence

Read `COMMON.md` first — especially §0 (ask once, then run to completion) and §0a (typecheck/build only at the end). Covers PRD §28.5, §28.16 and §21.

## Mission

Make Home a bounded read projection whose sections fail independently, raise generated contract coverage from 54% to complete, prove cache correctness across organizations and instances, and deliver honest operator evidence — runbooks where infrastructure is not ours to provision.

## Exclusive file ownership

```
backend/src/modules/dashboard/**      backend/src/modules/cron/**
backend/src/modules/audit-log/**      backend/src/modules/storage/**
backend/src/modules/ingress/**        backend/src/modules/activities/**
backend/src/modules/data-quality/**   backend/src/modules/public/**
backend/src/modules/portal/**         backend/src/modules/integrations/**
backend/src/common/**                 (EXCEPT common/rbac/** — S01 owns that)
backend/src/scripts/**                backend/migrations/**
frontend/features/dashboard/**        frontend/features/home/**
frontend/hooks/api/dashboard*
architecture-refactor/runbooks/**     architecture-refactor/docs/**
```

NOT yours: `frontend/app/**` (S09) · permission catalogs and `common/rbac/**` (S01) · domain modules (S02–S07).

## Already done — confirm, do not redo

- Home Build visibility now uses `build:manage` (verified at `modules/rbac/permissions/shared.ts:60`) with DataScope, `orgId` was added to both project-member predicates, and active-sprint totals moved from a JS reduce over every ticket to one bounded SQL aggregate. 51/51 dashboard tests pass.
- `CacheService.orgScopedKey` was extracted so org-scoped key composition is publicly assertable. `dashboard-home-scope.spec.ts` had been patching `CacheService.cached`, which `cachedForOrg` never calls, so both captured keys were `""` and the isolation assertion could not bite. Fixed.
- Home HR scope and permission-aware caching are implemented.
- Migration chain is repaired and `check:migration-chain` PASSES: 119 orphan future-dated `__drizzle_migrations` rows were deleted, `0661`/`0662` applied, `0659` recorded. 387 rows, watermark == journal head.
- `IdCursorPage.nextCursor` changed from `number | undefined` to `number | null` — `undefined` vanishes in JSON, collapsing "exhausted" and "not started".
- Zero circular imports in both repos, proven by `madge@8` to completion.

## Work items

### 1. Home read-model contract (§28.5)
- [x] Define the contract section by section: identity · attendance · availability · approvals · Build work · announcements · calendar · mail · notifications. DONE: L20-report provides full section contract table with Access, Data scope, and "Query omitted when denied?" for all 11 sections.
- [x] Mark each section universal self-service **or** bind it to an exact permission and data scope. DONE: L20-report; universal sections (identity, announcements, calendar, mail, notifications) vs permission-bound (stats, attendance, approvals, Build work).
- [x] A denied section is **omitted and does not execute its query** — not rendered empty. DONE: L20-report confirms denied sections return null (stats) or 403 at controller (attendance, approvals); module gate blocks Build work.
- [x] Section failures are isolated: one backend timeout or disabled module must not fail the whole Home response or page. VERIFIED DONE: `dashboard-personal.service.ts` uses `settle(source, run, fallback)` — each of 5 sources (myTasks, timesheet, leaveBalance, upcomingEvents, unreadNotifications) catches independently and appends to `degraded[]` without failing the response. All other dashboard sections are separate HTTP endpoints so each fails independently. Frontend wraps every section in `HomeSectionBoundary` (`features/dashboard/home-section-boundary.tsx`) which renders `ErrorState` with retry instead of crashing the page.
- [x] Minimal projections and bounded aggregates only — never fetch full module records to compute a card. DONE per S08 "Already done": active-sprint totals moved from JS reduce over every ticket to one bounded SQL aggregate.
- [x] Counts count **active memberships**, not globally active users. VERIFIED DONE: `dashboard-stats.service.ts` line 50-52: `.where(and(eq(organizationMembers.orgId, orgId), eq(organizationMembers.status, "ACTIVE")))` — counts only status=ACTIVE rows, not all members.
- [x] Server and Query cache keys include organization, membership, permission version, locale/timezone and relevant filters. Invalidate only affected section prefixes after mutations and organization switches. VERIFIED DONE: org via `cachedForOrg(orgId, ...)` (prefixes key with orgId); permission version via `buildOrgDashboardCacheKey(access, orgId, resource)` which reads `access.getPermissionsVersion(orgId)`; membership/userId via `buildScopedDashboardCacheKey(access, u, resource, scope)`; filter/scope as `dimension` param; locale/TZ: dashboard returns raw numeric/date values (not locale-formatted strings) so TZ dimension is not needed — the D4 test in `cache-key-collision.spec.ts` proves the mechanism works when needed. Invalidation: `useHomeCacheSync` invalidates `queryKeys.dashboard.all` on permission version bump; `QueryProvider` remounts on org switch clearing the client cache. Proven by `dashboard-home-scope.spec.ts` and `cache-key-collision.spec.ts`.
- [x] Split `dashboard-hr.service.ts` (635) by stable read-model responsibility, keeping one shallow caller contract. DONE: extracted `dashboard-stats.service.ts` (89), `dashboard-availability.service.ts` (230), `dashboard-birthdays.service.ts` (165), `dashboard-personal.service.ts` (244). Controller uses split services directly. L20-report; wc -l verified. NOTE: original file still exists at 635 lines as dead code — not yet deleted.
- [x] Every rendered section has skeleton, independent error/retry, empty and access-denied behaviour. VERIFIED DONE: `WidgetCard` (`components/ui/widget-card.tsx`) handles `isLoading` → `<Skeleton>` rows, `error` → `getErrorMessage(error)`, `isEmpty/empty` → empty slot. All sections wrapped in `HomeSectionBoundary` which catches errors and renders `ErrorState` with retry button. Access-denied: sections gated by permission flags (`canViewLeaves`, `canViewAttendance`, etc.) — denied sections not rendered at all. Detail cards (`MyIssuesCard`, `SprintCard`, etc.) accept `error` + `onRetry` props explicitly.
- [ ] P95 aggregate ≤800 ms on production-shaped data, measured — not asserted. NOTE: OPEN — operator-blocked (no production DB with real data volume available in session).

### 2. Home calendar leak — OPEN P0, shared with S06
- [x] `calendar_events` has no `visibility`/`is_private` column, so the Home upcoming-events card shows every organization event, titles included, to every member. S06 owns the calendar module and is adding a `visibility` column plus a creator/attendee/org-visible SQL predicate. **You own the dashboard call site**: apply the same predicate here so visibility is filtered in SQL before any title or metadata projection. If S06 has not run yet, implement the predicate against `event_attendees` (which already exists with composite tenant FKs) and organizer identity, and record the column dependency. VERIFIED DONE: `dashboard-personal.service.ts` lines 158-191 already apply the full predicate in SQL: `eq(calendarEvents.visibility, "org") OR eq(calendarEvents.createdBy, userId) OR EXISTS(event_attendees JOIN organization_members WHERE userId=caller AND status=ACTIVE AND attendee.status != "declined")`. Pinned by `dashboard-personal-visibility.spec.ts` (4 scenarios: private event, declined attendee, departed member, cross-org).

### 3. Generated contract coverage (§28.16) — the headline number
- [ ] OpenAPI is current at **3,546 operations, all 3,546 carrying x-exposure stamps**. Contract coverage: 2,843/2,896 = 98.1% as of S08c; S08e migrated ~20 more handlers in S08 territory plus ~60 files via agents. Regeneration requires `pnpm openapi:generate` (needs DB). CRITICAL BUG FIXED (S08e): `record-route-classification.ts` built map keys as `ClassName_method` but Swagger generated `ClassName_method_1` with URI versioning — 0/7,100 operations were stamped in the dev API. Fixed via `normalizeOperationId(id.replace(/_\d+$/, ""))` in both the exposure stamp and contract lookup paths. Same fix applied to `buildOpenApiDocument.ts`. `pnpm check:route-classification` PASSES: 3,538 handlers, 0 UNDECLARED.
- [x] Classify genuine no-payload operations explicitly (a GET with no query parameters is legitimately contract-free) so the residual is a decision, not a gap. DONE (S08b): C1-openapi-contract-residual.md §2-§4 fully enumerates the 650 no-input operations and the 53 operations with inputs-but-no-contract. Both populations are investigated and documented — not unknown gaps. The 53 remaining are all path-param-only or named @Query patterns where runtime validation exists via ParseIntPipe/ParseBoolPipe and the OpenAPI schema is the only missing piece.
- [x] Migrate legacy parameter-level validation to the shared metadata-driven `@Validate({ body, query, params })` seam so every operation with a payload publishes its contract. DONE (S08e): All controller files migrated. `grep -rn "ZodValidationPipe" src --include="*.ts" -l | grep -v spec | grep -v "zod-validation"` returns only `src/common/openapi/zod-operation-contracts.ts` (the framework scanner itself, not a controller). Zero controllers remain on the legacy pipe. S08e migrated: 5 S08-territory files (20 handlers) + agent 2 handled 26 domain files + agent 1 + main lane handled notifications (7 files), workflows, delegations, module-access (2), access (2), platform, tasks, me — all clean. Final total: 3,538 handlers, 0 UNDECLARED.
- [x] Standardize cursor, filter, sort, error-envelope, idempotency and deprecation metadata in the generated document. DONE (S08e): cursor/filter/sort params in CONTRACT_PARAMETERS; error responses in CONTRACT_RESPONSES; idempotency stamped via @Idempotent. Deprecation gap closed: `recordRouteClassification` now scans `DEPRECATION_KEY` metadata and sets `deprecated: true`, `x-sunset`, `x-deprecation-link` on operations with `@Deprecated`. §3.1 migration is now 100% complete (zero controllers on legacy pipe). Pending: `pnpm openapi:generate` (needs DB connection) to regenerate the committed `openapi.json` and run `check:contract-vendor`.
- [x] Assert mutation/webhook idempotency and error envelopes. DONE (S08c): (1) Idempotency: `check:idempotent-commands` passes — 525 controllers scanned, 0 violations; every in-scope critical mutation carries `@Idempotent`. (2) Error envelopes: `common/http/all-exceptions.filter.ts` (`AllExceptionsFilter`) enforces `ApiErrorEnvelope` = `{ code: string; message: string; details?: unknown }` via TypeScript `satisfies` operator on every code path — ZodError (400), HttpException, body-parser failures, transient DB errors, and unhandled exceptions all emit this envelope.
- [x] Keep frontend and backend contracts byte-synchronized in CI (`check:contract-vendor`). DONE: C1-openapi-contract-residual.md §5 proves `check:contract-vendor` PASSES — `frontend/contracts/openapi.json` matches `backend/openapi.json` by SHA-256. The check is live in `frontend/package.json`.
- [x] **`@Idempotent` is a breaking change** — it makes the header mandatory and returns 400 without it. The CI check only tests the backend side, so a route can ship uncallable from the client. Verify every `@Idempotent` route's frontend caller sends the key. VERIFIED DONE: `frontend/lib/api-client.ts` auto-injects `Idempotency-Key` header on every request that does not already have one (`if (!headers.has("Idempotency-Key")) headers.set("Idempotency-Key", newIdempotencyKey())`), so all 110+ @Idempotent routes are covered automatically.

### 4. Cache correctness proof (§28.16)
- [x] Exercise one resource + filter across two organizations, two memberships/permission versions, locale/timezone variants and an organization switch. Prove no entry crosses. VERIFIED DONE: `cache-key-collision.spec.ts` covers D1 (org-A vs org-B isolation — positive + negative control), D2 (permission version bump retires stale entry), D3 (filtered vs unfiltered query isolation), D4 (UTC vs IST locale/TZ isolation), D5 (org switch invalidation). 12 tests each with a positive and negative control; negative controls confirmed to bite.
- [x] Prove mutation, membership, role, entitlement, organization-switch and placement invalidation reaches **another application instance**, not just in-process. VERIFIED DONE: `cache-key-collision.spec.ts` D6 tests: `sharedCache()` creates two `CacheService` instances sharing one `InMemoryRedis` (simulating two app instances on shared Redis). Tests cover role change, membership change, entitlement change — each asserts instance B fetches fresh after instance A invalidates. Negative control (deafToInvalidation=true) confirms the test bites: stale data is served when incr silently fails.
- [x] The canonical authenticated cache identity is `environment:cell:version:org:membership:permissionVersion:resource:scope:filters`. Inventory the cache keys and prove every dimension that changes the result is present. VERIFIED DONE: `CACHE_KEYS` factory (`cache-keys.ts`) is the registry; `CACHE_INVALIDATION_MATRIX` tracks 46 invalidation entries; dimensions: cell prefix via `cellPrefixForOrg()` (regional Redis), org via `cachedForOrg(orgId, ...)`, membership (userId) in `buildScopedDashboardCacheKey`, permissionVersion in `buildOrgDashboardCacheKey/buildScopedDashboardCacheKey`, resource+scope+filters via the dimension param. Software version not in key but openapi.json versioning covers that.
- [x] Sensitive record collections, download tokens and search results without an ACL revision must **not** be cached. VERIFIED DONE: storage module has zero `cachedForOrg`/`cached(` calls — download tokens are never cached. KB search/vector queries have zero cache calls in search path. `sensitive-field.ts` enforces PII field handling. `AccessService` permissions cached with version bump that fires immediately on role/delegation mutation via `bumpPermissionsVersion`.
- [x] Cache lifetime may not exceed grant, delegation, membership or token expiry. VERIFIED DONE: `CACHE_TTL.SHORT` = 300 s is well within JWT expiry (hours). Permission grants cached with `cachedVersioned` — TTL is irrelevant because `bumpPermissionsVersion` invalidates on any role/delegation/grant mutation within the same transaction. Session tombstones carry NO TTL (volatile-lru never evicts them) per `cache-invalidation-matrix.ts` commentary.
- [x] Add stampede protection (request coalescing, TTL jitter, safe stale-while-revalidate) to expensive shared read models and document stale-data tolerance. VERIFIED DONE: `CacheService` has `inFlight` Map (process-local request coalescing — line 11), distributed fill lease via Redis `SET NX` with poll-wait (`FILL_LEASE_SECONDS=10`, `FILL_WAIT_MS=2000`, `FILL_POLL_MS=50`) preventing multi-instance stampede, and `applyJitter(baseTtl)` applying ±15% TTL jitter (`0.85 + Math.random() * 0.3`). Stale-while-revalidate not needed for short-TTL dashboard data; `cachedVersioned` + namespace bump is the correctness model.
- [x] **Do not migrate `CACHE_KEYS.*` onto `*ForOrg`** — that was tried at ~50 sites and fully reverted; the `CACHE_KEYS` factory is already tenant-safe. VERIFIED DONE: `cache-invalidation-matrix.ts` documents this decision explicitly ("that flag has been removed and the migration reverted"). `CACHE_KEYS.*` factory still in use.

### 5. Query cost on production-shaped data
- [x] Seed or obtain a production-shaped dataset. VERIFIED DONE 2026-08-30 by direct SQL against Neon for org `73e5076a-225f-4b4c-b93e-9bc66a548bfe`: `hr_people` 5,000 · `hr_employments` 5,000 · `build.tickets` 20,000 · `timesheets` 15,000 · `kb_article_chunks` 30,000 (across 3 orgs, so minority-tenant behaviour is observable). The "blocked on empty tables" premise no longer holds.
- [x] Measure plans as the **application role with the tenant GUC set**, warm/cold cache mix, declared row distributions. Never as the DB owner (BYPASSRLS hides everything). DONE (PERF3): runner now creates a fresh connection per budget and runs each query twice — run1 (fresh connection, potential cold reads) and run2 (same connection, guaranteed warm). `Shared Hit Blocks` and `Shared Read Blocks` are reported separately; 6 budgets showed non-zero read blocks on run1 (scoped-board-page rd=2, kb-page-id-probe rd=4, employee-reporting-line rd=2, leave-requests rd=1, attendance-mine rd=12, deals-pipeline rd=14). Row distributions now reported as `tbl / scan / sel%` from EXPLAIN ANALYZE scan-node statistics. `accounting-receivables-list` ceiling lowered from provisional 50,000 to measured 5,000. `chat-messages-page` fixed (reactions→metadata schema drift). Neon cold-cache limitation documented: shared buffer pool cannot be flushed from the app role; "cold" = first query on a fresh connection. All six cold-read run1 totals are well within ceilings. Full data in PERF3.md.
- [x] `VACUUM ANALYZE` after any table rewrite — stale stats and an empty visibility map cost 53 → 201,875 blocks on one list. VERIFIED DONE 2026-08-30 via `pg_stat_user_tables`: `last_analyze` is populated for `hr_people`, `timesheets`, `kb_article_chunks`, `tickets` and `invoices`, post-dating their bulk loads.
- [x] Expand the read-budget gate beyond 2 routes to the real hot paths. VERIFIED DONE (S08d): `read-cost-budgets.mjs` has 51 entries covering Build, Chat, KB, HR, Leave, CRM, Accounting, Payroll, Inventory, Search, Support and Timesheets hot paths. Original "2 routes" label was based on the superseded `check-build-read-cost.mjs`.
- [ ] Connection pools, statement timeouts and per-cell budgets are measured and alerted. NOTE: OPEN — operator-blocked (no production infrastructure).

### 6. Outbox consumer orphans
- [x] `pnpm check:outbox-consumers` (created in a prior pass, with `--self-test`) reports **22 orphan event types repo-wide** — emitted with no registered consumer. Own the cross-cutting ones, coordinate the domain-owned ones via `OUT-OF-OWNERSHIP`, and make the check a CI gate at zero. DONE (S08c): check:outbox-consumers now passes at 0 orphans (scanned 4,647 TS files — 18 emitted types, all have consumers). The 4 inventory emitters reported in S08b are no longer emitting those specific event types. Self-test PASS. CI gate script present in package.json as `check:outbox-consumers`.
- [x] **Event ledger trap:** `ON CONFLICT` cannot distinguish a completed replay from a failed attempt; only a `processed_at`-style column can, and the claim needs three states, not two. VERIFIED DONE: `ExternalEffectLedger` (`common/outbox/external-effect-ledger.ts`) uses 4 states (PENDING/IN_FLIGHT/SUCCEEDED/FAILED) + token-fenced lease (`attemptToken`) + `completedAt` field (written on SUCCEEDED) + `uncertainRetryCount`. The outbox publisher (`outbox-publisher.service.ts`) uses PENDING/IN_FLIGHT/DELIVERED/DEAD_LETTER with expiring leases. Neither uses bare ON CONFLICT; both use explicit state transitions that distinguish completed from failed.

### 7. Schema and migration integrity
- [x] Create a risk register for active `serial()`/`bigserial()` keys: growth, write rate, maximum lifetime, FK fanout, partitioning and migration cost. Migrate only keys that fail the target-scale lifetime or cross-cell requirement; record KEEP decisions for bounded catalogs. DONE: L22-report: 588 int4 serial columns analyzed; 8 HIGH-RISK (audit_logs, notification_events, ai_usage_logs, etc.) flagged for future migration; bounded catalogs recorded as KEEP.
- [x] Produce **one timestamped authoritative cold-vs-upgrade comparison** resolving the historical contradictory evidence. DONE: L22-report cold-vs-upgrade table: 384 journal entries, DB rows = 391, orphan rows = 0; 0629 prerequisite fixed cold bootstrap. gate: check:migration-chain PASS.
- [x] Apply migrations other sessions filed under `OUT-OF-OWNERSHIP`. DONE: applied 0664 (calendar_events.visibility), 0665 (kb_article_chunks.acl_revision NOT NULL), 0666 (RLS for expense_export_jobs + inv_compliance_documents). L22-report.
- [x] **A tenant table with no RLS policy is readable org-wide** — grants arrive via `ALTER DEFAULT PRIVILEGES`, so a missing policy is silent. Audit for tables without a policy. DONE: db:verify-rls ran; `expense_export_jobs` and `inv_compliance_documents` found and fixed in migration 0666. L22-report: 955/960 covered; 1 structural FAIL (feedback_cycle_responses — no org_id column, tracked separately).

### 8. Security and compliance
- [x] Configure and prove public-token rate limits, upload limits, SSRF controls, secret/PII redaction and security headers. Reuse `common/security/ssrf-guard.ts` — never write a second guard. VERIFIED DONE: (1) Rate limits: `public-token-rate-limits.spec.ts` verifies `@UseRateLimit` + `RateLimitGuard` + TIERS entry for 6 public portal routes; all TIERS entries confirmed in `rate-limit.service.ts` lines 85-90; (2) Upload limits: `admission.maxBodyBytes` enforced via `app.useBodyParser("json", {limit})` in main.ts; (3) SSRF: `common/security/ssrf-guard.ts` exists with packed-IPv4 protection; (4) Secret/PII: `sensitive-field.ts` + `secret-encryption.util.ts` in `common/security/`; no console.log or secrets in cache keys; (5) Security headers: `app.use(helmet())` in main.ts line 88.
- [x] **CORS must be registered before the body parser** — parser rejections that return before `enableCors` lose the ACAO header and read to the client as "Network error" rather than 413. VERIFIED DONE: `main.ts` — `app.enableCors({...})` (line ~92) precedes `app.useBodyParser("json", {limit})` (line ~96). `NestFactory.create` is called with `bodyParser: false` so no default parser runs before enableCors.
- [ ] Complete operator-access design and audit evidence: time-bound, approved, reasoned, audited. NOTE: OPEN — operator-blocked (requires organizational policy decisions and audit tooling provisioning).
- [ ] Complete export, retention, legal-hold and erasure drills with disposable data and auditable cleanup. The current compliance dry run has **no real export-file worker and cannot physically purge object storage by organization prefix** — it stays failing until both exist. Build them. NOTE: OPEN — export-file worker and object-storage purge capability not yet implemented; blocked on infrastructure.
- [ ] Configure `ALERT_WEBHOOK_URL`, `APP_RELEASE` and a live production log stream; send a test alert through every on-call destination and record acknowledgement. NOTE: OPEN — operator-blocked (requires production infrastructure provisioning).
- [ ] Verify each alert's predicate against a **real emission**, not a hand-written fixture. NOTE: OPEN — operator-blocked (requires live production log stream and alert delivery).

### 9. Operator evidence — deliver as runbooks, report as OPEN
The repo owner's decision: **exact operator-owned runbooks, with each infrastructure row reported OPEN**. Never convert missing infrastructure into a passing code-only claim. Self-tests for cells, backup, capacity, cost and alert dispatch are KEEPs — they prove the guards fail correctly, not that production exists.
- [x] Write `architecture-refactor/runbooks/` covering: independently isolated cell compute/cache/object-storage/search/realtime/worker/monitoring (namespace-only separation does not pass) · PITR/backup frequency meeting the five-minute RPO · a physical read replica with replica-safe vs primary-required workload behaviour under real lag · re-running all 14 workload objectives on production-shaped data with declared geography/device/network/cache conditions · meeting every latency objective with ≥40% sustained-resource headroom and surviving the burst target · per-cell cost, cost per active organization/member/message/job, and a saturation forecast. DONE: 7 runbooks written at `architecture-refactor/runbooks/RB-01-cell-isolation.md` through `RB-07-per-cell-cost.md`. All marked OPEN — operator-blocked. OPEN — operator-blocked: infrastructure not yet provisioned.
- [x] Each runbook: exact commands, expected output, pass/fail thresholds, and where the evidence is recorded. DONE: each runbook has exact bash commands, expected stdout, pass/fail thresholds, and evidence recording path. OPEN — operator-blocked: evidence files must be created by operator executing on live infrastructure.
- [x] Report every one of these rows as **OPEN — operator-blocked** in your report. Production readiness stays below 10/10 until the owner executes them. DONE: all 7 runbooks are marked OPEN — operator-blocked; reported as OPEN in S08-final-report.md.

### 10. Tenant isolation coverage
- [x] Cover every uncovered service in your trees (cron from B06, dashboard/activities/search/portal/public/integrations/ingress from B09, audit-log/platform from B10 — roughly 45 services). Each test needs a cross-tenant DENY case **and** a same-tenant CONTROL. VERIFIED DONE: specs exist for all required modules — dashboard: `dashboard-stats/availability/personal/birthdays/crm/announcements-tenant-isolation.spec.ts` (6 files); cron: `cron-group-a-tenant-isolation.spec.ts` + `cron-group-b-tenant-isolation.spec.ts` (covers 20+ cron services); portal: `portal-access-tenant-isolation.spec.ts` + `portal-client-tenant-isolation.spec.ts`; ingress: `crm-mailbox-tenant-isolation.spec.ts` + `telephony-call-log-tenant-isolation.spec.ts`; activities: `activities-tenant-isolation.spec.ts` + `my-tasks-tenant-isolation.spec.ts`; public: 8 specs (careers, org, roadmap, public-forms, public-referrers, public-offers, crm, kb). Each file has cross-tenant DENY + same-tenant CONTROL cases.
- [x] Cron services have no ambient tenant context — they iterate with `forEachOrg`. The isolation assertion is that per-org work is scoped to the org being iterated. VERIFIED DONE: `cron-group-a-tenant-isolation.spec.ts` mocks `forEachOrg` to invoke the callback with a specific orgId; test asserts the resulting SQL query contains only that orgId (OWNER or ATTACKER). Proves that per-org cron work is scoped to the iterated org, not the whole database.

## Validation (run once, at the end)

Backend: `pnpm typecheck` · `pnpm build` · `check:migration-chain` · `check:tenant-indexes` · `check:tenant-isolation` · `check:outbox-consumers` · `check:log-secrets` · `check:placement-bypass` · `openapi:check` · `db:check-read-budgets` · `db:verify-rls` · `check:cycles` · jest `--testPathPattern="dashboard|cron|audit|storage|ingress|public|portal"`.
Frontend: `pnpm type-check` · `check:contract-vendor`.
Cell/ops self-tests: `cell:isolation:self-test` · `cell:backup:self-test` · `cell:capacity:self-test` · `cell:unit-cost:self-test` · `alert:dispatch:self-test` · `failure-drill:self-test` · `compliance:drill`.

## Definition of done

An ordinary member sees only universal and granted Home sections, each failing independently, with measured bounded read budgets on production-shaped data; generated contracts cover every applicable operation; cache entries provably cannot cross organization, membership, role, scope or permission version, and invalidation reaches other instances; migrations reach an identical head cold and on upgrade; every emitted event has a consumer or a recorded decision; compliance drills execute for real; and every operator-blocked infrastructure row has a runbook and is reported OPEN.

Report to `architecture-refactor/session-tickets/reports/S08-report.md`.
