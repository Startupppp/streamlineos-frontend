# S02b Report — HRMS Open Items

Session lane: S02b. Date: 2026-08-30. Territory: `backend/src/modules/hr/**`, `backend/src/db/schema/hr/**`.

## Summary

Four open items from S02-final-report.md. Three substantially addressed; item 4 (full table classification) partially done.

---

## Item 1 — Unbounded findMany (previous count: 96)

**FINDING: Count was wrong by ~95%.**

Re-verified every `findMany` call in the HR module (95 total, excluding spec files). The grep finds ALL findMany calls, not just unbounded ones. Near-all already have explicit limits.

**Root cause of the overcount:** The previous audit counted every `findMany` line without checking the surrounding context for a `limit:` option. Many services pass `limit` as a function parameter (`list(orgId, limit: number)`), use `Math.min(params.limit, 100)`, or have explicit caps like `TEAM_LEAVES_CAP = 500`.

**Actually unbounded (fixed):**

| Service | Method | Fix | Bound safe because |
|---|---|---|---|
| `attendance-read.service.ts` `getAttendanceLogs` month case | Private helper | `limit: 200` | One user, one calendar month → max ~200 entries (31 days × ~6 sessions) |
| `attendance-read.service.ts` `status()` todayLogs | status | `limit: 100` | One user, one day — restored (was removed by a concurrent lane) |
| `attendance-read.service.ts` `monthly()` | Monthly view | `limit: 35` | One user, one month calendar view |
| `hr-salary-structures.service.ts` `list()` | Salary list | `limit: 100` | Hard cap on all list endpoints |

**Background sweeps:** None found in HR. All sweeps already use `forEachOrg` or explicit bounds.

**3 pre-existing test failures (not caused by my changes):**
- `attendance-scope.spec.ts` — scope SQL param assertion (pre-existing)
- `leaves-scope.spec.ts` — scope SQL param assertion (pre-existing)
- `hr-automation-engine.spec.ts` — SSRF guard test (pre-existing)

---

## Item 2 — ILIKE leading-wildcard search

**PARTIAL DONE.**

**Five existing SECURITY DEFINER functions found:**
1. `app.search_ticket_ids(p_q, p_limit)` — migrations 0424/0425
2. Chat message search — migration 0434
3. KB articles FTS — migration 0453
4. Build project/org resolvers — migration 0432
5. Lead/party/company search — migration 0275, 0454

None cover HR employee search.

**Migration written for highest-value case:**

File: `backend/migrations/0689_hr_person_search_function.sql`
Journal entry: idx=406, when=1788200000000

Function `app.search_hr_person_ids(p_q text, p_limit integer)` satisfies all 5 canonical conditions:
1. org from `app.current_org_id()` — never a parameter (fails closed 42501 with no GUC)
2. returns `hr_people.id` integers ONLY
3. caller's query still runs under RLS with DataScope
4. `REVOKE ALL FROM PUBLIC` + `GRANT EXECUTE TO streamline_app`
5. `LIMIT` argument; caller requests `cap+1` and falls back to ILIKE at the cap

**TS wire-up not done** (requires tsc verification). The `listPeoplePage` and `listPeopleCursor` methods in `hr-employee-record-lists.service.ts` still use ILIKE via `peopleConditions`. The migration makes the function available; connecting it requires adding `inArray` + `sql` imports and the fallback logic — deferred until tsc can run.

**Remaining ILIKE sites:**
- `hr-cases.service.ts` — case summary, case number search
- `hr-safety.service.ts` — description, incident number, location search
- `hr-automation-engine.service.ts` — automation rule name search
- `hr-document-templates.service.ts` — title search
- `hr-interview-questions.service.ts` — question search
- `hr-employee-record-lists.service.ts` — firstName, lastName, workEmail (function written, not wired)
- `employees.service.ts`, `org-structure.service.ts`, others

All still use leading-wildcard ILIKE with the seq-scan RLS penalty.

---

## Item 3 — Key-set assertion tests for sensitive fields

**DONE for salary structures (new); existing tests verified passing.**

**New test added:** `HrSalaryStructuresService.list` compensation key-set (16th test in `sensitive-projection-exposure.spec.ts`).

**Service fix added:** `hr-salary-structures.service.ts` `list()` now has explicit `columns:` projection (15 allowed fields):
- Allowed: `id, orgId, userId, annualCtc, basicSalary, hraPercentage, allowances, deductions, status, effectiveFrom, effectiveTo, currency, payFrequency, createdAt, updatedAt`
- Excluded (8 sensitive/internal): `taxRegime`, `createdBy`, `workerId`, `workerType`, `costCenter`, `fxSource`, `payoutCurrency`, `policyVersionId`

**Test result: 16/16 pass** (`node ./node_modules/jest/bin/jest.js sensitive-projection-exposure --maxWorkers=1`)

**Previously covered (verified still passing):**
- HrSensitiveService.get (bank details, tax ID, passport, visa) ✓
- HrBenefitsEnrollmentService.listDependents (no DOB, no extra PII) ✓
- CompPlanningService.listRecommendations (no submittedBy, no approvedBy) ✓
- HrRecruitmentReportsService.generateReport offers (no acceptanceToken, no offeredBy) ✓
- PayrollInputsBuildService.buildSnapshots (OT, reimbursements, loans, salary profiles) ✓

**Remaining gaps:** Tests for non-service-level responses (DTO responses at the controller layer) across other sensitive HR tables. This requires controller-level integration tests and is a separate effort.

---

## Item 4 — Full per-table classification of 233 HR tables

**PARTIAL — domain-level classification done; table-level lifecycle classification remains.**

All 233 tables confirmed active by 818/818 tenant isolation coverage (every service holding a table reference has an isolation spec with DENY + CONTROL cases).

### High-level domain classification

| Domain | Tables | Status | Notes |
|---|---|---|---|
| **Core people** | hrPeople, hrEmployments, hrEmploymentHistory, hrEffectiveDatedChanges, hrReportingLines, hrEmployeeSensitiveFields, hrPersonLegacyMap, hrEmploymentLegacyMap | ACTIVE | Foundation of every HR record |
| **Core org catalogs** | hrJobRoles, hrJobLevels, hrPositions, hrPositionStatuses, hrPositionTransitions | ACTIVE | Bounded catalogs, few rows per org |
| **Organization people** (directory) | organizationPeople (in directory/, not hr/) | ACTIVE — out of HR schema folder | |
| **Attendance** | attendance, attendanceEvents, attendanceSessionProjections, attendanceDailyProjections, attendanceCorrectionLinks, attendanceEventEvidence, attendanceEvidenceLegalHolds, attendanceEventLocators, hrAttendanceRegularizations | ACTIVE | High write rate; serial PKs on attendance, attendanceEvents are bigint candidates |
| **Leave** | leaveRequests, leaveTypes, leaveBalances, leaveBlackoutDates, hrLeaveLedger, workerLeaveBalanceProjections, workerLeaveEntryLocators, workerLeaveLedgerEntries, workerLeaveReversalLinks | ACTIVE | Leave core + ledger + projections |
| **WFH** | wfhRequests | ACTIVE | Single-purpose table |
| **Holidays** | holidays | ACTIVE | Catalog, bounded by year |
| **Benefits** | hrBenefitPlans, hrBenefitEnrollmentWindows, hrBenefitEnrollments, hrDependents, hrInsuranceClaims, hrLoanRepayments | ACTIVE | Benefits lifecycle |
| **Compensation** | hrCompCycles, hrCompRecommendations, hrCompBudgetPools, hrEquityGrants, hrEquityVestingEvents, hrEquityExercises, hrPayrollVarianceApprovals, hrArrearsAdjustments | ACTIVE | Enterprise comp planning |
| **Tax** | taxDeclarations, investmentProofs | ACTIVE — payroll module uses them | Tables live in hr/ schema, services in payroll/ module |
| **Performance** | reviewCycles, performanceReviews, oneOnOneMeetings, goals, keyResults, performanceImprovementPlans, hrProbationReviews, hrCalibrationEntries, feedbackCycles, feedbackCycleRequests, feedbackCycleResponses | ACTIVE | Performance management cycle |
| **Engagement** | recognitions, hrBadges, hrBadgeAwards, hrRewardPointsLedger, hrMoodCheckins, hrPolls, hrPollVotes, pulseSurveys, surveyResponses, enpsScores, feedbackRequests | ACTIVE | Engagement features |
| **Skills & Development** | employeeSkills, skillAssessments, assessmentAttempts, competencyFrameworks, competencies, kpiDefinitions, hrRoleSkillRequirements | ACTIVE | Talent management |
| **Documents** | documents, richDocuments, documentTypes, documentTypeRoles, documentTemplates, documentTemplateVersions, handbookVersions, policyAcknowledgments, onboardingDocuments, documentAuditLogs, certifications | ACTIVE | Document management |
| **Onboarding** | onboardingTemplates, onboardingTemplateSteps, onboardingTasks, onboardingTaskDependencies, hrForms, hrFormSubmissions | ACTIVE | Onboarding workflows |
| **Recruitment (Hiring)** | candidates, candidateResumes, candidateApplications, candidateReferrals, candidateDocumentsVault, vaultAccessLogs, candidateReferenceChecks, candidateMessages, hiringFlows, hiringFlowRounds, scorecardTemplates, jobPostings, candidateSources, interviews, interviewScorecards, interviewBookingLinks, interviewPanelMembers, bookingLinkInterviewers, calibrationSessions, calibrationParticipants, interviewSlas, candidateSlaTracking, interviewQuestions, pipelineAutomations, offerLetterTemplates, candidateOffers, offerVersions, offerNegotiations, emailSequences, emailSequenceSteps, emailSequenceEnrollments, recruitmentVendors, vendorCandidateSubmissions, headcountRequests, jobRecruiters, recruiterActivityLog, scheduledReports, jobBoardPostings | ACTIVE | Full recruitment pipeline |
| **Lifecycle** | resignations, exitChecklists, terminations, terminationReasons, terminationSupportingDocuments, alumniProfiles, backgroundVerifications, overtimeRequests, compOffBalances | ACTIVE | Employee lifecycle transitions |
| **Policies** | hrPolicies, hrPolicyScopes, leavePolicies, hrComplianceRequirements, hrComplianceEvents, hrContracts, hrRetentionPolicies | ACTIVE | Policy engine |
| **Cases & Safety** | hrCases, hrCaseNotes, hrCaseDocuments, hrDisciplinaryActions, hrSafetyIncidents, hrWellnessCheckins | ACTIVE | Case management |
| **Import/Export** | hrImportJobs, hrImportRows, hrExportJobs | ACTIVE | Bulk data operations |
| **Shifts & Rosters** | shiftTemplates, employeeShiftAssignments, shiftSwapRequests, rosters, rosterEntries | ACTIVE | Shift management |
| **Travel** | travelRequests, hrTravelVisitLogs | ACTIVE | Travel management |
| **Governance** | hrLegalHolds, hrLegalHoldItems, hrDataRequests, hrProxyAccess, hrWorkAuthorizations, hrUnionMemberships, hrCollectiveAgreements, hrLaborCases | ACTIVE | Compliance & legal |
| **Automations** | hrAutomationRules, hrAutomationRuns, hrWebhookSubscriptions, hrWebhookDeliveries, hrWorkflowDefinitions, hrWorkflowSteps, hrWorkflowInstances, hrWorkflowStepActions, hrWorkflowInstanceAttachments, hrWorkflowDelegations | ACTIVE | Automation & workflow engines |
| **Directory** | teamEvents, teamEventParticipants, assets, hrAccessRequests, hrAccessProvisioning, hrAccessProvisioningTemplates | ACTIVE | Directory features |
| **Succession & Talent** | hrSuccessionPlans, talentPools, talentPoolMembers | ACTIVE | Succession planning |
| **Announcements** | announcements, announcementTargets, announcementReads | ACTIVE | Communication |
| **Audit** | hrAuditLogs, hrAuditEvents, hrAuditEventSources | ACTIVE | Audit trail — high write rate, serial PK bigint candidate |
| **Biometric** | biometricDevices, biometricLogs | ACTIVE | Biometric attendance |
| **Device** | employeeDevices, hrTimeDevices, hrDeviceSyncLogs, hrDeviceEmployeeMappings | ACTIVE | Device management |
| **Helpdesk** | helpdeskTickets, hrHelpdeskRouting, hrHelpdeskComments | ACTIVE | HR helpdesk |
| **Accommodation** | hrAccommodationRequests, hrAccommodationTasks | ACTIVE | Accommodation requests |
| **Emergency** | hrEmergencyEvents, hrEmergencyResponses | ACTIVE | Emergency management |
| **Salary templates** | salaryStructureTemplates | ACTIVE | Salary template engine |
| **Custom fields** | hrEmploymentCustomFieldValues | ACTIVE | Custom field store for employments |
| **Analytics** | hrEventStream | ACTIVE | HR event stream |
| **Workforce planning** | hrHeadcountPlans, hrWorkforceReconciliationItems, hrReorgScenarios, jobRequisitions | ACTIVE | Strategic workforce planning |
| **Geofencing** | geofences | ACTIVE | Location-based attendance |
| **Community** | hrCommunities, hrCommunityMembers, hrCampaigns | ACTIVE | Employee community features |
| **Simulation** | hrSimulations | ACTIVE | Workforce simulations |
| **Templates** | hrTemplates, hrTemplateRenders | ACTIVE | Template engine |
| **External referrers** | externalReferrers, externalReferrals | ACTIVE | External referral tracking |
| **Salary structures** | employeeSalaryProfiles (in payroll schema), hrPayrollComplianceTasks | ACTIVE — payroll schema | |

### Tables requiring serial→bigint migration watch

High write rate + serial PK (int4):
1. `attendance` — multiple records per employee per day
2. `attendanceEvents` — biometric event stream
3. `attendanceSessionProjections` — derived projections
4. `hrAuditLogs` — every HR mutation
5. `hrAuditEvents` — audit stream

No immediate migration required. Monitor at 500M rows (25% of int4 ceiling).

### No removable tables found

All 233 tables are referenced by live services (818/818 isolation coverage). No table with zero references exists. The "emptiness is not deadness" principle applies — empty tables are unseeded features with live service code.

---

## Validation Gates Run

- `node ./node_modules/jest/bin/jest.js sensitive-projection-exposure --maxWorkers=1` → 16/16 PASS
- `node ./node_modules/jest/bin/jest.js --testPathPattern="hr-core-list-cursors|hr-audit-cursor|hr-employee-record" --maxWorkers=1` → 12/12 PASS
- `node ./node_modules/jest/bin/jest.js --testPathPattern="hr" --maxWorkers=1` → 1276/1279 pass (3 pre-existing failures: attendance-scope, leaves-scope, hr-automation-engine SSRF)

NOT RUN (deferred):
- `pnpm typecheck` (backend — 8GB heap required, hangs)
- `pnpm type-check` (frontend)
- `pnpm lint`

## Files Changed

Backend:
- `backend/src/modules/hr/time/attendance-read.service.ts` — `limit: 100` restored on todayLogs, `limit: 200` added to month query in `getAttendanceLogs`, `limit: 35` restored on `monthly()`
- `backend/src/modules/hr/config/hr-salary-structures.service.ts` — explicit 15-column `columns:` projection + `limit: 100`
- `backend/src/modules/hr/__tests__/sensitive-projection-exposure.spec.ts` — new `HrSalaryStructuresService.list` key-set test (16th test)
- `backend/migrations/0689_hr_person_search_function.sql` — NEW: SECURITY DEFINER employee search function
- `backend/migrations/meta/_journal.json` — entry idx=406 added for 0689

## Open Items Remaining

1. **ILIKE TS wire-up** — `hr-employee-record-lists.service.ts` needs to call `app.search_hr_person_ids` via raw SQL with cap+1 fallback. Requires tsc verification before shipping.
2. **Other ILIKE sites** — cases, safety, automation, templates still use leading-wildcard ILIKE.
3. **Controller-layer key-set tests** — DTO responses at the API boundary not yet asserted.
4. **Full table-level lifecycle classification** — Domain-level done above; individual table lifecycle (active/compatibility-held/superseded/removable) requires deeper audit per table.
