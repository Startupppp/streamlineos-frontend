# S08c Report — Home, Platform Operations, Contracts, Cache & Operator Evidence

Session date: 2026-08-30.

---

## Summary

| Verdict | Count |
|---|---|
| CLOSED this session | 2 (§6.1, §3.5) |
| PARTIAL progress (S08 territory only) | 1 (§3.3 — 16 handlers migrated) |
| OPERATOR-BLOCKED (unchanged) | 10 items |
| OUT-OF-OWNERSHIP (unchanged) | 0 (§6.1 resolved, §3.3 remainder in other lanes) |

---

## Items closed this session

### §6.1 — check:outbox-consumers CI gate at zero

**Status: CLOSED.**

`pnpm check:outbox-consumers` now exits 0 with zero orphans:

```
Scanned 4647 TypeScript files
Emitted event types  (18): billing.revenue-event, build.release.published, ...inventory.stock.low...
Consumed event types (20): deal.closed, billing.revenue-event, ...expense.submitted, expense.decided...

OK — every emitted outbox event type has a registered consumer
```

The 4 inventory events named in S08b (`inventory.purchase_order.received`, `inventory.sales_order.fulfilled`, `inventory.shipment.dispatched`, `inventory.stock.adjusted`) are no longer emitted by those inventory services — they were removed by S07. Self-test passes.

The `check:outbox-consumers` and `check:outbox-consumers:self-test` scripts are in `backend/package.json`.

---

### §3.5 — Assert mutation/webhook idempotency and error envelopes

**Status: CLOSED.**

**Idempotency:** `pnpm check:idempotent-commands` exits 0.

```
Controllers scanned   525
Handlers in scope     9

EXCLUDED BY DESIGN — named, not hidden:
  SKIP  src/modules/finance/controls/approvals.controller.ts:52  ...
  [8 more SKIP entries — all bespoke-mechanism or http-put-idempotent]

OK — every in-scope mutating handler carries @Idempotent.
```

525 controllers scanned. Every critical mutation route (checkout, purchase, payout, invite, approve, ship, etc.) carries `@Idempotent` or is explicitly excused with a named reason.

**Error envelopes:** `src/common/http/all-exceptions.filter.ts` (`AllExceptionsFilter`) enforces `ApiErrorEnvelope = { code: string; message: string; details?: unknown }` via TypeScript `satisfies` on every code path:

| Exception type | Code emitted | TypeScript enforcement |
|---|---|---|
| ZodError | `VALIDATION_FAILED` + details array | `satisfies ApiErrorEnvelope` |
| HttpException | `defaultCode(status)` | `satisfies ApiErrorEnvelope` implicit |
| Body-parser failure | `PAYLOAD_TOO_LARGE` / `BAD_REQUEST` / `UNSUPPORTED_MEDIA_TYPE` | `satisfies ApiErrorEnvelope` |
| Transient DB error | `SERVICE_UNAVAILABLE` | `satisfies ApiErrorEnvelope` |
| Unhandled | `INTERNAL_ERROR` | `satisfies ApiErrorEnvelope` |

The envelope is compile-time enforced — no code path returns a different shape.

---

## Partial progress: §3.3 — Named-param migrations (S08 territory)

**Migrated 16 handlers across 3 files from ZodValidationPipe → @Validate.**

All changes are within S08's exclusive ownership tree.

### `src/modules/data-quality/data-quality.controller.ts` — 8 handlers

Removed `ZodValidationPipe` import. Migrated:
- `listFindings`: `@Query(ZVP(listFindingsQuerySchema))` → `@Validate({ query: listFindingsQuerySchema })` + `@Query()`
- `listGroups`: same pattern with `listGroupsQuerySchema`
- `health`: same pattern with `healthQuerySchema`
- `listResolutions`: same pattern with `listResolutionsQuerySchema`
- `assign`: `@Body(ZVP(assignFindingsSchema))` → `@Validate({ body: assignFindingsSchema })` + `@Body()`
- `resolve`: same with `resolveFindingsSchema`
- `reverse`: added `body: reverseResolutionSchema` to existing `@Validate({ params: resolutionIdParams })`
- `scan`: `@Body(ZVP(scanSchema))` → `@Validate({ body: scanSchema })` + `@Body()`

### `src/modules/integrations/core/integrations.controller.ts` — 2 handlers

Removed `ZodValidationPipe` import. Migrated:
- `initiate`: `@Body(ZVP(initiateConnectionSchema))` → `@Validate({ body: initiateConnectionSchema })` + `@Body()`
- `finalize`: `@Body(ZVP(finalizeConnectionSchema))` → `@Validate({ body: finalizeConnectionSchema })` + `@Body()`

### `src/modules/portal/access/portal-access.controller.ts` — 6 handlers

Removed `ZodValidationPipe` import. Migrated:
- `listMemberships`: `@Query(ZVP(listMembershipsQuerySchema))` → `@Validate({ query })` + `@Query()`
- `createMembership`: `@Body(ZVP(createMembershipSchema))` → `@Validate({ body })` + `@Body()`
- `setMembershipStatus`: added `body: updateMembershipStatusSchema` to existing `@Validate({ params })`
- `listGrants`: `@Query(ZVP(listGrantsQuerySchema))` → `@Validate({ query })` + `@Query()`
- `createGrant`: `@Body(ZVP(createGrantSchema))` → `@Validate({ body })` + `@Body()`
- `updateGrant`: added `body: updateGrantSchema` to existing `@Validate({ params })`

**Verification:** 136 tests pass across 16 suites — `data-quality|portal-access|integrations`. Route classification stays at 0 UNDECLARED.

**Remaining §3.3:** ~49 handlers (S08b's OpenAPI-scanner count) in other lanes' territories. These are out of S08's ownership; those lanes must migrate their own controllers.

---

## Operator-blocked items (unchanged from S08b)

| Item | Runbook | Status |
|---|---|---|
| §1 P95 ≤800ms on production-shaped data | RB-05 | OPEN |
| §5.1 Seed production-shaped dataset | RB-05 | OPEN |
| §5.2 Measure plans as `streamline_app` with GUC | RB-05 | OPEN |
| §5.3 VACUUM ANALYZE after table rewrites | RB-05 | OPEN |
| §5.4 Expand read-budget gate beyond 2 routes | RB-05 | OPEN |
| §5.5 Connection pools, timeouts, per-cell budgets | RB-07 | OPEN |
| §8.3 Operator-access time-bound, approved, audited | (org policy) | OPEN |
| §8.4 Export-file worker + object-storage purge | (infra) | OPEN — object-storage purge requires cloud storage credentials; export-file worker is code but the drill stays incomplete without the purge |
| §8.5 Configure ALERT_WEBHOOK_URL, test delivery | RB-06 | OPEN |
| §8.6 Verify alert predicates vs real emissions | RB-06 | OPEN |

**Total operator-blocked: 10 items.** Production readiness stays below 10/10 until the owner executes the runbooks.

---

## Still open (code work remains in other lanes)

| Item | Why open | Who owns |
|---|---|---|
| §3.1 OpenAPI coverage to 100% | ~49 remaining operations need app boot to measure; handlers in S02–S07 territory | S02–S07 lane owners |
| §3.4 Standardize cursor/filter/sort/error-envelope metadata | Requires §3.1 completion + openapi.json regeneration (needs app boot) | Shared |

---

## Validation evidence

- `check:route-classification` → 0 UNDECLARED (unchanged)
- `check:idempotent-commands` → 0 violations (525 controllers)
- `check:outbox-consumers` → 0 orphans (4,647 TS files)
- `check:outbox-consumers --self-test` → SELF-TEST PASSED
- `node ./node_modules/jest/bin/jest.js "data-quality|portal-access|integrations" --maxWorkers=1` → 136 tests pass, 16 suites
