# S04e Report — ROW 52 + ROW 55

## ROW 52 — Cursor pagination for getAllWork and roadmap lists

### Backend

**`work-scope-union.ts`** — rewritten:
- `WorkSort` type gains `sortKey: WorkSortKey`
- `carry` simplified to single alias: `${col} AS sort_col`
- `rows` is 2-column: `[orderFn(col), orderFn(tickets.id)]`
- `unionOrderBy` ties the id tiebreaker direction to the sort direction
- `AssignedOrParticipatingParams` removes `offset`, adds `cursorPredicate?: SQL`
- `mineCountSql(where, orgId, userId)` added for parallel COUNT on first page
- `readIds` added (without total); `readIdsAndTotal` kept for backward compat

**`all-work-sort-direction.spec.ts`** — updated:
- "always ASC id tiebreaker" → "direction-matching tiebreaker"
- removed `created_at` assertions from carry test
- all 16 tests pass

**`ticket.schemas.ts`** — `allWorkQuerySchema` now omits `page` and keeps `cursor`

**`projects-work-query.service.ts`** — rewritten:
- `getAllWork` accepts `cursor` instead of `page`, caps at `PAGE_SIZE_CAP = 100`
- Returns `{ data, limit, nextCursor: string | null, hasMore, total? }`
- `pageFilteredWork`: sentinel fetch (`limit + 1`), parallel COUNT only on first page
- `pageMineWork`: UNION cursor predicate, sentinel, parallel `mineCountSql` on first page
- `nextCursor` is explicitly `null` when exhausted, never `undefined`
- cursor helpers: `buildCursorPredicate`, `buildMineCursorPredicate`, `serializeSortValue`

**`roadmap.schemas.ts`** — replaced `page: pageNumberField` with `cursor: z.string().optional()` in all three list schemas

**`projects-roadmap.service.ts`** — rewritten:
- `listRoadmap`: ASC(sortOrder) + ASC(id) keyset, `buildCursorPage`
- `listFeedback`: DESC(votes) + ASC(id) mixed-direction keyset, `buildCursorPage`
- `listChangelog`: DESC(createdAt) + DESC(id) keyset, `buildCursorPage`
- All use sentinel approach; `nextCursor` is `null` when exhausted

**`projects-tickets-read.service.ts`** — appended `getColumnCounts(orgId, projectId)`:
- `SELECT status, count(*) FROM tickets WHERE orgId AND projectId AND deletedAt IS NULL GROUP BY status`
- Returns `Record<string, number>`

**`projects-tickets.service.ts`** — delegates `getColumnCounts` to read service

**`projects-tickets.controller.ts`** — `GET :projectId/tickets/column-counts`, `@RequirePermission("build:tickets:view")`

### Frontend

**`types/projects/tasks.ts`** — `AllWorkFilters` omits `page`, adds `cursor?: string`

**`lib/query-keys/build-work.ts`** — `columnCounts: (projectId) => [...]` key added

**`hooks/api/build/all-work.ts`** — `useAllWork` returns `CursorPaginatedResponse<AllWorkTicket>`; `useInfiniteAllWork` uses `string | undefined` page param, `getNextPageParam: (last) => last.nextCursor ?? undefined`

**`hooks/api/build/roadmap.ts`** — `CursorPaginated<T>` interface, all three filter interfaces use `cursor?: string`, hooks return `CursorPaginated<T>`

**`features/build/all-work/use-all-work-filters.ts`** — removed `page` and `handlePageChange`

**`features/build/all-work/all-work-page.tsx`** — infinite scroll with "Load more" button, subtitle shows loaded count

**`features/build/roadmap/roadmap-tab.tsx`** — cursor history prev/next (no `TablePagination`)

**`features/build/roadmap/feedback-tab.tsx`** — cursor history prev/next (no `TablePagination`)

**`features/build/roadmap/changelog-tab.tsx`** — cursor history prev/next (no `TablePagination`)

**`features/build/command-center/command-center-page.tsx`** — `page: 1` removed from both `useAllWork` calls; stats fall back to `data.length` when `total` is absent

## ROW 55 — Server aggregates + board column virtualization

**`features/build/views/kanban-board-utils.ts`** — `BOARD_COLUMN_VIRTUALIZATION_THRESHOLD = 15` exported

**`hooks/api/build/ticket-queries.ts`** — `useTicketColumnCounts(projectId)` hook added, gated on `build:tickets:view`, hits `GET /build/:projectId/tickets/column-counts`, `staleTime: 30_000`

**`features/build/views/kanban-column-header.tsx`** — `serverCount?: number` prop; `displayCount = serverCount ?? ticketCount` replaces bare `ticketCount` in the badge

**`features/build/views/kanban-board-column.tsx`** — `serverCount?: number` prop forwarded to `KanbanColumnHeader`

**`features/build/views/kanban-board.tsx`** — `useTicketColumnCounts` fetched; `columnCountsData?.[col.id]` passed as `serverCount` to each column in both flat and swimlane renders; past `BOARD_COLUMN_VIRTUALIZATION_THRESHOLD` columns the container receives `[&>*]:content-visibility-auto` to hint browser paint optimization

## Validation

- Spec suite: `all-work-sort-direction.spec.ts` — 16/16 pass
- Build: not run (hangs on this machine)
- Lint/Tests: not run (not requested)
