# S08b Report — Home, Platform Operations, Contracts, Cache & Operator Evidence

Session date: 2026-08-30 (follow-up to S08). Addresses the code-closable remainder from S08's open items.

---

## Summary

| Verdict | Count |
|---|---|
| CLOSED this session | 2 (§3.2, §3.6) |
| CODE FIXES applied | 4 handler migrations |
| OPERATOR-BLOCKED (runbook path) | 10 items |
| OUT-OF-OWNERSHIP (inventory lane) | 1 item |

---

## Code-closable items: what was fixed

### §3 OpenAPI contract residual — 4 handler migrations

All four are now using the `@Validate({body, query, params})` shared seam.

| File | Handler | Fix |
|---|---|---|
| `src/modules/billing/core/billing-enterprise.controller.ts` | `listEnterpriseQuotes` | Removed inline `listEnterpriseQuotesSchema.parse(query)`; added `@Validate({ query: listEnterpriseQuotesSchema })` |
| `src/modules/email/controllers/notifications-dispatch.controller.ts` | `dispatch` | Removed inline `dispatchSchema.parse(raw)` and `@Body() raw: unknown`; added `@Validate({ body: dispatchSchema })` and typed `@Body() body: DispatchInput` |
| `src/modules/payroll/payout/payout-batches.controller.ts` | `getBankDetails` | Class-level `@Controller("payroll/employees/:employeeUserId")` blind spot; added `employeeUserIdParams` schema and `@Validate({ params: employeeUserIdParams })` |
| `src/modules/integrations/git/integrations-git.controller.ts` | `webhook` | Named `@Query("connectionId")` without schema; added `gitWebhookQuerySchema` and `@Validate({ query: gitWebhookQuerySchema })` |

**Evidence:** route classification check: 0 UNDECLARED (3,557 total handlers). 107 tests pass (`dashboard|cache-key-collision|openapi|zod-validation`).

---

### §3.2 No-payload classification — CLOSED

The C1-openapi-contract-residual.md report (written in the prior pass) documents:
- 650 operations with no input surface (no requestBody, no path params, no query params) — legitimately contract-free and excluded from the denominator.
- 53 operations with inputs but no published Zod schema — all are path-param-only handlers (ParseIntPipe validates the integer but no Zod schema is published to the OpenAPI document) or named `@Query("field")` patterns where runtime validation exists but the schema is not in the shared seam.
- Neither population is an unknown gap — both are investigated and documented decisions.

**Ticket item ticked: `[ ] Classify genuine no-payload operations explicitly` → `[x]`.**

---

### §3.6 check:contract-vendor CI sync — CLOSED

C1-openapi-contract-residual.md §5 proves `check:contract-vendor` (in `frontend/package.json`) passes: `frontend/contracts/openapi.json` matches `backend/openapi.json` by SHA-256. The check is live in CI.

**Ticket item ticked: `[ ] Keep frontend and backend contracts byte-synchronized` → `[x]`.**

---

### Inline safeParse audit — all boundary cases resolved

Exhaustive grep of `*.controller.ts` for `.parse(` and `.safeParse(` found these patterns:

| Controller | Pattern | Verdict |
|---|---|---|
| `billing-enterprise.controller.ts` | `listEnterpriseQuotesSchema.parse(query)` | FIXED this session |
| `notifications-dispatch.controller.ts` | `dispatchSchema.parse(raw)` | FIXED this session |
| `chat-assistant.controller.ts` | `createBonusSchema.parse({...})` inside switch-case | LEGITIMATE: parsing AI tool call payload, not HTTP boundary |
| `crm-ai.controller.ts` | `scoreLeadBatchSchema.parse(body)` in conditional branch | LEGITIMATE: branching dispatch on body shape — single endpoint, two schemas |
| `feedbucket-public.controller.ts` | `publicSubmitSchema.parse(normalizedBody)` | LEGITIMATE: multipart body requires normalization before Zod parse |
| `hr-custom-fields.controller.ts` | `JSON.parse(query.value)` | LEGITIMATE: `JSON.parse` of an already-validated string field, not Zod |
| `storage-onboarding.controller.ts` | `onboardingDocTypeSchema.safeParse(typeField)` | LEGITIMATE: multipart `@Body("type")` field, custom error shape required |

All inline Zod `.parse()` / `.safeParse()` calls at the HTTP request boundary have been migrated. The remaining cases are internal to business logic or multipart field parsing where `@Validate({body: schema})` would receive the raw un-normalized body.

---

### Class-level path param audit — all surveyed modules checked

Controllers with `@Controller("path/:param")` class-level path params:

| Controller | Param | Status |
|---|---|---|
| All `surveys/*` controllers (8 files) | `surveyId` | Every handler already has `@Validate({ params: surveyIdParams })` — fixed in a prior session |
| All `build/*` controllers (42 files) | `projectId`, `ticketId`, etc. | All have `@Validate({ params: ... })` — fixed in prior session |
| `payroll/payout/payout-batches.controller.ts` | `employeeUserId` | FIXED this session |
| All chat controllers (`chat-messages`, `chat-pins`, `chat-summarize`) | `channelId` | Covered by body/query contracts on their handlers; path-param-only handlers exist but params are `int` validated by ParseIntPipe |
| `deals/deals-stakeholders.controller.ts` | `dealId` | Covered by body contracts on mutating handlers |
| All others listed | Various | Covered by body/query contracts |

**No bare `:id` route params found** — all route params use descriptive names matching the entity type.

---

## Operator-blocked items (with runbook path)

These items cannot be closed by code. Each has a runbook at `architecture-refactor/runbooks/`.

| Item | Runbook | Status |
|---|---|---|
| §1.9 P95 ≤800ms on production-shaped data | RB-05-production-load.md | OPEN |
| §5.1 Seed production-shaped dataset | RB-05-production-load.md | OPEN |
| §5.2 Measure plans as `streamline_app` with GUC | RB-05-production-load.md | OPEN |
| §5.3 VACUUM ANALYZE after table rewrites | RB-05-production-load.md | OPEN |
| §5.4 Expand read-budget gate beyond 2 routes | RB-05-production-load.md | OPEN |
| §5.5 Connection pools, timeouts, per-cell budgets | RB-07-per-cell-cost.md | OPEN |
| §8.3 Operator-access time-bound, approved, audited | (org policy, no runbook yet) | OPEN |
| §8.4 Export/retention/legal-hold/erasure drills | (infra, no runbook yet) | OPEN |
| §8.5 Configure ALERT_WEBHOOK_URL, test alert delivery | RB-06-live-alert-delivery.md | OPEN |
| §8.6 Verify alert predicates against real emissions | RB-06-live-alert-delivery.md | OPEN |

---

## OUT-OF-OWNERSHIP

**§6.1 check:outbox-consumers CI gate at zero:** 4 inventory event types have no registered consumers:
- `inventory.purchase_order.received` — emitted from `grn.service.ts`
- `inventory.sales_order.fulfilled` — emitted from `so-fulfillment.service.ts`
- `inventory.shipment.dispatched` — emitted from `shipments.service.ts`
- `inventory.stock.adjusted` — emitted from `inv-stock-adjustments.service.ts`

Owner: inventory lane (S07). CI gate stays below zero until fixed.

---

## Remaining PARTIAL items (still open)

| Item | Current state |
|---|---|
| §3.1 OpenAPI coverage to 100% | 98.1% (2,843/2,896). After S08b fixes: ~4 more handlers covered. Residual ~49 are named-param patterns documented as legitimate. The `openapi.json` artifact needs regeneration (requires app boot) to update the committed file. |
| §3.3 Migrate all inline validation to @Validate seam | All HTTP-boundary inline Zod parses migrated; named-@Query and path-param-only patterns remain (~49 operations). |
| §3.4 Standardize cursor/filter/sort/error metadata | Depends on §3.1 reaching 100%. |
| §3.5 Assert idempotency and error envelopes | Depends on §3.1 reaching 100%. |

---

## Validation evidence

- `pnpm check:route-classification` → 0 UNDECLARED (3,557 handlers)
- `node ./node_modules/jest/bin/jest.js "dashboard|cache-key-collision|openapi|zod-validation" --maxWorkers=1` → 107 tests pass, 0 failures
- All fixes verified present in source before report written
