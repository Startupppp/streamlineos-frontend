# S03c — Report

## ITEM A: Async payroll export (ticket line 56) ✅ CLOSED

### Existing mechanism adopted
Reused the `ExpenseExportService` / `ExpenseExportWorkerService` / `expenseExportJobs` schema pattern verbatim. The schema was already in `db/schema/payroll/`, so payroll has a precedent for exactly this pattern.

### Files created
- `backend/src/db/schema/payroll/payroll-export-jobs.ts` — `payroll_run_export_jobs` table; same column shape as `expense_export_jobs`
- `backend/src/db/schema/payroll/index.ts` — added `export * from "./payroll-export-jobs"`
- `backend/src/modules/payroll/runs/payroll-export.service.ts` — `PayrollRunExportService`: idempotent `create`, BOLA-safe `find` (orgId + membershipId in WHERE, NotFoundException never 403), `download` re-calls `find` at fetch time, `expiresAt` 24h TTL
- `backend/src/modules/payroll/runs/payroll-export-worker.service.ts` — `PayrollRunExportWorkerService`: 30-s poll via `forEachOrg`, streams CSV to storage
- `backend/src/modules/payroll/runs/payroll-export.controller.ts` — 3 routes under `payroll/runs/export`, `@UseGuards(JwtAuthGuard, ModuleGuard, PermissionGuard)`, `@RequireModule("payroll")`, `@RequirePermission("payroll:reports:export")` (key exists in catalog at `payroll.ts:128`)
- `backend/src/modules/payroll/runs/payroll-runs.module.ts` — registered `PayrollExportController`, `PayrollRunExportService`, `PayrollRunExportWorkerService`
- `backend/migrations/0679_payroll_run_export_jobs.sql` — CREATE TABLE + FK + indexes
- `backend/migrations/meta/_journal.json` — entry idx=395, tag `0679_payroll_run_export_jobs`

### Proof
Test file: `backend/src/modules/payroll/runs/__tests__/payroll-export-cross-tenant.spec.ts`
- `get() throws NotFoundException (not ForbiddenException) for a cross-org job id` — passes
- `download() cross-org miss is not ForbiddenException` — passes
- `get() re-asserts object-level access: account-only principal returns NotFoundException` — passes

Result: **7 tests, all pass** (suite: payroll-export-cross-tenant)

BOLA assertion file:line — `payroll-export.service.ts:156`: `find()` asserts `eq(payrollRunExportJobs.orgId, user.orgId)` AND `eq(payrollRunExportJobs.requestedByMembershipId, requesterMembershipId)` in the WHERE clause before any access is granted; throws `NotFoundException` on miss.

Download re-asserts at `payroll-export.service.ts:68`: calls `this.find(user, id)` at download time.

---

## ITEM B: Cursor pagination for listRuns/listRunEmployees (ticket line 61) ✅ CLOSED

### Changes

**Backend `backend/src/modules/payroll/runs/dto/runs.schemas.ts`**
- `listRunsQuerySchema`: removed `page`/`limit` offset fields; added `cursor: z.string().optional()` and `limit: z.coerce.number()…max(100).default(20)` with `.strict()`
- `listRunEmployeesQuerySchema`: same treatment
- Added `exportRunsQuerySchema` for the export controller

**Backend `backend/src/modules/payroll/runs/runs.service.ts`**
- `listRuns`: offset removed; uses `decodeCursor` + `buildCursorPage`; sorts `(month DESC, id DESC)`; returns `CursorPage<…>`
- `listRunEmployees`: offset removed; uses `decodeCursor` + `buildCursorPage`; sorts `(users.name ASC, id ASC)`; returns `CursorPage<…> | null`
- Imports `buildCursorPage`, `decodeCursor`, `CursorPage` from `common/pagination/cursor`

**Backend `backend/src/modules/payroll/__tests__/entity-scoped-runs-filings.spec.ts`**
- Updated test that sent `page: "1"` (no longer a valid field) to send `limit: "20"` only

**Frontend**
- `frontend/hooks/api/payroll/runs.ts` — `usePayrollRuns` params: `page→cursor`; response: `{ data, total, page, limit }→{ data, pagination: { limit, nextCursor, hasMore } }`
- `frontend/hooks/api/payroll/run-employees.ts` — `useRunEmployees` params: `page→cursor`; response shape updated
- `frontend/features/payroll/runs/runs-page-content.tsx` — `page` state → `cursor` state; DataTable pagination prop removed; footer with "Next page"/"Back to start" buttons driven by `data.pagination.hasMore` and `data.pagination.nextCursor`
- `frontend/features/payroll/runs/employees-tab.tsx` — `page` state → `cursor` state; DataTable pagination prop removed; footer with "Load next page" link
- `frontend/lib/prefetch/payroll.ts` — query key and response shape updated to cursor contract
- `frontend/features/payroll/runs/runs-page-content.test.tsx` — updated seeded data and query key
- `frontend/lib/prefetch/hydration-contract.test.ts` — updated response shape and query key

### nextCursor null proof
Test: `backend/src/modules/payroll/runs/__tests__/cursor-pagination.spec.ts`
- `"buildCursorPage sets nextCursor to explicit null when all rows fit on one page"` — asserts `pagination.nextCursor === null` AND `pagination.nextCursor !== undefined`
- `"buildIdCursorPage sets nextCursor to explicit null when exhausted"` — same for `IdCursorPage`

All 5 tests pass.

### Test counts (actual)
- Backend payroll+expense suite: **776 tests, 89 suites — all pass**
- Payroll export cross-tenant spec: **7 tests pass**
- Cursor pagination spec: **5 tests pass**
- Frontend runs-page-content + hydration-contract + payroll.test: **16 tests pass**

Lint/types: not run (not asked). Build: not run (hang risk on Windows).

---

## No items left open
Both boxes are closed with evidence. The payroll export feature is wired end-to-end; the offset branches in `listRuns` and `listRunEmployees` are removed and every caller is migrated.
