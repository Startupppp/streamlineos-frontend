# S06d Report — Communications: admin gating, mail checkpoint, isolation tests

## ROW 70 — Admin route gating

Backend controllers were already correct:
- `chat-org-settings.controller.ts`: class-level `@UseGuards(JwtAuthGuard, PermissionGuard)`, GET uses `chat:channels:read`, PATCH uses `chat:org-settings:manage`.
- `chat-huddles.controller.ts`: class-level `@UseGuards(JwtAuthGuard, PermissionGuard)`, kick handler uses `chat:huddles:moderate`.
- `calendar.controller.ts`: universal handlers have no `PermissionGuard`, admin handlers have `@UseGuards(PermissionGuard)` on the handler.

Frontend changes made:
- `frontend/hooks/api/chat-huddles.ts`: converted `useKickParticipant` from raw `useMutation` to `useAuthorizedMutation("chat:huddles:moderate", ...)`.
- `/chat/settings` and `/chat/moderation` route-access extensions were already set in `route-access-extensions.ts`.
- `/calendar/settings` route-access extension was added by a parallel lane using `calendar:admin:manage` (key now exists in both catalogs after coordinator correction and lane fix). The key `calendar:admin:manage` is confirmed present in `backend/src/modules/rbac/permissions/calendar.ts`.

## ROW 79 — Mail sync checkpoint

New files created:
- `backend/src/db/schema/mail/mail-sync-checkpoints.ts` — Drizzle schema for `mail_sync_checkpoints` table.
- `backend/src/db/schema/mail/index.ts` — added `export * from "./mail-sync-checkpoints"`.
- `backend/src/modules/mail/mail-sync-checkpoint.service.ts` — `MailSyncCheckpointService` with `loadPosition`, `savePosition` (own `runInNewTenantTransaction`), `clearPositions` (takes `TenantTx`).
- `backend/src/modules/mail/mail.module.ts` — added provider + export.
- `backend/src/modules/mail/mail.service.ts` — `fetchMessagesForAccount` fires `void this.checkpoints.savePosition(...)` after successful metadata upsert; checkpoint loaded at fetch start.
- `backend/migrations/0704_mail_sync_checkpoints.sql` — DDL with `SET lock_timeout = '5s'`; unique index on `(account_id, folder)`; composite unique constraint `(org_id, id)`.
- `backend/migrations/meta/_journal.json` — entry added: idx=532, when=1798000032000, tag=`0704_mail_sync_checkpoints`.

## ROW 88 — Isolation test coverage

Services covered and test files:

| Service | File | New? |
|---|---|---|
| ChatPresenceService | `chat-services-tenant-isolation.spec.ts` | pre-existing |
| ChatSearchService | `chat-services-tenant-isolation.spec.ts` | pre-existing |
| ChatSummarizeService | `chat-services-tenant-isolation.spec.ts` | pre-existing |
| ChatChannelMembersService | `chat-services-tenant-isolation.spec.ts` | pre-existing |
| ChatNotificationsService | `chat-services-tenant-isolation.spec.ts` | pre-existing |
| ChatReplyRemindersService | `chat-services-tenant-isolation.spec.ts` | pre-existing |
| ChatOrgSettingsService | `chat-services-tenant-isolation.spec.ts` | **ADDED** (2 tests) |
| ChatPinsService | `chat-mutation-tenant-isolation.spec.ts` | pre-existing |
| ChatSavedService | `chat-mutation-tenant-isolation.spec.ts` | pre-existing |
| MailAccountsService | `mail-accounts-tenant-isolation.spec.ts` | pre-existing |
| MailMetadataService | `mail-metadata-isolation.spec.ts` | pre-existing |
| MailSyncCheckpointService | `mail-sync-checkpoint-isolation.spec.ts` | **CREATED** (4 tests) |
| ExternalCalendarEventsService | `external-calendar-events-tenant-isolation.spec.ts` | pre-existing |
| CalendarReminderSweepService | `calendar-reminder-sweep-tenant-isolation.spec.ts` | pre-existing |

Test run results:
- 7 suites, 32 tests — all PASS.
- `resetAllMocks` used in all specs; `db.transaction` mock invokes callback via `runInNewTenantTransaction` mock.

## Tests run

```
PASS src/modules/chat/chat-services-tenant-isolation.spec.ts
PASS src/modules/chat/chat-mutation-tenant-isolation.spec.ts
PASS src/modules/mail/mail-accounts-tenant-isolation.spec.ts
PASS src/modules/mail/mail-metadata-isolation.spec.ts
PASS src/modules/mail/mail-sync-checkpoint-isolation.spec.ts
PASS src/modules/calendar/external-calendar-events-tenant-isolation.spec.ts
PASS src/modules/calendar/calendar-reminder-sweep-tenant-isolation.spec.ts
Tests: 32 passed, 32 total
```

Lint/types: not run (hard rule for this session).
