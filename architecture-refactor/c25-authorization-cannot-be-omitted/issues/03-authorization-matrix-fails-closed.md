# 03 — The authorization matrix fails closed in CI

**Status:** in-progress

## Acceptance criteria

- [x] CI enumerates every route and fails on missing exposure metadata. — `RouteClassifierGuard` wired at `app.module.ts:195`; `REQUIRE_ROUTE_CLASSIFICATION=true` in CI satisfies this. Verified: guard exists at `backend/src/common/auth/route-classifier.guard.ts`
- [x] Permission keys on routes exist in the backend and frontend catalogs. — `backend/src/scripts/check-permission-keys.mjs` (`pnpm check:permission-keys`), wired as a required CI step at `backend/.github/workflows/ci.yml:61`. It enumerates every `@RequirePermission` key and fails exit 1, naming **file and line**, for any key absent from the backend catalog (a ghost key nobody can be granted) or from the frontend `PermissionKey` union (`useCan` false forever). **Verified against the real extractor, not just its self-test:** injecting the historical `hr:employees:export` ghost key into a live controller made the check exit 1 and name `entitlements.controller.ts:28`; reverting made it pass. The frontend `PERMISSIONS` runtime array is a deliberate subset and is correctly **not** the comparison target.

  **Two blind spots closed 2026-08-27, both of which made it decline to check without saying so.** (1) The catalog was *greped* for `name: "…"`, so it never saw the twelve `<module>:access:view` / `:manage` keys generated as template literals at `permissions/module-access.ts:14` — 690 real keys, 664 visible. It now loads the real module through ts-node (`src/scripts/dump-permission-catalog.ts`). (2) `@RequirePermission` is not always a string literal: **21 routes pass a module-level constant** (`autonomy-review.controller.ts`, `data-quality.controller.ts`, `issues.controller.ts`, `issue-view-scope.ts`), and the literal-only regex skipped all of them silently. Constants now resolve, and an argument that resolves to nothing is a **failure**, never an absence. Coverage went 3,055 → **3,085 usages, 617 → 621 unique keys, 664 → 690 catalog keys**; the tree is still clean, so the new coverage found no violations rather than hiding them. Extractors moved to `src/scripts/permission-key-extractors.mjs` so this check and `check:navigation-permissions` share one implementation, and the superseded duplicate `src/common/auth/verify-permission-catalog.mjs` is deleted with `verify:permissions` repointed here (closing `lane-requests/lane-2.md` §3).
- [ ] Every non-universal navigation destination names the exact route permission. — **The check now exists and names every offender; four destinations genuinely do not.** `backend/src/scripts/check-navigation-permissions.mjs` (`pnpm check:navigation-permissions`, with `--self-test`) walks navigation → route, the opposite direction to `check:permission-keys`, and fails on a nav gate naming a key that no `@RequirePermission` enforces. **416 gates, 178 unique keys, 4 failing:** `accounting:view` (~40 destinations, `sidebar-nav-groups-finance.ts:9` onward — routes enforce `accounting:read`), `accounting:report` (6 destinations, `:273-303` — routes enforce `accounting:reports:read`), `crm:deals:approve` (`sidebar-nav-groups-crm.ts:107` → `/crm/deals/approvals`, whose route enforces `crm:deals:read` at `deals-approvals.controller.ts:53`), `hr:goals:view` (`sidebar-nav-routes-hr-employee-experience.ts:72` → `/hr/goals`, whose route enforces `hr:performance:view`). **BLOCKED: all four fixes are `frontend/components/layout/sidebar/**`, outside this session's territory** — handed over with exact per-file lines in [`lane-requests/s3.md`](../../lane-requests/s3.md) §1. Not wired to CI while it exits 1, for the same reason `check:route-classification` is not.
- [x] Every tenant table approved for RLS has a policy and a leading tenant index. — both halves are now asserted by a named check. **Index half, built and run here:** `backend/src/scripts/check-tenant-indexes.mjs` (`pnpm check:tenant-indexes`) parses every `pgTable` in `db/schema/**` and fails on any tenant table whose declared indexes do not *lead* with its tenant column — **703 of 703 pass**, and it is wired as a required step in `backend/.github/workflows/ci.yml` beside the permission-key check. It needs no database, which is what lets it gate every commit. **Policy half:** `db-verify-rls.mjs:228-279` asserts RLS enabled, a policy referencing the org column, and `FORCE ROW LEVEL SECURITY`, wired at `ci.yml` in the `tenant-isolation` job — that job is `schedule || workflow_dispatch` only, because it needs real credentials, so it is asserted but has no executed run in this program (c25-04's gate). Stated rather than implied.
- [x] Permission mutations bump the access version in the same transaction. — `bumpPermissionsVersion(tx, orgId)` called with `tx` at: `access.service.ts:541`, `entitlements.service.ts:281`, `delegations.service.ts:282,350`, `module-access-groups.service.ts` (8 call sites), `module-access.service.ts:443`, `module-standing-mutations.service.ts:137,207,322`, `user-permission-grants.service.ts:190,234`
- [x] The report names intentional exceptions rather than hiding them in a broad allowlist. — `PLATFORM_GLOBAL_TABLES` in `backend/src/scripts/db-verify-rls.mjs:43-48` has per-entry rationale embedded in source comments ("the tenant row itself; RLS would use… Access is controlled by application-layer membership checks"); absence from the set is treated as a gap, not global-by-default

## Unknown-key closes

`authorize()` fails closed on an unknown key without any special handling: `scopeFor` returns `"none"` for any key not in the resolved map, which maps to `{ allow: false, scope: "none", reason: "FORBIDDEN" }`.

- [x] `"fails closed on a completely unknown key that appears in no module catalog"` — a member gets `FORBIDDEN`. (`backend/src/modules/access/authorize.spec.ts:103`)
- [x] `"fails closed on a malformed key with no module segment"` — same path. (`backend/src/modules/access/authorize.spec.ts:110`)

**Correction, 2026-08-27: the mechanism described here was wrong, and the two tests citing it had never passed.** This section used to claim the deny came from `moduleAvailability` returning `NO_MODULE` "before `scopeFor` is even called", and both tests asked an **org owner** and asserted `NO_MODULE`. Neither is true. `isCoreModuleKey` treats an *unknown* module as core (`module-registry.ts:170` — `definition === undefined || !definition.planGated`), so availability answers yes; and an org owner's `scopeFor` returns `"all"` for every key (`access.service.ts:655`), so an org owner is **allowed**. Unknown-module-is-core is deliberate: the registry lists what is *plan gated*, not what exists, so treating an absent entry as gated would 402 `settings`, `tasks` and every other ungated namespace — which is `backend/CLAUDE.md` §5's warning about `MODULE_CATALOG` read from the other side.

The guarantee is real, it is just a different one: **`FORBIDDEN` from `scopeFor`, for everyone who is not an org owner.** The tests now assert that, plus two new cases pinning the owner path — that an owner is allowed by design, and that an owner restricted by `tokenScopes` is still denied — so the wrong assertion is not reintroduced as a bug report.

The dates settle that this was never a regression: `76ae7053` (2026-08-25) introduced unknown-module-is-core, and `77e20610` (2026-08-26) added these two tests **afterwards**. They were written red and ticked without being run. `authorize.spec.ts` is an ordinary spec, not `*.e2e-spec.ts`, so it is executed coverage — nobody ran it. `npx jest src/modules/access/authorize.spec.ts` → **25 tests passing.**

**Audit note (2026-08-26):** Ticked "permission mutations" (verified bumpPermissionsVersion call sites) and "report names exceptions" (verified PLATFORM_GLOBAL_TABLES per-entry rationale). Stale blocker removed from "wire CI check" — app.module.ts wiring is done.

## Todo

- [x] Join route, catalog, navigation and RLS checks into one report — all four now exist as separate, individually actionable checks rather than one opaque report: `pnpm check:route-classification`, `pnpm check:permission-keys` (`ci.yml:61`), `pnpm check:navigation-permissions` (new), `pnpm check:tenant-indexes` (new, `ci.yml`) + `pnpm db:verify-rls` (scheduled). Kept separate deliberately: one report that fails is one thing to ignore, four named checks each fail on their own line and say which file to open.
- [x] Make each missing classification actionable by file and route — `pnpm check:route-classification` names `file → handler` for all 107 undeclared routes; `pnpm check:permission-keys` names `file:line` for each offending key. The earlier `ClassName#methodName`-only limitation applied to the runtime guard, not to these.
- [ ] Wire the report as a required CI check — **PARTIALLY DONE, and the remainder is blocked on findings this session cannot fix.** Wired and required: `check:permission-keys` (`ci.yml:61`) and `check:tenant-indexes` (new, passing 703/703). Not wired, each because it exits 1 on real findings outside this session's territory and wiring a red check reds every build: `check:route-classification` (the undeclared routes — see c25-01 for the live count) and `check:navigation-permissions` (4 drifted nav keys — see the criterion above). Wire each, and set `REQUIRE_ROUTE_CLASSIFICATION=true`, the day its finding count reaches zero. **BLOCKED on `lane-requests/s3.md` §1 and §2.**
