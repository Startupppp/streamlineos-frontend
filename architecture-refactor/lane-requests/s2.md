# Requests from S2 (billing ledger) — 2026-08-27

**Items 1–3 are CLOSED — S2 completed them itself** on the user's instruction, after checking
contention. Kept here as a record of what changed outside S2's original territory, so the owning
sessions know.

---

## 1. ~~Journal entry for `0565`~~ — DONE, `idx` 304

`0565_billing_invoice_snapshot_immutability` is in `migrations/meta/_journal.json` as `idx` 304.
Appended by reading the live file and re-verifying monotonic `idx` and `when` after the write,
because four sessions were appending concurrently; entries went 303 → 304 and no existing entry
was touched. **It remains unapplied**, which is the standing condition for this whole program.

## 2. ~~Four membership sites~~ — DONE, five sites

`SeatLedgerService.recordSeatEvent` is now called from every membership write, each passing the
transaction that performed the write:

| File | Line | Event |
|---|---|---|
| `modules/organization/core/invitations.service.ts` | 268 | `INVITE_SENT` |
| `modules/organization/core/invitations.service.ts` | 554 | `INVITE_CANCELLED` |
| `modules/organization/core/invitation-acceptance.service.ts` | 93 | `INVITE_ACCEPTED` |
| `modules/users/users.service.ts` | 128, 191 | existing-user and new-user direct create |
| `modules/hr/directory/employee-onboarding.service.ts` | 161, 280 | employee onboarded |

`recordSeatEvent`'s executor widened from `TenantTx` to `DbOrTx` so a caller can hand over the
handle it already has. Four specs that build these services gained a `SeatLedgerService` stub
provider; none of their assertions changed.

**`INVITE_EXPIRED` is still unwritten.** Nothing in the codebase transitions an invitation to
expired — expiry is evaluated by predicate (`expires_at > NOW()` inside `seatCount()`), not by a
sweep. The seat maths is already correct without it, because an expired invitation stops counting
the moment it lapses. A future expiry sweep should record the event.

## 3. ~~Plan change~~ — DONE

`billing.service.ts:263` calls `recordProrationForPlanChange` (`:99-146`) inside `verifyAndActivate`'s
transaction. It passes neither an amount nor a line type. Three guards, because this runs **after**
the money is captured and the commercial catalog is unseeded: same-plan renewals and first
subscriptions are skipped, a change outside the billed period is skipped rather than letting
`assertInterval` throw, and a missing price version is logged at error level and skipped rather than
rolling back a captured payment. `getActivePriceForPlanTier` was added to `VersionedCatalogService`
to bridge the legacy `subscriptions.plan` enum to a commercial price version.

**S1: this touched `billing.service.ts`, which is yours.** It was committed and clean when I edited
it, and the change is confined to one new private method plus one call and two constructor
parameters. Three of your specs gained `ProrationLedgerService` and `VersionedCatalogService` stub
providers.

---

## 4. `unregistered-injectables.mjs` is clean — the standing offer in `lane-1.md` is claimable

`pnpm -C backend exec node scripts/unregistered-injectables.mjs src` now reports:

```
injectables=945 unreferencedOutsideOwnFile=0
```

`VersionedCatalogService` was the only offender and is registered at `billing/core/billing.module.ts:23`.
Per `lane-requests/lane-1.md`'s standing offer, the script can be promoted to an `app-*.spec.ts`
guard beside `app-route-uniqueness.spec.ts`. Note that the count rises as sessions land services —
S2 added five, all registered — so promote it after the batch settles, not mid-flight.

---

## 5. Eight suites are red on `main`, none of them S2's

`tsc --noEmit` is clean. But as of 2026-08-27, `src/modules/organization` and `src/modules/hr/directory`
have eight failing suites, in two groups, neither touching anything S2 wrote:

**Five fail on a `CacheService` method the test doubles do not have** —
`this.cache.invalidateForOrg is not a function` and `invalidateNamespaceForOrg is not a function`, in
`org-hierarchy-cost-centers.service.spec`, `invitation-decline.spec`, `invitations-state-machine.spec`,
`organization-member-status.spec` and `invitations-plan-limit.spec`. `invalidateForOrg` entered
`CacheService` in commit `3b1c7cd7`; the doubles were never updated. Whoever owns that commit owns
these.

**Three fail on a pagination cap that no longer bites** — `listMembersSchema.parse({ limit: 101 })`
does not throw, in `organization.schemas.spec`, `org-chart-cursor.spec` and
`skills-matrix-contract.spec`. That is the hard 100/page cap in `backend/CLAUDE.md` §3, currently
unenforced for these schemas. S4's territory.

---

## What S2 changed that another session may want to know

- `billing/core/billing.module.ts` now registers and exports, in addition to what was there:
  `VersionedCatalogService`, `SeatLedgerService`, `ProrationLedgerService`, `UsageMeteringService`,
  `InvoiceSnapshotService`. S1: if you need a provider line, ask here rather than editing it.
- `seatCount()` and the `quota:${orgId}:members` lock key moved out of `plan-limits.service.ts` into
  `billing/core/seat-definition.ts`. Behaviour is identical and `plan-limits.service.ts` imports it;
  a test renders both the enforcement and the ledger statement through `PgDialect` and asserts they
  are the same string.
- New shared leaves in `billing/core/`: `quota-counts.ts` (`readCount`, which refuses a count it
  cannot compute) and `money-rounding.ts` (`bigint` minor-unit arithmetic, four rounding rules).
  Use these rather than writing a second copy.
