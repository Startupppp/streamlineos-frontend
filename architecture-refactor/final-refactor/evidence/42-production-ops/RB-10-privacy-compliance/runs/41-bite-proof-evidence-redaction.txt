$ # BITE PROOF for check-evidence-redaction.mjs and evidence-redaction-gate.spec.ts.
$ # Two defects are reintroduced one at a time into the SHIPPED gate, the one spec is
$ # run against each, and the file is restored and re-hashed. Fixture literals in the
$ # spec are assembled from fragments so this transcript itself stays redacted.
$
$ shasum -a 256 src/scripts/check-evidence-redaction.mjs   # pristine
048052c9fc0074287f8a5a693ef418cc141e779afab1fe05f7c12a839802033b  src/scripts/check-evidence-redaction.mjs

$ # DEFECT A — the e-mail exemption is widened to 'return true || ...', so the pattern
$ #   still exists and still runs but can never report. This is the weakened-assertion
$ #   shape CLOSURE-DEFINITION.md names, applied to the gate itself.
$ npx jest --runInBand --testPathPattern="evidence-redaction-gate"
FAIL src/scripts/evidence-redaction-gate.spec.ts
  check-evidence-redaction gate
    ✕ passes its own self-test, controls and bite proofs included (69 ms)
    ✓ reports a clean fixture bundle as clean (85 ms)
    ✓ fails on a credential planted in a SEALED file (76 ms)
    ✓ fails on a credential planted in an UNSEALED file inside a sealed directory (79 ms)
    ✕ fails on a real person's e-mail address (88 ms)
    ✓ fails on a connection string carrying a real password (92 ms)
    ✓ fails on a managed database host name (80 ms)
    ✓ accepts an already-redacted connection string and a reserved-TLD address (76 ms)
    ✓ refuses to pass a tree with too few seals (67 ms)
    ✓ refuses to pass a tree below the file floor (84 ms)
    ✓ refuses to pass a tree below the byte floor (93 ms)
    ✓ declares its floors and patterns without touching the disk (67 ms)
    ✓ scans the real release evidence tree and finds it redacted (118 ms)

  ● check-evidence-redaction gate › passes its own self-test, controls and bite proofs included

    expect(received).toBe(expected) // Object.is equality

    Expected: 0
    Received: 1

      117 |   it("passes its own self-test, controls and bite proofs included", () => {
      118 |     const run = runGate(["--self-test"]);
    > 119 |     expect(run.status).toBe(0);
          |                        ^
      120 |     const report = JSON.parse(run.stdout) as { pass: boolean; checks: Record<string, boolean> };
      121 |     expect(report.pass).toBe(true);
      122 |     for (const [name, ok] of Object.entries(report.checks)) expect([name, ok]).toEqual([name, true]);

      at Object.<anonymous> (src/scripts/evidence-redaction-gate.spec.ts:119:24)

  ● check-evidence-redaction gate › fails on a real person's e-mail address

    expect(received).toBe(expected) // Object.is equality

    Expected: 1
    Received: 0

      148 |   it("fails on a real person's e-mail address", () => {
      149 |     const run = runGate([`--root=${makeRoot({ "who.txt": `reported by ${REAL_EMAIL}\n` })}`]);
    > 150 |     expect(run.status).toBe(1);
          |                        ^
      151 |     expect(run.stderr).toContain("non-synthetic-email");
      152 |   });
      153 |

      at Object.<anonymous> (src/scripts/evidence-redaction-gate.spec.ts:150:24)

Test Suites: 1 failed, 1 total
Tests:       2 failed, 11 passed, 13 total
Snapshots:   0 total
Time:        1.725 s, estimated 2 s
Ran all test suites matching /evidence-redaction-gate/i.
EXIT_CODE=1

$ # restored. DEFECT B — the corpus drops the 'every regular file directly inside a
$ #   sealed directory' clause, so a leak parked in an unsealed sibling is never read.
$ #   This is the narrowed-corpus shape.
$ npx jest --runInBand --testPathPattern="evidence-redaction-gate"
FAIL src/scripts/evidence-redaction-gate.spec.ts
  check-evidence-redaction gate
    ✕ passes its own self-test, controls and bite proofs included (88 ms)
    ✓ reports a clean fixture bundle as clean (97 ms)
    ✓ fails on a credential planted in a SEALED file (89 ms)
    ✕ fails on a credential planted in an UNSEALED file inside a sealed directory (88 ms)
    ✓ fails on a real person's e-mail address (87 ms)
    ✓ fails on a connection string carrying a real password (85 ms)
    ✓ fails on a managed database host name (78 ms)
    ✓ accepts an already-redacted connection string and a reserved-TLD address (79 ms)
    ✓ refuses to pass a tree with too few seals (61 ms)
    ✓ refuses to pass a tree below the file floor (61 ms)
    ✓ refuses to pass a tree below the byte floor (69 ms)
    ✓ declares its floors and patterns without touching the disk (59 ms)
    ✓ scans the real release evidence tree and finds it redacted (102 ms)

  ● check-evidence-redaction gate › passes its own self-test, controls and bite proofs included

    expect(received).toBe(expected) // Object.is equality

    Expected: 0
    Received: 1

      117 |   it("passes its own self-test, controls and bite proofs included", () => {
      118 |     const run = runGate(["--self-test"]);
    > 119 |     expect(run.status).toBe(0);
          |                        ^
      120 |     const report = JSON.parse(run.stdout) as { pass: boolean; checks: Record<string, boolean> };
      121 |     expect(report.pass).toBe(true);
      122 |     for (const [name, ok] of Object.entries(report.checks)) expect([name, ok]).toEqual([name, true]);

      at Object.<anonymous> (src/scripts/evidence-redaction-gate.spec.ts:119:24)

  ● check-evidence-redaction gate › fails on a credential planted in an UNSEALED file inside a sealed directory

    expect(received).toBe(expected) // Object.is equality

    Expected: 1
    Received: 0

      142 |     writeFileSync(join(root, "a", "not-in-the-seal.txt"), `token=${AWS_KEY}\n`, "utf8");
      143 |     const run = runGate([`--root=${root}`]);
    > 144 |     expect(run.status).toBe(1);
          |                        ^
      145 |     expect(run.stderr).toContain("not-in-the-seal.txt");
      146 |   });
      147 |

      at Object.<anonymous> (src/scripts/evidence-redaction-gate.spec.ts:144:24)

Test Suites: 1 failed, 1 total
Tests:       2 failed, 11 passed, 13 total
Snapshots:   0 total
Time:        1.85 s, estimated 2 s
Ran all test suites matching /evidence-redaction-gate/i.
EXIT_CODE=1

$ # restored again, byte-identical, and green.
$ shasum -a 256 src/scripts/check-evidence-redaction.mjs
048052c9fc0074287f8a5a693ef418cc141e779afab1fe05f7c12a839802033b  src/scripts/check-evidence-redaction.mjs
$ npx jest --runInBand --testPathPattern="evidence-redaction-gate"
PASS src/scripts/evidence-redaction-gate.spec.ts
  check-evidence-redaction gate
    ✓ passes its own self-test, controls and bite proofs included (69 ms)
    ✓ reports a clean fixture bundle as clean (78 ms)
    ✓ fails on a credential planted in a SEALED file (82 ms)
    ✓ fails on a credential planted in an UNSEALED file inside a sealed directory (84 ms)
    ✓ fails on a real person's e-mail address (81 ms)
    ✓ fails on a connection string carrying a real password (83 ms)
    ✓ fails on a managed database host name (86 ms)
    ✓ accepts an already-redacted connection string and a reserved-TLD address (88 ms)
    ✓ refuses to pass a tree with too few seals (69 ms)
    ✓ refuses to pass a tree below the file floor (68 ms)
    ✓ refuses to pass a tree below the byte floor (78 ms)
    ✓ declares its floors and patterns without touching the disk (65 ms)
    ✓ scans the real release evidence tree and finds it redacted (98 ms)

Test Suites: 1 passed, 1 total
Tests:       13 passed, 13 total
Snapshots:   0 total
Time:        1.601 s, estimated 2 s
Ran all test suites matching /evidence-redaction-gate/i.
EXIT_CODE=0
