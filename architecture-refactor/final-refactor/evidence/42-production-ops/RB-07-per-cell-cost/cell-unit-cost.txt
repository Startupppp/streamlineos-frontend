$ npm run --silent cell:unit-cost
--- stdout+stderr ---
◇ injected env (55) from .env // tip: ⌘ multiple files { path: ['.env.local', '.env'] }

Cell: local-scratch-1010 — unit cost snapshot
────────────────────────────────────────────────────────────────────────────────
  per active organization              0
  per active user                      1
  per 1,000 requests                   UNMEASURED — requires: HTTP request count for this cell. Primary source: .load-driver-results.json written by the load driver with field requestCount:number and durationMs:number. Fallback: application request logs or an APM tool.
  per 1,000 realtime minutes           UNMEASURED — requires: Channel-minutes for this cell's Ably app. Set ABLY_API_KEY to fetch from the Ably REST stats API (GET /stats?unit=month). Quantity is channelMean * intervalMinutes or channelPeak * intervalMinutes.
  per GB stored                        0.105
  per million indexed chunks           0
  per million outbox events            0
  per notification delivered           0
  per AI token                         0 tokens · 0 credits · $0.0000
────────────────────────────────────────────────────────────────────────────────

Vendor cost data:
  Neon:       REFUSED — Set NEON_API_KEY and NEON_PROJECT_ID (Neon console → Account Settings → API Keys; project ID from project settings).
  Ably:       channel-minutes n/a · 52 messages · Channel-minutes estimated from peak (mean not in response); may overcount.
  Resend:     KEY PRESENT — Resend does not expose per-email cost or billing totals via its public REST API. Dollar cost per notification = (notifications count from DB) * (per-email rate from your Resend invoice). The key is present; no dollar figure is derivable from the API alone.
  R2:         REFUSED — Set CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID (Cloudflare dashboard → My Profile → API Tokens; account ID from any zone overview URL). Note: R2_ACCESS_KEY_ID/R2_SECRET_ACCESS_KEY are S3-compat keys and cannot query billing analytics.

Cost-per-unit trends (from history):
  AI cost/token: REFUSED — 1 well-spaced sample(s) of 1 total; need 3 each ≥1d apart

Monthly cost + saturation forecast:
  REFUSED — only 0 active org(s); cell may be empty; forecast would be misleading

AI cost anomaly check (last 30 days, sample of up to 50 orgs):
  REFUSED — only 0 org(s) with AI usage; need at least 3 for anomaly detection

Per-org DB and cache attribution (from span log):
  SKIPPED — APP_LOG_FILE not set — pipe app stdout to a file and set APP_LOG_FILE=<path>
  How: set APP_LOG_FILE=<path-to-app-stdout-log> and re-run.
  Why available: every db.query.execute and cache.roundtrip span carries org.id from ObservabilityContext.
  Note: pg_stat_statements and pg_stat_database are per-database, not per-org — span log is the correct source.
  Egress: Network egress is measured at the CDN or load balancer, not in the application process. Source: Cloudflare analytics dashboard or host bandwidth billing. Runbook: EGRESS section in RUNBOOKS.md.

Load driver: .load-driver-results.json not found at /Users/tarunchintakunta/Personal/streamline/streamlineos-backend/.load-driver-results.json
  Expected fields: requestCount, durationMs, realtimeConnectionMinutes

Sampling status:
  Total cost samples    1
  Well-spaced (≥1d)    1 of 3 needed
  Trend ready           NO — need more daily samples
  Next sample after     2026-09-04 16:21:33 UTC
--- exit code: 0 ---
