$ APP_DATABASE_URL=postgresql://streamline_app:***@localhost:5432/scratch_head_1010 node drill-c181-sensitive-route-rejections.mjs
# run 2026-09-03T16:30:17Z  backend 45f8a2e99 branch release/code-10-10-v2
role=streamline_app database=scratch_head_1010 bypassrls=false
predicate source: platform-operator-access.service.ts:325-338 (authorizeRequest)
transition source: platform-operator-access.service.ts:145-168 (approveGrant)

PASS  CONTROL-allow
      expected: an active, unexpired, correctly-scoped grant AUTHORIZES
      observed: authorized grant 9256cd7e-c148-4ec8-b4f6-368ad5cb6826

PASS  C181-1-expired
      expected: expired grant is REJECTED
      observed: FORBIDDEN: No active operator access grant for this organisation and scope
      detail:   predicate: expires_at > now()

PASS  C181-2-revoked
      expected: revoked grant is REJECTED
      observed: FORBIDDEN: No active operator access grant for this organisation and scope
      detail:   predicates: status='active' AND revoked_at IS NULL

PASS  C181-3-cross-tenant
      expected: a grant for org A does not authorize org B
      observed: FORBIDDEN: No active operator access grant for this organisation and scope
      detail:   predicate org_id = :orgId, plus the RLS tenant_isolation policy on the same table

PASS  C181-4-wrong-scope
      expected: a read_payments grant does not authorize a read_customer_data route
      observed: FORBIDDEN: No active operator access grant for this organisation and scope
      detail:   predicate scope = :scope

PASS  C181-5a-concurrent-approval
      expected: exactly one of two concurrent approvals commits; the other CONFLICTS
      observed: 1 approved, 1 conflicted
      detail:   loser: CONFLICT: Grant was changed before approval completed

PASS  C181-5b-self-approval-requester
      expected: the requester (granted_by) cannot be the approver
      observed: new row for relation "operator_access_grants" violates check constraint "chk_oag_self_approval"
      detail:   DB CHECK chk_oag_self_approval backstops the service check at service.ts:143

PASS  C181-5d-same-person-twice-concurrent
      expected: the requester firing two concurrent approvals of their own request satisfies nothing — both are rejected
      observed: 0 approved, 2 rejected
      detail:   rejections: new row for relation "operator_access_grants" violates check constraint "chk_oag_self_approval" | new row for relation "operator_access_grants" violates check constraint "chk_oag_self_approval"

FAIL  C181-5c-self-approval-beneficiary
      expected: the operator who RECEIVES the access cannot be the approver
      observed: ACCEPTED — the beneficiary approved their own break-glass access
      detail:   neither service.ts:143 nor chk_oag_self_approval compares approver_id with operator_user_id

PASS  C181-6-audit-write-failure
      expected: when the audit insert fails the authorization FAILS CLOSED — the caller never receives permission
      observed: DENIED: insert or update on table "operator_access_log" violates foreign key constraint "fk_operator_access_log_grant_id_org"
      detail:   operator_access_log rows before=3 after=3 (the SELECT and the INSERT share one transaction, so the read authorization is rolled back with the failed write)


NOTE — what this drill does NOT establish:
  * It replays the service's SQL against a local database. It is not a deployed-route probe.
  * Case 6 proves the DB transaction aborts. That the HTTP handler is then never entered is a
    property of OperatorSessionGuard.canActivate awaiting authorizeRequest before returning
    true (operator-session.guard.ts:49-59); Nest does not invoke a handler when a guard rejects.

SUMMARY: 9/10 checks passed.
  FAILED: C181-5c-self-approval-beneficiary — ACCEPTED — the beneficiary approved their own break-glass access
exit=1
