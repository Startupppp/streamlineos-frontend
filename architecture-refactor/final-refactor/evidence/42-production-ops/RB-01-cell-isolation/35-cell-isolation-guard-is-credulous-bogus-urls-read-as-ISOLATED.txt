COMMAND         : nice -n 10 npm run --silent cell:isolation -- --region=cell-2
CWD             : /Users/tarunchintakunta/Personal/streamline/streamlineos-backend
STARTED  (UTC)  : 2026-09-03T17:01:31Z
FINISHED (UTC)  : 2026-09-03T17:01:31Z
HOST            : Taruns-MBP / macOS 26.6.2 / node v25.9.0
BACKEND SHA     : 66f09164f7056b377331bcc1fff5f128ada06b95 (release/code-10-10-v2)
PG SERVER       : PostgreSQL 18.4 (Homebrew) on aarch64-apple-darwin25.4.0, compiled by Apple clang version 21.0.0 (clang-2100.0.123.102), 64-bit
DATABASE_URL          (control plane owner) : postgresql://tarunchintakunta@localhost:5432/scratch_head_1010
DIRECT_DATABASE_URL   (control plane owner) : postgresql://tarunchintakunta@localhost:5432/scratch_head_1010
APP_DATABASE_URL      (control plane app)   : postgresql://streamline_app:***@localhost:5432/scratch_head_1010
REGION_CELL_2_DATABASE_URL     (cell owner) : postgresql://tarunchintakunta@localhost:5432/scratch_cold_1010
REGION_CELL_2_APP_DATABASE_URL (cell app)   : postgresql://streamline_app:***@localhost:5432/scratch_cold_1010
DB_REPLICA_URL                 (replica)    : <unset>
REGION_KEYS / PRIMARY_REGION / CELL_ID      : primary,cell-2 / primary / <unset>
EXIT CODE       : 0
NOTE            : GUARD-VACUITY PROBE — THIS IS NOT A PROVISIONING CLAIM AND MUST NOT BE READ AS ONE. Nothing is provisioned. Every non-database value below is a deliberately unreachable placeholder on the reserved .invalid TLD. The run establishes a limitation of the check itself: verify-cell-isolation.mjs decides cache, object storage, realtime and search purely by string comparison of environment variables against the control plane's values, and makes no network call to any of them. Declaring a per-cell URL that reaches nothing is therefore enough to flip four resources from SHARED to ISOLATED. Consequence for PRD-C168: a green cell:isolation run cannot by itself evidence that a cell's cache, bucket, search cluster or realtime application exists. Only the database and queue verdicts in run 31 are backed by a live probe.
---------------- FULL OUTPUT ----------------
◇ injected env (55) from .env // tip: ⌘ enable debugging { debug: true }
cell        : cell-2
cell app url: postgresql://streamline_app:***@localhost:5432/scratch_cold_1010
control app : postgresql://streamline_app:***@localhost:5432/scratch_head_1010

ISOLATED   database identity                          cell=scratch_cold_1010 control-plane=scratch_head_1010 as streamline_app
ISOLATED   application role privilege                 streamline_app bypassrls=false in scratch_cold_1010
ISOLATED   cell application schema                    1027 application table(s) confirm pnpm cell:bootstrap ran in scratch_cold_1010
ISOLATED   control-plane rows visible from the cell   probe isolation-probe-dab6d8d3-112e-4cd1-9ddd-288cc1d6d94c written in scratch_head_1010; rows visible in scratch_cold_1010: 0
ISOLATED   cell rows visible from the control plane   probe isolation-probe-dab6d8d3-112e-4cd1-9ddd-288cc1d6d94c-cell written in scratch_cold_1010; rows visible in scratch_head_1010: 0
ISOLATED   cross-database bridge                      neither dblink nor postgres_fdw is installed in the cell
ISOLATED   foreign servers                            0 foreign server(s) defined in scratch_cold_1010
ISOLATED   row-level security in the cell             983 tables have row-level security enabled in scratch_cold_1010
ISOLATED   cache (Redis)                              the cell declares its own Redis URL (REGION_CELL_2_UPSTASH_REDIS_REST_URL)
ISOLATED   object storage bucket                      the cell declares its own bucket (REGION_CELL_2_R2_BUCKET_NAME=streamlineos-cell-2-not-provisioned)
ISOLATED   object storage endpoint                    the cell declares its own endpoint (REGION_CELL_2_R2_ENDPOINT=https://cell-2-not-provisioned.invalid)
ISOLATED   realtime broker                            the cell declares its own value
ISOLATED   search index                               the cell declares a dedicated search cluster "cell-2" with its own API key
ISOLATED   queues and dead letters                    outbox and delivery tables live in the cell's own database, so a queue row cannot cross cells
NAMESPACED worker pools                               cron and outbox relay lease keys are scoped to cell "cell-2" (cron:lease:cell-2:<job>). A cell-2 process started with CELL_ID=cell-2 runs its own job slots and cannot steal a legacy-1 lease. NAMESPACED: job slots are separated by lease key; both cells still share the same Upstash instance for the lease store — set REGION_CELL_2_UPSTASH_REDIS_REST_URL to fully isolate.
                                                      would be isolated by: a worker process per cell, each with its own CELL_ID and ideally its own Redis
NAMESPACED monitoring                                 every log line emitted by this process carries cellId="cell-2"; read-cell-logs.mjs reads those lines from stdin and prints a per-cell breakdown. NAMESPACED: log lines are tagged; a cross-cell aggregated view requires shipping logs to a collector and querying by the cellId field.
                                                      would be isolated by: a per-cell log stream routed to a dedicated dashboard in the log collector
ISOLATED   secrets                                    the cell's database credentials are declared under REGION_CELL_2_*; search and cache credentials follow the same per-cell env-key pattern

RESULT: DATA ISOLATION PROVED cell=cell-2 isolated=15 namespaced=2 shared=0 unproved=0
NOTE: NAMESPACED resources use key/prefix separation within a shared instance. An attacker holding the master credential of that instance can read all cells' data.
---------------- END OUTPUT -----------------
EXIT CODE       : 0
