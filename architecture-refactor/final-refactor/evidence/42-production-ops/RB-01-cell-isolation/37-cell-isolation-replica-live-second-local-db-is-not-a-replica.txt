COMMAND         : nice -n 10 npm run --silent cell:isolation:replica
CWD             : /Users/tarunchintakunta/Personal/streamline/streamlineos-backend
STARTED  (UTC)  : 2026-09-03T17:02:00Z
FINISHED (UTC)  : 2026-09-03T17:02:03Z
HOST            : Taruns-MBP / macOS 26.6.2 / node v25.9.0
BACKEND SHA     : 66f09164f7056b377331bcc1fff5f128ada06b95 (release/code-10-10-v2)
PG SERVER       : PostgreSQL 18.4 (Homebrew) on aarch64-apple-darwin25.4.0, compiled by Apple clang version 21.0.0 (clang-2100.0.123.102), 64-bit
DATABASE_URL          (control plane owner) : postgresql://tarunchintakunta@localhost:5432/scratch_head_1010
DIRECT_DATABASE_URL   (control plane owner) : postgresql://tarunchintakunta@localhost:5432/scratch_head_1010
APP_DATABASE_URL      (control plane app)   : postgresql://streamline_app:***@localhost:5432/scratch_head_1010
REGION_CELL_2_DATABASE_URL     (cell owner) : <unset>
REGION_CELL_2_APP_DATABASE_URL (cell app)   : <unset>
DB_REPLICA_URL                 (replica)    : postgresql://streamline_app:***@localhost:5432/scratch_cold_1010
REGION_KEYS / PRIMARY_REGION / CELL_ID      : primary,cell-2 / primary / <unset>
EXIT CODE       : 1
NOTE            : ANSWERS THE QUESTION 'do the two local databases satisfy this drill?' — for the replica drill they do not. DB_REPLICA_URL is pointed at the second local database (scratch_cold_1010), the same database that stands in for cell-2 everywhere else in RB-01. The drill is expected to refuse it: a second independent database on the same host is not a streaming standby. Expected failures: replica-endpoint-distinct (both are localhost) and replica-replication-lag (pg_last_xact_replay_timestamp() is NULL, so the endpoint is a primary). PRD-C170 needs a physically provisioned read replica; this run shows the guard cannot be satisfied without one.
---------------- FULL OUTPUT ----------------
◇ injected env (0) from .env // tip: ⌘ custom filepath { path: '/custom/path/.env' }
mode      : cell:isolation:replica
INFO   replica-host                                     localhost
INFO   primary-host                                     localhost
PASS   replica-reachable                                SELECT 1 succeeded on localhost
FAIL   replica-endpoint-distinct                        replica and primary resolve to the same host (localhost). DB_REPLICA_URL must point to a dedicated read-replica endpoint, not the primary.
PASS   replica-guc-read-only-tx                         GUC round-trips correctly inside BEGIN READ ONLY transaction
PASS   replica-rls-enabled                              983 tables have RLS enabled on the replica
FAIL   replica-rls-fails-without-guc                    query without tenant GUC succeeded — either RLS is not enforced on organization_members or the replica connection string uses the BYPASSRLS owner role. Use APP_DATABASE_URL (app role).
FAIL   replica-replication-lag                          pg_last_xact_replay_timestamp() is NULL — this endpoint is a primary, not a read replica. DB_REPLICA_URL must point to a dedicated read-only Neon compute endpoint.
FAIL   replica-lsn-receiving                            pg_last_wal_receive_lsn() is NULL — replica is not receiving WAL from primary. Check that DB_REPLICA_URL points to a streaming standby, not a new empty DB.
PASS   replica-phantom-org-empty                        phantom org ffffffff-ffff-ffff-ffff-ffffffffffff returns 0 rows — no cross-tenant leak
CHECK FAILED: permission denied for schema drizzle
---------------- END OUTPUT -----------------
EXIT CODE       : 1
