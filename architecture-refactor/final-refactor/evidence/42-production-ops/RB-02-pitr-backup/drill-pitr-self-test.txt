$ cd backend && npm run drill:pitr:self-test
  (node src/scripts/drill-pitr-restore.mjs --self-test)
run_at: 2026-09-03T16:23:35Z
---
self-test: fixture verification for drill-pitr-restore

  PASS  watermark-mismatch: correctly detected failure — migration count mismatch: branch=5 main=6
  PASS  before-marker-absent: correctly detected failure — before-marker row not found on branch — branch may be too far back
  PASS  after-marker-present: correctly detected failure — after-marker row present on branch (id=x99) — PITR cut was not applied at the target timestamp
  PASS  policy-count-zero: correctly detected failure — zero RLS policies on branch — tenant isolation was lost in restore
  PASS  policy-count-reduced: correctly detected failure — branch has 10 policies, main has 42 — 32 policy/policies missing from restore
  PASS  watermark-match: correctly passed — 42 migration(s) on both branch and main
  PASS  before-marker-present: correctly passed — before-marker found on branch (migration_id=mig-001)
  PASS  after-marker-absent: correctly passed — after-marker absent from branch — PITR cut confirmed
  PASS  policy-count-ok: correctly passed — 42 RLS policies on branch (main=42)

self-test PASS — all verification fixtures behave correctly
--- exit code: 0

$ cd backend && npm run drill:pitr    (live path, no NEON_API_KEY present)
---
DRILL BLOCKED — missing env var(s): NEON_API_KEY, NEON_PROJECT_ID

PITR RESTORE DRILL — PREREQUISITES MISSING

  NEON_API_KEY      — Neon console → Account Settings → API Keys
  NEON_PROJECT_ID   — Neon console → project settings URL
  DATABASE_URL      — owner-role connection string (neondb_owner or equivalent)
  APP_DATABASE_URL  — app-role connection string (streamline_app role, optional but recommended)

  Set these in .env (backend/) and re-run:
    node --env-file-if-exists=.env src/scripts/drill-pitr-restore.mjs

  What this drill does:
    1. Reads the migration watermark from the source branch (owner role).
    2. Writes a synthetic after-marker row to audit_logs.
    3. Creates a Neon branch at the target timestamp (just before the after-marker write).
    4. Connects to the branch and verifies:
         a. Migration watermark matches.
         b. A known pre-target row is present.
         c. The after-marker row is absent.
         d. RLS policy count is non-zero and matches the source.
         e. The app role can connect (with tenant GUC).
    5. Deletes the branch and the after-marker row.

--- exit code: 1
