RB-03 Step 3 "Enforcement check" — which code paths resolve to the replica connection.
run_at: 2026-09-03T16:32:30Z

$ grep -rn 'DATABASE_REPLICA_URL|DB_REPLICA_URL|readReplica|read_replica' src/common/ src/db/
---
src/db/drizzle.module.ts:53:            "DB_REPLICA_URL is unset — replica reads fall through to primary connection string",
src/db/pool.config.ts:51:  DB_REPLICA_URL: z.preprocess(emptyToUndefined, z.string().min(1).optional()),
src/db/pool.config.ts:200:  const replicaRaw = tuning.DB_REPLICA_URL;

$ grep -rn 'DRIZZLE_REPLICA|REPLICA_ROUTER|ReplicaRouter' src/
---
src/degradation/read-replica.spec.ts:5:  ReplicaRouter,
src/degradation/read-replica.spec.ts:13:      "poolEnvShape declares DB_REPLICA_URL — the routing seam (ReplicaRouter) was built at the same time, " +
src/degradation/read-replica.spec.ts:90:  describe("ReplicaRouter routing decisions (observable via PoolHandle.id)", () => {
src/degradation/read-replica.spec.ts:101:      const router = new ReplicaRouter(primary, replica);
src/degradation/read-replica.spec.ts:111:      const router = new ReplicaRouter(primary, replica);
src/degradation/read-replica.spec.ts:116:      const router = new ReplicaRouter(primary, replica);
src/degradation/read-replica.spec.ts:124:        const router = new ReplicaRouter(primary, replica);
src/degradation/read-replica.spec.ts:132:      const router = new ReplicaRouter(primary, replica);
src/degradation/read-replica.spec.ts:142:        const router = new ReplicaRouter(primary, null);
src/degradation/read-replica.spec.ts:149:      const router = new ReplicaRouter(primary, replica);
src/db/drizzle.module.ts:13:import { DB_POOL_CONFIG, DRIZZLE, DRIZZLE_REPLICA, REPLICA_ROUTER } from "./drizzle.constants";
src/db/drizzle.module.ts:18:import { ReplicaRouter, type PoolHandle } from "./replica-router";
src/db/drizzle.module.ts:46:      provide: DRIZZLE_REPLICA,
src/db/drizzle.module.ts:64:      provide: REPLICA_ROUTER,
src/db/drizzle.module.ts:66:      useFactory: (config: ResolvedPoolConfig): ReplicaRouter => {
src/db/drizzle.module.ts:71:        return new ReplicaRouter(primary, replica);
src/db/drizzle.module.ts:82:    @Inject(DRIZZLE_REPLICA) private readonly replicaDb: DbWithClient,
src/db/replica-router.ts:59:export class ReplicaRouter {
src/db/drizzle.constants.ts:3:export const DRIZZLE_REPLICA = "DRIZZLE_REPLICA";
src/db/drizzle.constants.ts:4:export const REPLICA_ROUTER = "REPLICA_ROUTER";

$ grep -n 'exports:' src/db/drizzle.module.ts
---
75:  exports: [DRIZZLE, DB_POOL_CONFIG],

$ grep -n 'replicaDb' src/db/drizzle.module.ts
---
82:    @Inject(DRIZZLE_REPLICA) private readonly replicaDb: DbWithClient,
167:    await this.replicaDb.__client.end({ timeout: this.config.shutdownTimeoutSeconds });

$ npx jest --runInBand --testPathPattern='degradation/read-replica.spec.ts'
---
PASS src/degradation/read-replica.spec.ts
  Read replica degraded — correctness-sensitive reads go to primary
    single-connection ratchet: replica env var and routing seam were added together
      ✓ poolEnvShape declares DB_REPLICA_URL — the routing seam (ReplicaRouter) was built at the same time, so a half-built replica cannot ship: adding the env var alone makes the routing tests go red (2 ms)
      ✓ resolvePoolConfig returns an optional replicaConnectionString alongside connectionString — undefined when DB_REPLICA_URL is absent, a normalized URL when it is present (3 ms)
    stale-tolerant projections shed before ordinary writes when the primary is under load
      ✓ analytics-refresh is sheddable — it does not hold primary capacity
      ✓ search-freshness is sheddable — it does not hold primary capacity
      ✓ analytics-refresh sheds before ordinary-write (1 ms)
      ✓ search-freshness sheds before ordinary-write
    routing strategy — which work classes are replica-safe vs primary-required
      ✓ RBAC and authentication classes are primary-required
      ✓ financial-ledger and audit classes are primary-required
      ✓ analytics-refresh is replica-safe — it produces an eventually-consistent projection
      ✓ search-freshness is replica-safe — it populates a search index, not a user-visible answer
      ✓ ordinary-write and other sheddable classes are primary-required despite being sheddable (1 ms)
    ReplicaRouter routing decisions (observable via PoolHandle.id)
      ✓ routes primary-required classes to primary even when a healthy replica is configured
      ✓ routes analytics-refresh to the replica when the replica is configured and healthy
      ✓ routes search-freshness to the replica when the replica is configured and healthy
      ✓ sheds analytics-refresh rather than silently falling back to primary when the replica is configured but faulted — a silent fall-back would allow stale projections to compete for primary capacity (4 ms)
      ✓ sheds search-freshness rather than silently falling back to primary when the replica is faulted
      ✓ falls through to primary when no replica is configured — this is not a fault condition, just an unconfigured single-node deployment
      ✓ ReplicaShedError names the work class that was refused
    lag simulation — no physical replica is available
      ○ skipped PHYSICAL REPLICA NOT PROVISIONED. The staleness behaviour below is proved against a REPEATABLE READ snapshot, which is real lag but not replication lag. To close this row fully: provision a Neon read-replica endpoint, set DB_REPLICA_URL to its connection string, and re-run the staleness assertions against that endpoint so the lag measured is the replica's own.
  replica staleness — a lagging snapshot is real, and routing respects it
    ○ skipped a replica-safe read served from a lagging snapshot misses a committed write, while the same read on the primary sees it — the staleness is real, not asserted
    ○ skipped routing a read-after-write class to the lagging snapshot would lose its own write — which is why billing-ledger is primary-required, demonstrated rather than asserted

Test Suites: 1 passed, 1 total
Tests:       3 skipped, 18 passed, 21 total
Snapshots:   0 total
Time:        0.665 s, estimated 1 s
Ran all test suites matching /degradation\/read-replica.spec.ts/i.
--- exit code: 0
