NEGATIVE CONTROL — proving cell:replica cannot be satisfied by a non-replica.
run_at: 2026-09-03T16:26:10Z

There is no streaming standby on this machine. The question this run answers is not
"does the replica pass" (it cannot) but "would the check notice if someone pointed
DB_REPLICA_URL at an ordinary primary and called it a replica?" That is the most likely
real misconfiguration, and it is the one that would turn RB-03 into a vacuous pass.

CASE A — DB_REPLICA_URL points at the primary itself (same host, same database).
$ PGSSLMODE=disable DB_REPLICA_URL=<app role @ scratch_head_1010> \
    APP_DATABASE_URL=<app role @ scratch_head_1010> \
    node src/scripts/verify-replica-routing.mjs
---
◇ injected env (56) from .env // tip: ◈ secrets for agents [www.dotenvx.com]
mode      : cell:replica
INFO   replica-host                                     localhost
INFO   primary-host                                     localhost
PASS   replica-reachable                                SELECT 1 succeeded on localhost
FAIL   replica-endpoint-distinct                        replica and primary resolve to the same host (localhost). DB_REPLICA_URL must point to a dedicated read-replica endpoint, not the primary.
PASS   replica-guc-read-only-tx                         GUC round-trips correctly inside BEGIN READ ONLY transaction
PASS   replica-rls-enabled                              983 tables have RLS enabled on the replica
FAIL   replica-rls-fails-without-guc                    query without tenant GUC succeeded — either RLS is not enforced on organization_members or the replica connection string uses the BYPASSRLS owner role. Use APP_DATABASE_URL (app role).
FAIL   replica-replication-lag                          pg_last_xact_replay_timestamp() is NULL — this endpoint is a primary, not a read replica. DB_REPLICA_URL must point to a dedicated read-only Neon compute endpoint.
FAIL   replica-lsn-receiving                            pg_last_wal_receive_lsn() is NULL — replica is not receiving WAL from primary. Check that DB_REPLICA_URL points to a streaming standby, not a new empty DB.

RESULT: REPLICA ROUTING FAILED failed=4
--- exit code: 1

CASE B — DB_REPLICA_URL points at a SEPARATE, fully-populated database (scratch_drill_1010,
a pg_restore copy of head). This is the "someone took a copy and called it a replica" error:
the data is all there and every query works, but nothing is streaming.
$ PGSSLMODE=disable DB_REPLICA_URL=<app role @ scratch_drill_1010> \
    APP_DATABASE_URL=<app role @ scratch_head_1010> \
    node src/scripts/verify-replica-routing.mjs --isolation
---
◇ injected env (56) from .env // tip: ⌘ override existing { override: true }
mode      : cell:isolation:replica
INFO   replica-host                                     localhost
INFO   primary-host                                     localhost
PASS   replica-reachable                                SELECT 1 succeeded on localhost
FAIL   replica-endpoint-distinct                        replica and primary resolve to the same host (localhost). DB_REPLICA_URL must point to a dedicated read-replica endpoint, not the primary.
PASS   replica-guc-read-only-tx                         GUC round-trips correctly inside BEGIN READ ONLY transaction
PASS   replica-rls-enabled                              983 tables have RLS enabled on the replica
FAIL   replica-rls-fails-without-guc                    query without tenant GUC succeeded — either RLS is not enforced on organization_members or the replica connection string uses the BYPASSRLS owner role. Use APP_DATABASE_URL (app role).
FAIL   replica-replication-lag                          pg_last_xact_replay_timestamp() is NULL — this endpoint is a primary, not a read replica. DB_REPLICA_URL must point to a dedicated read-only Neon compute endpoint.
FAIL   replica-lsn-receiving                            pg_last_wal_receive_lsn() is NULL — replica is not receiving WAL from primary. Check that DB_REPLICA_URL points to a streaming standby, not a new empty DB.
PASS   replica-phantom-org-empty                        phantom org ffffffff-ffff-ffff-ffff-ffffffffffff returns 0 rows — no cross-tenant leak
CHECK FAILED: permission denied for schema drizzle
--- exit code: 1
