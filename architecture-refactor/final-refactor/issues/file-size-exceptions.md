# File Size Exceptions — §7 Registry (backend)

Backend files exceeding 500 lines that are **exempt** from the 500-line hard-review limit
per §7 of the shared CLAUDE.md.

`backend/src/scripts/check-file-sizes.mjs` (`pnpm -C streamlineos-backend check:file-sizes`)
reads the `## Exceptions` table below and **fails closed**. A row grants an exception only
when all of the following hold; any failure fails the gate rather than silently granting or
dropping an exemption:

- the row carries all nine columns, none of them blank;
- the path is a concrete file — wildcard and directory-wide entries are rejected by the parser;
- the file exists on disk;
- the recorded line count equals the measured one exactly;
- the file still exceeds 500 lines — **a file that falls to 500 or below automatically loses
  its exception** and the row must be deleted;
- the review date parses as an ISO calendar date.

A path mentioned only in the audit trail below is not an exception; only table rows are read.

The frontend has its own registry at `frontend/scripts/file-size-exceptions.md`, enforced by
the same rules.

---

## CLI scripts: structural scope decision

`src/scripts/**` are one-off CLI utilities, not production application modules. They are not
imported by any module, are never split into sub-services, and the "target 300 without
fragmenting" concern applies to application code rather than self-contained CLI tools. They
are still listed individually below — the scope decision is a reason, never a wildcard.

## Exceptions

| Path (relative to backend repo root) | Lines | Category | Owner | Public interface | Cohesion argument | Alternatives considered | Review date | Removal trigger |
|---|---|---|---|---|---|---|---|---|
| `src/scripts/relocate-org-data.ts` | 767 | CLI script | Platform / DB | `main()` entry point | Top-level conductor for a multi-step data-migration CLI; the phases share one `db` handle and a fixed execution order, and already delegate their table work to `relocation/catalog-tables.ts` and `relocation/copy-org.ts`. | Splitting per phase was tried in the S01 pass and rejected: the phases have no shared interface, so the split produced numbered fragments that had to change together. | 2026-12-01 | Drops to 500 lines or below, or the migration it performs is retired. |
| `src/scripts/seed-enterprise-workspace.ts` | 668 | CLI script | Platform / DB | `main()` entry point | Enterprise-workspace seed that must execute in a fixed order; each block is a distinct seeding phase sharing local bindings and a single `db` handle. | Extracting per-entity seeders was considered and rejected: every block reads ids produced by the block above it, so the extraction would pass a growing bag of ids between mutually dependent files. | 2026-12-01 | Drops to 500 lines or below, or the seed moves to a fixture-driven loader. |
| `src/scripts/check-referential-action-drift.ts` | 606 | Gate script | Platform / DB | 20 exports: the `main()` entry point and `--self-test` harness plus the 18 symbols the self-test drives directly (`MIN_DECLARED_FKS`, `MIN_LIVE_FKS`, `LIVE_FK_QUERY`, the `Action`/`DeclaredFk`/`LiveFk`/`Stated`/`Verdict`/`Mismatch` types, and `normalizeDeclared`, `normalizeCatalog`, `classify`, `blastRadiusOf`, `statedActionsFrom`, `statedOf`, `declaredFksOf`, `liveFksOf`, `matchLive`, `compare`, `unbaselined`) | One detector for one invariant: it reads every declared `foreignKey(...).onDelete(...)` from the Drizzle tree, reads `confdeltype` from `pg_catalog`, and reports the difference. The declaration reader, the catalog reader and the comparison share the normalisation table that maps drizzle`s referential-action words onto Postgres`s single-character codes; separating them would put that mapping behind an interface and let the two halves drift, which is the exact defect class the gate exists to detect. Its 32-case self-test fixture is co-located so a planted mismatch and its expected verdict are read together. | Splitting the catalog reader into a sibling was rejected: it would need the same normalisation table, so the two files would have to change together on every drizzle or Postgres version bump. Extracting the self-test fixture was rejected because it is the gate`s bite proof and reads as documentation of the invariant. | 2026-12-01 | Drops to 500 lines or below, or the 62 baselined mismatches are resolved and the gate retires to a simple assertion. |
| `src/modules/party/party-mirror-fields.ts` | 561 | Cohesive catalog | Party / HR | `PARTY_FIELD_MIRROR` (a `Record<keyof PartyRow, PartyFieldMirror>`, not an array), `LEGACY_OWNED_COLUMNS` (`Record<MappedLegacyKind, …>`) and 8 exported types (`PartyRow`, `PartyPatch`, `LeadInsert`, `ClientInsert`, `ContactInsert`, `CrmOrgInsert`, `MirrorCell`, `ErasedRow`) | Two keyed catalogs over the same party↔legacy column correspondence, plus the types that describe a cell. It is not pure data — a handful of cells call the local `withKey` helper (`:249`, `:360-363`) to fold a legacy column into a JSON blob — but that helper exists only for these cells and means nothing outside them. | Splitting by subsystem was rejected: the consumers iterate the whole record, so per-subsystem files would need a barrel that reassembles them, which is a re-export shell. Splitting `LEGACY_OWNED_COLUMNS` out was rejected: it names the same legacy columns the mirror maps, so the two would have to change together. | 2027-03-01 | Drops to 500 lines or below, or the mirror is replaced by a schema-derived projection. |
| `src/modules/organization/core/membership-artifacts.ts` | 3089 | Cohesive catalog | Organization | `MEMBERSHIP_ARTIFACTS` (const array) plus four derived exports | One `as const` array in which each entry is one artifact definition; the tail (`MEMBERSHIP_ARTIFACT_IDS`, `MEMBERSHIP_ARTIFACT_TABLES`, two `artifactsRequiring*` filters) derives from it and is meaningless apart from it. | Splitting by letter range or artifact type was rejected as numbered fragmentation with no shared interface. Generating the catalog from the schema was considered but the artifact set is a policy decision, not a schema fact. | 2027-03-01 | Drops to 500 lines or below, or the catalog becomes table-driven. |

| `src/db/schema/common/auth.ts` | 540 | Cohesive catalog | Platform / Auth | 22 `pgTable`/`pgEnum` declarations + 36 exports (tables, relations and their inferred types) | Identity and session table declarations and **zero** control-flow statements — no `function`, `if`, `for`, `while` or `return` anywhere in the file, verified by grep rather than assumed from the filename. `organizations`, `organization_members`, `users`, `accounts`, `sessions`, `verification_tokens` and `invitations` are mutually referential, and `organizations.owner_membership_id` / `organization_members.org_id` point at each other, so the set cannot be cut anywhere without putting an FK's two ends in different files. | An identity/organisation split was rejected for the circular owner-membership FK above. An alphabetical or per-table split was rejected as numbered fragmentation: every consumer imports from the `db/schema` root barrel, so the fragments would have no interface of their own. Re-measured 537 -> 540 on 2026-09-12. | 2027-03-01 | Drops to 500 lines or below, or the identity tables move behind a generated schema. |
| `src/config/env.validation.ts` | 654 | Cohesive catalog | Platform / Config | `CONFIG_VARIABLE_NAMES` (derived from `baseSchema.shape`), the `AppConfig` type and `validateEnv()` | One Zod object over every environment variable the process reads, plus three cross-field `.refine`/`.superRefine` checks and the per-variable validators they need. backend/CLAUDE.md §2 requires a single startup `validationSchema` that fails fast, so the schema is one object by construction, and `CONFIG_VARIABLE_NAMES` is `Object.keys(baseSchema.shape)` — a split schema could not produce that list without being re-merged first. | A split by domain (database, AI, storage, region, mail) was considered and rejected: the fragments have to be recombined into one `z.object` for both `validateEnv` and `CONFIG_VARIABLE_NAMES`, which is a barrel, and the cross-field refinements read variables from more than one domain so they would sit above the fragments anyway. The split that WOULD work is moving the three refinements and the `databaseUrl`/`optionalUrl` helper builders into a sibling `env-refinements.ts`; it is deferred rather than argued away. | 2026-12-01 | Drops to 500 lines or below, or the refinements move to `env-refinements.ts`. |
| `src/modules/auth/auth.service.ts` | 540 | Cohesive service | Platform / Auth | `AuthService` (5 public methods: `register`, `logout`, `logoutAll`, `googleOAuth`, `getSessionData`) | Two entry points into one provisioning sequence. `register` (193 lines) and `googleOAuth` (150 lines) both create a user, place an organisation, write the owner membership, seed system roles, provision modules and open a trial subscription, and all of it has to commit or none of it — `register` carries a compensation path that unplaces the organisation when the transaction fails, which is why the sequence and its unwind are in one file. | A concrete split exists and is NOT argued away: `register` inlines `seedSystemRolesForOrg` + `provisionOrgModules` + `seedDemoDataset` at :138-140 while the file already has a private `provisionWorkspace()` (:210-219) that is exactly those three lines. Calling it from both places, and lifting `provisionWorkspace` plus the placement/compensation block into a `lib/register-provisioning.ts`, removes the duplication and takes the file under the limit. Deferred here only because this pass is a gate-regression sweep, not an auth refactor. | 2026-12-01 | Drops to 500 lines or below, or the provisioning sequence moves to `lib/register-provisioning.ts` and `register` calls `provisionWorkspace()` like the other branch. |
| `src/modules/cron/cron-platform.controller.ts` | 576 | Cohesive controller | Platform / Cron | `CronPlatformController` (30 handlers, a GET/POST pair per sweep, over 16 injected services) | Every handler is the same six lines — `assertCronSecret(authorization)` then one delegate call — and carries a `@Public()` route plus its OpenAPI contract decorators, which is where the length is. The GET/POST pairing is not duplication to remove: the GET form is what an external scheduler can call and the POST form is what the platform's own runner uses, and both must exist per route. | Splitting cron controllers by product domain was already done once, on 2026-09-08 (that pass produced `cron-hr.controller.ts`, `cron-hr-notifications.controller.ts` and the rest); this file is the platform-domain residue and every remaining sweep is genuinely platform-level. A further split by sweep family (retention / lifecycle / workflow) would work and would drop the file; it is deferred because each new controller has to be registered in `app.module.ts` and given its own cron-secret assertion, and three controllers of ~190 lines is not obviously better than one of 576 whose handlers are six lines each. | 2026-12-01 | Drops to 500 lines or below, or the retention sweeps move to a `cron-retention.controller.ts`. |
| `src/modules/e-sign/sign-envelope-sweeps.service.ts` | 529 | Cohesive service | SignOS | `remindableRecipients` (exported for its own spec), `SignEnvelopeSweepsService` (6 public methods: `sendManualReminder`, `runReminderSweep`, `runExpirationSweep`, `runSweepAllOrgs`, `previewSweep`, `lastRuns`) and 4 exported result interfaces | One sweep engine over `sign_envelopes` / `sign_recipients` / `sign_sweep_runs`. The six public methods are four views of the same selection: `previewSweep` must return exactly what `runReminderSweep` would act on, `runSweepAllOrgs` is the per-org loop around both sweeps, and `lastRuns` reads the run rows the others write. Six private helpers hold the shared selection and the run-row bookkeeping, and they are what the public methods are thin over. | Splitting reminder from expiration was rejected: they share the recipient selection (`remindableRecipients`), the `sign_sweep_runs` bookkeeping and the same tenant-scope helper, so the split produces two files plus a third holding everything they share. Extracting the four result interfaces to a `sign-sweep.types.ts` takes ~30 lines and would not clear the limit on its own. | 2027-03-01 | Drops to 500 lines or below, or the sweep selection moves behind a query object the two sweeps and the preview share. |
| `src/modules/email/email.service.ts` | 512 | Cohesive service | Platform / Email | `EmailService extends EmailSendersBase` (27 `send*`/`queue*` methods, one per transactional template) | A dispatch surface, not logic: each method builds one template's parameters and hands them to the shared sender on the base class. The length is the number of templates the product sends, and the per-method body is 5-15 lines of parameter assembly. The template bodies themselves are already out of the file, under `email/templates/`. | A split by product domain (expenses, tickets, tasks, attendance) was considered and rejected: every method routes through the same `EmailSendersBase` sender and the same `EmailOutboxService`, so per-domain services would each re-inject the pair and the callers would have to know which of five services owns their template. The overage is 12 lines; moving any two expense methods out clears it without making the surface better, which is the definition of fragmentation. | 2026-12-01 | Drops to 500 lines or below, or the template surface moves to a registry the sender looks up by key. |
| `src/modules/mail/mail.service.ts` | 670 | Cohesive service | Mail | `MailService` (9 public methods: `listAccounts`, `countUnread`, `listMessages`, `getMessage`, `getThread`, `sendMail`, `replyMail`, `performAction`, `getAttachment`) | The seam between two mail providers (Gmail, Outlook) and the local mirror. Every method resolves the account, picks the provider, and reconciles what the provider returns against `mail_metadata` — `listMessages` (234 lines) is the largest because it merges a provider page with the mirror page and a cursor, and that reconciliation cannot be split from the read it reconciles. | A per-provider split was rejected: the providers are already separate classes (`GmailMailProvider`, `OutlookMailProvider`) and this file is the part that must NOT be per-provider, because its job is to make the two answer identically. The split that would work is lifting `listMessages`' merge into `mail-mirror-page.ts` beside `listFromMetadata`/`pageFromMetadata`, which already live there; deferred rather than argued away. | 2026-12-01 | Drops to 500 lines or below, or the `listMessages` reconciliation moves into `mail-mirror-page.ts`. |
| `src/modules/inventory/channels/sync/channel-job.drizzle-store.ts` | 516 | Cohesive service | Inventory | `CHANNEL_JOB_LEASE_MS`, `CHANNEL_JOB_BATCH_SIZE` and `DrizzleChannelJobStore implements ChannelJobStore` (11 methods: `claimDue`, `loadContext`, `readOffers`, `recordSnapshot`, `markPublications`, `enqueue`, `applyImport`, `complete`, `fail`, `requeue`, `listFailures`) | The single Drizzle implementation of the `ChannelJobStore` port. The file is one class implementing one interface, and the interface is what makes it cohesive: splitting the class puts part of an implements-clause in another file. `applyImport` (128 lines) is the bulk — it turns one channel order into a sales order inside one tenant transaction — and it is the method the lease and the failure bookkeeping exist for. | A read/write split was rejected because the port is one interface and a partial implementation cannot satisfy it. Lifting `applyImport`'s body into a `lib/channel-order-import.ts` that takes the transaction WOULD drop the file under the limit and is the split this row expects; it belongs to the inventory lane, not to this pass. | 2026-12-01 | Drops to 500 lines or below, or `applyImport` moves to `lib/channel-order-import.ts`. NOTE: registered 2026-09-12 during an active inventory refactor — re-measure or delete this row rather than trusting the count. |
| `src/modules/inventory/channels/sync/shopify-admin.adapter.ts` | 622 | Cohesive service | Inventory | `ShopifyAdminAdapter implements ChannelCommerceAdapter` (7 methods: `isConfigured`, `configurationProblem`, `fetchSnapshot`, `pushStock`, `fetchOrders`, `confirmShipment` and the private `credentialsFor`) | One vendor adapter behind one port. Roughly the first 115 lines are the Zod schemas for Shopify's GraphQL responses — the boundary validation backend/CLAUDE.md §4 requires on a third-party response — and the rest is one method per port operation, each a GraphQL document plus the mapping from Shopify's shape to this codebase's. | Splitting the adapter by operation was rejected: each fragment would re-import the same credential resolution and the same response schemas, and the port is a single interface one class must satisfy. Moving the ~115 lines of Zod response schemas to a sibling `shopify-admin.schemas.ts` WOULD clear the limit and is the split this row expects; it belongs to the inventory lane, not to this pass. | 2026-12-01 | Drops to 500 lines or below, or the GraphQL response schemas move to `shopify-admin.schemas.ts`. NOTE: registered 2026-09-12 during an active inventory refactor — re-measure or delete this row rather than trusting the count. |

---

## Audit trail

- **2026-08-31** — Initial enumeration (lane Q6). Frontend: zero files over 500 lines. Backend: 6 files over 500. `access.service.ts` (640 lines) split into `access-error-utils.ts`, `denied-modules.resolver.ts`, `access-policy.ts` and `access.types.ts`.
- **2026-08-31** — Final inventory (lane L34). Backend: 7 files over 500 — 5 registered, 2 new. CLI scope decision formalised. The PRD's "88 backend and 22 frontend files over 500 lines" is wrong by orders of magnitude.
- **2026-08-31** — Over-300 ratchet raised 392 → 394 with cause (`chat-message-timeline.service.ts` 239 → 322, `hr-import.service.ts` 300 → 304, `build-openapi-document.ts` 296 → 302), all correctness work rather than growth.
- **2026-09-02 (ticket 37)** — The registry was made to fail closed, and re-measuring every row against disk found **seven of twelve entries stale**. The old gate only checked that a path appeared in a table row with at least seven cells, so none of this was visible:
  - **Five files had fallen to 500 lines or below and kept an exception they no longer needed** — `src/modules/access/access.service.ts` (registered 510, measured 486), `src/modules/gdpr/gdpr-export-worker-implementation.ts` (966 → 405), `src/modules/hr/analytics-plus/hr-analytics-plus.service.ts` (502 → 494), `src/modules/hr/lifecycle/onboarding-views.service.ts` (521 → 496) and `src/modules/notifications/notification-routing.service.ts` (671 → 472). All five rows were removed; the gate now removes the exemption automatically by failing on the next stale row.
  - **Two files had grown past their recorded figure** — `membership-artifacts.ts` (registered 2496, measured 3216: the catalog grew by 720 lines under an exception nobody re-measured) and `chat-channel-members-implementation.ts` (506 → 522). Both re-measured and re-justified.
  - Four unregistered files over the limit were **split by cohesive responsibility rather than registered**: `gdpr-subject-erasure.service.ts` 653 → 244, `ai-gateway-runner.helper.ts` 527 → 211, `storage.service.ts` 509 → 389 and `cron-hr-retention.service.ts` 504 → 317. See `reports/37-file-cohesion.md` for the extraction seams and the before/after spec runs.
  - Every surviving row was rewritten to the nine-column record §7 asks for: exact path, measured lines, category, owner, public interface, a concrete cohesion argument, alternatives considered, review date and removal trigger.
- **2026-09-10** — Re-measured on `feat/inventory-world-class-implementation`: **36** backend files over 500 lines, against the 7 recorded on 2026-08-31. Three are registered above as cohesive catalogs (`enums.ts`, `inventory/admin.ts`, `common/auth.ts`) — each verified to contain **zero** control-flow statements before being called a catalog, rather than assumed from the filename.

  **The other 33 are NOT exempted and must not be.** They are services carrying real behaviour — `inv-projects.service.ts` (1016), `inv-stock-transfers.service.ts` (927), `channel-snapshot.service.ts` (892), `quick-commerce-inbound.service.ts` (867), `inv-product-crud.service.ts` (856) and so on down to 502. That is genuine sprawl from the inventory build-out, exactly the §7 concern, and the fix is splitting them, one at a time, with the suite green after each. Registering them here would convert a measured backlog into a permanent exemption, which is the failure mode this registry exists to prevent — the 2026-08-31 entries each argue why a split would produce *meaningless* fragments, and none of that argument applies to a 1000-line service.

- **2026-09-11** — **Backend is green: 3,721 files scanned, all within 500 lines, 10 exceptions registered.** The 33 files the entry above refused to exempt were split instead, as it said they must be. No exception was added for any of them; the registry is unchanged except for this line.

  The last eight, each verified with `tsc --noEmit`, eslint and the full suite green before commit: `grn-post.service.ts` 758→255, `channel-snapshot.service.ts` 893→142, `quick-commerce-inbound.service.ts` 868→336, `inv-product-crud.service.ts` 857→253, `customer-returns.service.ts` 789→425, `quality-recalls.service.ts` 541→195, `party-legacy-writer.ts` 558→295.

  Two constraints shaped where the cuts fell, and both are worth knowing before the next one:

  - **A deliberately-private ungated read may not become an export.** Five of these services keep a read that skips the warehouse-scope predicate (`detailUnscoped`, `loadAsnUnscoped`, `loadCustomerReturnUnscoped`, `loadRecallUnscoped`), named so a future route cannot be pointed at it by accident, and each is pinned by a spec. Moving a flow to `lib/` would normally mean exporting that read. Instead the flow receives a bound closure in its deps bag, the read stays private, and each spec now asserts that **no file under that `lib/` calls it**. The cost is that three services stayed between 300 and 500 rather than going under 300; that is the right trade.
  - **`legacy-reader-ratchet.spec.ts`'s list may only shrink.** `party-legacy-writer.ts` was split along the line that keeps every `leads`/`clients`/`contacts`/`crm_organizations` reference in the existing file, so the new one is not a reader and the list did not grow. The ratchet fails in both directions, so running it proves both halves.

  Two gates had gone quiet because of this work and were repaired rather than left: the INV-109 scope check was matching predicate-builder NAMES once builders moved to `lib/` (now reads a service together with its sibling `lib/`, and its allowlist shrank from ten names to seven — one of the dropped entries, `stockScope`, was a local *variable* name any file could have satisfied), and the T04 idempotency check walked only `*.service.ts` and had been missing five claiming files, two of them dropped by an earlier commit in this same branch. Both now carry a floor asserting they still reach `lib/`.

  **`check:over-300` is a separate programme and is NOT closed: 476 files over 300, 82 above the baseline of 394.** Splitting a 900-line service into files of 250 and 300 is net zero against that count, and sometimes +1. Do not read a green `check:file-sizes` as progress on it.

---

## The over-300 ratchet (backend) — 2026-09-03

`src/scripts/check-over-300.mjs` is a **separate** gate from the 500-line registry above and reads
**nothing** from this document. Its `## Exceptions` table cannot hold an over-300 file: the table
fails closed on any registered path measuring 500 lines or fewer ("exception no longer needed"), so
a 301-line file added there would red `check:file-sizes`. Everything in this section is therefore
prose on purpose, and the parser ignores it — `check-file-sizes.mjs`'s own self-test asserts that a
path mentioned only in prose grants nothing.

### What was actually wrong, measured rather than assumed

The gate was red at head: **406 files over 300 against a baseline of 394 (rc=1)**. It had been
characterised elsewhere as "the one unjustified historical raise". Measured against the history,
that characterisation is **wrong in both halves**, and the truth is worse in one way and better in
another:

- There is exactly **one** raise in the gate's history, `c3f0b73d` (392 → 394), and it **is**
  justified: the commit message names the three crossings with before/after line counts, and the
  audit trail above records them. It is not an unjustified raise.
- It **undershot**. Measured hermetically with `git archive c3f0b73d src` into a scratch tree and
  running that commit's own gate against it: **395 files over 300, baseline 394, rc=1**. The commit
  that "fixed" the gate shipped it red by one. The gate was last genuinely green at `f613bb3d`,
  where it was created at 392 against a measured 392 (rc=0).
- So the 12-file gap decomposes as **1 undershoot + 11 net new crossings**, not "12 unjustified
  baseline points". The churn underneath is much larger than the net: **42 files crossed 300 and 31
  fell back below it** between `c3f0b73d` and head. There is no identifiable set of "the twelve
  files" to fix — any twelve-plus reductions are equally valid.

### How it was made green — downward only

The baseline moved **394 → 392**, and the count moved **406 → 392** to meet it. Fourteen files were
split along a responsibility seam. Every extraction is a move of exported plain functions (or, for
the fence store, a second implementation class), so no Nest provider, module registration, DI
constructor or public method signature changed; `check:module-di` stays clean and no importer of
any of the fourteen services needed an edit.

- `src/common/idempotency/command-fence-store.ts` 304 → 212 — the in-memory store implementation left for `command-fence-store-memory.ts`; the file held two independent implementations of one interface.
- `src/modules/gdpr/gdpr.service.ts` 301 → 160 — six inline per-source queries sharing one cap/truncation protocol left for `gdpr-sync-export-fetchers.ts`, matching the pattern the module already uses for its async export (`gdpr-export-fetchers-*.ts`). This is the 301-line service the review flagged, and it was the right call: it was orchestration, authorization and table-shape knowledge in one file.
- `src/modules/activities/activities.service.ts` 302 → 249 — keyset timeline read to `activities-timeline.ts`, away from the write lifecycle.
- `src/modules/hr/lifecycle/hr-dashboard-reports.service.ts` 303 → 140 — attendance analytics to `hr-dashboard-attendance.ts`; headcount, time-to-fill and attendance were three unrelated reports in one service.
- `src/modules/hr/workflows/hr-workflow-engine.service.ts` 305 → 239 — the scheduled overdue sweep to `hr-workflow-overdue-sweep.ts`, away from the request-path start/act lifecycle.
- `src/modules/hr/time/work-logs.service.ts` 311 → 246 — CSV serialisation to `work-logs-export.ts`.
- `src/modules/email/email-outbox.service.ts` 307 → 214 — the retry/dead-letter drain to `email-outbox-retry.ts`, away from request-path enqueue.
- `src/modules/module-access/user-permission-grants.service.ts` 302 → 246 — membership resolution to `user-permission-grants.helpers.ts`, beside the existing `module-access.helpers.ts`.
- `src/modules/ai/core/services/chat-history.service.ts` 304 → 242 — conversation-scoped message paging to `chat-conversation-messages.ts`; the file carried two persistence grains.
- `src/modules/inventory/quality/quality-inspections.service.ts` 309 → 207 — stock disposition to `quality-disposition.ts`, away from inspection state.
- `src/modules/inventory/purchase-orders/po.service.ts` 305 → 233 — the PO state machine to `po-lifecycle.ts`, away from CRUD.
- `src/modules/billing/core/billing-payment-activation.ts` 309 → 209 — the three post-activation bookkeeping recorders to `billing-activation-recorders.ts`.
- `src/modules/hr/governance/labor/labor.service.ts` 308 → 248 — labor cases to `labor-cases.ts`; one service owned three entities, against §7's "name the entity service for the entity".
- `src/modules/timesheets/core/timer.service.ts` 317 → 258 — pause/resume/stop/discard to `timer-transitions.ts`, away from the read path.

Proof: `node src/scripts/check-over-300.mjs --self-test` rc=0 (15 passed);
`node src/scripts/check-over-300.mjs` rc=0, **392 of 3606**, baseline 392.
`pnpm check:spec-typecheck` rc=0 · `pnpm typecheck` rc=0 · `pnpm check:cycles` rc=0 (5,647 files,
no cycle) · `pnpm check:module-di` rc=0 · `pnpm check:kebab-case` rc=0 ·
`pnpm check:import-direction` rc=0 · `pnpm check:db-call-count` rc=0.

### Files over 300 that were deliberately NOT split, and why

§7 allows a cohesive catalogue to stay whole rather than fragment. These four are the strongest
cases in the current over-300 set, and each has **already been split by domain** — splitting further
would produce numbered fragments behind a re-export shell, which §7 calls out as worse than one
honest file. They stay counted in the 392; recording them here is a decision, not an exemption, and
the gate has no exemption mechanism to abuse.

- `src/modules/notifications/notification-events.catalog.ts` (301) — one flat `NotificationEventDefinition[]` plus the derived `NOTIFICATION_EVENT_MAP`, `EVENT_KEY_SET` and `isNotificationEventKey`, which are meaningless apart from it. Seven per-domain catalogues (chat, build, accounting, hr, ownership, knowledge, security-support) have **already** been extracted into siblings; what remains is CRM plus the derivation tail. The next split would be by letter range.
- `src/common/cache/cache-invalidation-matrix.ts` (301) — the same shape: four per-domain entry arrays (finance, inventory, rbac-auth, crm) are already siblings, and the file is the remaining namespace rows plus the concatenation. Consumers iterate the whole array.
- `src/modules/rbac/role-templates-crm-hr.constants.ts` (312) — a permission catalogue, which §7 names explicitly as allowed to exceed even 500 rather than split artificially.
- `src/modules/crm/import/import-entities.ts` (302) — an entity/field mapping table behind eight one-line accessors, already re-exporting `./import-fields`; there is no logic to separate, only data.

- **2026-09-04 (v2 ticket 28)** — Both backend size gates were RED at head (`check:file-sizes` rc=1 on one stale row plus four unregistered files over 500; `check:over-300` rc=1 at 406 against a baseline of 392). Both are green without touching BASELINE and without adding a single registry row.
  - **Four unregistered files over 500 were SPLIT, not registered** — `mail.service.ts` 595 → 459 (`mail-mirror-page.ts` 116, `mail-union-cursor.ts` 71), `calendar.service.ts` 590 → 434 (`calendar-event-wire.ts` 79, `calendar-attendee-sync.ts` 102), `notification-routing.service.ts` 534 → 450 (`notification-preference-resolution.ts` 100), `unified-inbox.service.ts` 527 → 431 (`unified-inbox-sources.ts` 112).
  - **One registered row RETIRED by honouring its own removal trigger** — `chat-channel-members-implementation.ts` recorded "the authorization predicate is extracted for reuse elsewhere" as its retirement condition; the predicate moved to `chat-channel-authorization.ts` (62 lines) and the file fell 524 → 484, so the row is gone rather than re-measured.
  - **Three rows carried a false `Public interface` record.** `CrmScoringService` recorded five public methods over a class with six; `party-mirror-fields.ts` recorded "a const array … no logic to separate" over a `Record`, a second catalog (`LEGACY_OWNED_COLUMNS`) and a `withKey` helper; `check-referential-action-drift.ts` recorded "main() plus the --self-test harness" over twenty exports. All three corrected against disk.
  - **The gate now machine-checks two more staleness axes.** `check-file-sizes.mjs` gained `statedSurface`/`measureSurface`: a `Public interface` cell that STATES a count ("N public methods", "N exports") is compared against the file's measured surface, and a `Review date` in the past now fails. Both are bite-proven — with the old `5 public methods` record restored, the previous gate exits 0 and the new one exits 1 naming "registered 5 public methods, actual 6"; with a 1999 review date, likewise. Self-test 49 → 63 assertions.
  - **Fourteen files were split back under 300** to clear the ratchet, each along a responsibility seam and each with its call sites repointed rather than shelled: `inv-warehouses` (locations + location-grain stock out), `web-push` (subscription store out), `depreciation-runs` (posting arithmetic out), `role-templates-crm-hr` (split per module domain, matching `role-templates-build.constants.ts`), `clients` (activity timeline out), `sales` (playbook out), `mail-to-inbound-event` (ingress privacy policy out), `hr-checklist-reconciliation` (signal probes out), `organization-settings` (holidays and custom domains out as their own entities), `crm.prompts` (split by the two consumers: judge-a-record vs write-text), `journal-posting` (the four document recipes out as pure drafts), `finance/banking/imports` (statement row parser out), `accounting/gl/periods` (close-readiness probe out), `ticket-triage-ai` (compose-time draft assistance out).
  - Every extraction was verified: backend `tsc --noEmit` rc=0, and the 156 spec suites covering the touched modules pass (1316 tests, rc=0). One spec reached into a now-extracted private (`calendar-exception-reminder-cancellation.spec.ts` called `svc.updateAttendeesInTx`) and was repointed at the exported function with its assertions unchanged.
  - **Frontend, same pass**: `check:file-sizes` was rc=1 on `contrast-tokens.test.ts` (604) and `hooks/api/kb/pages.ts` (502); `check:over-300` rc=1 at 523 against 516. The contrast suite split three ways along the seam its own mid-file import already marked (default-palette base tokens / default-palette status ramp / all seventeen selectable themes, shared math in `test-utils/globals-css-tokens.ts`); `kb/pages.ts` shed its wire contract to `page-types.ts` with all 18 type importers repointed. Six more files were split under 300. Both gates rc=0, `tsc --noEmit` rc=0, 487 tests over the touched areas pass.

### Left open (v2 ticket 28)

- `check:file-sizes` still scans only `src/**/*.ts` on the backend and excludes `scripts/` on the frontend, so `.mjs`/`.js`/`*.spec.ts` and the gate scripts themselves are exempt by EXTENSION and DIRECTORY rather than by a path-classified generated/vendor rule. That is the PRD-C038/C040 gap and it is untouched here: widening the corpus makes ~105 backend and ~13 frontend authored files newly visible, each of which then has to be split or given a nine-column row. `check-file-sizes.mjs` is itself 728 lines and exempt from its own limit purely because it is `.mjs`.

### Left open

- The **frontend** twin was red at 520/519 when this note was first written. **Re-measured 2026-09-08: green at 513 of 5,911, baseline 513, rc=0** — four files were split (`webrtc-huddle`, `message-input`, `ai-actions-menu`, `filter-command-menu`) and the baseline was NOT lowered.
- ~~`pnpm check:file-sizes` is red at head on two files neither split nor owned here — `src/modules/support/core/support-tickets.service.ts` (520) and `src/scripts/check-declaration-column-drift.ts` (534).~~ **RESOLVED — re-measured 2026-09-07: 347 and 191 lines.** Both were brought under 500 by a later split; neither needed an exception row. The table above stays empty, which is the intended end state — an exception is a debt record, and none was ever granted.

- **2026-09-09** — Re-measured `membership-artifacts.ts`: registered 3222, actual 3212 (catalog shrank by 10 lines since the 2026-09-02 measurement). Row updated; cohesion argument and removal trigger unchanged — file remains one `MEMBERSHIP_ARTIFACTS` const array plus four derived exports, splitting was rejected at the previous review and nothing has changed to revisit that decision. Next review 2027-03-01.

- **2026-09-08 (over-300 ratchet, backend)** — `check:over-300` was rc=1 at **414 against a baseline of 392**. It is green at **390 of 4,086** and the baseline is **LOWERED 392 → 390** (`check-over-300.mjs` and its `ratchets.json` registration together, so `check:baseline-integrity` stays rc=0 at 0 unregistered / 0 stale). No exception row was added, no exclusion, no rename: **26 files were split along a responsibility seam** and every call site repointed.
  - **Cron controllers split by product domain**, each new controller registered in `cron.module.ts`: `cron-build.controller` shed the three finance sweeps to `cron-finance.controller`; `cron-support.controller` shed the four KB retention sweeps to `cron-kb.controller`; `cron-notifications.controller` shed the three retention sweeps to `cron-notification-retention.controller`. Routes are unchanged — `openapi:generate` still reports 3,666 operations with **0 undeclared** exposure.
  - **DTO contract files split by the controller that owns them**: `rbac-response.schemas` → catalog/discovery + `roles-response.schemas` + `principal-groups-response.schemas`; `users-response.schemas` → administration + `user-profile-response.schemas`; `kb-wiki-response.schemas` → pages + `kb-space-response.schemas`; `e-sign.schemas` → sender-side + `e-sign-public.schemas`; `ticket.schemas` → the ticket + `ticket-subresources.schemas`.
  - **Services split on the read/write axis** where the repo already names that seam: `approvals.service` → `approvals-read.service` + shared `approval-lookup.ts`; `proration-ledger.service` → `proration-ledger-reports.service`; `kb-page-reviews.service` → `kb-page-reviews-query.service`; `quotes.service` → `quotes-query.service`; `timesheets-ai.service` → `timesheets-billing-ai.service` + shared `timesheets-ai-invoke.ts`.
  - **Path-keyed coverage was re-pointed in the same change, not left dangling.** `s05-retention-scheduling-contract.spec.ts` reads cron controllers BY FILENAME and asserts on their text; its anchors for `kb-chat-history-purge`, `kb-chunk-retention-sweep`, `notifications-retention-sweep` and `notification-outbox-retention-sweep` were moved to the new files and the module-registration list extended. `check-namespace-coverage.mjs` and `check-cache-invalidation.mjs` both hold literal cache-matrix filenames and were extended with the new sibling. The BOLA membership-gate cases in `approvals.service.spec.ts` and the streaming case in `timesheets-ai-stream.spec.ts` were re-pointed at the extracted class with their assertions unchanged.
  - **One split introduced a cycle and was corrected rather than shipped.** `service-delivery-inbox.service.ts` ↔ `service-delivery-queries.ts` closed a loop through the shared `ServiceDeliveryItem` type; per §9's first remedy the type moved to a neutral `lib/service-delivery-item.ts`. `madge --circular` is back to **zero**.
  - Verification: backend `tsc --noEmit -p tsconfig.json` rc=0 · `madge --circular` zero · `check:file-sizes` rc=0 (4,086 scanned, 8 exceptions) · `check:over-300` rc=0 and `--self-test` 15 passed · `check:baseline-integrity` rc=0 · `check:route-classification` ALL ROUTES CLASSIFIED · `check:module-registration` 0 unreachable · `check:kebab-case` 0 violations · `openapi:generate` boots clean (the DI proof) · the 27 spec suites over the touched areas pass (360 tests).
  - **Two of the 414 crossed 300 from work outside this change** and are counted in the 390: `hr/core/person-employment-sync.service.ts` and `hr/time/attendance-read.service.ts`. `check:type-assertions` is also rc=1 on one stale ledger row for `hr/directory/employee-bulk-onboarding.service.ts`, which no file in this change touches.
