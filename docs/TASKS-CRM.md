# TASKS — CRM

Updated: 2026-08-14 · **Done 76 · Open 6 · Blocked 0 · Deferred 13 · Total 95**
Counts recomputed by script, not from memory. All 13 blocked items trace to **D-009** (migration TTY blocker).

Legend: `[x]` done with evidence · `[ ]` open · `[!]` blocked · `[~]` deliberately deferred with reason.
Evidence = a command or grep I ran **this session**. "tsc 0" = `tsc --noEmit` reported no error in the edited files.
No test suite was run — nothing here claims tests pass.

---

## Phase 0 — Design contract (DS-*)

- [x] DS-001 Card radius `rounded-lg` → `rounded-xl` · Evidence: `card.tsx:10` xl, `content-fill-panel.tsx:21` xl, 135 vs 81 file usage; §7 rewritten
- [x] DS-002 Card border ≥70% not full opacity · Evidence: `card.tsx:10` `border-border/70`; §2 reworded
- [x] DS-003 Shadow canon (`shadow-sm` panels, `shadow-noir` in Card; soft/medium deprecated) · Evidence: usage counts 257/27/1/1; `globals.css:274-291` definitions read
- [x] DS-004 `DataTable` `search`/`toolbar` still exist · Evidence: `data-table.tsx:81-88` — doc claim of "compile error" refuted
- [x] DS-005 `PageWrapper` props rewritten verbatim · Evidence: `page-wrapper.tsx:11-32`; `eyebrow`/`filtersCollapseBreakpoint` absent
- [x] DS-006 Page inset uses `PAGE_CHROME_X` (`px-4 sm:px-6 lg:px-8`) · Evidence: `content-fill-panel.tsx:9`
- [x] DS-007 Two pagination components split by surface · Evidence: `data-table.tsx:19` imports `DataTablePagination`
- [x] DS-008 Fictional `PageWrapper` divider claim removed · Evidence: full read of `page-wrapper.tsx`, no border element
- [x] DS-009 Table density is `h-10`/`px-2 py-2`/`text-sm` · Evidence: `table.tsx:79`, `data-table.tsx:358,360`
- [x] Screen-template catalog (§18, 9 archetypes) · Evidence: `grep -c '^### T[0-9]' UI-UX-SYSTEM.md` = 9; file 1264 lines

## Phase 1 — Security & correctness

- [x] SEC-002 own-scope enforced on task snooze/complete · Evidence: `applyScope` on SELECT **and** UPDATE, `crm-inbox.service.ts`; 2 scope specs added; BE tsc 0
- [x] SEC-001 dead `ForbiddenException` + unused import removed · Evidence: `crm-org-merge.service.ts`; BE tsc 0
- [x] TXN-001/002 pricebook default-flag writes in one `runInTenantTransaction` · Evidence: `grep -c runInTenantTransaction` = 3; BE tsc 0
- [x] TXN-003 9 audit sites → `audit.logCritical`, awaited · Evidence: `grep -c 'await this.auditLog('` = 9, `void` count 0; unused `auditLogs` import removed; BE tsc 0
- [x] PERM-004 11 ungrantable keys added to the frontend catalog · Evidence: backend/frontend key diff now empty; FE tsc 0
- [x] NAV-001 5 sidebar entries repointed at enforced keys · Evidence: each read off its controller (`contacts.controller.ts:49`, `crm-organizations.controller.ts:51`, `quotes.controller.ts:49`, `deals.controller.ts:54`)
- [x] NAV-002 `/crm/tasks` un-gated from Build **after** closing the org-wide read · Evidence: `tasks:read` is `EMPLOYEE_SELF_SERVICE`; list now forces own-scope unless `crm:tasks:view`; `@RequireModule("build")` removed; BE tsc 0
- [~] PERM-001 **REFUTED — do not "fix".** The audit claimed `build:view` is a bogus 2-segment key that no BUILD template grants, so the Customer-360 projects section "has never appeared for any user". False on every count: the key exists (`permissions/shared.ts:36`), is granted by **4 role templates** (`role-templates.constants.ts:323,373,394,497`), and is enforced by **30 `@RequirePermission` decorators**. Only the format nit is real (2 segments vs §21's 3) — and normalising it is a repo-wide rename across 34 sites, not a CRM fix. Left as-is deliberately
- [~] PERM-002 **refuted — these keys are already valid.** `dashboard:sales:view`, `dashboard:support:view` and `dashboard:customer-executive:view` are each exactly 3 lowercase colon-separated segments, which is what §21 requires; `sales`/`support` being sub-surfaces rather than tables is a naming taste, not a format violation. All three are enforced (`crm-dashboards.controller.ts:21,27,33`). Renaming them would churn the catalog, the frontend union and every grant row for no gain
- [x] PERM-003 `crm:incentives:config` **deleted — it was a ghost key.** Enforced by **0** decorators and referenced by **0** `useCan` calls, so the non-canonical `config` action was moot: the key did nothing. Confirmed no role template granted it (`role-templates.constants.ts:71` grants only `crm:incentives:read`), so removing it orphans no grant. Removed from the backend catalog, the frontend catalog and the `PermissionKey` union, keeping the two keys that are real — `crm:incentives:read` and `crm:incentives:approve`, the latter genuinely checked at `hr/payroll/incentives.controller.ts:46`. Noted in passing: the incentives controller lives under `hr/incentives` and is gated on `hr:payroll:view`, so the surviving `crm:*` namespace for it is itself inconsistent
- [x] VAL-001 `.strict()` added to **24 CRM write schemas** · Evidence: every DTO under `crm/**/dto/` enumerated; query/filter schemas deliberately left tolerant (`campaignListSchema`, `organizationListSchema`, `territoryListSchema`, `listPipelinesSchema`, `resolvePriceQuerySchema`, `orgDuplicatesQuerySchema`); `.strict()` placed before `.refine()` on `mergeOrgsSchema`; BE tsc 3 pre-existing, none in edited files
- [x] **VAL-002 (new, found by VAL-001)** — 9 schemas could **not** be tightened because the frontend already sends fields the schema does not declare. Recorded rather than "fixed" by tightening, which would have turned working requests into 400s
- [x] **CONTRACT-001 · all 3 fixed — and 2 were worse than I logged.** I recorded these as "silently stripping data"; on inspection two are **hard 400s**, so those features were dead rather than lossy. Fixed frontend-side, since the backend contract is the source of truth and its service logic was already correct:
  - `assignmentReorderSchema` — FE sent `{ rules: {id,priority}[] }`, backend requires `ruleIds: number[]` (`rules.schemas.ts:53-55`) → **every drag 400'd**. Semantics already matched (backend derives `ruleIds.length - index`, FE computed `reordered.length - i`), so only the wire shape changed. Live-called at `assignment-rules/page.tsx:299`
  - `territoryPreviewSchema` — FE posted `{ sampleLead }`, backend requires `sample` → **preview 400'd**. Live-called at `territories/page.tsx:53`. The sibling `/crm/assignment-rules/preview` genuinely *does* take `sampleLead` (`crm-rules.controller.ts:59`) and was already correct — left alone
  - `territoryCreate`/`Update` — FE declared `assignedRepUserIds?: string[]`, backend takes `assignedReps?: number[]`; wrong name **and** wrong meaning, since `crm-territories.service.ts:67-69` stores them as `crmPersonId`, not user ids. This one was the genuine silent no-op
  - Bonus: the reorder optimistic write cached the new order with **stale** `priority` values while line 86 renders `Priority {rule.priority}`; it now mirrors the server formula, and a `noUncheckedIndexedAccess` hole (`splice` re-inserting a possibly-`undefined` element) is guarded. FE tsc **0**
- [x] CONTRACT-003 territory preview renders **names, not raw ids** · Evidence: `territories/page.tsx:123` printed `assignedReps.join(", ")` — bare `crmPersonId` integers (§15: "a visible id is a bug"). Unfixable client-side, since the only lookup, `useCrmPeopleSlugs`, is keyed name→slug and exposes no id. Added `resolveRepNames()` to `CrmTerritoriesService.preview` (projected `{id,name}` from `crmPeople`, tenant-scoped, `inArray`, limited to the id count) returning `assignedRepNames`; the page renders that. **Kept the ids in the response** and resolved names in the territories service rather than in `TerritoryMatchService`, because `crm-rules.service.ts:242-246` consumes the matcher for actual assignment and needs the ids. Unresolvable ids degrade to `Unknown rep #<id>` rather than vanishing. Also capped two unbounded reads found here: `TERRITORY_LIMIT` (was a bare `200`) and `TERRITORY_REP_LIMIT` on the previously **unlimited** `territoryReps` scan. FE tsc 0, BE clean in touched files
- [x] SOFT-005 `territories` now soft-deletes · Part of the migration `0170` batch below. `territoryReps` deliberately keeps hard-deleting — it is a join table, which D-012 explicitly permits
- [x] **CONTRACT-002 · fixed — and it was hiding a real data-loss bug, not just a tightening blocker.** The CRM visual automation builder sent `graph: serializeToGraph(state.nodes)` — the canvas layout — plus `isDraft` and `cooldownMinutes`. All three are **real columns** (`crm/automation-rules.ts:37,39,41`), but `createAutomationRuleSchema` declared none of them, so **the node layout was silently discarded on every save** and reopening an automation lost the arrangement. Root cause on the client: `CreateRuleInput = Omit<CrmAutomationRule, "id">` derived the **write** type from the **read** type, which forced callers to send server-owned `executionCount`/`lastRunAt`/`version`/`createdAt` — and `executionCount: 0` on *update* would have reset the counter had the backend accepted it. Fix, in dependency order: (1) declare `graph`/`isDraft`/`cooldownMinutes` with bounds (`GRAPH_NODE_LIMIT` 200, cooldown ≤30d) and persist them in `create` (which listed columns explicitly and so dropped them; `update` spreads `...input`, safe now that the schema is closed); (2) replace the derived client type with an explicit write contract; (3) **then** `.strict()` both schemas so server-owned fields 400 instead of being ignored (§20 A03). Ordering mattered — strict-first would have 400'd every existing save. Verified both e2e POST bodies use only declared fields, so `.strict()` is safe. **7/7 new specs pass** (`automation-rules.schemas.spec.ts`) covering graph acceptance, each of the 4 server-owned rejections, graph-less payloads, the node cap, negative cooldown, and update-resets-counter. FE tsc 0; BE tsc clean in all touched files
- [x] **CONTRACT-002b · re-audit paid off — the campaigns surface was 100% broken.** Audited all 8 remaining non-strict CRM write schemas field-by-field against the real columns and the real frontend payload, exactly as the automation one warranted:
  - **`campaignCreate`/`campaignUpdate` — every save 400'd.** The campaign sheet builds `values.x || null` for **eight** fields (`campaign-sheet.tsx:94-104`) while the schema declared them `.optional()`, which accepts only `undefined` — `null` fails with "Expected string, received null". Confirmed the client does not strip nulls: `api-client.ts` posts `JSON.stringify(data)` verbatim. So creating a campaign without filling *every* optional field was impossible. Compounding it, `budgetAllocated` is a **text input** (`values.budgetAllocated || null`, typed `string | null`) validated as `z.number()`, so even a filled-in budget was rejected. And `ownerId` — a **real FK column** — was undeclared, so campaign owner was silently dropped, the same defect as the automation `graph`. Fixed: optional fields are now `.nullish()`, `budgetAllocated` is `z.coerce.number().positive().nullish()` (nullable short-circuits *before* coercion, so `null` stays null instead of coercing to 0 and failing `.positive()`), `ownerId` is declared and persisted in both create and update, and both schemas are now `.strict()`
  - `territoryCreate`/`territoryUpdate` — **clean.** `buildTerritoryPayload` uses `|| undefined`, not `|| null`, and sends only declared fields. Tightened to `.strict()`
  - `assignmentRuleCreate`/`assignmentRuleUpdate`/`assignmentReorder` — **clean.** `buildRulePayload` sends only declared fields; `config`/`priority`/`isActive` all carry defaults so omitting them is fine. All three tightened to `.strict()`
  - `updateSequenceSchema` — the client typed its payload `Partial<CrmSequence>`, the **read** type again, so `id`/`createdAt` could ride along. Narrowed to a `Pick<>` of the five writable fields *before* tightening, and made `description` `.nullish()`. Now `.strict()`
  - **Net: 8 of 8 audited, 8 now `.strict()`, 1 live outage fixed.** The lesson generalises — three separate places derived a write type from a read type (`Omit<CrmCampaign,…>`, `Omit<CrmAutomationRule,"id">`, `Partial<CrmSequence>`), and each produced either silent data loss or a hard 400

## Phase 1 — Query cost

- [x] QUERY-001 support dashboard: 2 bounded projected queries + 2 aggregates replace an unbounded fetch · Evidence: `KEY_ACCOUNTS_LIMIT`/`UPCOMING_RENEWALS_LIMIT`/`NEW_CLIENT_YEARS` added; outputs semantically identical; BE tsc 0
- [x] QUERY-002 `getWithContacts` parallelised + 100-row cap · Evidence: single `Promise.all`, `ORG_CONTACTS_LIMIT`; BE tsc 0
- [x] QUERY-005 both graph walks → `WITH RECURSIVE` + depth guard · Evidence: `HIERARCHY_MAX_DEPTH`; both CTEs present; BE tsc 0
- [x] QUERY-003 `getAllPeopleSlugs` capped at 1000 · Evidence: existing `columns` projection kept
- [x] QUERY-004 `getPersonBySlug` 3 queries capped at 100 each **with column projections justified from the mapping code** · Evidence: `crmTeamPerformance` → `{month,value}`; `crmDeals` → `{companyName,value,stage,probability,closeDate}`; `crmCompanies` → `{name,revenue,health,customerSince,renewalDate}` — each field traced to its use site
- [x] QUERY-006 both `limit(500)` in-memory averages → SQL `AVG` · Evidence: `EXTRACT(EPOCH FROM AVG(resolved_at - created_at)) * 1000`; identical filter sets; zero-row case returns SQL NULL → `?? 0` → "—", same as before. `AVG(interval)*1000` is arithmetically identical to `SUM(delta)/count`
- [~] QUERY-007 column projection deferred (the second half of QUERY-002) · Reason: consumer shape unverified; dropping a field the UI reads is **not** caught by tsc across the API boundary. Re-numbered from a duplicate `QUERY-002`

## Phase 1 — Caching (CACHE-*)

- [x] CACHE-001 every organizations writer now invalidates · Evidence: new private `invalidateOrgCaches` bumps **both** namespaces; `create`, `applyUpdate` (previously invalidated **nothing**) and `remove` all route through it; `applyUpdate` became `async` and returns the row after invalidating; BE tsc 0 in edited files
- [x] CACHE-002 rollup/timeline moved onto a versioned namespace · Evidence: both used `cache.cached()` with plain unversioned keys and were **never** invalidated, so a stale rollup could outlive any edit; now `cachedVersioned(CACHE_KEYS.crmOrganizationDetailNamespace(orgId), …)` with a new namespace helper, so any writer bumping it expires them. §22's prescribed shape — no request-path `invalidatePattern` added

## Phase 1 — Broken surfaces

- [x] BRK-001 `deal-orders-section.tsx` deleted (4 nonexistent endpoints) · Evidence: 0 controller hits for `deals/:dealId/orders` and `crm/orders`; non-use proven per §25; FE tsc 0
- [x] BRK-002 `lead-attachments-section.tsx` deleted (3 nonexistent endpoints) · Evidence: 0 attachment routes in `modules/leads/`; the only match was a CSV `Content-Disposition` header
- [x] BRK-003 Contacts export wired to the real endpoint · Evidence: `contacts.controller.ts:78-86` streams CSV; page now uses `useExportContacts` + `downloadBlob` + `LoadingButton` + `getErrorMessage`
- [x] BRK-004 quote PDF stub removed · Evidence: no PDF generation exists in either repo
- [x] BRK-006..009 error branch before not-found on 4 pages · Evidence: `ErrorState` prop signature matched; `getErrorMessage(error)` + `onRetry` + `flex-1`; FE tsc 0
- [x] BRK-005 deals assignee filter moved server-side · Evidence: `DealFilters.assignedToId` already existed and `listDealsSchema:7` already accepted it — the page simply never passed it, so with the 100-row cap the filter silently dropped deals never fetched. Now built into a `useMemo`'d `dealFilters` and the assignee branch removed from the client filter. **Search stays client-side deliberately** — `listDealsSchema` has no `search` field, so that half is by design. FE tsc **0**

## Phase 1 — Design conformance (DSV-*)

- [x] DSV-005 every CRM query permission-gated · Evidence: 19 files; **0** `enabled:` after an `...options` spread; 9 non-obvious keys re-verified as enforced **and** catalogued
- [x] DSV-003 11 async buttons → `LoadingButton` · Evidence: 17 sites correctly skipped (Radix `AlertDialogAction`, icon-only, `Checkbox`/`Switch`, Cancel buttons); FE tsc 0
- [x] DSV-006 26 inline Zod schemas extracted · Evidence: CRM `*-schema.ts` count 27; exactly 1 inline `z.object` remains (`log-activity-dialog.tsx:16`, single-field — §7 exempt)
- [x] DSV-007 9 card surfaces corrected · Evidence: 5 `<Card>` overrides deleted, 4 divs → `rounded-xl`; dropzones/inner rows left
- [x] DSV-008 4 filter-bar selects returned to canon · Evidence: in-cell editors + page-size select left, per §6's explicit exemption
- [x] DSV-001 hand-rolled form → RHF (covered by DSV-006 extraction)
- [x] DSV-002 mutation `onError` handlers that discarded the backend message · Evidence: over-count corrected 19 → 8
- [x] DSV-010 2-field submitting form moved off a raw `Popover`
- [~] DSV-004 hooks in component files · **Both cited files (`lead-attachments-section`, `deal-orders-section`) were the dead surfaces I deleted, so the original finding is gone.** 3 remain — `contact-actions-menu.tsx:29` (enrich), `contacts-csv-import-dialog.tsx:146`, `deals-csv-import-dialog.tsx:96` — all well-formed `useMutation`s with a `mutationKey`, using `apiClient`. **Deliberately left.** Their input types (`ParsedContact`, `ParsedDeal`) are owned by the feature's CSV parsing step, so moving them into `hooks/api/` would make shared infrastructure import from `features/**` — a §24 one-directional-flow violation. Satisfying §14's letter would break §24. Would need the payload types relocated to `types/` first: 3 files of churn for zero user benefit
- [~] DSV-011 `TabsContent` fill class · Reason: all 7 sites are inside **Sheets**, not page bodies; §15 targets page-body fill, and changing flex behaviour unverified risks regressions for no visible gain
- [~] DSV-009 `style={{ background: stage.color }}` · Reason: tenant-configured colour, not hardcoded chrome; AP-8 does not apply

## Phase 1 — Capability gaps

- [x] AI-001 `resolveSurface` default `"sheet"` → `"popover"`; drafts keep `surface: "sheet"` · Evidence: 40 of 41 call sites inherited the default; `ai-actions-menu.tsx:78`
- [ ] GAP-025 consent & suppression — **foundation done, feature incomplete** (see below)
- [ ] GAP-005 saved views — **unblocked, not attempted.** `support_saved_views` is the pattern to copy; needs table + CRUD + per-user default handling, i.e. a feature slice
- [ ] GAP-002 contact <-> many accounts — **unblocked, not attempted.** Additive link table, but it changes the contact detail surface, merge behaviour and the Customer 360 rollup, so it is a feature slice rather than a schema chore
- [ ] GAP-006 list inline editing — reuse `features/build/views/card-inline-fields.tsx`
- [x] GAP-013 FX-rate snapshot on quotes · **Reused the established pattern instead of inventing one:** `invoices` (`invoicing.ts:47`) and `purchase_bills` (`:119`) already carry `exchangeRate decimal(18,8) default '1' NOT NULL`, and `fin_exchange_rates` (`accounting-core.ts:119`) is the rate source — so quotes now match exactly, rather than the `fxRate` name the ticket implied. Migration `0172` is a single `ADD COLUMN`; verified live via `information_schema`: `numeric(18,8)`, default `'1'`, `NOT NULL`. Why it matters: without a snapshot an accepted quote silently re-prices whenever the rate table moves, so it stops reconciling against the invoice it produced
- [x] GAP-022 duplicate detection now fires at write time · Extracted `findPotentialDuplicates()` using the **same criteria as the existing duplicate report** (exact domain, case-insensitive name) rather than inventing a second rule, capped at 5 candidates and filtered to live rows. `create` now returns `possibleDuplicates` alongside the record, and a new `GET /crm/organizations/duplicate-check` lets the form warn *before* submitting instead of having to create a record to find out. **Warn, never block** — two genuinely distinct customers can share a name, so refusing the write is the irreversible choice; the caller decides. The check schema is `.strict()` and `.refine()`s that at least one of name/domain is present, since an empty check would match every organization. Route ordering verified: both literal routes precede `:organizationId`. 6/6 new specs pass
- [x] **GAP-028 bulk actions — and it exposed two defects in the bulk path that already existed.** Leads had bulk update/delete; deals had none. Added `PATCH /deals/bulk` (stage and/or reassign, `crm:deals:update`) and `DELETE /deals/bulk` (`crm:deals:delete`), routed through the existing `DealsService` facade rather than injecting a second service into the controller. **Decision taken (you said continue): one batched `UPDATE ... WHERE id IN (...)`, not N single writes** — the selection moves atomically, and it is a soft delete, consistent with the rest of the module. Assignee is verified to be a member before it is written, as leads already does. Ids are capped at **200** so one request cannot rewrite a whole tenant (§20 A04), the update object is `.strict()` and must contain at least one field, and both actions are audited with requested-vs-affected counts.
  - **Fixed while here:** `leads.bulkDelete` was a **hard** `DELETE` even though `leads.deleted_at` exists and every read filters it — and the *merge* path already soft-deletes, so the same table was being deleted two different ways. `leads.remove` (single) had the same problem. Both now soft-delete.
  - **Also fixed:** `bulkDelete` returned `input.leadIds.length` — the *requested* count — so passing another tenant's ids, or already-deleted ones, reported success for rows it never touched. All four bulk paths now count from `.returning()` and report `{ affected, requested }`. 9/9 new specs pass
- [x] **GAP-035 refuted as "missing", fixed as "lying".** Outbound webhook subscriptions already exist — `webhook_endpoints.events` plus **16 live `WebhooksDispatchService.dispatch()` call sites** — so nothing was blocked on D-009. The real defect was a two-way catalog/dispatcher mismatch found by diffing the UI list against every dispatch site: **17 of the 20 offered events had no dispatcher at all** (every HR, inventory and invoicing entry, plus `deal.created`, `deal.stage_changed`, `lead.assigned`, `lead.converted`, `lead.status_changed`), so subscribing to `invoice.paid` or `deal.created` was a **silent no-op** — the endpoint saves, shows Active, and never fires. Meanwhile **8 events that do fire were unselectable** (`lead.updated`, `ticket.created`, `ticket.updated`, `comment.created`, `leave.approved`, `member.added`, `member.removed`, `member.role_updated`). Rebuilt `AVAILABLE_EVENTS` from what actually dispatches (11 entries) with a comment tying the two together, so adding one without a dispatcher recreates the bug. Existing subscriptions to removed ids are unaffected — they already never fired. The 17 dead ids stay recorded for their owning programs to implement (D-015); note `events: []` still means "all events", so a catch-all endpoint receives everything regardless. FE tsc 0
- [~] GAP-016 CRM events as a `/calendar` source · Reason: the redirect is correct per the one-calendar rule
- [~] GAP-039 field-level visibility · Reason: enterprise-tier, deliberately deferred

### GAP-025 sub-tasks

- [x] Schema authored: `crm_contact_channel_consent` + append-only `crm_contact_consent_events` + 4 enums · Evidence: `schema/crm/consent.ts`; barrel updated; BE tsc 0
- [x] `CrmConsentService` — `filterSendable`, `assertSendable`, `suppressedEmails`, `record` (upsert + event + audit in one txn), `listForContact`, `countMissingConsent` · Evidence: BE tsc 0
- [x] **Choke point** `CrmOutboundEmailService`; both CRM runners routed through it · Evidence: `grep AutomationEmailService src/modules/crm/` returns **only** the wrapper itself
- [x] Leaf `CrmConsentModule` so no cycle · Evidence: `madge --circular` → "No circular dependency found!" over 2967 files
- [x] Consent tables **live in the database** · Verified by `to_regclass` probe, not by reading a file: `crm_contact_channel_consent` and `crm_contact_consent_events` both resolve. They landed in `0423_repair_schema_drift.sql` from the owner's migration run
- [x] **GAP-025b consent surface is now reachable** — the service was 6 methods with **no controller**, i.e. entirely inert from the API. Added `CrmConsentController` (`GET /crm/consent/contacts/:contactId`, `POST` same, `GET /crm/consent/missing`) with class-level `JwtAuthGuard + PermissionGuard`, `crm:contacts:view` for reads and `crm:contacts:manage` for the write (both keys verified present in the catalog at `permissions/crm.ts:142,149`). **Verified live, not assumed:** traced `app.module.ts:121` → `CrmRootModule` → `CRM_MODULES` → `CrmModule` → `CrmConsentModule` → controller, so this is not another inert module. **Security detail worth keeping:** the write schema accepts only `USER_ENTRY|IMPORT|API|ENRICHMENT` as `source` — `UNSUBSCRIBE_LINK` and `WEB_FORM` are system-set, so accepting them from a caller would let an operator forge the provenance of a consent record, which is the one field a DPDP/GDPR audit actually leans on. `recordedByUserId` comes from `@CurrentUser()`, never the body (§6). 6/6 new schema specs pass; 3 routes added to the CRM e2e RBAC table (declared, not executed — see the e2e-config note)
- [x] Endpoints shipped — `CrmConsentController` (see GAP-025b above). Verified reachable through `app.module -> CrmRootModule -> CrmModule -> CrmConsentModule`
- [x] `@Public()` unsubscribe handler · **Stateless HMAC token, not a stored id.** A guessable identifier in an unsubscribe link lets anyone opt out arbitrary contacts, so the token is `u1.<base64url payload>.<HMAC-SHA256>` signed with a key **namespaced** off `ENCRYPTION_KEY` (`hmac(secret,"crm:unsubscribe")`) so a signature can never be replayed against another feature deriving from the same secret. No token table, no expiry sweep, no extra round-trip on send; the trade-off — individual tokens are not revocable — is acceptable because the only action they authorise is *withdrawing* consent, which is idempotent and reversible by the org. Verification is `timingSafeEqual` with a length pre-check (it throws on mismatched lengths) **and** re-validates the decoded payload's shape, so a correctly-signed but structurally invalid token is still rejected. The route lives on a **separate controller** so a future route added to the authenticated one cannot inherit `@Public()`, returns an identical response for valid and invalid tokens (otherwise it is a contact-existence oracle), and is rate-limited at 20/hour — with the `crm:public-unsubscribe` **TIERS entry added**, since a `@UseRateLimit` key with no tier silently disables the limit. 8/8 token specs pass, including forged-payload, forged-signature and wrong-key cases
- [~] **Refuted — those 4 paths must NOT be gated.** Every recipient is internal staff, not the lead: `lead-status.service.ts:257,270` email the sales rep and CRM assignee (template arg `recipientName: … ?? "Team Member"`), `leads-ops.service.ts:79` emails the salesperson a distribution summary, and `leads-detail.service.ts:55` emails the assignee. Gating internal alerts on a *contact's* marketing consent would suppress notifications to your own employees. **Verified no path emails a lead or contact at all** — `to: lead.*` / `to: contact.*` / `lead.email` / `contact.email` return zero send sites. The only contact-facing channel is `CrmOutboundEmailService`, which already resolves through `suppressedEmails`, and it is **not inert**: consumed by `crm-automation-runner.service.ts:29` and `crm-sequences-runner.service.ts:19` — precisely the two bulk send paths that could otherwise forget the check
- [ ] UI + tests
- [x] DPDP/GDPR erasure-surviving suppression (**decision taken: retain hashed addresses**) · The gap: erasure hard-deletes the contact, the `ON DELETE cascade` takes its consent rows with it, and re-importing the same address then silently resumes emailing someone who withdrew consent. New `crm_suppression_hashes` (migration `0185`, applied and verified) stores only `sha256(normalised address)` per org+channel — **no readable PII**, so it satisfies erasure while still answering "is this address suppressed?". `suppressedEmails` now returns the **union** of live consent opt-outs and surviving hashes, so the safety gate cannot regress when a record is erased; `retainSuppressionOnErasure()` is the call to make *before* the hard delete, documented as such because afterwards the address is unrecoverable. Unique on (org, channel, hash) with `onConflictDoNothing`, so repeated erasures are idempotent

## Phase 1 — Object model

- [x] SIG-01 resolved: canonical entities identified · Evidence: `contacts`/`crm_organizations`/`deals`/`leads`/`quotes` have full CRUD services; legacy family is read-only
- [x] SIG-02 answered: 49 of 53 routes work end to end
- [x] SIG-03 answered: no pgEnum/text drift in CRM · Evidence: all 8 CRM enums created in `0000_light_vance_astro.sql:18-26`
- [x] OPEN-04 answered · Evidence: measured **0 rows / 0 orgs** in all 8 CRM tables this session
- [x] OPEN-05 answered: contact merge **is** wired · Evidence: `contact-roles.controller.ts:47-54`, `@RequirePermission("crm:contacts:merge")`
- [x] SCH-002 **discrepancy report produced; no figures changed** · Evidence: `docs/crm-dashboard-discrepancy-2026-08-11.md`. Measured legacy-vs-canonical symbol counts per service — `sales-dashboard` 63 legacy vs 3 canonical, `crm-sales-dashboard` 18 vs 3, **`sales-analytics` reads BOTH (18 legacy, 23 canonical)** so one screen mixes two sources of truth, `crm-support-dashboard` 15 legacy. Per the protocol default for untrusted report data, the repoint itself is **not** done: it needs a populated environment to record the per-metric delta first, and `territory_reps`/`crm_activities` must be re-pointed off `crm_people` before the dashboards, which is blocked on D-009
- [x] SCH-001 dead `crm_leads` table **dropped** · Proved dead four ways before touching it (§25): (1) the only references to the `crmLeads` *table symbol* were its own definition — the `crmLeads` hits in `billing/plan-*` are the plan-limit **string key** `"crmLeads"`, and `plan-limits.service.ts:173` counts from `leads`, a different table; (2) no raw `crm_leads` string anywhere in `src`; (3) `pg_constraint` shows **0 dependent FKs**; (4) nothing queries the `crmCampaignsRelations.leads` relation (`with: { leads }` → 0 hits). Removed the table, `crmLeadsRelations`, the campaign-side `leads: many(...)` edge, and the now-unused `crmLeadStatusEnum` import. Migration `0171` is a single `DROP TABLE "crm_leads" CASCADE` — CASCADE is safe *because* I verified zero dependents. Applied and confirmed: `to_regclass('crm_leads')` → **DROPPED**, while the real `leads` table is untouched
  - **⚠️ Production precondition:** this DB had **0 rows**. `DROP TABLE` is irreversible, so before deploying `0171`, run `SELECT count(*) FROM crm_leads` on production. Any rows there are unreadable by the app (nothing selects that table) but they are still data
- [~] SCH-004 **refuted — `contacts.deal_id` is not orphaned, do NOT drop it.** Now that D-009 is unblocked I could have dropped it; verifying first showed it is load-bearing in two places: the Drizzle relation `deal: one(deals, { fields: [contacts.dealId] })` (`crm/deals.ts:731`) and a live correlated `EXISTS` subquery in `search.service.ts:130-140` that scopes contact search by the caller's access to the linked deal. Dropping the column would have broken cross-entity search and the relation graph. A column with no obvious writer is not the same as a column with no reader
- [ ] SCH-005/011 polymorphic `crm_contact_roles` -> exclusive arc — **unblocked, not attempted.** A real §19 violation (`entity_type` + `entity_id` with no FK), and the fix shape is settled: one nullable FK per target plus a CHECK that exactly one is set, with `org_id` leading the composite index. Left open deliberately because it needs a data migration that reclassifies existing rows by `entity_type`, and getting that wrong silently detaches contact roles
- [~] SCH-010 `territory_reps.crm_person_id` -> `users.id` — **deferred as a product decision.** Not blocked any more, but the change is semantic, not mechanical: `crm-territories.service.ts:67-69` stores rep assignments as **CRM person** ids, and CRM people are records in `crm_people`, not platform users — an external partner rep can be a CRM person without having a login. Repointing the FK at `users.id` narrows who can own a territory and would orphan every existing assignment. Needs an explicit ruling on whether territory reps must be platform users
- [~] SCH-012 FX snapshot on deals — **deferred as a product decision, not a blocked chore.** Unlike quotes, `deals` has **no currency column at all** (`crm/deals.ts:50` is a bare `value decimal(15,2)`), so every deal is implicitly in the org's base currency. Snapshotting a rate first requires deciding that deals *are* multi-currency, which changes how forecast, pipeline value and every deal aggregate are computed. I will not invent that semantics; GAP-013 was safe precisely because quotes already had `currency`
- [x] SCH-003 **task completions no longer fabricate won deals.** The real defect was sharper than the title: `tasks.service.ts:229-233` mapped CALL/EMAIL/MEETING to their activity types and fell back to **`"deal_won"`** for everything else — so ticking off an ordinary TODO inserted a *deal-won* activity with `category: "sales"`, which is exactly what the sales dashboard feed reads (`crm-sales-dashboard.service.ts:182`). The enum had no neutral value to fall back to, which is why the original author reached for `deal_won`. Added `task_completed` to `crm_activity_type` (migration `0184`, applied and verified — `pg_enum` now lists all 8 labels) and pointed the fallback at it. **Also fixed the cause:** `CrmActivityType` was a hand-maintained literal union in `tasks.service.ts` that had already drifted from the pgEnum; it now derives via `(typeof crmActivityTypeEnum.enumValues)[number]` (§7 — never maintain a parallel type), so the next enum change cannot silently desync
- [x] SCH-008 `crm_sla_policies` moved to its own `crm/sla.ts` · It is operational configuration read on every SLA evaluation, not a reporting rollup, and `deals.ts` holds an FK to it — so keeping it in `analytics.ts` made analytics look like a dependency of the deal graph. Moved the table, `SlaConditions` and `crmSlaRelations` together (§24: co-locate, don't split mutually-referencing definitions), added the barrel export, and repointed the two importers (`deals.ts`, and `crm-sla.service.ts` resolves via the barrel). **Proved it is a pure refactor:** `drizzle-kit generate` reports **"No schema changes, nothing to migrate"**, and `madge --circular` reports **"No circular dependency found"**
- [~] SCH-006 money `decimal` → integer cents · Reason: see D-007; needs a migration and touches every financial path

## Soft delete (CRM slice of `docs/soft-delete-audit-2026-08-11.md`)

- [x] V-08/09 `leads` · Evidence: 24 `isNull(leads.deletedAt)` across 8 services; list/count share one conditions object on all 4 pairs; 4 sites deliberately unfiltered per D-013
- [x] V-14 `crm_organizations` · Evidence: `remove()` now sets `deletedAt` guarded by `isNull`; `queryList`/`getWithContacts`/`exists` filter; **both** CTE arms carry `deleted_at IS NULL`
- [x] Org-detail contacts read (my catch) · Evidence: contacts service filters in 4 places; this read was the outlier
- [x] **V-07 `deals` · V-13 `quotes` · V-25 `crm_campaigns` · SOFT-005 `territories` — soft delete shipped end to end** (D-009 unblocked; owner confirmed the migration path works). Sequence: added `deletedAt` to all four tables → generated `0170_crm_soft_delete_and_partial_indexes` → **read the SQL before applying** → applied → verified against the live DB. All four `deleted_at` columns confirmed **present** by an `information_schema` probe, not by reading the migration file. Converted all four hard `DELETE`s to `UPDATE … SET deleted_at`, each guarded by `isNull(...)` so a repeat delete is idempotent rather than silently re-stamping. Then the half that actually matters (§19: "a `deleted_at` column that reads ignore is worse than none"): **90 read filters added across 34 files** — 65 `deals` sites, 26 `quotes`, plus campaigns and territories — covering analytics, dashboards, search, AI services, Customer 360, attribution, data-quality and automation runners. BE tsc clean: same 5 pre-existing errors in the same 3 unrelated files
  - **⚠️ The sweep introduced one real bug and I caught it:** filtering the quote-numbering `count()` would have re-issued an existing `QT-<date>-NNN`, because `idx_quotes_number` is a **non-partial** unique index on `(org_id, quote_number)` — so deleting today's QT-001 would make the next quote collide and 500. That one filter is reverted with a comment explaining why it must count deleted rows. Audited the rest for the same class (numbering, uniqueness probes, restore paths): none remaining, and there are no restore endpoints, so no `isNotNull` counterpart is needed yet
- [x] Partial indexes shipped and **proven used, not merely created** · `idx_deals_org_live_stage (org_id, stage)`, `idx_quotes_org_live_status (org_id, status)`, `idx_territories_org_live (org_id, priority)`, `idx_crm_campaigns_org_live (org_id)`, each `WHERE deleted_at IS NULL`. Verified two ways: `pg_indexes` confirms all four exist, and `EXPLAIN` on the exact live-rows predicate each service issues shows the planner **choosing** them (`USES idx_deals_org_live_stage`, etc.). That second check matters — a partial index is invisible to any query that omits its predicate, so "created" is not "used"
- [x] Cascade reviewed — **retention is correct here, and the real risk is the opposite one.** `contacts` soft-deletes, so the `onDelete: "cascade"` on both consent tables never fires; that is desirable, since a DPDP/GDPR audit needs proof of the opt-in/opt-out that applied at the time. Checked both readers: `countMissingConsent` correctly filters `isNull(contacts.deletedAt)` (a coverage metric), while `suppressedEmails` correctly does **not** — an opt-out must outlive its record, or deleting and re-adding an address silently resumes emailing someone who opted out. Added a comment at that query saying so, because "add the missing filter" is exactly the plausible-looking regression a later reader would make. **Recorded, needs legal input (not code):** DPDP erasure is a permitted *hard* delete (D-012), and a hard contact delete WOULD fire the cascade and destroy the suppression proof — standard practice is to retain a hashed-email suppression entry post-erasure
