# Pages

Route inventory + change log. `[x]` = built and verified · `[~]` = deleted/retired · `~~strike~~` = removed or redirected.
When I say **"go"**, take the next unchecked page. New work adds ONE line to the Changelog: `- YYYY-MM-DD — what changed. \`files\` · verification`.
Full text of any pre-2026-08-03 entry is in git history.

---

## Pending (operator actions)

- [ ] **Run `VACUUM ANALYZE` after any migration that rewrites a table.** It cannot be a migration — `VACUUM` is illegal inside a transaction block and drizzle-kit wraps every migration in one (verified: the attempt failed, and the file was removed rather than left broken). A rewrite invalidates planner stats **and** empties the visibility map: `ANALYZE` alone fixes the first, but index-only scans stay degraded until `VACUUM` runs. Measured this session — the ticket list `COUNT(*)` went 37 → **3,340** buffer blocks after the rewrites and back to 37 only after `VACUUM ANALYZE`. Already run manually for `tickets`; make it a deploy step after schema changes.
- [x] ~~**Apply migration `0398_backfill_hr_admin_branch_hr_recruitment_grants`** (journal idx 125)~~ — **APPLIED 2026-08-10.** Verified: `hr:interviews:view` 9 grants, `hr:offers:view` 9, `hr:interviews:manage` 6; `access_versions.permissions_version` bumped (max 39 across 5 orgs). HR admins now see interview/offer queues on `/hr`.
- [ ] **Apply migrations `0117`–`0118`** (journal idx 117–118) for RBAC hardening: `invitation_events.org_id` FK (lock-safe NOT VALID→VALIDATE) + module-access/audit indexes. Not applied in this validation pass.
- [ ] **Apply migration `0392_payroll_worker_subject`** — worker_id on salary profiles + payroll run employees (nullable user_id, backfill, composite FK NOT VALID→VALIDATE).
- [ ] **Apply migration `0393_payroll_lifecycle_worker_subject`** — worker_id on payslip publications, bank batch items, TDS YTD ledger (nullable user_id, backfill from run employees, partial uniques on TDS).
- [ ] **Apply migrations `0384`–`0387`** (journal idx 109–112). Until then every `@Public()` token flow (invitation accept, e-sign, offers, portals, public forms/surveys/CSAT, webhooks) stays broken, AI credits are never granted after purchase, and dunning/exception crons stay dead.
- [ ] **Projects Members/Teams:** `pnpm -C backend db:generate` (choose "＋ create table" for BOTH `project_workspace_members` and `project_team_assignments`) → `db:migrate`, then backfill:
      `INSERT INTO project_workspace_members (org_id,user_id,role,added_at) SELECT org_id,user_id,'member',now() FROM organization_members ON CONFLICT DO NOTHING;`
- [x] ~~**Build indexes** migration~~ — **APPLIED 2026-08-10** as `0126_build_ticket_hot_path_indexes` (journal idx 126, `generate --custom` because `db:generate`'s differ is blocked by the unrelated timesheets enum drift, see below). Adds `idx_tickets_org_project_order`, `idx_tickets_org_assignee_status`, partial `idx_tickets_org_assignee_due_open`; drops non-org-led `idx_tickets_assignee`. All statements idempotent.
- [ ] **⚠️ PLATFORM-WIDE — RLS is making every text search a full table scan.** `app.current_org_id()` is `STABLE` but not `LEAKPROOF`, so under RLS the planner cannot use any GIN/trigram/FTS index. Measured on the same query and data: as `neondb_owner` (BYPASSRLS) `Bitmap Index Scan using idx_tickets_title_trgm`, **1.73ms / 336 blocks**; as `streamline_app` (what the app uses) `Seq Scan`, **~80ms / 11,665 blocks**. **16 of the platform's 17 GIN indexes have `idx_scan = 0` — never used once**, including `idx_kb_articles_fts`, `idx_kb_pages_fts`, `idx_users_*_trgm`, `idx_inv_*_trgm`, `idx_leads_company_trgm`. Remedy is one statement but **superuser-only** (`neondb_owner` has `rolsuper = false`) — needs `neon_superuser` or Neon support: `ALTER FUNCTION app.current_org_id() LEAKPROOF;` Safe (the function reads a GUC and returns it; it cannot leak row data). **Re-measure with `capture-build-baseline.mjs` after applying — the fix is expected, not proven.**
- [ ] **Resync the Drizzle snapshot at a TTY.** The DB and the migration files are correct, but `generate --custom` copies the previous snapshot instead of diffing, so `migrations/meta` is behind reality after five custom migrations — snapshot `0143` records **0 timesheet enums** and still has `tickets.order` instead of `tickets.rank`. Until resynced, every `db:generate` re-proposes work already applied. Fix: run `db:generate` once at a real TTY (`winpty` is installed), answer **"create new enum"** for all 17 (none is a rename — none existed before), then **inspect the emitted SQL before applying** — it will restate changes already in the DB, so make it idempotent or record it as applied without executing. Do not blind-run it, and do not hand-edit the snapshot JSON.
- [x] ~~**`db:generate` is blocked by the timesheets enum drift (SCH-009)**~~ — **RESOLVED 2026-08-10.** The 17 enums are applied (migration `0143`): 18 columns converted, 150,150 rows intact, 0 nulls. Note the enum prompt still appears until the snapshot above is resynced — that is snapshot drift, not the original drift. The Drizzle schema declares 17 pgEnums (`schema/timesheets/`) that were never migrated — the live columns are still `text` — so drizzle-kit's `enumsResolver` demands an interactive new-vs-renamed answer and aborts in any non-TTY shell. Any future `db:generate` needs a real TTY (`winpty` is available) **and** a deliberate decision on those 17 enums, including verifying the generated `ALTER … TYPE` statements carry `USING col::enum` casts. Until then, use `generate --custom` for unrelated schema changes.
- [~] ~~**Build module rename:** run `rename-projects-to-build-data.sql` before wiring `ModuleGuard`~~ — **resolved, verified 2026-08-10.** `org_modules` already holds `build` (5 rows, zero `projects`), and `ModuleGuard` is a global `APP_GUARD` (`app.module.ts:176`) so all 39 Build controllers are already entitlement-gated.
- [ ] **Backend restarts run from `dist`** — `nest build` + restart is required for any backend fix to take effect.
- [ ] Frontend work from the HRMS wave is **uncommitted**; the frontend index is shared with concurrent sessions — commit with explicit paths (`git commit -- <paths>`).

---

## Changelog

**2026-08-11**
- Build refactor Phase 1 continued. **SCH-001 ranking**: `tickets.order` integer → `tickets.rank` numeric, server-authoritative — a drag now sends two neighbour ids and updates ONE row instead of the client renumbering both columns; 0 ordering violations across 200k tickets, 30 successive midpoints still strictly between neighbours using 13 of 16383 decimal places. **SCH-002 status model**: the usage mapping shrank this from "merge three competing systems" to "type one column, repoint the burndown, drop the orphan" — `custom_states` had **zero write sites** and `tickets.state_id` was NULL on 100% of 204k rows, so the burndown's COALESCE fallback had *always* been taken; closes RPT-003. **SCH-009**: 17 timesheets enums applied, after finding my own seed had written `DRAFT`/`SUBMITTED` values absent from the enum — the `USING` cast would have failed on 75,075 rows. **SCH-005/006/006b/006c**: optimistic locking + soft delete across tickets/projects/sprints/comments/roadmap/feedback/goals (156+ `isNull` filters); `uniq_projects_org_key` made partial so a deleted project releases its key; `deleteProject` stamps children in one transaction, closing §19's orphaned-but-visible cascade hazard. **RPT-001**: append-only `sprint_scope_events`, 200,000 backfilled, point-in-time reconstruction verified. **RPT-002**: daily snapshot cron sweep — I had wrongly called this blocked; HTTP cron controllers already existed. **TIME-004**: rates snapshotted at approval, billing prefers the stored rate. **API-012**: the regression batch 1 introduced, closed by a 2-branch UNION (49.31ms/2,914 blocks → 1.20ms/354). **FE-001**: 33 ungated query hooks gated, all 27 keys verified present in both catalogs. Rollbacks written **and executed** for all 7 schema migrations. Metrics: board 3,345→54 blocks, My Work 11,477→54. `docs/refactor/build-changelog.md` · `TASKS-BUILD.md` 50/67 · backend + frontend typecheck 0 errors (lint not run).

**2026-08-10**
- Build refactor batch 5 — pagination envelopes + measurement-driven triage. **PM-008/009**: four list endpoints returned bare arrays so callers could not distinguish a full page from a truncated one (portfolios had no `page`/`offset` at all, just a hardcoded `.limit(100)`); all four now return `{data, pagination}` with a parallel `COUNT`, and the frontend hooks plus all four surfaces are wired to the shared `TablePagination` per §14, resetting to page 1 on filter change. All 7 consumer call sites updated; the new `useCan("build:portfolios:view")` verified to be a real key in **both** catalogs (a ghost key fails `useCan` closed forever). **API-009 escalated out of Build** — RLS defeats every GIN/trigram/FTS index for `streamline_app` because `app.current_org_id()` isn't `LEAKPROOF`; 16/17 GIN indexes never scanned; superuser-only fix, logged as a pending operator action. **API-003 downgraded P1→P3 on measurement** — offset 400 (the board's real ceiling) costs 1.05ms, so keyset would break the `page`/`limit` contract for ~0.6ms; deliberately not done. Added `seed:build-load` + `baseline:build` scripts so the perf harness is reproducible. `portfolios.{service,schemas}.ts` · `projects-roadmap.service.ts` · `hooks/api/build/{portfolios,roadmap}.ts` · roadmap tabs + portfolios/programs pages · backend + frontend typecheck ✓ 0 errors (lint/tests not run).
- Build refactor batch 4 — remaining security/integrity findings. **API-012** (the regression batch 1 introduced): benchmarked 6 query shapes, shipped a 2-branch UNION for the scoped board path — **49.31ms/2,914 blocks → 1.20ms/354**, with the visibility partition (2,858/476/3,334) and the page-1 id sequence both re-verified identical, since a perf change to a P0 security predicate is where silent drift costs most. **SEC-001**: public whiteboard had a TOCTOU between the token SELECT and the UPDATE — an admin revoking sharing mid-window was ignored; UPDATE now re-asserts shareToken/visibility/publicAccess/expiry atomically and 404s on 0 rows; GET no longer loads `orgId`/`projectId`/`createdBy`/`shareToken` before projecting. Expiry, token rotation, allowlisted write, rate tiers and the RLS `WITH CHECK` arm were all already correct. **PM-007**: `voterIpHash` HMAC + partial unique indexes (migration `0137`) — and two inert-fix gaps closed by hand, the unwired controller (`voterIp` never passed, binding did nothing) and an empty-string HMAC key now backed by the startup-validated `BACKEND_JWT_SECRET`. **PM-012**: owner FK `restrict`→`set null`. **API-008 analysed, deliberately not fixed** — the rollup plan is already index-optimal, so the only real fix is a materialised aggregate, which is blocked on RPT-002 (no scheduler exists). `projects-tickets-read.service.ts` · `whiteboard-sharing.service.ts` · `public.controller.ts` · `public/roadmap.service.ts` · `schema/build/{roadmap,managed-products}.ts` · migration `0137` applied · backend + frontend typecheck ✓ 0 errors (lint/tests not run).
- Build refactor batch 2 — sub-domain sweep, driven by audits of the 15 non-core sub-domains (18 findings) and the product-management side (14 findings). Security: **`roadmapItems.isPublic` defaulted to `true`**, so every roadmap item was live on the unauthenticated public feed unless manually toggled off (migration `0127`, now `false`, 0 existing rows so no backfill); the 3 public roadmap endpoints had **no rate limiting** while `public:contact` did — guards added **and** the three `TIERS` entries registered, because `RateLimitService` fails open on an unregistered key so the guard alone is a no-op; QA `createRun` accepted `caseIds` unvalidated, allowing cross-**project** test-case injection (RLS blocks cross-tenant only). Correctness: approvals notification fired on a committed ALS transaction handle → `42501` silently swallowed (the §20 zero-rows pattern) → now `registerAfterCommit` + `runInNewTenantTransaction` with logged failures; 3 FK-nullify-then-delete pairs wrapped in transactions; `updateIntake` gained `pg_advisory_xact_lock` (duplicate ticket numbers) + explicit `orgId` on writes; meetings filtered *after* pagination → correlated `EXISTS` (third instance of this defect class). Efficiency: 9 unbounded reads capped, `teamTimesheets` `limit:500` → real `page`/`limit`; 3 roadmap hooks gated on `build:roadmap:view` (were 403-spamming). Hygiene: `/build/page.tsx` still re-exported `./all/page` (§17), module mislabelled "Product Management" vs §16 "Build", 2 banned legacy redirect routes deleted. **Dead code: knip v6.31.0 run on both repos per §25 — backend 0 unused files, frontend 5 (all HR, none in Build); Build has no dead code, nothing deleted.** Phase 0 audit now COMPLETE (56 findings + 14 verified non-findings, `docs/refactor/build-phase0-audit.md`). **§9: all 3 oversized Build files split — `schema/build/tasks.ts` 607→barrel+4 (zero pgTable renames, all 43 tables verified present in DB), `execution/iterations.service.ts` 533→barrel+4 single-class services, `core/projects-tickets.controller.ts` 518→185+3 sub-controllers (41 routes before and after, identical URLs). No Build file over 500 lines remains in either repo.** `public.controller.ts` · `rate-limit.service.ts` · `approvals.service.ts` · `iterations.service.ts` · `workspace.service.ts` · `meetings.service.ts` · `test-runs.service.ts` · `sidebar-nav-items.ts` · +6 · migrations `0126`/`0127` applied · backend + frontend typecheck ✓ 0 errors (lint/tests not run).
- Build refactor Phase 0 + first fix batch. Audit (`docs/refactor/build-phase0-audit.md`, 25 findings / 14 verified non-findings) measured against a seeded 1.53M-row dataset (`seed-build-load.mjs`) with `EXPLAIN` captured as the RLS-enforced `streamline_app` role (`capture-build-baseline.mjs` → `docs/refactor/baseline/`). Fixed: **SEC-002** ticket list applied no DataScope while `getTicket` did, leaking title+description of tickets the detail endpoint 403s (now an SQL predicate incl. the `COUNT`); **API-002/007** added `idx_tickets_org_project_order` + partial `idx_tickets_org_assignee_due_open`, dropped non-org-led `idx_tickets_assignee` — board **3,345→55 blocks**, My Work **11,477→53 blocks**; **API-001** board hook's 5-way parallel fanout that silently truncated at 500 tickets → `useInfiniteQuery` with the real envelope + `isTruncated`; **TIME-001/002/003** Build's second time-entry writer omitted `project_id` (billing rollup dropped those rows) and `timesheet_period_id` (invisible to approval) — now written in one txn via `EntriesPeriodService`, `recomputeTimeSpent` org-scoped, and the project filter moved out of post-pagination JS into SQL; **SCH-003b** `epicId` cycles were unguarded and the parent walk silently gave up at 100 hops, plus blocking-edge cycle detection added; **UI-001** all 9 workspace routes de-re-exported (§17 bans importing one `app/**/page.tsx` from another) after extracting 5 inline page components to `features/build/{project-list,settings,project-detail,epics,views}/`, all under the 500-line cap. Retracted **API-006** (`Seq Scan on users` is correct on a 7-row table — seed artifact). Disclosed regression **API-012**: restricted-scope board is 87ms/2,911 blocks vs 0.20ms/55 for `all`; a covering index was tested and rejected. `projects-tickets-read.service.ts` · `db/schema/build/tasks.ts` · `build/execution/timesheets.service.ts` · `projects-tickets-{update,create}.service.ts` · `projects-ticket-relations.service.ts` · `hooks/api/build/ticket-queries.ts` · backend + frontend typecheck ✓ 0 errors (lint/tests not run).

**2026-08-09**
- Review fixes (module-access refactor + HR display pass), 11 verified defects. Backend: `renameGroup` never called `bumpPermissionsVersion`, so the version-keyed `moduleGroupsList` cache served the OLD name until TTL (every sibling mutation bumps — this was the one gap); `resolveActorRankContext` counted suspended members and expired assignments, inflating `bestRank` and weakening the grantability rank check (added the ACTIVE + unexpired filters its sibling `resolveModuleAuthorityFacts` already had); `removeGroupMember` gained the self-removal guard `addGroupMember`/`updateMemberGroups` already enforce (a non-owner Module Admin could silently demote themselves); `as { code?: string }` → narrowing; the byte-identical ~35-line access guard duplicated across `module-access.service` and `module-access-groups.service` extracted to one `assertModuleAccessPolicy` helper (drift risk on security-critical code). Frontend: 17 files had a column-0 `>` left by the `variant="display"` sweep; `/hr/loading.tsx` still rendered the employee-directory skeleton after `/hr` became the hub (rebuilt to mirror hero→queues→recruitment→today→metrics); dead `EmployeesLoadingSkeleton` export removed (file kept — `EmployeesGridSkeleton` is still live); `useHrDepartments`/`useDocumentTemplates`/`useOnboardingTemplateDepartments` were ungated against gated endpoints (§11); HR hub fired `/hr/exit` twice per render because queues and activity passed different params to `useResignations` (now one shared `HUB_RESIGNATIONS_PARAMS`). Refuted: employees-export "removal" (that file's only change was violet→blue per §14) and the dashboard recruitment widget (correctly caller-gated). Deferred: `listGroupMembers` truncates at 100 with no pagination envelope (§19 — API-shape change). frontend + backend prod typecheck ✓.
- `/hr/employees` directory polish (Approach A): removed redundant “People grid / Page X of Y”; denser left-aligned employee cards; `StatCardGrid` with Total (filtered) + Active/Inactive from command-center when `hr:analytics:read`; shared `TablePagination` pinned at bottom for grid and list. `employees-list-page.tsx` · `employee-card.tsx` · `employees-directory-stats.tsx` · frontend typecheck.
- Permission-aware navigation and actions completed across all permissioned products: the shared navigation model now filters desktop/mobile sidebars, product switching, command search, and HR hub destinations by exact server permissions (authorized children are promoted when a parent is unavailable); unauthorized create/edit/delete/import/export/bulk/builder controls are hidden across Inventory, CRM, Build, HR, Accounting, Support, Surveys, Payroll, Sign, Timesheets, and Administration. All ten module Access pages share one policy: Ownership is visible only to the canonical module owner, while member/role management is limited to Org Owner/Admin or canonical same-module Owner/Admin; custom/delegated `*:access:manage` grants cannot impersonate a module admin. HR onboarding now uses `hr:onboarding:manage` end to end and independently gates documents/probation. `sidebar-nav-items.ts` + shared consumers · module-access UI/backend · cross-product action surfaces · CLAUDE/UI rules · frontend typecheck + 10 suites/82 tests · backend typecheck + module-access 5 suites/59 tests + HR boundary 4 tests · ESLint/Knip cycles/diff-check ✓.
- `/hr` hub: navigation tiles dropped (they duplicated the sidebar) and replaced with state-bearing panels — **Today** (out today · joining soon · interviews today · celebrations · next holiday · live attendance, 6 permission-gated cards) and **Needs attention** (ops-inbox items + probation due + in-flight exits merged into one urgency-sorted actionable row list, each row linking to the record). Final order: hero → queues → recruitment → today → attention → metrics. `hr-hub-today.tsx` · `hr-hub-activity.tsx` · `hr-hub-destinations.tsx` deleted · frontend typecheck ✓.
- `/hr` rebuilt as the HR module landing page — a role-aware operations hub (queues-first). Composition: `HrHero` + permission-filtered quick actions → **Needs you** queue band (leave approvals, WFH pending, probation due, resignations, docs expiring 30d, onboarding in progress, plus the purpose-built service-delivery ops inbox with cases/safety/helpdesk/SLA-breach sub-counts) → recruitment section (interviews today, awaiting scorecard, offers pending, requisitions pending) → metrics row (`useHrCommandCenter`: headcount/active/attrition/attendance/leave-utilisation/open cases) → destination tiles derived from the real `NAV_GROUPS` (no parallel list to drift). Every section self-gates on the endpoint's exact key and renders nothing when unheld, so a 2-permission user gets a short clean page, not empty-state walls. Expensive endpoints (ATS kanban, /hr/leaves/team, expenses page-data, reimbursements, unfiltered helpdesk) deliberately excluded. Employee directory rehomed to `/hr/employees`; sidebar gains an "Overview" entry at `/hr`. Enablers shipped with it: 30 HR/recruitment hooks gained the correct `useCan` gate (every key verified against its controller — they previously fired ungated and 403-spammed; `useExpensePageData` keeps its `self:expenses` path for ESS), `useTalentPools` moved into the query-key factory, and HR_ADMIN/BRANCH_HR templates gained `hr:interviews:view/manage` + `hr:offers:view` (`hr:offers:approve` and `hr:sensitive:view` deliberately NOT granted). `features/hr/hub/**` (6 files) · `hr/page.tsx` · `sidebar-nav-items.ts` + test · `hooks/api/hr/**` · `role-templates.constants.ts` · migration `0398` (pending) · frontend + backend prod typecheck ✓.
- Home-module defect sweep (3 audits → 3 fix lanes, 30 defects, all fixed). Dashboard (12): `hr.interviews` key factory made a true prefix (stale recruitment widget); `LeaveBalanceWidget` own-requests no longer gated on the admin `hr:leaves:view`; `LeavesToday`/`PendingApprovals`/`MyIssues`/`RecentProjects`/`RecentActivity`/`PublicDocuments`/`Sprint` now surface real errors + retry instead of fake empties; executive KPI hrefs gated on module access; `BusinessPulse` query gated before render; `MyAttendanceWidget` gate switched to `self:attendance`; Check-In quick action → `/me/attendance`; timesheet link label. `/me` (9): expense edit clear-drop fixed end-to-end (frontend `""` + new `clearableMerchant/ExpenseLabelSchema` normalizing ""→null so the service guard fires — merchant/paymentMethod were previously unclearable); time-off + payslips/reimbursements/tax error branches (tax previously claimed "window closed" on any failure); FeedbackSheet mount-guarded (state leaked between interviews); dead `attendanceLogs` invalidation removed; `useHrToggleBreak` orgId coercion aligned; time-off Suspense fallback. Notifications/Calendar/Ask (9): `notifications.broadcasts` prefix fix (filtered tabs never refreshed after any write); mark-read/bulk/all now optimistic with rollback; calendar error banner + `calendar:read`/`directory:people:view` gates + HR-supplemental key moved into the factory; dead article-scoped ask spread removed (backend has no `articleId`); SSE token-failure retry; directory search debounced. `dashboard/**` · `me/**` + ESS · `notifications.ts` · `calendar.ts` · `kb-ask-panel.tsx` · `expense.schemas.ts` · frontend + backend prod typecheck ✓.
- Fill-chain sweep COMPLETE — all 5 route groups, ~545 authenticated pages enumerated with per-route verdicts: settings 25 (6 fixed), home 17 (4 fixed), Build ~70 (3 fixed), other modules ~251 (~66 fixed), HR+Payroll ~120 (32 files fixed). Root cause was per-page, not shared: `PageWrapper`'s scroll column already reaches the viewport bottom, but page body surfaces were auto-height. Canonical fix everywhere: root gets `flex flex-1 min-h-0 flex-col`, primary surface gets `flex-1 min-h-0` (or the shared `CONTENT_FILL_PANEL`/`PAGE_BODY_*`/`TABS_CONTENT_PAGE_BODY_CLASS`/`DataTable className="flex-1 min-h-0"`). No fixed heights, no shared-primitive edits, no visual redesign. Exempt (documented): app-shells (chat/mail), builders/editors/canvases, kanban/timeline, natural-height card grids and analytics dashboards. Frontend typecheck ✓.
- Fill-chain sweep (HR + Payroll — ~120 routes audited): dead background band above the Ask OS bar eliminated across HR and Payroll pages. Patterns fixed: DataTable missing `className="flex-1 min-h-0"` (primary body surface); Tabs missing `className="flex flex-1 min-h-0 flex-col"`; TabsContent missing fill class; loading `space-y-N` → `flex flex-1 min-h-0 flex-col gap-N`; motion.div wrapper swallowing fill chain; TabsList missing `shrink-0`. Natural-height pages (analytics dashboards, card grids, wizard/setup, forms, detail pages, card lists) exempt. Files fixed (32): `features/hr/enterprise/ops/accommodations/accommodations-page-content.tsx` · `features/hr/enterprise/ops/emergency/emergency-page-content.tsx` · `features/hr/safety/safety-page-content.tsx` · `features/hr/enterprise/ops/event-stream/event-stream-page-content.tsx` · `features/hr/enterprise/ops/identity/identity-page-content.tsx` · `features/hr/governance/components/legal-holds-table.tsx` · `features/hr/performance/performance-page-client.tsx` · `features/hr/governance/components/labor-tabs.tsx` · `features/hr/governance/components/positions-page-content.tsx` · `features/hr/governance/components/reorg-scenarios-tab.tsx` · `features/hr/governance/components/retention-page-content.tsx` · `features/hr/governance/components/retention-policies-tab.tsx` · `features/hr/governance/components/data-requests-tab.tsx` · `app/(authenticated)/hr/overtime/page.tsx` · `features/hr/overtime/overtime-list.tsx` · `app/(authenticated)/hr/benefits/page.tsx` · `features/payroll/inputs/inputs-section-tabs.tsx` · `features/payroll/inputs/inputs-page-content.tsx` · `features/payroll/taxes/declarations-tab.tsx` · `features/payroll/taxes/tax-windows-tab.tsx` · `features/hr/cases/cases-page-content.tsx` · `features/hr/shifts/shift-assignments-tab.tsx` · `features/hr/shifts/shift-swaps-tab.tsx` · `features/hr/documents/templates-list-page.tsx` · `features/hr/forms/components/forms-data-table.tsx` · `features/hr/forms/components/submissions-data-table.tsx` · `features/hr/custom-fields/components/custom-fields-data-table.tsx` · `app/(authenticated)/hr/settings/forms/page.tsx` · `app/(authenticated)/hr/settings/forms/[formId]/submissions/page.tsx` · `app/(authenticated)/hr/settings/custom-fields/page.tsx` · `features/hr/enterprise/comp/devices-table.tsx` · `app/(authenticated)/hr/devices/page.tsx` · typecheck not run.
- Fill-chain sweep (12 owned modules — ~248 routes verified): CRM (52), Inventory (44), Accounting (74), Timesheets (9), Support (26), Surveys (6), Sign (5), Workflows (10), Parties (1), Knowledge (17), Chat (3), Mail (1). Dead background band above the Ask OS bar eliminated where present. ~64 unique files fixed across five modules; Chat/Mail/builder pages exempt (no PageWrapper); stat-card/chart/form pages exempt (no fill surface). Patterns fixed: DataTable missing `className="flex-1 min-h-0"`; root content div using `space-y-N` instead of `flex flex-1 min-h-0 flex-col`; `TabsContent` missing `TABS_CONTENT_PAGE_BODY_CLASS`; Card/CardContent double-card anti-pattern wrapping DataTable; motion.div wrapper swallowing fill. Key files: `features/accounting/banking/components/bank-account-detail-client.tsx` · `features/accounting/core/recurring-journals-tab.tsx` · `features/accounting/sales/collections-tab.tsx` · `features/timesheets/billing/billing-view.tsx` · `features/timesheets/exceptions/exceptions-view.tsx` · `features/timesheets/payroll/payroll-page-client.tsx` · `features/timesheets/reports/reports-view.tsx` + 4 report tabs · `features/inventory/components/control/count-detail-shared.tsx` · `features/inventory/components/tools/import-client.tsx` · `app/(authenticated)/workflows/[workflowId]/page.tsx` · `crm/settings/validation-rules/page.tsx` · `features/shared/automations/module-automations-settings.tsx` · +51 more · typecheck not run.
- Fill-chain pass (Build module — 3 pages): `backlog/page.tsx` loading `DataTableSkeleton` gains `flex-1 min-h-0`; data `PmPanel` gains `flex-1 min-h-0 flex flex-col` and `DataTable` gains `flex-1 min-h-0` so the bg-card surface extends to the bottom. `comment-drafts-page.tsx` loading and data `PmPanel solid` gain `flex-1 min-h-0` (and retain `overflow-hidden` on data) inside their `PmSection flex-1` containers. `managed-product-detail-page.tsx` `PmSection index={0}` gains `flex min-h-0 flex-1 flex-col` so built-in `flex-1` on `EmptyState`/`ErrorState` activates. All other Build routes (75+) verified as already-conforming — kanban/board pages exempt (noInternalScroll + full-height own layout). `backlog/page.tsx` · `comment-drafts-page.tsx` · `managed-product-detail-page.tsx`.
- Fill-chain pass (dashboard / calendar / me/recruitment / organization): root content elements gain `flex flex-1 min-h-0 flex-col` so the dead background band above the Ask OS bar is eliminated. Dashboard loading + main render divs: `flex flex-col gap-4` → `flex flex-1 min-h-0 flex-col gap-4`. Calendar root: `h-full` → `flex-1 min-h-0` (canonical flex form). Org-structure root: `space-y-4` → `flex flex-1 min-h-0 flex-col gap-4`. Me/recruitment: adds root wrapper + `PAGE_BODY_SKELETON_CLASS` for loading + `PAGE_BODY_EMPTY_CLASS` for empty. `dashboard-client.tsx` · `calendar-view.tsx` · `organization-structure-page.tsx` · `my-recruitment-page.tsx`.
- Universal employee baseline enforced (living-rule update): `EMPLOYEE_SELF_SERVICE` (self time-off/attendance/expenses/pay/recruitment, chat, mail, calendar, directory, etc.) now applies to EVERY active member unconditionally in `access.service.ts` — it previously only reached members with an explicit RBAC role assignment, leaving bare members with 4 keys and mail/chat/all self-service surfaces blocked in nav + API. Applied post-cache (no staleness/bump needed), gated on ACTIVE membership, never downgrades broader scopes; owners/admins unchanged. Payslip self-download un-module-gated (`@RequireModule("payroll")` moved class→admin routes only; §16) and its cross-tenant probe now 404s (§20, was 403 oracle). No frontend changes needed — nav gates on exactly these keys. Pending follow-ups: self-service endpoint for own contracts/letters (none exists — admin `hr:documents:view` only); referrals + internal-openings surface (chosen Recruitment scope) doesn't exist yet (`/me/recruitment` is interviewer self-view); scope-review `hr:expenses:view`/`timesheets:entries:view` universal breadth. `access.service.ts` · `publishing.controller.ts` · `publishing.service.ts` · CLAUDE.md §16 rule · backend prod typecheck ✓.
- Directory identity and access reconciliation: member links win over invitations; normalized work email (personal email only when work email is absent) recovers legacy unlinked people, pending/expired invitations get truthful management actions, and creating a worker from a member creates or restores the directory person instead of returning “no directory person record.” List/detail caches invalidate after invitation mutations. `directory-identity.service.ts` · `directory.service.ts` · person access presentation + focused tests.
- Workforce IA and engagements: operational Workers moved from Settings to Home Company at `/directory/workers` (the old Settings route is removed). Planned engagements can be edited in the shared sheet; active/cancelled/history remains lifecycle-controlled; overlap and primary conflicts are explained before submission, while database race conflicts map through `getErrorMessage`. `worker-engagements-sheet.tsx` · Workers hooks/types · sidebar/route tests.
- Hierarchy lifecycle safety: all six structure pages use the shared archive confirmation flow and a read-only dependency preflight, showing exact blocking record types/counts before mutation. The backend rechecks atomically and returns a structured 409 if state changes; archive/retire preserves history and never hard-deletes. Cost-center copy now explains its payroll, budget, expense, and reporting purpose. `org-hierarchy-dependencies.service.ts` · hierarchy controller/service · shared archive hook/dialog + tests.
- Delegations repaired end-to-end: one RBAC permission governs page/nav/API, recipients and grantable keys come from RBAC discovery, self-delegation is rejected, permissions are grouped by module, list/revoke failures are retryable and formatted, and delegated permissions are normalized into `user_delegation_permissions` child rows with migration/backfill, RLS, access resolution, and session invalidation. `0397_normalize_delegation_permissions.sql` · Delegations/RBAC/Access services + focused tests.
- `/settings/users`: invitations list refetches on every window focus (`refetchOnWindowFocus: "always"` — status changes out-of-band on accept). Member role change post-acceptance shipped: `useUpdateUserRole` → existing `POST /settings/users/:userId/role`, "Change role" row action + compact dialog (`user-change-role-dialog.tsx`), gated `settings:rbac:manage` AND `isOrgOwner` (service hard-requires owner; ORG_ADMIN would 403), hidden for the owner row, roles limited to grantable set (never OWNER — transfer flow only), invalidates users/detail/stats/members/directory. `users.ts` hooks · `user-actions-menu.tsx` · `user-change-role-dialog.tsx` · frontend typecheck ✓.

**2026-08-09**
- Review fixes (HR/module-access wave): 25 backend-canonical keys mirrored into the frontend permission catalogs (23 `hr:*` + `crm:deals:create`/`crm:ai:use` — §9 drift; runtime was unaffected since `useCan`/roles read server-resolved data); Ownership UI is module-owner-only, while Org Owner break-glass remains a backend recovery capability; `<module>:access:manage` catalog description rewritten in BOTH catalogs to state the structural-authority requirement (the effective grant now confers view only — no more grantable-but-misleading key); two §7 comment blocks removed from module-access backend. Verified-refuted: HR proxy-delegation mutations on `hr:workflows:view` are correct (self-scoped + object-level grantor checks); nav `hr:automations:view` aligns with the dedicated HR automations controller; `filterRoute` child inheritance causes no lockouts; Radix tabs activate on mousedown so the module-access page test is not vacuous. `hr.ts`/`crm.ts`/`module-access.ts` catalogs · `module-access-page.tsx` + test · backend `module-access` helpers/groups · frontend + backend prod typecheck ✓.

**2026-08-08**
- `/settings/organization/chart` showed "No organization structure yet" for orgs with real structure: `getTree` rooted only at business units and dropped every unit with a null/invisible parent (BU and branch parents are optional). Backend now assembles bottom-up with orphan promotion (roots = BUs · orphan branches · orphan departments · orphan teams, per-node shapes unchanged); frontend renders mixed roots via exhaustive `type` dispatch, `filterTree` generalized per type, `OrgTreeNode` became the root union (BU type renamed `OrgTreeBusinessUnit`), empty-state copy updated. `org-hierarchy.service.ts` · `organization-chart-page.tsx` · `types/org-hierarchy.ts` · frontend typecheck ✓ · backend prod typecheck ✓.
- Person-detail Worker tab: a just-created worker never appeared because the tab located it via `search=<workEmail>` and backend `listWorkers` search matched only names + workerNumber. Replaced with a precise `organizationPersonId` filter (workers list query schema + service condition, controller wiring unchanged) and the tab now queries `{ organizationPersonId, limit: 1 }`; workers search also gains `workEmail` matching; `useCreateWorker`'s `workersAll` prefix invalidation covers the new key. `directory.schemas.ts` · `directory.service.ts` · `hooks/api/directory/workers.ts` · `person-detail-page.tsx` · frontend typecheck ✓ · backend prod typecheck ✓.
- Backend review fixes (org-hierarchy + directory): unit updates spread the RAW stored `metadata` instead of rebuilding known keys — a branch/location/team edit no longer wipes other modules' keys (`hrContactUserId` from HR branches, `city/state/country/postalCode` from the HR org catalog); branch writers cross-invalidate both cache families (`orgUnits:*BRANCH` ↔ `branches:list:*`); `GET /directory/people/:id` no longer reconciles identity (no writes in a GET — link repair stays on write paths; dead `reconcileIdentity` param removed); `ensurePersonForMember` 23505 race re-reads and returns the winner instead of 409; `memberUserId` validated per the users-id convention (`trim().min(1).max(128)` — user ids are not UUIDs); `!` assertions replaced with narrowing. `org-hierarchy-{branches,locations,teams}.service.ts` · `branches.service.ts` · `directory{,-identity}.service.ts` · `directory.schemas.ts` · backend prod typecheck ✓.
- Org-hierarchy family fixed end-to-end (3 parallel audits → FE+BE fix lanes): (1) edit-not-saving — cleared fields became `undefined` and `JSON.stringify` dropped them from PATCH bodies (200 + toast, nothing persisted); all four pages now send explicit `null`, update DTO schemas accept `.nullable()`, services normalize null (metadata key-removal / column NULL). (2) "—" despite data — dept/branch/location list endpoints returned raw `org_units` rows (`parentId`, `metadata` blob) against typed frontend contracts; all five services now DTO-map responses (teams `toOrgTeam` precedent). (3) Columns added — branches: BU/Manager/Phone/Email (+`postalCode` end-to-end, was absent every layer); departments: Head/Description; teams: Lead/Description; names resolve from cached org-members. Also: create-sheet form-id race guarded on all four pages, cost-center list gets `deletedAt` filter, hierarchy query-key factory drops trailing-`undefined` slots, teams invalidations narrowed. Hierarchy services+dto · 4 hierarchy pages · `org-hierarchy.ts` hooks · `query-keys.ts` · frontend typecheck ✓ · backend prod typecheck ✓ (4 pre-existing spec drifts in access/ai/payroll specs remain, untouched by this work).
- Organization settings loading boundaries un-shadowed: the async `organization/layout.tsx` guard suspended ABOVE its own segment, so every `/settings/organization/*` navigation showed the `/settings` Account-Settings skeleton instead of the per-route ones. Guard moved into 9 async page adapters (`requirePermission("settings:view")`), layout deleted, root client page relocated to `features/settings/organization/organization-settings-page.tsx`; structure loading action buttons h-9→h-8. `organization/*/page.tsx` · `organization-settings-page.tsx` · `structure/loading.tsx` · frontend typecheck.
- Directory person "Invite as member" opens `UserInviteDialog` in place with the person's work/personal email prefilled (new `defaultEmail` prop) instead of dead-linking to `?create=1` the users page never read; users page now honors `create=1` (lazy state init) so the quick-create "Invite user" entry auto-opens too. Fixed §11 trailing-`undefined` invalidations that made `/settings/directory` edit/delete (and worker create) never refresh lists: new `directory.peopleAll`/`workersAll` prefix keys, and mutations now write the server-confirmed row through the cache (`setQueryData`/`setQueriesData` patch detail + all cached list pages; delete decrements pagination + marks stale `refetchType:"none"`; engagement create/terminate patch the array) — refetch invalidation only where placement is unknowable (person/worker create lists). `person-detail-page.tsx` · `user-invite-dialog.tsx` · `users-page.tsx` · `hooks/api/directory/{people,workers}.ts` · `query-keys.ts` · frontend typecheck.
- Pre-commit review fixes: MFA-enforcement redirect restored in the authenticated layout after the wizard gate (global backend `MfaGuard` 403s otherwise had no frontend handler); `/settings/incoming-transfer` server-gated on `ownership:transfer:respond` via thin adapter + feature component; suspended-access card conformance (`LoadingButton`, named handlers, extracted `OrganizationRow`); bulk Restore now confirms like Suspend/Archive; frontend permission catalog mirrors backend `billing`/`directory` files so `billing:ai-credits:view` + directory/workforce keys appear in `/settings/roles`; delegations query keys moved into the `queryKeys` factory. frontend typecheck.
- Settings skeleton conformance pass (4 parallel lanes, audit-first): every `/settings/*` loading state is now a structural Xerox of its real page. Billing: shared `BillingPageSkeleton` (full Plan-tab mirror) unifies loading.tsx + Suspense fallback + `PlanTabSkeleton`; ai-credits loading rebuilt with real headers + `DataTableSkeleton` (7/5/8 cols, 20 history rows). Organization: 8 per-child loading files (correct titles, filter/action placeholders, true column counts; chart=tree rows, structure=link-card grid); root's two skeleton phases share `OrgSettingsSectionsSkeleton`. Roles: 5-col stat grid, filters placeholder, phantom action removed, `RoleEditorSkeleton` extracted to feature + reused by loading.tsx, members `Loader2` spinner → Skeleton, new audit/simulate loading files. Core: root cards get real chrome + MFA block, api-tokens flat bars → `DataTableSkeleton`, audit-log 4-filter row + `flex-1`, delegations/modules phase alignment, new users/webhooks/directory/incoming-transfer loading files, directory skeleton 5→6 cols; the Workers skeleton now follows its operational `/directory/workers` owner. `billing-page-skeleton.tsx` · `org-settings-skeleton.tsx` · `role-editor-skeleton.tsx` · ~30 files · frontend typecheck.
- Session-state hardening: live account deactivation signs out at the server gate; an organization-only suspension keeps the identity valid and resolves to `/access-suspended`, where data-safe recovery can recheck access or explicitly switch to another ACTIVE membership. The suspended selected org remains the recovery context—siblings are never selected silently—and org creation is intentionally unavailable there. Identity-scoped RLS reads now protect setup and cross-organization recovery; setup refuses stale suspended claims, and restoration makes the org available while selecting it only when no other valid current org exists. `auth.service.ts` · `auth-tokens.service.ts` · `jwt-auth.guard.ts` · `org-setup.service.ts` · `org-membership.service.ts` · `org-profile.service.ts` · auth/session UI · focused specs + frontend/backend typecheck.
- Administration route ownership: canonical settings navigation stays under `/settings/*` for members, the administrative directory, organization hierarchy, platform billing, and AI credits. Operational Workers belongs to the Home Company surface at `/directory/workers`; `/directory` and `/settings/directory` still share base-path-aware Directory components. Old moved routes are deleted outright rather than kept as redirects. Route-ownership rules are enforced in `CLAUDE.md` and `UI-UX-SYSTEM.md`. `sidebar-nav-items.ts` · settings route adapters · shared feature pages · route tests + frontend typecheck.
- `ERR_TOO_MANY_REDIRECTS` root-caused: wizard gating (`org-setup` / `employee-onboarding`) ran in two places from two sources — stale-JWT claims in `proxy.ts` vs live session in the server layouts — so any JWT↔DB disagreement produced opposing redirects. Now one live-session predicate (`resolveWizardGate`) drives all three layouts (`(authenticated)`, `/org-setup`, `/employee-onboarding`); proxy keeps authN + path aliases only. `lib/wizard-gate.ts` · `lib/onboarding-gate.ts` · `proxy.ts` · the three layouts · frontend typecheck.

**2026-08-06**
- `/organization` Structure hub: removed duplicate top StatCard row (counts already in Reporting hierarchy / Places & finance). `organization-structure-page.tsx` · frontend typecheck.
- Backend startup restored by isolating attendance policy evaluation in a narrow module boundary, removing the Calendar -> HR Time -> Automation -> AI -> Calendar import cycle while preserving existing HR Time consumers. `attendance-policy.module.ts` · `calendar.module.ts` · `hr-time.module.ts` · backend typecheck + production emission + startup smoke.
- Universal member access review: Home, My Documents, and Knowledge Base reading remain available for every active organization member, including custom roles with no grants. Typed baseline scopes drive effective RBAC and the role editor; backend core-module metadata drives locked “Included” controls; targeted module-access reads validate membership; request schemas are boundary-validated; document ownership, KB content ACLs, and management permissions remain enforced. Access/RBAC services · permission catalogs · module-access controller/DTO · sidebar/product navigation · permission matrix · user module access · frontend + backend typecheck.
- Org Structure hub is the base `/organization` route (sidebar Structure → `/organization`); `/organization/structure` redirects there. `organization/page.tsx` · `organization/structure/page.tsx` · `sidebar-nav-items.ts` · frontend typecheck.
- Dialog density pass (shared): AlertDialog `p-4 gap-3`, Dialog title `text-base` / header `gap-1`, AppDialog zones `px-4`, compact simple dialogs (create-group, rename-role, tokens, signature pad, member dialogs, workspace/board/save-view/decide/decline/exception, etc.). Account `/settings` Profile+Security card chrome; MFA nested subsection. `dialog.tsx` · `alert-dialog.tsx` · `app-dialog.tsx` · `settings/page.tsx` · `mfa-settings.tsx` · `settings-security.tsx` · dialog call sites · frontend typecheck.
- Module Access (`/hr/access`, `/crm/access`, `/build/access`, …): Roles/Members actions (New group / Add member) sit inline on the tabs row via shared `ModuleAccessPage`; Ownership tab fills page body height (`TABS_CONTENT_PAGE_BODY_CLASS` + `CONTENT_FILL_PANEL`). `module-access-page.tsx` · `ownership-section.tsx` · `roles-tab.tsx` · `module-members-tab.tsx` · frontend typecheck.
- Unified calendar attendance is policy-aware: canonical holidays, employment start, roster/weekly-off rules, approved leave/WFH precedence, multi-session net hours, stored/derived Present · Half day · Late · Absent states, and past-day missing-attendance exceptions; `/me/attendance` History now uses server pagination (10/20/50) instead of the ten-record status payload. `calendar-events-aggregate.service.ts` · `attendance-policy.service.ts` · `daily-history-table.tsx` · attendance history API · frontend + backend typecheck.
- Upload Document sheet: document-type select restores options (controlled value, sheet pointer-events, loading/empty/error + self-upload fallback from my docs). `upload-doc-sheet.tsx` · `select.tsx` · frontend typecheck.
- `/hr/announcements` card redesigned: denser padding, inline meta (date · audience · reads), title-case status chip, expand only when content is long. `announcement-card.tsx` · frontend typecheck.
- `/dashboard` card rows regain vertical gap: deferred widget stack uses `flex flex-col gap-4`, in-row grids use `gap-4`. `deferred-dashboard-content.tsx` · `dashboard-client.tsx` · frontend typecheck.
- `/me/attendance` Today timer redesigned: status chip, clearer digit clock, one primary CTA per state (check-in / check-out / resume), quieter secondary break action; Work/Break totals as twin metric tiles with icons and active-state tint. `check-in-button.tsx` · `my-attendance-page.tsx` · frontend typecheck.
- Organization switcher now eagerly loads memberships with the authenticated shell instead of waiting for interaction; `/calendar` supplements its aggregate feed from the deployed self-attendance monthly API and deduplicates by date for rollout-safe attendance visibility. `org-switcher.tsx` · `use-attendance-calendar-events.ts` · `calendar-view.tsx` · frontend typecheck.
- `/me/pay` tab panels (bank empty + all ESS skeletons/empties) fill remaining height via `h-full` tab chain and `PAGE_BODY_*` surfaces; attendance/announcements tab panels match. `me-page.tsx` · `ess-*-section.tsx` · `content-fill-panel.tsx` · frontend typecheck.
- ESS/HR page bodies keep a full-width `bg-card` surface for empty, skeleton, and list states (`PAGE_BODY_EMPTY_CLASS` / `PAGE_BODY_SKELETON_CLASS`): `/me/attendance` · `/me/expenses` · `/me/pay` · `/me/documents` · `/me/time-off` · `/hr/announcements` · `/directory`. `content-fill-panel.tsx` · page components · frontend typecheck.
- `/calendar` list/history empty: full-height centered EmptyState with calendar illustration. `calendar-events-panel.tsx` · frontend typecheck.
- Attendance calendar consolidated into `/calendar`: `/me/attendance` now keeps only Today + History with an Open Calendar action; the unified feed adds a persistent Attendance source, removes duplicate HR feed types, and scopes tasks/interviews/attendance to the signed-in employee. `my-attendance-page.tsx` · `calendar/*` · `calendar-events-aggregate.service.ts` · frontend + backend typecheck.
- `/me/time-off` WFH/Leaves empty states fill remaining tab body height (`h-full` flex chain through TabsContent). `wfh-tab-content.tsx` · `leaves-tab-content.tsx` · `leaves-wfh-content.tsx` · `empty-state.tsx` · frontend typecheck.
- `/me/pay` tab actions lifted to the tabs row (year filter, Submit Claim, tax Edit/Submit, Request Loan, Bank Update, FNF download); sections support `hideToolbar` + controlled sheets. `me-page.tsx` · `ess-*-section.tsx` · frontend typecheck.
- `/me/pay` layout: title → stats → tabs → content; honesty note folded into subtitle; action alerts as compact chips; year filter on Payslips tab row; no repeated section heading. `me-page.tsx` · `ess-payslips-section.tsx` · frontend typecheck.
- `/me/attendance` redesigned: tab-level actions (correction / month nav / export+email), stripped nested card headers, empties fill height; HR attendance keeps chrome via default props. `my-attendance-page.tsx` · `check-in-button.tsx` · `attendance-calendar.tsx` · `daily-history-table.tsx` · frontend typecheck.
- ESS list empties (time-off, expenses, pay, documents, announcements): removed wrapping cards so empty/list body fills remaining height. `leaves-tab-content.tsx` · `wfh-tab-content.tsx` · `expense-list.tsx` · `ess-*-section.tsx` · `my-documents-page.tsx` · frontend typecheck.
- `/me/expenses`: removed summary StatCards and nested list Card chrome; empty fills the page body. `my-expenses-page.tsx` · `expense-list.tsx` · frontend typecheck.
- `/me/time-off` (and HR leaves): removed Request History / WFH card headers; Export + WFH status filter sit inline with tabs. `leaves-wfh-content.tsx` · `leaves-tab-content.tsx` · `wfh-tab-content.tsx` · frontend typecheck.
- `/me/pay` redesigned to Settings pattern: removed redundant section `h2` titles on every ESS tab; overview stat cards only on Payslips; tab panels fill height with proper empties. `me-page.tsx` · `ess-*-section.tsx` · frontend typecheck.
- Home navigation excludes Build-owned Tasks and Projects; added `/me/recruitment` for assignment-only interview actions, join links, paginated history, and one-time scorecard feedback without exposing candidates, jobs, pipelines, or HR recruitment settings. `sidebar-nav-items.ts` · `my-recruitment-page.tsx` · `employee-recruitment.controller.ts` · frontend + backend typecheck.

**2026-08-05**
- Notifications never worked: `notifications`/`notification_deliveries`/`notification_queue` held **0 rows**. `TenantContextInterceptor` wraps each request in one transaction, so every `void dispatch.emit(...)` ran on a handle that had already committed and lost its tenant GUC — RLS denied it `42501`, and `.catch(() => undefined)` hid it at all ~50 call sites. Added `registerAfterCommit` (interceptor drains hooks only on success), `emit` defers / `emitNow` opens its own tenant transaction, deferred failures are logged. Recipient reads moved inside the live transaction; `expireStaleTransfers` swept per-org via `forEachOrg`. `tenant-context.ts` · `tenant-context.interceptor.ts` · `notification-dispatch.service.ts` · ownership/invitation/membership notify paths · verified against the live DB + 5 regression specs.
- Circular dependencies removed repo-wide, `madge --circular` now 0/0 (was 9 backend, 8 frontend). New: `db/drizzle.types.ts` (`Db`/`TenantTx` out of the module file), `accounting/posting/accounting-posting.module.ts`, `ai/core/gateway/ai-gateway.module.ts`, `hr/time/hr-time-ledger.module.ts`, and 6 frontend `*-schema.ts`/`*-types.ts` extractions. All 4 `forwardRef` pairs deleted; `bugs` merged into `qa.ts` (mutual FK); CRM runner's unused `bus` injection dropped. tsc clean both repos.

- Home navigation rebuilt around universal employee needs: Overview · Communication · For Me · Company; interviews and admin document destinations removed from Home. Added module-independent `/me/time-off`, `/me/attendance`, `/me/expenses`, `/me/pay`, `/me/documents` pages backed by JWT-self-scoped `self:*` APIs; private document upload, attendance correction, editable own expense claims, and core leave-policy provisioning now work, while HR/payroll/finance administration stays module-gated. `sidebar-nav-items.ts` · `employee-*-controller.ts` · `employee-self-service/*` · HR/payroll hooks/controllers · frontend + backend typecheck.
- Employee onboarding now preloads HR-provided personal/address/emergency, encrypted bank, and statutory data; employee drafts win, each step explains the prefill, and the final review shows masked values with Edit actions before confirmation. Self-only endpoints use authenticated active membership plus tenant/user-scoped queries instead of HR task permissions; draft navigation/success UI waits for confirmed persistence with explicit load/save failure states. Writes synchronize member + canonical HR records under tenant membership scope. `onboarding-details.service.ts` · `onboarding.controller.ts` · `employee-onboarding/page.tsx` · `step-review.tsx` · frontend + backend typecheck.
- Org/access notifications wired: 11 new `SECURITY` catalog events (`ownership.transfer.*`, `ownership.module_owner.changed`, `organization.invitation.*`, `organization.member.reactivated|left`) — the whole ownership/invitation/membership domain previously emitted nothing. Email only where access changes or the recipient is unreachable in-app (`filterOrgMemberIds` is ACTIVE-only, so removed/suspended members get a direct email, not dispatch). `notification-events.catalog.ts` · `org-admin-recipients.ts` · `templates/organization.ts` · `email-senders.base.ts` · tsc frontend + backend typecheck · 11 focused specs.
- Invitation decline built — the `DECLINED` enum value, `invitations.declined_at`, and the `DECLINED` invitation event were all unreachable, and the UI's "Decline invitation" button only did `router.push("/signin")`. `POST /organization/invitations/decline` (`@Public()`, status-predicated update + affected-row check) + a confirm dialog on `/invitation/[token]`. `invitation-acceptance.service.ts` · `organization.controller.ts` · `organization.schemas.ts` · `invitation/[token]/page.tsx` · `auth-hooks.ts` · `api-client.ts` · 5 decline specs.
- §9 splits: `ownership.service.ts` 892 → 222/399/469 (records · transfers · transfer responses) + `ownership-members.helper.ts`; `invitations.service.ts` 1133 → 419/152/217/426 (send · read · lifecycle · acceptance) + `invitations.helpers.ts`. Controllers/module wiring/specs follow. **`org-membership.service.ts` is 732 and still over the line** — pre-existing 620 plus this pass's notices; split not attempted (10 consumers).
- `CronOrganizationService.expireStaleInvitations` now records the `EXPIRED` invitation event and notifies the inviter; deleted the never-called duplicate that lived on `InvitationsService`. `cron-organization.service.ts`.
- `expireStaleTransfers` had **no caller** — pending ownership transfers never expired (only lazily, when a recipient tried to accept a stale one). Added `GET|POST /cron/ownership-transfer-expiry` beside the invitation-expiry job. `cron-platform.controller.ts` · `cron.module.ts`.

**2026-08-04**
- Platform domain redesign slice 7: module-boundaries close-out — offer-fulfillment API requires CRM+inventory (`ModuleGuard` fix + array `@RequireModule`); party/inventory-vendor/payroll-without-HRMS contracts documented; schema-inventory expanded (composite-FK + RLS key spine, compatibility-authorities, module table counts); forward-repair runbook expanded (0392/0393 repair + telemetry). `offer-fulfillment.controller.ts` · `module.guard.ts` · `module-boundaries-contracts.md` · `compatibility-authorities.md` · tsc frontend + backend typecheck + focused specs.
- Platform domain redesign slice 6: access-convergence close-out — payroll period-summary scope gate (`timesheets:payroll:view` scopable); onboarding-views admin widener gates (`hr:onboarding:manage` scopable); portal grant create validates ACTIVE membership + project/workspace alignment; portal JWT blocked on internal API (spec); forward-repair runbook stub + portal/PM matrix rows. `payroll-summary.service.ts` · `onboarding-views.service.ts` · `portal-access.service.ts` · `forward-repair-runbook.md` · tsc frontend + backend typecheck + focused scope specs.
- Platform domain redesign slice 5: worker-only payroll lifecycle (`0393` payslip/batch/TDS worker subject); shared `loadRunEmployeePayees` for publish/payout/filings/validation; `/payroll/workers/[workerId]` salary detail + list row routing; payroll run inputs list scope gate (`payroll:runs:view`). `0393_payroll_lifecycle_worker_subject.sql` · `payroll-run-payee.ts` · `publishing.service.ts` · `payout-batches.service.ts` · `filings.service.ts` · `worker-detail-page.tsx` · tsc frontend + backend typecheck + 16 focused specs.
- Platform domain redesign slice 4: worker-keyed payroll schema (`0392_payroll_worker_subject`); `/payroll/workers/:workerId/profiles` API; salary-profile picker login-less payees; timesheets-core + build team timesheets + HR performance expiry fail-closed; PM workspace project deep routes + legacy `/build/[projectId]/…` redirect preserving query; composite-FK matrix rows for salary profiles/run employees. `0392_payroll_worker_subject.sql` · `worker-profiles.controller.ts` · `timesheets-core-scope.ts` · `build/workspaces/[pmWorkspaceId]/[projectId]/*` · tsc frontend + backend typecheck + 29 focused specs.
- Platform domain redesign slice 3: payee eligibility without org membership (`assertPayrollPayeeEligible`); payroll salary-profile UI payee/worker copy when HRMS off; CRM churn-alerts + renewals scoped; Ask OS copilot team payroll fail-closed; PM Workspace path switcher `/build/workspaces/[pmWorkspaceId]/…` + server-validated layout; `worker_engagements` matrix rows. `payroll-payee-eligibility.ts` · `clients.service.ts` · `client-accounts.service.ts` · `ops-copilot-tools.ts` · `pm-workspace-context-chip.tsx` · `pm-workspace-path.ts` · tsc frontend + backend typecheck + scope specs.
- Platform domain redesign slice 2: `/directory/[personId]` detail tabs (Profile · Membership · Worker · Modules); scope fixes for CRM client accounts list/detail + feedbucket stats (`applyScope`); PM Workspace multi-workspace switcher; tenant “Workspace” copy cleanup (Ask OS, All Work loading). `person-detail-page.tsx` · `client-accounts-scope.ts` · `feedbucket-stats-scope.spec.ts` · `pm-workspace-context-chip.tsx` · tsc frontend + backend typecheck.
- Platform domain redesign slice 1: Wave 0 docs (`wave-0-reconciliation.md`, `composite-fk-matrix.md`, `rls-matrix.md`); billing orgId repair documented (`0370`); OrganizationSwitcher + Modules/Product Management terminology; `/portal` chromeless client portal + `/build/portal` redirects. `workspace-switcher.tsx` · `organization-switcher.tsx` · `product-switcher-menu.tsx` · `sidebar-nav-items.ts` · `dashboard-shell.tsx` · `global-header.tsx` · `portal/*` · tsc frontend + backend typecheck.

**2026-08-03**
- `validation-residual-audits` closed for RBAC/access hardening: residual adversarial pass found **zero new P0/P1** (search DataScope, cross-module role bind, delegation grantability, module-enablement-before-owner, MFA fail-closed, org members gate, invite concurrency, seat limits, org-scoped eviction, FE focus/poll/fan-out, org security UI, simulate/audit gates all verified). Test-only fixes: audit implied-view diff expectation, groups `selectDistinct` mock, access-regression `useSession` mock, module-access e2e `version` on permission PUTs. FE `tsc --noEmit` exit 0 · BE `typecheck` exit 0 · FE focused 6/6 suites 21/21 · BE unit 16/16 suites (authorize/delegations/module-access/invitations/org lifecycle/MFA/entitlements/catalog) · e2e invitations 9/9 + module-access 42/42 · migrations `0117`/`0118` journaled (not applied). Lint/full suite not run.
- RBAC/access hardening plan closed (validation + residual audits): authz spine (search DataScope, module-access cross-module 404, delegations grantability, ModuleGuard before owner bypass, MFA fail-closed, seat/invite concurrency, org lifecycle tx, ACTIVE-only module members) · FE query budgets (global no focus-refetch; simulate keys org-scoped; deferred dashboard; org security controls; catalog sync) · migrations `0117`/`0118` journaled lock-safe · version-bump now also prunes `rbac:*` + `module-access:*` Redis keys. Residual P1 fixed in validation: focus-refetch opt-ins removed from chat/dashboard hooks, simulate query keys carry `orgId`, access.service bump pruning + tenant-tx test mocks, module-access groups `selectDistinct` test chain. `access.service.ts` · `cache-keys.ts` · `simulate.ts` · `query-keys.ts` · `dashboard.ts` · `chat.ts` · `0117`/`0118` · `PAGES.md` · FE tsc + BE typecheck · focused tests FE 6 suites / 17 passed · BE 15 suites / 177 passed.
- Org archive/restore path: archive returns ACTIVE sibling `nextOrgId` and soft-revokes owner only · `POST /organization/restore` is `@AllowNoOrg` + body `orgId` (owner+ARCHIVED verified) · `GET /organization/archived` · `resolveOrCreateOrg` ignores ARCHIVED · JwtAuthGuard degrades inactive org on `@AllowNoOrg` · FE routes archive→dashboard/org-setup and restores from org-setup + workspace switcher (lying danger-zone Restore removed). `org-lifecycle.service.ts` · `organization.controller.ts` · `org-setup.service.ts` · `jwt-auth.guard.ts` · `organization.ts` · `archived-orgs-restore.tsx` · `org-danger-zone-section.tsx` · `workspace-switcher.tsx` · `step-welcome.tsx` · tsc frontend + backend typecheck.
- Membership-cache / role / leave-CTA residual gaps: `createUser`/`updateUser`/`bulkUpdateUsers`/`settings.updateUserRole`/invite `accept`/HR onboard bust membership status · `updateMemberRole` permission → `settings:organization:manage` · Leave menu requires explicit `canLeave` + dialog waits for `isOrgOwner === false`. `users.service.ts` · `user-ops.service.ts` · `settings.service.ts` · `invitations.service.ts` · `organization.controller.ts` · `employee-onboarding.service.ts` · `leave-organization-control.tsx` · `workspace-switcher.tsx` · tsc frontend + backend typecheck.
- Leave/role spine residual gaps: `useLeaveOrg`/`useDeleteOrg` suppress auto-signout (same as switch) and no longer invalidate before session `update()` · `updateMemberRole` busts membership status cache · Leave CTA hidden until `access.isOrgOwner === false`. `organization.ts` · `org-membership.service.ts` · `workspace-switcher.tsx` · `leave-organization-control.tsx` · `org-danger-zone-section.tsx` · tsc frontend + backend typecheck.
- Leave/session spine gaps: MEMBER Leave CTA in WorkspaceSwitcher · `lastGoodSessionData` clears on intentional null-org · `leaveOrg` deletes `orgUnitMembers` + bumps perms · membership status cache moved to Redis with explicit bust · `useOrgSettings` gated on `settings:view`. `workspace-switcher.tsx` · `leave-organization-control.tsx` · `auth.ts` · `org-membership.service.ts` · `membership-state.service.ts` · `organization.ts` · tsc frontend + backend typecheck.
- Leave-org spine: `leaveOrg` busts membership cache + revokes org agent tokens, picks ACTIVE-only `nextOrgId`, Leave CTA no longer gated on `settings:manage`, INVITED denied in `MembershipStateService.resolve`, `fetchProfile` ACTIVE-only. `org-membership.service.ts` · `membership-state.service.ts` · `org-profile.service.ts` · `org-danger-zone-section.tsx` · tsc backend+frontend.
- Session slimmed to identity — permissions/MFA moved to `/me/access`; the NextAuth session no longer carries a second, staler RBAC copy.
- `hasDashboardAccess` removed end-to-end — every member reaches Home; what they see is RBAC + module enablement, not a per-user flag.
- MFA enforcement moved from `proxy.ts` to the backend — it was a JWT claim read only by the client redirect, so "require 2FA" was bypassable.
- Access token stripped to identity; ownership/role resolved from the DB — `isOrgOwner` came verbatim off the token and short-circuited `authorize()` to full org access.
- Logout actually revokes — `SessionService.revoke` was invalidating a cache key nothing read.
- Personal access tokens conform to the agent-token pattern (were bcrypt-per-candidate over an unindexed prefix scan).
- Client token cache reads the real `exp` instead of a hardcoded 540s.

**2026-08-02**
- Header account avatar opens on click/tap only (desktop hover-to-open removed). `components/layout/header/user-avatar-menu.tsx`.

**2026-08-01**
- **RBAC/entitlements engine + auth guards were entirely unwrapped under RLS.** Guards run before interceptors in NestJS, so `TenantContextInterceptor` hasn't set the GUC when a guard's own queries run — `AccessService`/`EntitlementsService` had zero tenant wrapping. Masked because `authorize()` short-circuits for org owners before touching the DB; the first non-owner on a cache miss 500s. Fixed by wrapping every public `orgId` entry point in `runInTenantTransaction`. Also fixed: `JwtAuthGuard.fetchOrgContext` (empty rows → false "Organization not found", the likely cause of invite failures right after signup), `AgentTokenGuard`, and 2 cron discovery queries → `forEachOrg`. Migrations `0384`–`0387`.
- **Every `@Public()` token flow was dead under RLS** — 28 routes across 16 controllers 500'd on their first SELECT. Canonical fix: two-phase resolution via `withPublicToken` (sets only `app.public_token`, reads the row's `org_id`, re-enters through `runInTenantTransaction`). Migration `0384` adds a second `USING` arm on 19 token tables; `WITH CHECK` stays strict (a token can FIND an org, never write into one). Guessable serial ids (`csat_surveys.id`, `survey_response_sessions.id`, public-intake `projects.id`) deliberately got narrow SECURITY DEFINER resolvers instead (`0385`/`0386`) rather than a policy arm that would expose every column.
- **Org creation broken by the RLS rollout** (P0, signup dead) — `TenantContextInterceptor` opens no transaction for a user with no workspace, so the `organization_members` insert died with 42501. Also: `runInTenantTransaction` opened the transaction but never established the ALS context; `UsersService.updateUserStatus`/`deleteUser` had no owner guard (delete left orgs permanently ownerless); HR onboard/link/update skipped the `org_unit_members` dual-write; FE `PERMISSIONS` shipped 2 of 20 `*:access:*` keys.
- `/dashboard` feature-coverage completion — every dashboard-tier module feature surfaced except Issues/Projects, with zero new UI primitives.
- `/org-setup` pass: Country→phone dial-code rewrite · launch-failure support mailto · status pill removed · horizontal-scroll fix · full-width step rail · invitee row density · invite UI alignment · dedicated `GenerationFailureStage` recovery screen · Country↔phone sync · goal-chip outcome tooltips.

**2026-07-30**
- **Module enablement is deny-by-default.** Three read-side fail-opens made every module look enabled while the real gate (`ModuleGuard`) failed closed — nav said "enabled", the API 403'd. One source of truth: `EntitlementsService.getEffectiveModuleMap` (core → on, row → its value, absent → off). Applies only to the 11 toggleable catalog keys. **Org owners are no longer exempt** (module enablement is tenant config, not a permission).
- Root contracts fixed: `SelectTrigger` gets `text-left` (was centering); `SearchInput` gets a root width contract (43 per-site overrides + 50 wrapper divs removed); `TabsList` = `w-full md:w-fit`, `TabsTrigger` = `flex-1 min-w-fit` (126 call sites de-overridden).
- ⚠️ Dead-code gotcha: bare side-effect imports (`import "@/lib/dom-mutation-guard";`) are invisible to `from`-based scanners — only `next build` caught it. Never prune a dir named `build` here (it is the Build module).
- Removed `/users/suspended` + `/users/archived` — status filtering stays on `/users`.
- Wiki moved to `/knowledge/wiki/…` with its own `WikiShell`; Ask KB stays at `/knowledge/chat`.

**2026-07-25**
- KB module complete: content-unchanged guard was hashing rejoined chunks vs raw source, so **every save re-embedded the whole article** (migration `0300` adds `content_hash`); `kb-pages.service.ts` split 782→455; backend security P0 (`resolveCanManage` arg-swap had killed locked-page enforcement); full end-to-end review closed.
- KB dead-code sweep: 7 orphaned Wiki-KB hook files, 15 dead query-key factories, `types/kb.ts` 418→250, orphaned `translate()` AI feature.
- Projects Members/Teams backend: workspace members → teams → M:N project↔team, with access inheritance (a team member is treated as a project member across `assertProjectAccess`, project list scope, and the ticket gate).
- HRMS structural wave (~25 parallel agents): ~30 oversized files split under the §9 cap, 152 `mutationKey`s added, 7 client-only HR pages converted to server components with `requirePermission`, 23 recruitment `loading.tsx` skeletons, 4 dead career/learning tables dropped.
- HRMS backend: performance module had 5 write endpoints gated only on `:view` and scoped by `orgId` alone (BOLA/priv-esc) · stale leave-analytics cache · unbounded `with:{reviews:true}` reads removed · shared `SemanticBadge` consolidates 7 hand-rolled badges · real server-side termination pagination.
- Projects overhaul Slice 0 — backend security + correctness.
- Neon transient-drop resilience (`ECONNRESET` retry) in payroll/notification workers.
- App theme scoped to `(authenticated)` — root layout no longer paints the shell theme on public routes.
- Huddle mic hard-blocked by `Permissions-Policy` (distinct from the 2026-07-19 realtime fix).
- `/settings/security` removed — org MFA / email-domain / session policy lives on `/settings/organization`.

**2026-07-24**
- HRMS + PayrollOS API-call audit: hidden-tab and closed-dialog fetches gated with `enabled`.
- People Hub polish; invitation onboarding docs removed.
- HR Settings A–Z full-chain contract verification (page → hook → endpoint → Zod both sides → permissions → states).
- Inbox: in-place preview, desktop-only two-pane, mobile master-detail with back, preview error recovery.

**2026-07-23**
- Access/RBAC visibility pass — sidebar / ⌘K / product switcher / danger zone derive owner status from `/me/access`, not the session claim. Global loading + module toggles.

**2026-07-22**
- **ProjectOS Linear-parity wave** (8 surfaces, parallel agents): My Issues rebuilt, Projects list display-properties + grouping, Inbox two-pane, Members, Drafts (new `comment_drafts`), Teams as a first-class entity, wiki-in-place, upload compression. Parity gaps closed via migration `0006` (member `lastSeen`+teams, project priority/health, `ticket.customer_id`, project-scoped wiki).
- `PageWrapper` no longer wraps `filters` in a mobile-only Filters popover — filters render inline at all breakpoints with horizontal scroll.
- Inbox list card redesign; Comment Drafts row redesign; Inbox preview responsive fix.

**2026-07-21**
- Unified single sidebar — the per-project nav nests inline under "Projects" in the one global `AppSidebar` (double sidebar killed).
- Settings → Profile display-name persistence (session callback never refreshed `name`) + 100-char cap.
- Labels create-on-Enter; `/users` Invite dialog stripped of 5 fields that were accepted but never persisted.
- Employee onboarding: canonical route `/employee-onboarding`, country-driven documents/bank/payroll catalog, per-step draft persistence, live People-profile brand column.

**2026-07-20**
- Landing `/` premium redesign + real logo mark; `/org-setup` layout flip, ceremonial launch stage, Modules step merged into Basics, draft + step resume, required phone.
- Org-setup "Organization not found" on Launch — generate ran before the org existed.
- Google login `ERR_TOO_MANY_REDIRECTS` after JWT soft-fail (proxy trusted the cookie the RSC path rejected).
- Login outage root-caused: backend was down/stale-dist, not a code bug. Auth flow hardened.
- Dead-file sweep across both repos (knip + import-graph): 13 frontend files + 2 route dirs.
- One customer email + env-driven branding; `/org-setup` contract hardening (`enabledModules` `.min(1)` against the allowlist).

**2026-07-19**
- `middleware.ts` → `proxy.ts` (Next 16 convention); frontend `app/api/` reduced to NextAuth only; legacy direct-OAuth `google-calendar` module deleted (Composio-only per §6).
- Onboarding routing + skip persistence (30-day gate cookie + claim refresh, replacing a 30-minute cookie).
- Huddle + chat realtime end-to-end fix — root cause was the `{success,data}` envelope passed verbatim through the auth-bridge.
- Unified auth entry (`/signup` → `/signin`); cellular login via first-party `api.streamlineos.in`; session capture restored (no `user_sessions` rows were being created); Electron device labels; landing mobile overflow + SEO.
- `/mail` shipped (Composio live proxy, zero mail tables); mobile chat chrome; quick-create menu; New Issue AI assist.

**2026-07-18**
- `PermissionGuard` denies guarded handlers with no permission metadata; explicit permissions evaluated before privileged bypasses; controller-wide metadata audit.
- Public pricing/legal copy matched to the Free-plan 5-member limit; `POST /public/contact` validated + rate-limited.
- Production passwordless redirect loop fixed (`__Secure-authjs.session-token` checked first).
- Feedbucket snapDOM R2 CSP + CORS fixes.

**2026-07-16**
- **UI/UX conformance mega-sweep** (10 module agents, ~190 files): `h-8`/`h-10`/`text-xs` control overrides stripped repo-wide for the h-9 canon.
- Animated-icon program (~300 files) + the new shared `AnimatedIconButton` primitive.
- Skeleton fidelity: all 287 `loading.tsx` rebuilt as a visual Xerox of their real page; shimmer; overflow tooltips.
- `/billing` consolidated (Plan · Invoices & Payments · Billing Profile); `/settings/subscription` and `/billing/seats` became redirects. Plan entitlements (FREE/PAID/ENTERPRISE) enforced at every limited create.

**2026-07-13/15**
- Selectable interface themes: Light/Dark/System × 18 accents, scoped to the authenticated shell; dark-theme conformance + Ink monochrome pass; neutral dark palette (navy → zinc).
- Fill-chain wave — content no longer ends mid-screen; Ask OS bar space reserved shell-side.
- Search conformance across ~70 surfaces (45 files fixed to the canonical debounced pattern).
- Table title overflow fix; flat filter toolbars; translucent panel-fill floor.
- Feedbucket AI triage (vision model, feedback → ticket).

**2026-07-11/12**
- CRM metadata-first program: migrations 0251–0255 applied, metadata seed backfilled for all 41 orgs, hardcoded stage/status constants deleted.
- Accounting & Finance program: 11 backend modules on a hardened `FinancePostingService` contract (balanced/immutable/period-locked/idempotent/multi-currency), 38 new tables.
- Inventory full-module overhaul (14 agents): 19 unreachable routes reachable, P0 contract fixes.
- HRMS PeopleOS rebuild (44-doc PRD): hardcoded HR rules refactored onto shared policy/workflow/automation/template engines; migrations 0201–0225.
- Verify-email auto-login + oversized session cookie (485 permission keys ≈ 11.4KB → chunked 5-cookie JWE); OAuth incomplete-onboarding signout loop.

**2026-07-02 → 2026-07-07**
- UI/UX system program — `UI-UX-SYSTEM.md` authored; CLAUDE.md §14 reconciled to the real ink-first system (violet gradients retired from the shell).
- Visual QA wave (Playwright, 375/768/1280, ~50 files fixed from real pixels); DataTable `search`/`toolbar` props removed so filters can never share the table card.
- Module-owned settings: `/settings/{integrations,automations,ai,custom-fields,data-hub,email-templates}` deleted or redirected into module settings.
- PM engine slices 1–11: QA/Test + first-class Bugs · Client Portal + Change Requests · Approvals · AI Project Manager · Risks & Decisions · Meetings/Standups · Incidents & SLA · Forms builder · Portfolios · Workflow transitions · Chat-connected actions.
- PM foundation harden: every mutating PM endpoint carries `@RequirePermission`; board no longer hydrates tickets through `GET /projects/:id`.
- Sales/CRM conformance program (34 pages, 95 files); Projects UI wave (22 pages); email system overhaul (63 templates); schema normalization wave 3 (`roles.permissions` JSONB removed → 23,650 grants backfilled); auth platform hardening wave 2 (typed `{ code, message, details? }` error contract); multi-org made first-class (`users.lastActiveOrgId`).

**Earlier**
- 2026-06-28 — Organization & Authorization completion pass (archive/restore across all org entities; `api-tokens` module; `maxConcurrentSessions`).
- 2026-06-16 — E2E hardening pass (Playwright, 105 routes × 3 viewports): currency `$`→`₹` app-wide, dead `/accounting/*/new` forms, missing create triggers on CRM lists, mobile line-item grids, empty states that didn't fill height.
- 2026-06-29/30 — Workspace Setup Wizard (`/setup`, 8 steps) with server-seeded persistence and step gating.

---

## Route inventory

> Verified against `frontend/app/**/page.tsx` on 2026-08-04 — every path below exists on disk, spelled in full so it is greppable.

### Shell & Home
- [x] `layout` — Activity Bar (48px icon rail) + sidebar (logo/workspace, `getNavGroupsForProduct` nav, search+bell, profile menu); `GlobalHeader` hidden below `md`
- [x] Mobile chrome — module bottom tabs (top 5 sidebar routes) + `MobileShellFab` (Ask OS · Menu · Search · Profile); Chat keeps `ChatMobileBottomNav`
- [x] Quick create menu — grouped (Comms/Work/CRM/People/Docs), each item `useCan` + module gated
- [x] `/dashboard` — Access-driven composition: `useDashboardAccess` gates every widget/stat/action by module enablement + RBAC; queries `enabled`-gated so non-permitted endpoints never fire
- [x] `/calendar` — The ONE calendar (month/week/day, single-row toolbar, toggleable module sources)
- [x] `/chat` · `/chat/channels` · `/chat/invite/[token]` — Chat with composer-aware mobile chrome
- [x] `/mail` — Unified Gmail+Outlook inbox via Composio live proxy; **zero mail tables**
- [x] `/ai` · `/ai/executive-brief` · `/ask` — AI assistant surfaces
- [x] `/notifications` · `/notifications/preferences` · `/notifications/broadcasts` · `/notifications/events` · `/notifications/policy` · `/notifications/providers` · `/notifications/templates`
- [x] `/settings/users` — **Members & access** workspace; authenticated organization members and permission-gated Invitations share one route via `?view=invitations`; status filtering stays URL-backed; org-scoped lifecycle SoT = `organization_members.status`; the old `/users` route is deleted
- [x] `/directory` · `/directory/[personId]` · `/directory/workers` · `/settings/directory` · `/settings/directory/[personId]` · `/parties` — shared **Person records** in Home and Administration, operational Workers in Home Company, and CRM Business Parties; base-path-aware detail/back links retain product context; `directory:people:*` and `workforce:workers:*` gates; HRMS or Payroll enables Workers

### Build (delivery + product management)

> Namespace is `/build`; `/projects` and `/product-management/**` redirect to `/build` (deep links preserved). `/build/[projectId]` is a project. `project` ≠ `managed product`. PM Workspace context chip in header when in Product Management module.

- [x] `/build` · `/build/all` — Project directory (inline Lead/Members assign via popover, full-field edit sheet) + legacy deep-link
- [x] `/build/[projectId]` — Board: dense toolbar, DnD jank fixed (CSS drag chrome, memoized columns/cards)
- [x] `/build/[projectId]/backlog` · `/build/[projectId]/sprints` · `/build/[projectId]/cycles` · `/build/[projectId]/cycles/[cycleId]` · `/build/[projectId]/epics` · `/build/[projectId]/milestones` · `/build/[projectId]/modules`
- [x] `/build/[projectId]/timeline` · `/build/[projectId]/views` · `/build/[projectId]/intake` · `/build/[projectId]/triage`
- [x] `/build/[projectId]/my-tickets` · `/build/[projectId]/tickets/[ticketKey]` — Board/List/Table via `?view=`; Jira-like 2-column detail
- [x] `/build/[projectId]/analytics` · `/build/[projectId]/reports` (velocity, burnup, CFD + daily snapshots) · `/build/[projectId]/budget`
- [x] `/build/[projectId]/settings` · `/build/[projectId]/workflow` · `/build/[projectId]/automations` · `/build/[projectId]/webhooks` — Editable workflow statuses (inline CRUD, optimistic), WIP limits, from→to transition rules
- [x] `/build/[projectId]/qa` · `/build/[projectId]/qa/runs/[runId]` · `/build/[projectId]/bugs` — Test management + first-class bugs
- [x] `/build/[projectId]/approvals` · `/build/[projectId]/risks` (probability×impact heatmap) · `/build/[projectId]/decisions` — Governance
- [x] `/build/[projectId]/meetings` · `/build/[projectId]/meetings/[meetingId]` — Meetings, standups, action items
- [x] `/build/[projectId]/incidents` · `/build/[projectId]/incidents/[incidentId]` — Severity + SLA timers with breach chips
- [x] `/build/[projectId]/forms` · `/build/[projectId]/forms/[formId]` — 11 field types, create-task/bug actions, submissions
- [x] `/build/[projectId]/change-requests` · `/build/[projectId]/client-portal` — 8-state CR workflow + per-ticket visibility toggles
- [x] `/build/[projectId]/whiteboard` · `/board/[shareToken]` — Excalidraw scenes; private/project/public; rate-limited public token endpoints
- [x] `/build/[projectId]/feedbucket` · `/build/[projectId]/feedbucket/[submissionId]` — Widget setup + vision-model feedback→ticket triage
- [x] `/build/[projectId]/releases` · `/build/[projectId]/chat` · `/build/[projectId]/ai` (6 capability cards, `projects:ai:use`) · `/build/[projectId]/wiki` · `/build/[projectId]/wiki/[pageId]`
- [x] `/build/my-work` · `/build/all-work` · `/build/approvals` · `/build/inbox` · `/build/drafts` · `/build/members` · `/build/customers`
- [x] `/build/teams` · `/build/teams/[teamId]` — First-class teams (members + projects, M:N to projects, access inheritance)
- [x] `/build/command-center` · `/build/portfolios` · `/build/portfolios/[portfolioId]` · `/build/programs` · `/build/templates` · `/build/roadmap`
- [x] `/build/goal` · `/build/goal/[goalId]` — Goals & OKRs
- [x] `/build/managed-products` · `/build/managed-products/[managedProductId]` — Managed Products (`projects:managed-products:*`)
- [x] `/build/pm-workspaces` — Container list, default-workspace delete guard, members sheet
- [x] `/build/portal` · `/build/portal/[projectId]` — Client portal (redirects to `/portal/**`; legacy paths preserved)
- [x] `/portal` · `/portal/[projectId]` — Client portal (chromeless shell: no module/org switcher sidebar)
- [x] `/build/settings/integrations` — Connections · Agent access (GitHub); `/agent-tokens` + `/agent/v1/*`
- [x] `/build/access` — Module access management

### CRM
- [x] `/crm` · `/crm/access` · `/crm/inbox` · `/crm/activities` · `/crm/tasks` · `/crm/analytics` · `/crm/reports` · `/crm/calendar`
- [x] `/crm/leads` · `/crm/leads/[leadId]` · `/crm/leads/distribute` · `/crm/leads/duplicates` · `/crm/leads/smart-search` · `/crm/leads/source-report`
- [x] `/crm/deals` · `/crm/deals/[dealId]` · `/crm/deals/aging` · `/crm/deals/approvals` · `/crm/deals/forecast` · `/crm/deals/win-loss` — Metadata-driven stages, optimistic moves, blueprint transitions
- [x] `/crm/contacts` · `/crm/contacts/[contactId]` · `/crm/companies` · `/crm/companies/[companyId]` · `/crm/clients` · `/crm/clients/[clientId]`
- [x] `/crm/quotes` · `/crm/quotes/[quoteId]` · `/crm/campaigns` · `/crm/campaigns/[campaignId]`
- [x] `/crm/settings/pipelines` · `/crm/settings/options` · `/crm/settings/blueprints` · `/crm/settings/territories` · `/crm/settings/validation-rules` · `/crm/settings/pricebooks` · `/crm/settings/products` · `/crm/settings/quotes` · `/crm/settings/custom-fields`
- [x] `/crm/settings/assignment-rules` · `/crm/settings/scoring-rules` · `/crm/settings/sla` · `/crm/settings/sequences` · `/crm/settings/email-templates` · `/crm/settings/data-quality`
- [x] `/crm/settings/automations` · `/crm/settings/automations/new` · `/crm/settings/automations/[automationId]` — Rules + DnD builder; depth guard + cooldown; cron flush
- [x] `/crm/settings/ai` · `/crm/settings/audit-log` · `/crm/settings/import-export`

### HR
- [x] `/hr` · `/hr/access` · `/hr/approvals` · `/hr/analytics` · `/hr/workforce` · `/hr/event-stream` · `/hr/service-delivery` · `/hr/simulator`
- [x] `/hr/employees` · `/hr/employees/[employeeId]` · `/hr/employees/find-expert` · `/hr/employees/skills-matrix` — 360 profile (timeline, gated sensitive tab, change governance)
- [x] `/hr/org` · `/hr/org-chart` · `/hr/identity` · `/hr/positions`
- [x] `/hr/attendance` · `/hr/shifts` · `/hr/rosters` · `/hr/overtime` · `/hr/comp-off` · `/hr/biometric` · `/hr/geofencing` · `/hr/work-logs`
- [x] `/hr/leaves` · `/hr/leaves/analytics` · `/hr/leave-policies` · `/hr/holidays`
- [x] `/hr/onboarding` · `/hr/onboarding/[userId]` · `/hr/onboarding/my-tasks` · `/hr/onboarding/probation`
- [x] `/hr/exit` · `/hr/termination` · `/hr/fnf` · `/hr/asset-returns` — Exit lifecycle with asset-recovery gate
- [x] `/hr/performance` · `/hr/performance/analytics` · `/hr/goals` · `/hr/kpis` · `/hr/feedback`
- [x] `/hr/engagement` · `/hr/announcements` · `/hr/handbook` · `/hr/helpdesk` · `/hr/cases` · `/hr/safety` · `/hr/emergency`
- [x] `/hr/documents` · `/hr/documents/templates` · `/hr/documents/templates/new` · `/hr/documents/templates/[templateId]/edit` · `/hr/documents/editor/new` · `/hr/documents/editor/[documentId]`
- [x] `/hr/document-review` · `/hr/document-types` · `/hr/email-templates`
- [x] `/hr/assets` · `/hr/devices` · `/hr/expenses` · `/hr/reimbursements` · `/hr/travel` · `/hr/travel/approvals`
- [x] `/hr/compliance` · `/hr/background-verification` · `/hr/contingent` · `/hr/benefits` · `/hr/retention` · `/hr/legal-holds` · `/hr/delegations` · `/hr/labor-relations` · `/hr/accommodations`
- [x] `/hr/compensation-planning` · `/hr/equity` · `/hr/workforce-cost`
- [x] `/hr/settings` — No-code config hub · `/hr/settings/policies` (15 types, scoped, effective-dated, versioned) · `/hr/settings/workflows` · `/hr/settings/automations` · `/hr/settings/templates` · `/hr/settings/versions` · `/hr/settings/preview`
- [x] `/hr/settings/forms` · `/hr/settings/forms/[formId]` · `/hr/settings/forms/[formId]/submissions` · `/hr/settings/custom-fields` · `/hr/settings/company` · `/hr/settings/integrations` · `/hr/settings/import-export`

**HR — Recruitment**
- [x] `/hr/recruitment` · `/hr/recruitment/settings` · `/hr/recruitment/analytics` · `/hr/recruitment/reports` · `/hr/recruitment/diversity-report` · `/hr/recruitment/headcount` · `/hr/recruitment/sla` · `/hr/recruitment/sla-report`
- [x] `/hr/recruitment/jobs` · `/hr/recruitment/jobs/new` · `/hr/recruitment/jobs/[jobId]/edit` · `/hr/recruitment/internal-jobs` · `/hr/recruitment/requisitions`
- [x] `/hr/recruitment/candidates` · `/hr/recruitment/candidates/[candidateId]` · `/hr/recruitment/candidates/import` · `/hr/recruitment/candidates/intake` · `/hr/recruitment/pipeline` · `/hr/recruitment/talent-pools` · `/hr/recruitment/inbox`
- [x] `/hr/recruitment/interviews` · `/hr/recruitment/scorecard-templates` · `/hr/recruitment/scorecard-analytics` · `/hr/recruitment/question-bank` · `/hr/recruitment/interviewer-performance` · `/hr/recruitment/booking-links`
- [x] `/hr/recruitment/offers` · `/hr/recruitment/offer-templates` · `/hr/recruitment/hiring-flows` · `/hr/recruitment/automations` · `/hr/recruitment/email-sequences`
- [x] `/hr/recruitment/recruiters` · `/hr/recruitment/refer` · `/hr/recruitment/referrals` · `/hr/recruitment/vendors`

### Payroll (PayrollOS)
- [x] `/payroll` — Command center (live run status, exception counts, pending actions) · `/payroll/access`
- [x] `/payroll/me` — ESS (payslips, breakdown, declarations, bank details, loans) · `/payroll/team`
- [x] `/payroll/setup` · `/payroll/templates` · `/payroll/components` · `/payroll/salary-structures`
- [x] `/payroll/runs` · `/payroll/runs/[runId]` — Generate → approve → lock → reopen → mark paid; exceptions
- [x] `/payroll/inputs` · `/payroll/employees` · `/payroll/employees/[employeeUserId]` · `/payroll/workers/[workerId]`
- [x] `/payroll/reimbursements` · `/payroll/bonuses` · `/payroll/loans` · `/payroll/taxes` · `/payroll/bank-transfers` · `/payroll/payslips` · `/payroll/fnf` · `/payroll/reports`
- [x] `/payroll/settings` · `/payroll/settings/import-export`

### Timesheets
- [x] `/timesheets` — My Time (timer + weekly grid + day timeline, submit/recall) · `/timesheets/access`
- [x] `/timesheets/team` · `/timesheets/approvals` · `/timesheets/billing` · `/timesheets/payroll` · `/timesheets/exceptions` · `/timesheets/reports` · `/timesheets/settings`
  - Standalone product: own tile + sidebar, RBAC-only core module (no `@RequireModule`); backend `timesheets-core/`

### Accounting
- [x] `/accounting` · `/accounting/access` · `/accounting/setup` (6-step wizard) · `/accounting/settings` · `/accounting/settings/automations`
- [x] `/accounting/coa` · `/accounting/coa/[accountId]` · `/accounting/journal` · `/accounting/journal/new` · `/accounting/journal/[entryId]` · `/accounting/general-ledger` · `/accounting/opening-balances` · `/accounting/dimensions` · `/accounting/period-close`
- [x] `/accounting/trial-balance` · `/accounting/profit-loss` · `/accounting/balance-sheet` · `/accounting/cash-flow`
- [x] `/accounting/invoices` · `/accounting/invoices/[invoiceId]` · `/accounting/recurring-invoices` · `/accounting/credit-notes` · `/accounting/payments-received` · `/accounting/payment-reminders` · `/accounting/customers` · `/accounting/customers/[clientId]` · `/accounting/aged-receivables`
- [x] `/accounting/purchase-bills` · `/accounting/purchase-bills/new` · `/accounting/purchase-bills/[billId]` · `/accounting/recurring-bills` · `/accounting/vendor-credits` · `/accounting/vendor-payments` · `/accounting/vendors` · `/accounting/vendors/[vendorId]` · `/accounting/aged-payables`
- [x] `/accounting/payment-runs` · `/accounting/payment-runs/[runId]` — Create from filters → approve → execute
- [x] `/accounting/banking` · `/accounting/banking/[bankAccountId]` · `/accounting/banking/import` · `/accounting/banking/reconciliation` · `/accounting/banking/transfers`
- [x] `/accounting/expenses` · `/accounting/expenses/receipts` · `/accounting/expenses/reimbursements` · `/accounting/expenses/reimbursements/[batchId]` · `/accounting/expenses/policies`
- [x] `/accounting/taxes` · `/accounting/taxes/codes` · `/accounting/taxes/payments` · `/accounting/taxes/reports` · `/accounting/gstr-1` · `/accounting/gstr-3b`
- [x] `/accounting/budgets` · `/accounting/budgets/[budgetId]` · `/accounting/forecast` · `/accounting/scenarios`
- [x] `/accounting/assets` · `/accounting/assets/[assetId]` · `/accounting/assets/depreciation` · `/accounting/approvals`
- [x] `/accounting/reports` · `/accounting/reports/burn-rate` · `/accounting/reports/customer-statement` · `/accounting/reports/department-profitability` · `/accounting/reports/expense-by-category` · `/accounting/reports/project-profitability`
- [x] `/accounting/reports/sales-by-customer` · `/accounting/reports/sales-by-item` · `/accounting/reports/tax-summary` · `/accounting/reports/vendor-statement` · `/accounting/reports/working-capital`

### Billing
- [x] `/settings/billing` — Platform Plan (plan cards, promo code, seats, usage meters) · Payments · Billing Profile; the old `/billing`, `/billing/seats`, and `/settings/subscription` routes are deleted
- [x] `/settings/billing/ai-credits` — Wallet, top-up packs (Razorpay), auto-top-up, server-paginated usage history; the old `/billing/ai-credits` route is deleted
- [x] `/billing/invoices` · `/billing/invoices/new` · `/billing/invoices/[invoiceId]` — The org's own customer invoicing, not platform billing

### Inventory
- [x] `/inventory` · `/inventory/access` · `/inventory/settings` · `/inventory/import` (CSV wizard) · `/inventory/barcode`
- [x] `/inventory/products` · `/inventory/products/new` · `/inventory/products/[productId]` · `/inventory/products/categories` · `/inventory/products/uom`
- [x] `/inventory/stock` · `/inventory/stock/adjustments` · `/inventory/stock/movements` · `/inventory/stock/transfers` · `/inventory/stock/transfers/[transferId]`
- [x] `/inventory/warehouses` · `/inventory/warehouses/[warehouseId]` · `/inventory/vendors` · `/inventory/vendors/[vendorId]`
- [x] `/inventory/purchase-orders` · `/inventory/purchase-orders/new` · `/inventory/purchase-orders/[poId]` · `/inventory/sales-orders` · `/inventory/sales-orders/new` · `/inventory/sales-orders/[soId]`
- [x] `/inventory/operations` · `/inventory/operations/receipts` · `/inventory/operations/issues` · `/inventory/operations/picking` · `/inventory/operations/packing` · `/inventory/operations/shipping` · `/inventory/operations/returns`
- [x] `/inventory/cycle-counts` · `/inventory/cycle-counts/[countId]` · `/inventory/physical-audits` · `/inventory/physical-audits/[auditId]`
- [x] `/inventory/lots` · `/inventory/lots/[lotId]` · `/inventory/serials` · `/inventory/serials/[serialId]` — Traceability chains
- [x] `/inventory/expiry` · `/inventory/replenishment` · `/inventory/replenishment/rules` · `/inventory/forecasting` (SMA) · `/inventory/valuation` · `/inventory/costing`
- [x] `/inventory/quality` · `/inventory/quality/inspections` · `/inventory/quality/holds` · `/inventory/quality/recalls`
- [x] `/inventory/packages` · `/inventory/shipments` · `/inventory/loads` · `/inventory/carriers` · `/inventory/channels` · `/inventory/3pl`
- [x] `/inventory/reports/stock-summary` · `/inventory/reports/movements` · `/inventory/reports/reorder` · `/inventory/reports/expiry` · `/inventory/reports/slow-moving`

### Support
- [x] `/support` · `/support/access` · `/support/inbox` · `/support/routing` · `/support/macros` · `/support/ai-report` · `/support/knowledge-gaps`
- [x] `/support/reports` · `/support/reports/agent-performance` · `/support/reports/queue-performance` · `/support/reports/channel-performance` · `/support/reports/automation-performance` · `/support/reports/csat`
- [x] `/support/kb` · `/support/kb/[articleId]` · `/support/kb/research-briefs` · `/support/kb/research-briefs/[briefId]`
- [x] `/support/portal` · `/support/portal/[portalTicketId]` — Customer ticket portal (`CLIENT_USER` auth; own tickets only, no internal notes)
- [x] `/support/settings/sla` · `/support/settings/business-hours` · `/support/settings/channels` · `/support/settings/automations` · `/support/settings/agent-routing` · `/support/settings/custom-fields` · `/support/settings/audit-log`

### Knowledge Base
- **Core access:** Home, `/me/documents`, and the Knowledge Base read experience are available to every active organization member. Roles still control authoring, administration, analytics, and settings.
- [x] `/knowledge/wiki` — Home (recents, favorites, root pages, templates) with collapsible page-tree panel
- [x] `/knowledge/chat` — Ask KB
- [x] `/knowledge/wiki/pages/[pageId]` · `/knowledge/wiki/pages/[pageId]/history` — Plate document editor (slash commands, tables, R2 media) + two-pane version history with restore
- [x] `/knowledge/wiki/recent` · `/knowledge/wiki/favorites` · `/knowledge/wiki/trash` · `/knowledge/wiki/private` · `/knowledge/wiki/shared` · `/knowledge/wiki/templates` · `/knowledge/wiki/import`
- [x] `/knowledge/wiki/spaces` · `/knowledge/wiki/spaces/[spaceId]` · `/knowledge/wiki/reviews` · `/knowledge/wiki/analytics` · `/knowledge/wiki/settings`
- [x] `/knowledge-base` — Back-compat redirect
- **Phases:** 0 first-class module · 1 CRUD + reader/editor · 2 granular permissions + public help center · 3 search + Ask AI (keyword + pgvector RRF) · 4 analytics + verification · 5 AI authoring. *Enterprise features pending.*
- **RAG:** pgvector on Neon (`kb_article_chunks`, `vector(1536)` + HNSW cosine), PDF/`.docx`/text extraction, content-hash re-embed guard

### E-Sign
- [x] `/sign` · `/sign/access` · `/sign/envelopes` · `/sign/envelopes/[envelopeId]` · `/sign/templates` · `/sign/bulk-send` · `/sign/reports` · `/sign/settings`
- [x] `/sign/[token]` — Public signing surface (no auth)

### Surveys
- [x] `/surveys` · `/surveys/access` · `/surveys/new` · `/surveys/[surveyId]` · `/surveys/[surveyId]/participants` · `/surveys/live/[sessionId]/host`

### Organization & Settings
- [x] `/settings/organization/structure` · `/settings/organization/departments` · `/settings/organization/teams` · `/settings/organization/locations` · `/settings/organization/branches` · `/settings/organization/business-units` · `/settings/organization/cost-centers` · `/settings/organization/chart` — HR-gated organization structure; the legacy `/organization/*` routes are deleted; setup generates industry defaults plus teams for enabled modules; teams require departments; chart is searchable and read-only
- [x] `/settings/organization` — Org profile + security policies (MFA, email domain, concurrent sessions; absorbed `/settings/security` 2026-07-25)
- [x] `/settings/roles` · `/settings/roles/[roleId]` · `/settings/roles/simulate` · `/settings/roles/audit` — Role editor with permission matrix + simulator
- [x] `/settings/modules` — Organization module enablement (`settings:manage`); each enabled product exposes access management in its own visible module sidebar
- [x] `/settings` — Exact account profile, MFA, active-session controls, and recent sign-ins route; unknown Settings URLs no longer fall through to this page
- [x] `/settings/api-tokens` — Personal access tokens only; grantable scopes are limited by live user permissions and enabled modules, administrative scopes are non-delegable, expiration is mandatory, and revocation retains the audit record
- [x] `/crm/api-keys` — CRM-scoped, time-limited lead-ingestion keys; available only with CRM enabled and `crm:settings:manage`
- [x] `/settings/devices` · `/settings/login-history` · `/settings/sessions` — Retired 2026-08-05; security tools consolidated into Account Settings, and unused trusted-device controls removed
- [x] `/settings/audit-log` · `/settings/webhooks` · `/settings/delegations` · `/settings/incoming-transfer`
- [x] `/accounting/settings/payment-providers` — Finance-module payment provider and credential setup, visible only with payment-provider access
- [x] `/settings/connected-accounts` · `/settings/module-access/[moduleKey]` — Retired 2026-08-05; unused sign-in-provider management and duplicate generic module-access routes removed

### Workflows & Automation
- [x] `/workflows` — Dashboard (analytics stats, filters + search, create/duplicate/delete)
- [x] `/workflows/[workflowId]` · `/workflows/[workflowId]/builder` — Detail + React Flow canvas builder
- [x] `/workflows/templates` · `/workflows/executions` · `/workflows/approvals` · `/workflows/scheduler` · `/workflows/analytics` · `/workflows/secrets` · `/workflows/variables`

### Onboarding & Auth
- [x] `/org-setup` — Welcome → Basics (goals + modules merged) → Launch; draft + step resume; ceremonial generation with dedicated failure recovery
- [x] `/employee-onboarding` — Shell-free wizard; never shown to owners/platform admins; HR-prefilled personal/bank data with employee-overridable drafts; value-level review/edit; country-driven documents/bank/payroll
- [x] `/signin` — The only account entry (`/signup` retired) · `/magic-link` · `/verify-email` · `/accept-invitation` · `/invitation/[token]`
- [x] `/access-suspended` — Recovery screen for an organization-only suspension (`resolveWizardGate`); recheck access, switch to another ACTIVE membership, or sign out; org creation intentionally unavailable
- [x] `/access-denied` · `/build/client-access`

### Public (no auth)
- [x] `/` · `/about` · `/pricing` · `/contact` · `/legal/privacy` · `/legal/terms` · `/legal/security`
- [x] `/blogs` · `/blogs/[slug]` · `/blogs/category/[slug]` · `/blogs/tag/[tag]`
- [x] `/careers/[orgSlug]` · `/careers/[orgSlug]/jobs/[jobId]/apply` · `/application-status/[token]` · `/interview-booking/[token]` · `/offer/[token]`
- [x] `/help/[orgId]` · `/help/[orgId]/[slug]` · `/wiki/[shareToken]` · `/board/[shareToken]` · `/roadmap/[orgId]`
- [x] `/forms/[token]` · `/intake/[projectId]` · `/nps/[token]` · `/ticket-feedback/[token]` · `/s/[token]` · `/live/[sessionCode]` · `/live-chat/[orgId]`
- [x] `/refer/[orgId]` · `/refer/link/[token]` · `/vendor-portal/[token]`

---

## Retired / not on disk

Documented as done in an earlier revision, but **no route directory exists** as of 2026-08-03. Retire the entry or restore the page:

- `/sales/**` (11 pages) · `/customer-executive/**` (10 pages)
- `/marketplace` · `/billing/checkout` · `/billing/analytics` · `/billing/affiliate` · `/billing/enterprise-quotes` · `/billing/bundles` · `/billing/addons` · `/billing/coupons` · `/billing/trials` · `/billing/referrals` · `/billing/recurring`
- `/setup` — Workspace Setup Wizard, superseded by `/org-setup`
- `/marketing/**` · `/digital-marketing/**` — removed end-to-end (pages, APIs, schema, hooks, nav, billing gates)
- `/reports` — redirects to `/crm/reports`; reports are module-owned
- `/build/whiteboards` · `/build/resource-allocation` — deleted 2026-07-14 as orphan surfaces
