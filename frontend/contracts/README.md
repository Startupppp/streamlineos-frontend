# contracts/

Vendored API contract for build-time drift detection (`scripts/check-contract-drift.mjs`).

## openapi.json

Generated by the backend and checked in here so the frontend CI can compare its hook types against the real Zod schemas without booting the API.

**To vendor after a backend change:**

```bash
pnpm --filter streamlineos-api openapi:generate
cp backend/openapi.json frontend/contracts/openapi.json
```

The check fails with a clear actionable message if this file is absent.

**Vendor-copy freshness gate** (`scripts/check-contract-vendor.mjs`) verifies that `frontend/contracts/openapi.json` is byte-identical to `backend/openapi.json`. This gate must pass before any frontend CI run that also runs the drift check, to ensure the drift check compares against the current backend, not a stale copy. In a frontend-only CI checkout (no `backend/` present), skip the vendor check and rely on the committed artifact.

```bash
node scripts/check-contract-vendor.mjs             # verify copy is current
node scripts/check-contract-vendor.mjs --self-test # smoke-test the hash comparison
```

## permission-catalog.json

The backend RBAC catalogue, vendored for the same reason `openapi.json` is: CI clones ONE repository, and eight jest suites used to read `../streamlineos-backend/src/...` directly. Measured on run 33719474880 the `tests` job reported 9 failed suites, 8 of them that cause — so the page-level RBAC gate had never once been enforced in CI.

It holds exactly four facts, all sorted and deduplicated, and nothing else — no timestamp, no git SHA — so the file is a pure function of the backend sources and "byte-for-byte" is a meaningful claim:

| Key | From |
|---|---|
| `permissions` | every literal `name:` in `backend/src/modules/rbac/permissions/*.ts`, plus the generated `<moduleId>:access:view\|manage` for each delegable module |
| `delegableModuleIds` | `backend/src/common/rbac/module-registry.ts`, entries whose `ladder` is `delegable` |
| `memberDefaultPermissions` | the block of `permissions/role-defaults.ts` above `ROLE_DEFAULT_PERMISSIONS` |
| `ownerOnlyOperations` | id -> reason from `backend/src/common/rbac/owner-only-operations.ts` |

**To vendor after a backend RBAC change:**

```bash
pnpm -C frontend generate:permission-catalog   # needs the backend checked out beside this repo
```

**The drift check is the load-bearing part** (`scripts/check-permission-catalog.mjs`). A committed copy that silently goes stale is worse than no copy, because it turns "the backend renamed a key" into a green run. Three rules, and the first two need nothing but this repository:

1. **Well-formed** — floors (>=400 permissions, >=5 delegable modules, >=20 member defaults, >=1 owner-only operation), sorted, unique, every key well-shaped, every member default present in `permissions`. An empty or truncated artifact cannot satisfy the suites' "a silent empty sweep cannot pass" assertions.
2. **Agrees with the API contract** — every `x-permission` an operation declares in `openapi.json` appears in the catalogue. `openapi.json` is generated by the same backend and is already byte-compared by `check:contract-vendor`, so it is an independent witness that lives here. This is what makes drift detectable in a frontend-only CI job instead of permanently inconclusive.
3. **Byte-identical to a fresh regeneration** — the complete check. Needs the backend beside this repo; otherwise reported NOT RUN and the run ends INCONCLUSIVE (exit 2). `STREAMLINE_ALLOW_FRONTEND_ONLY=1` downgrades that to a labelled PARTIAL (exit 0) and does NOT mask rules 1 and 2.

```bash
pnpm -C frontend check:permission-catalog:self-test   # 19 assertions over synthetic inputs
pnpm -C frontend check:permission-catalog
```

Read it from a spec through `@/test-utils/permission-catalog` (`backendPermissionNames`, `delegableModuleIds`, `memberDefaultPermissions`, `ownerOnlyOperations`), never by walking to a sibling checkout.

## __fixtures__/

`timesheets-fixture.json` — a hand-crafted subset of real timesheets operations (derived from `backend/src/modules/timesheets/**/*.controller.ts` and their `dto/*.schemas.ts`). Used by `--use-fixture` to demonstrate drift rules against request body schemas. It is a partial contract — not all paths are present — so running against it produces path violations and may produce stale-baseline warnings for entries that only appear in the real contract.

## What the check detects (timesheets module)

1. A path the frontend calls that the contract does not contain — renamed or deleted endpoint.
2. A method mismatch on a known path.
3. A request-body field the frontend sends that the contract's schema does not list in `properties` — silently stripped by Zod; the mutation has no effect.
4. A `required` field the contract demands that the frontend never sends — backend returns 400 every time.
5. An enum member the frontend can send that the contract's schema does not include — the value is silently rejected.

Narrowings (contract allows values the frontend type does not use) are reported as informational, not failures.

## Resolution floor

The check prints `body resolved: N` on every run and fails if the resolved fraction drops below 40%. A drop signals a refactor broke the body-type extractor.

## KNOWN_DRIFT baseline

`KNOWN_DRIFT` in `scripts/check-contract-drift.mjs` is a dated list of confirmed frontend/backend disagreements that are not yet resolved because they depend on a product decision. The baseline exists so CI does not fail every commit while that decision is pending.

**These are NOT accepted behaviour.** Each entry is an unfixed defect with a `since` date, a specific endpoint+field+offending-members identity, and a `reason` stating exactly what decision is blocking the fix. The baseline is printed in full on every run so no CI pass can hide the existence of these defects.

**The check enforces two invariants:**
- A violation whose identity matches a baseline entry is reported but does not fail CI.
- A baseline entry that no longer matches any actual violation fails CI and instructs the reader to delete the entry. This ensures the list cannot silently re-permit a defect that someone already fixed.

**A violation on the same endpoint with a different enum member or a new field is NOT baselined** — it produces a new failure. Matching is exact: `type + endpoint + field + sorted offending members`. A partial change (e.g. the fix removes one member of a multi-member drift) produces a new violation string that does not match the old key, so it is caught.

### Current baseline entries (as of 2026-08-28)

Seven field-level drifts covering six violation strings — all are unresolved frontend/backend enum disagreements awaiting product decisions.

| Endpoint | Field | Frontend sends | Contract accepts | Since |
|---|---|---|---|---|
| `POST /timesheets/entries` | `billingType` | `INTERNAL` | `BILLABLE, NON_BILLABLE, FIXED` | 2026-08-28 |
| `POST /timesheets/entries` | `source` | `GRID` | `MANUAL, TIMER, API, IMPORT` | 2026-08-28 |
| `PATCH /timesheets/entries/{entryId}` | `billingType` | `INTERNAL` | `BILLABLE, NON_BILLABLE, FIXED` | 2026-08-28 |
| `POST /timesheets/exceptions/{exceptionId}/resolve` | `reason` | (extra field) | not in schema | 2026-08-28 |
| `POST /timesheets/rates` | `billingType` | `INTERNAL` | `BILLABLE, NON_BILLABLE, FIXED` | 2026-08-28 |
| `PATCH /timesheets/rates/{rateId}` | `billingType` | `INTERNAL` | `BILLABLE, NON_BILLABLE, FIXED` | 2026-08-28 |
| `PATCH /timesheets/settings` | `approvalMode` | `NONE, PROJECT, CLIENT` | `MANAGER, AUTO, MULTI_LEVEL` | 2026-08-28 |

`INTERNAL` is an `INVALID_ENUM_VALUE` at the Zod boundary — the field parses to `undefined` and the update silently does nothing. `GRID` and the approval-mode members have the same effect.

### Removing a baseline entry

Once the product decision lands and either the frontend type or the backend schema is corrected:

1. Fix the source of the disagreement (frontend `types.ts` or backend `dto/*.schemas.ts`).
2. Run `node scripts/check-contract-drift.mjs` to confirm the violation no longer appears.
3. Delete the corresponding entry from `KNOWN_DRIFT` in `scripts/check-contract-drift.mjs`.
4. Run again — the stale-baseline guard will fail if you leave the entry after the fix.

### Adding a baseline entry

Adding a `KNOWN_DRIFT` entry is a deliberate act that requires the same evidence and decision process as removing one. An entry must carry:
- The exact `type`, `method`, `path`, `field`, and `offendingMembers` that appear in the violation output.
- A `since` date.
- A `reason` that names the specific product question blocking the fix — not a generic placeholder.

Do not add an entry to silence a CI failure without first understanding and documenting what decision needs to be made. An undocumented entry is a blindfold, not a ratchet.

## Commands

```bash
node scripts/check-contract-drift.mjs              # check against vendored openapi.json
node scripts/check-contract-drift.mjs --use-fixture # check against the hand-crafted fixture
node scripts/check-contract-drift.mjs --self-test   # run 16 in-memory cases, exit 0 if all pass
```
