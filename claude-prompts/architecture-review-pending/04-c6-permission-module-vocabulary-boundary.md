# Complete C6 safely: one explicit permission-module vocabulary · **DONE 2026-09-10**

Landed as `7991acb02` (backend repo) and `91bd50eb3` (root repo). Evidence per item below.

Work directly in the StreamlineOS repository and complete the audit, cleanup, tests, and enforcement. Read applicable `CLAUDE.md` files before editing, preserve unrelated changes, and do not commit or push unless explicitly requested.

## Source and objective

The source requirement is candidate C6 in:

`C:/Users/Aditya_Lappy/AppData/Local/Temp/architecture-review-20260909-233331.html`

The frontend copies have already moved to a manifest-derived helper. The backend review wording conflated two distinct questions. Complete the architectural outcome by making both answers canonical and preventing ambiguous/private implementations:

- `namespaceOf(permissionKey)`: runtime entitlement and per-user module-denial namespace.
- `administeringModuleOf(permissionKey)`: module whose administrative ladder owns configuration of the permission.

Replacing backend runtime `namespaceOf()` calls with `administeringModuleOf()` would weaken Chat and other module denials. Preserve the distinction.

## Required work

- [x] **1. Search** — `rg` over both repos for `split(":")` · `indexOf(":")` · substring · regex · local `moduleOf`/`namespaceOf`/`administeringModuleOf` · pass-through exports. Backend production, backend `src/scripts/**`, `backend/test/**`, frontend `lib|components|features|hooks|app|scripts`.
- [x] **2. Classify** — every hit assigned one of runtime namespace · administering module · display grouping · fixture construction · unrelated string parsing. Full inventory in the two commit messages; the residue is now the two architecture tests' allowlists, each entry carrying its reason.
- [x] **3. Replace policy copies** — `grantability.isOrgOnlyNamespace` → `namespaceOf`; `grantability.buildPermissionModuleMap` → `administeringModuleOf`; `permissions/catalog.moduleScopedPermissions` → `namespaceOf`; `ai/tool-access` → `permissionAreaLabel`; `check-permission-keys.mjs` and `key-inventory/registries.mjs` → shared `namespaceOf`.
  - Defect found doing it: `rbac.service.getDiscoveryPermissions` **filtered** by `administeringModuleOf` but **labelled** `moduleKey` with the raw namespace, so a CRM admin saw `party:*` labelled `party` while the DB catalogued the same key as `home`. Both now `administeringModuleOf`.
- [x] **4. Disambiguating renames** — `buildPermissionModuleMap` → `buildPermissionAdministeringModuleMap` (+ its type `PermissionAdministeringModuleMap`, params `permissionMeta` → `administeringModules`, locals `keyModule`/`mod` → `administeringModule`) across 8 call sites; spec-local naive parser → `naivePrefixNamespace`; `frontend/lib/module-vocabulary.ts` → `org-module-keys.ts`, which answers the third question (stored-key normalization).
- [x] **5. Canonical helpers together** — `namespaceOf`, `administeringModuleOf`, `moduleOwningNamespace`, `impliedViewKey`, `permissionAreaLabel` all in `backend/src/common/rbac/module-vocabulary.ts`. Frontend administering helper stays `lib/rbac/administering-module.ts`, built from `MANIFEST.modules[].administersNamespaces`.
- [x] **6. Dead aliases and duplicated tables removed** — `impliedViewKey` was defined **twice, character-for-character**; `verify-rbac-verdict.mjs` and `route-attribution.mjs` each hand-coded the namespace→module table; `verify-rbac-referential-integrity.mjs` re-exported symbols nothing imported; frontend `lib/module-vocabulary.ts` survived in HEAD beside its own rename. All gone. Zero hand-coded `chat → home` mappings remain in either repo.
- [x] **7. Architecture test** — `backend/src/common/rbac/no-private-permission-key-parsers.spec.ts` and `frontend/lib/rbac/__tests__/no-private-permission-key-parsers.test.ts`. Each fails on a new private parser, on a **stale allowlist entry** (a fixed file keeping its exemption is a hole), on a second declaration of the helper, and on a hand-coded mapping. Both assert they scanned a real tree first, so an empty result cannot mean "read nothing".
- [x] **8. Comments at the runtime authorization sites** — one line each at `authorize.ts`, `access-policy.stripDeniedModules`, `permission.guard.ts`, each naming why it is `namespaceOf` and not `administeringModuleOf`.
- [x] **9. Manifest remains the frontend source** — confirmed `frontend/lib/module-manifest.json` is generated by `backend/src/scripts/export-module-manifest.ts` from `MODULE_REGISTRY` and hand-copied nowhere. The `.mjs` scripts now read the **committed** manifest rather than spawning ts-node, because the alerting scripts import `route-attribution`.

## Tests that must bite

- [x] `chat:*` · `mail:*` · `calendar:*` · `notifications:*` administered by Home — `module-vocabulary.spec.ts`, `administering-module-exists.spec.ts`, `administering-module.test.ts`.
- [x] Their runtime namespaces stay `chat`/`mail`/`calendar`/`notifications` — `module-vocabulary.spec.ts` asserts both answers on the same key and that they differ.
- [x] `party:*` → CRM administration, runtime namespace stays `party` — same spec.
- [x] Denying Chat strips `chat:*` although Home administers them — `module-vocabulary.spec.ts` runs `stripDeniedModules` both ways and pins that resolving the denial through `administeringModuleOf` would strip **nothing**.
- [x] A future non-core administered namespace cannot use naive prefix grouping — registry-driven on the backend, `MANIFEST`-driven on the frontend, so a new claim is covered with no test edit.
- [x] Reintroducing a private production parser fails the architecture test — **proved by construction**: a parser appended to `authorize.ts` failed naming `modules/access/authorize.ts:41`; one appended to `route-access.ts` failed naming `route-access.ts:75`. Both reverted.
- [x] Manifest synchronization catches ownership drift — `module-manifest-sync.spec.ts` (both committed manifests vs `MODULE_REGISTRY`) and new `rule-5` in `check-module-manifest.mjs`. Proved: removing `chat` from home's `administersNamespaces` produced `[rule-5] Module "home" administersNamespaces mismatch`. Reverted.

## Verification results

| Check | Result |
|---|---|
| Backend focused suites (`common/rbac`, `access`, `module-access`, `rbac`, `slo`) | 116 suites / 1339 tests pass |
| Frontend vocabulary + manifest + arch suites | 7 suites / 127 tests pass |
| `tsc --noEmit` backend | 19 errors, **all** `AuthContextLookups` missing `membershipState`/`mfaState` from a concurrent AuthContext refactor (absent at HEAD). Zero outside that class, none in these files. |
| `tsc --noEmit` frontend | clean |
| `madge --circular` backend / frontend | zero / zero |
| `check-permission-keys.mjs` | 3,148 usages, 630 unique keys, all resolve |
| `verify-rbac-self-test.mjs` | 33/33, incl. chat→home, party→crm through the shared helper |
| `route-attribution --self-test` | 19/19 (was 14; added calendar, notifications, dashboard, auth, platform) |
| `check-module-manifest.mjs` | passes; bites on injected drift |

## Found here, left open — needs an on-call decision

`route-attribution.mjs`'s `MODULE_OWNERS` says it mirrors `MODULE_SLO_OWNERSHIP`. It does not — 16 entries against 21, and they **disagree on who gets paged**. `alert-p95.mjs` and `alert-seam-latency.mjs` both resolve the owner through `resolveRouteAttribution`, so today:

- `/notifications/*` pages **communications-team** (the namespace folds into Home) while the notifications SLO is owned by **notifications-team**;
- `/tasks/*` reports **unattributable** (`tasks` is absent from `MODULE_OWNERS`) while the tasks SLO is owned by **delivery-team**.

Same family of confusion as this task: route attribution applies the **administering** fold to a question that is neither entitlement nor administration but *operational ownership*. `chat`/`mail`/`calendar` survive it only because Home shares `communications-team` with them by coincidence.

Not fixed here on purpose — every repair changes who is woken at 3am. Two ways out: derive `MODULE_OWNERS` from `MODULE_SLO_OWNERSHIP` and drop the fold for this question, or declare the fold correct and delete the two SLO owners it contradicts.
