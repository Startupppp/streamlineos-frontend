# TASKS — Administration (`/settings/*`)

Updated: 2026-08-12 · **Done 36 · Open 5 · Blocked 1 · Deferred 10 · Total 52**
Counts recomputed by script, not from memory.

Legend: `[x]` done with evidence · `[ ]` open · `[!]` blocked · `[~]` deferred with reason.
Evidence = a command or grep run **this session**. No test suite was run.

---

## Security (ADSEC-*)

- [x] ADSEC-001 **P0 privilege escalation** — `addRoleMember` now asserts grantability · Evidence: new `assertMayAssignRole` loads the role's permission grants and runs `assertPermissionsGrantable` with the actor's rank; rank resolver extracted to `common/rbac/resolve-actor-rank.ts` and shared with `roles.service.ts`; BE tsc 0
- [x] ADSEC-004 revoked invitations no longer resurrectable · Evidence: `resend()` outer fetch now `inArray(status, ["PENDING","EXPIRED"])`; enum values read from `enums.ts:371`
- [x] ADSEC-003 webhook SSRF closed — **and my duplicate removed** · Evidence: I first wrote `common/security/safe-external-url.ts`, then found `common/security/ssrf-guard.ts` **already existed** and was strictly better: it normalises the packed `::ffff:7f00:1` form (mine only handled the dotted spelling), strips IPv6 zone ids, blocks `ff00::/8` multicast and `192.0.0.0/24`, returns a typed rejection reason, ships a spec, and was already used by 3 modules. Migrated the schemas to `assertSafeWebhookUrl` and the dispatcher to `checkWebhookUrl` (now logging the typed reason), **deleted my duplicate**, verified 0 remaining references. Dispatcher still re-checks DNS before every send and uses `redirect: "error"`; blocked sends recorded as failed deliveries. A §0.2 reuse-before-create violation I introduced and then corrected
- [x] ADSEC-005 delegation create/revoke audited · Evidence: `audit.logCritical` **inside** the existing `runInTenantTransaction`, so the record commits atomically with the grant
- [x] ADSEC-006 permission catalog gated · Evidence: `GET /rbac/permissions` now `PermissionGuard` + `settings:rbac:manage`; both consumers (`permission-matrix.tsx`, `grant-delegation-sheet.tsx`) already sit behind pages requiring that key
- [x] ADSEC-009 swallowed deferred failures removed · Evidence: 2 request-path notifications → `registerAfterCommit` + logged; `grep -rc 'catch(() => undefined)' src/modules/ownership/` → **0**
- [x] ADSEC-009b 3 more of the same defect the audit missed · Evidence: `ownership-transfer-response.service.ts:184,444,530` now log instead of discarding
- [x] ADSEC-F03 last unguarded settings page · Evidence: 23 of 25 pages already gated; `webhooks/page.tsx` split so the route file can call `requirePermission("settings:webhooks:manage")` (key read from `webhooks.controller.ts:38`). **All 25 now gated**
- [x] ADSEC-002 **refuted, not a P0** · Evidence: `createTenantAwareDb` (`common/tenant/tenant-db.ts`) routes *every* property incl. `transaction` to the ambient tx, so in-request `this.db.transaction()` inherits the GUC. Real severity P3
- [x] ADSEC-F04 **withdrawn** — owner: API tokens are CRM-level only (D-011); my change fully reverted, 0 residual references
- [x] ADSEC-007 + ADGAP-030 webhook signing secrets **encrypted at rest, reveal-once, rotatable** · **Encrypted, not hashed** — HMAC signing needs the plaintext back, so a digest would break every consumer. Reused the existing `common/security/secret-encryption.util.ts` (AES-256-GCM) rather than writing anything new. **No migration needed, so not blocked by D-009:** its `enc:v1:` prefix makes a *lazy* migration possible — new/rotated secrets are written encrypted, and `readSigningSecret()` in the dispatcher decrypts only when the prefix is present, so pre-existing plaintext rows keep signing correctly. Verified `ENCRYPTION_KEY` is **required at startup** (`env.validation.ts:60`, with specs asserting it throws when missing or too short), so `encryptSecret` cannot fail at runtime. `create` now returns the plaintext exactly once alongside `secretHint`; `rotateSecret` regenerates and returns it once; list/get/update continue to strip the field. New `POST /webhooks/:webhookId/rotate-secret` carries the same `settings:webhooks:manage` key and is added to the controller e2e route table so it is guard-tested. **4/4 new specs pass** (`webhooks-secret-at-rest.spec.ts`): ciphertext never contains the raw value, the HMAC is byte-identical after a round-trip, a legacy plaintext row still signs correctly, and two encryptions of the same secret differ so equal secrets aren't correlatable
- [x] ADM-004 the one-time secret is finally shown to the user · Evidence: the API already returned it on create, but `useCreateWebhook` was typed `WebhookEndpoint` — which has **no `secret` field** — so the frontend silently discarded it and users had nothing to verify `X-StreamlineOS-Signature` with. Added `WebhookSecretReveal`, a `WebhookSecretRevealDialog` (copy button, "only time it is shown", rotate-if-lost guidance) shared by create **and** rotate, and a `KeyRoundIcon` rotate action on each card behind a confirm dialog that warns deliveries will fail until the receiver is updated. `KeyRoundIcon` export verified present in the animated catalog before use (§8). FE tsc 0
- [~] ADM-002/003 **already satisfied** — `webhook-create-sheet.tsx` uses `LoadingButton` with `loadingText` and full `useForm` + `zodResolver(webhookCreateSchema)` with `FormMessage` per field. Completed by the earlier page split (ADSEC-F03); nothing left to do
- [x] **ADSEC-011 · AC-04 FIXED structurally** (owner-approved, D-017) · Org-admin standing now comes only from an active `organizationMembers` row that is owner or `ORG_ADMIN` — new `common/rbac/is-structural-org-admin.ts`. **The `authorize.ts` branch was deleted, not replaced:** `access.service.ts:657-658` already returns `allCatalogScopes()` (every key at scope `all`) structurally for owners and ORG_ADMIN, so a real admin never depended on the short-circuit — it was redundancy for them and escalation surface for everyone else. Zero new queries on the hot path. **7 sites, not 6:** `assert-may-grant-role.ts` used `ORG_ADMIN_PERMISSION_KEY` *directly* so my `grantsOrgAdmin` grep missed it, and it gates **who may grant the ORG_ADMIN role** across invitations, employee onboarding, org membership, settings and users — a custom role with `settings:manage` could promote others to admin. Its signature now takes `db` not `access`; all 7 callers updated (each verified to inject `db` first). `grantsOrgAdmin()` **deleted** so the unsafe path is unrepresentable (§20); the reserved-key constants stay for `rbac.service.ts:314,321`, which legitimately asks "may this actor *propagate* this key". 4 regression specs added to `authorize.spec.ts` (holder of each reserved key denied an unrelated permission; structural admin unaffected; a scoped grant keeps `own` instead of being widened to `all`). **Verified:** backend `tsc --noEmit` → 8 errors, **none in any of the 14 files I touched**; my 3 known pre-existing ones persist and 5 are new arrivals in `inventory/products/inv-products.controller.ts` and `notifications/notification-dispatch-after-commit.spec.ts`, i.e. concurrent sessions' files. Original finding retained below for context:
  - *Original finding, retained for context (not a separate task):* Holding **one** permission key (`settings:manage` **or** `settings:rbac:manage`) returns `{ allow: true, scope: "all" }` for **every** permission key on the platform. Chain I verified end to end: `grantability.ts:20-26` `grantsOrgAdmin()` tests only those two keys → `authorize.ts:37-39` returns allow-all → `permission.guard.ts:8,37` calls `authorize()`, and `PermissionGuard` is what every `@RequirePermission` endpoint uses. So an owner who creates a custom role meaning "let this person manage settings" silently mints a **full platform superuser** with `all` scope over payroll, HR and finance. This is verbatim the path CLAUDE.md §21 forbids: *"Test org-admin structurally via `organizationMembers.role`/`isOwner`, never by checking whether a principal holds `settings:manage` or `settings:rbac:manage` — the permission-derived path is AC-04 and creates an accidental parallel superuser bypass."* Also used at `module-access.service.ts:319,530`, `module-access.helpers.ts:107,149`, `module-access-groups.service.ts:344`, where `module-access.helpers.ts:95-100` documents it as *intentional* — so **code comment and constitution directly contradict each other**. **Not fixed deliberately:** rewriting the platform authorization path is not a reversible change that follows from the CRM/Admin brief. Encouraging sign for the fix — `role-defaults.ts:65` gives `ORG_ADMIN: ALL_PERMISSION_NAMES`, so a genuine org admin already passes the normal `resolved.get(key)` check and the short-circuit is only an optimisation; removing it should not lock real admins out. **Must confirm first:** what scope `role_permission_grants` stores for ORG_ADMIN defaults, since the short-circuit currently forces `all`
- [~] ADSEC-008 **refuted — the missing decorator is correct here.** `module-access.controller.ts` has 21 routes under `@UseGuards(JwtAuthGuard)` with no `@RequirePermission`, but the key would have to be `${moduleKey}:access:*` and `moduleKey` is a **path param**, which a static decorator cannot express. §21 also requires that a delegated `<module>:access:manage` grant confer *view only*, so decorating would grant the very authority the rule forbids. Authorization is structural in `assertModuleAccessPolicy` (`module-access.helpers.ts:122-154`). Real defect here is ADSEC-011, not the missing decorator
- [~] ADSEC-010 **refuted — CLAUDE.md was the stale side, now corrected.** Both enforcement points agree digits are legal: `rbac.schemas.ts:22-27` `/^[A-Z0-9_]+$/` and the canonical `CreateRoleDialog.slugify()` (`create-role-dialog.tsx:59-64`) strips `[^A-Z0-9_]`, deliberately preserving digits. Tightening to §21's `/^[A-Z_]+$/` would have made a legitimate `TIER_2_SUPPORT` uncreatable for zero security gain. No template slug contains a digit, so nothing depended on the stricter form. Fixed CLAUDE.md §21 instead of the code
- [x] ADM-005 raw `e.message` removed **and a double-toast bug fixed** · Evidence: `useUpdateBillingProfile` (`subscription.ts:180`) fired *its own* `toast.success` **and** `onError: (e: Error) => toast.error(e.message)`, while its only caller (`billing-profile-tab.tsx:92-95`, sole caller confirmed) already fired both correctly via `getErrorMessage`. Users therefore saw **two** toasts per outcome, one of them a raw backend string. Removed both from the hook so the caller owns feedback and the hook owns invalidation; the now-unused `sonner` import was dropped too. FE tsc 0

## Functional (ADM-*)

- [x] ADM-001 org hierarchy un-gated from HRMS at all 3 layers · Evidence: controller `@RequireModule("hr")` removed, 8 page wrappers removed, sidebar `module: "hrms"` removed from the Organization group only; **verified after**: `@UseGuards(JwtAuthGuard, PermissionGuard)` intact and **31** `@RequirePermission` unchanged; the 2 genuine HR nav groups keep their gate
- [x] ADM-006 delete dialog states the real blast radius · Evidence: needed a backend change — `getRoleAnalytics` returns only org-wide aggregates, so a per-role `memberCount` was added as a **correlated subquery** (a second `leftJoin` would fan out and corrupt `permissionCount`)
- [x] ADM-007 audit-log user search moved server-side · Evidence: `userSearch` in the list schema; `escapeLike` helper; count query gained the same join. §19 leading-wildcard check passed — `idx_users_name_trgm`/`idx_users_email_trgm` already exist (`0007_search_trgm_indexes.sql`)
- [x] ADGAP-033 audit-log CSV export · Evidence: same `audit-log:read` key (read from `audit-log.controller.ts:17`), streamed via `exportCsvChunks` with a 10k cap, following the `contacts.controller.ts:78-86` pattern
- [x] ADGAP-031 webhook delivery log exposed + retryable · Evidence: `GET /webhooks/:webhookId/logs` reuses `getEndpoint` so a wrong-tenant id 404s; retry calls the existing private `deliver()` after re-validating active + public host

## UI conformance (ADS-*)

- [x] ADS-003 42 control-size overrides removed across 11 files · Evidence: agent report + leave-list showing labels/captions/badges/row-actions/skeletons correctly retained
- [x] ADS-014 **all 8 over-500-line files cleared** · Evidence: 6 hierarchy pages 784/688/655/530/482/460 → 468/443/431/370/407/384; `delegations-page` 687→490; `roles-page` 556→318. Verified: **0 files over 500** across `features/crm`, `features/settings`, `components/rbac`, `components/shared`
- [x] ADS-012 6 hierarchy schemas extracted · Evidence: 0 inline `z.object` in any hierarchy page
- [x] ADS-013 18 `aria-label`s added to icon-only controls · Evidence: `title` alone fails WCAG 2.1
- [x] ADS-007 12 hardcoded `min-h-[200px]` → flex-fill chain · Evidence: 0 remain in the hierarchy folder
- [x] ADS-002 **narrowed — mostly not a violation** · Evidence: `PencilIcon`/`ArchiveIcon`/`RotateCcwIcon` return **0** matches in the 248-export `@animateicons/react/dist/lucide.d.ts`; §8 says absent icons fall back to static lucide. Genuine sites remaining: `roles-page.tsx:154,162`, `users-page.tsx:609,641`
- [x] ADUX-005 permission diff-before-apply · Evidence: Save → "Review & save" opens a `ConfirmDialog` diffing `effective` vs `baseline` (granting / revoking / rescoping with `from → to`); file 411 lines. **Audit correction:** the matrix already staged via `draft` + Reset + `role.version` 409 handling — only the preview was missing
- [x] ADUX-006 typed-name confirmation on role delete · Evidence: follows the `org-danger-zone-section.tsx` controlled-state pattern; resets on open/close/success
- [x] Administration → HRMS treatment · Evidence: kit promoted to `components/shared/rich-surface.tsx`, `hr-ui.tsx` re-exports under `Hr*` so all **14** HR importers untouched; `OrgSettingsCard` rebuilt on `RichPanel` so all 9 org sections convert from one edit
- [x] ADS-005 tab-panel fill-chain fixed — **the audit had the remedy backwards** · Evidence: the base `TabsContent` (`components/ui/tabs.tsx:76`) **already** applies `data-[state=inactive]:hidden data-[state=active]:flex data-[state=active]:flex-1 data-[state=active]:flex-col`, and its own comment (`:74-75`) warns that *unconditional* `flex` overrides the HTML `hidden` attribute and stacks inactive panels under the tab list. `roles-page.tsx:221,225` and `group-detail-sheet.tsx:213,264` each passed `className="flex min-h-0 flex-1 flex-col …"`, re-adding exactly that hazard — tailwind-merge keeps `flex` and `data-[state=active]:flex` as distinct classes, so both survive. Fix was to **delete** the duplicated fill-chain and keep only the extras (`gap-3 overflow-hidden`, padding), not to add `TABS_CONTENT_PAGE_BODY_CLASS`. `delegations-page.tsx` was already correct (it uses the state-scoped constant) and was left alone
- [~] ADS-019 **refuted — `tone="accent"` is valid.** `StatTone` declares `"accent"` explicitly (`stat-card.tsx:36`) and `:68` maps `purple → accent`, making it the *sanctioned* replacement for legacy purple per §14 "blue replaces purple". Proof it was never a type error: frontend `tsc` is 0 and `tone` is a strict union, so an invalid literal could not compile. Used deliberately in 11 places across HR, KB, billing, CRM and timesheets
- [~] ADS-018 **justified exception, same family as ADS-017.** `org-branding-section.tsx:360`'s `bg-white/30` is a logo *placeholder* inside an **email-template preview**, sitting on `style={{ backgroundColor: primaryColor }}` (`:356`) — the tenant's own brand colour — mirroring how the real logo renders (`:358` forces it white via `brightness-0 invert`). A semantic token would be invisible or clash against an arbitrary user-chosen colour. §14 bans hardcoded *app chrome*; this is a simulation of an external artifact over data-driven colour
- [~] ADS-001 **justified exception for icon-only controls.** `delegation-row.tsx:112-121` swaps an `AnimatedIconButton` for a disabled spinner Button while revoking. `LoadingButton` **appends** its spinner (`loading-button.tsx:30-31` renders `Loader2` *and* `children` unless `loadingText` is set), so in a `w-7` icon button it would paint two glyphs side by side and lose the §8 animated-icon treatment. Converting would degrade the UI to satisfy the letter of §14; the swap is the icon-only equivalent of the rule's intent
- [x] ADS-016 mobile filter Popover → `ResponsivePopover` · Evidence: `audit-log-page.tsx:220` wrapped a `md:hidden` filter trigger in a **raw** `Popover`, which §14 forbids ("Below `md`, any filter/display/menu panel that would be a Popover or Sheet becomes a Drawer — via `ResponsivePopover`"). Verified it is a genuine drop-in: `ResponsivePopover` switches to `Drawer`/`DrawerTrigger`/`DrawerContent` on mobile via `useIsMobile`, and `ResponsivePopoverContent` still forwards `align`/`sideOffset`/`className` to `PopoverContent` on desktop, so the existing widths are unchanged. Added `title="Filters"` for the drawer's `sr-only` `DrawerTitle`. The old `Popover` import is gone. FE tsc 0
- [x] ADS-004 header actions 4 → 3 · Evidence: `roles-page.tsx` rendered Audit · Simulate · Template · **New role**, exceeding the 3-action cap and diluting §14's "one primary button per view". The two *navigation* links collapsed into an overflow `DropdownMenu` behind an `EllipsisIcon` `AnimatedIconButton` (§8: `MoreHorizontal` → `EllipsisIcon`, and `AnimatedIconButton` works under `DropdownMenuTrigger asChild`), leaving `[⋯] [Template] [New role]`. `canViewAudit` still gates the Audit entry. FE tsc 0 — which also proves the `EllipsisIcon` export exists, per §8's verify-the-export rule
- [~] ADS-006 **not reproducible.** `<TabsList … mb-4>` returns **0 matches** anywhere in `features/settings`; the two `TabsList` uses carry no margin override. Either already fixed or the audit mislocated it
- [x] ADS-010/011 error-extraction sweep · Evidence: 3 real fixes (`settings-profile.tsx` ×2 incl. a bare `catch {}` that discarded the error entirely, `invoice-detail.tsx`); 5 `ErrorState`s gained `getErrorMessage(error)` + `onRetry` + `flex-1` across audit-log, settings-security (was a bare `<p>` with no retry), ai-credits ×3, plan-tab, invoices-client. **9 literals deliberately left** — client-side file/size/format validation and structured server fields (`couponResult.message`, `isConfigured` flags), none of which are error extraction. FE tsc 0
- [~] ADS-017 inline `style` in branding preview + org chart · Reason: live brand-colour preview and computed tree indent are legitimately data-driven; AP-8 bans hardcoded chrome, not computed values

## Capability gaps (ADGAP-*)

- [x] ADGAP-007 **permission-groups vertical shipped** · Evidence I verified myself, not from the agent's report: 9 routes on `@Controller("principal-groups")` with class-level `@UseGuards(JwtAuthGuard, PermissionGuard)` + `@RequirePermission("settings:rbac:manage")`; `assertMayAssignRole` **extracted** to `assert-role-assignment.ts:13` and imported by **both** `principal-groups.service.ts:23` and `role-member.service.ts:30` (shared, not copied); 5 `bumpPermissionsVersion` calls inside 7 `runInTenantTransaction` blocks; **3 `NotFoundException`, 0 `ForbiddenException`** so a wrong-tenant group id 404s; the only two `@Delete`s are on **join tables** (`:groupId/members/:membershipId`, `:groupId/roles/:roleId`) — **no group-delete endpoint**, correctly omitted per D-012 since `principal_groups` has no `deleted_at`. All files under 500 (service is **337**, not the 233 the agent reported). `madge` still 0 cycles. FE tsc 0
- [ ] ADGAP-021 seat-management overview
- [x] **ADGAP-015 was not a missing feature — it was a non-functional one.** Admin force-logout already existed (`DELETE /users/:userId/sessions` + `/sessions/:sessionId`, `settings:organization:manage`, org-scoped, audited). **But it never logged anyone out.** `JwtAuthGuard` decides revocation *solely* from Redis `revoked:session:<id>` (`jwt-auth.guard.ts:122-131`) and **never reads `userSessions.isRevoked`**, while `user-profile.service.ts` wrote only the column. So a force-logged-out member — offboarded, or with a compromised session — kept full access until their JWT expired naturally. Self-service "sign out other devices" worked, because `SessionsService` does tombstone, which is exactly why the gap went unnoticed. Fix: exposed `SessionsService.publishRevocations()` (wrapping the existing private `tombstone`) and called it from both admin paths; `revokeAllSessions` now selects the active ids first so it can tombstone each. **Reused rather than rebuilt:** I had started a parallel `DELETE /sessions/members/:userId` + `adminRevokeAllForUser` and **reverted it entirely** on finding the existing endpoints — §0.2, and the same duplicate-helper mistake I made with the SSRF guard. Verified `UsersModule` already imports `SessionsModule` (which exports the service), so nothing was left inert. 3/3 regression specs pass; `madge` shows no cycle from the new `user-profile → sessions` edge
- [x] ADGAP-015b fixed the sessions e2e spec, which tested nothing · Evidence: it asserted 401 against `/hr/sessions*`, paths `SessionsController` never served (`@Controller("sessions")`) — those would 404, not 401. Repointed at the real paths. It went unnoticed because the default jest config lists **`e2e-spec` in `testPathIgnorePatterns`** (`package.json:130-131`), so `*.e2e-spec.ts` runs only under `pnpm test:e2e` (`jest-e2e.json`) — worth knowing before trusting any e2e route table as executed coverage
- [~] ADGAP-011/012 SSO/SAML then SCIM — **explicitly descoped by the owner (D-019)** as enterprise backlog. Each is a program, not a fix, and SSO touches the auth path so it needs its own verification pass. Sizing note for whoever picks it up: SAML needs IdP metadata storage, an ACS endpoint, JIT provisioning and per-org config UI; SCIM needs `/Users` + `/Groups` with PATCH semantics and a bearer-token scheme separate from the personal-token policy
- [ ] ADGAP-035 per-user GDPR data export (org purge already exists)
- [ ] ADGAP-034/039/040 audit retention policy · org notification preferences · branding preview + domain verification
- [x] ADUX-004 **verified closed by ADM-007** · Evidence: re-read `audit-log-page.tsx` — `userSearch` is `useState` → `useDebouncedValue(…, 400)` → passed as a query param at both the list (`:88`) and export (`:141`) call sites. **Zero `.filter(` calls** on the returned rows, so no client-side narrowing survives; the CSV export uses the same server-side predicate, meaning the file matches what's on screen
- [ ] ADUX-007 "last changed by / when" provenance on settings sections
- [ ] ADUX-010 new-org setup checklist

## Soft delete (Administration slice)

- [!] Partial indexes on `hr_people`, `hr_employments`, `kb_spaces` — BLOCKED on D-009, and those tables belong to other programs (D-015)
- [~] Build/HR delete violations · Reason: D-015 — actively edited by other sessions; recorded per-program instead
