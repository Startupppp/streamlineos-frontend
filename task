you should act as 50 years experienced full stack developer and code reviewer the code and you should fix it as 50 years experienced developer in the best way
task summary :- 
Review the code and you should fix the bugs, schema, api, refactoring UI and UX etc 

task flow :- 
you should understand the Projects Management in that you should 
check the each and every api, schema, components, pages, everything without missing the single point and you should make a proper map of what are need to change based on the rules mentioned below format 

1. now you should understand the schemas and you should make the schema should be efficient and easy to manage the large data like Millions of users, so keeping that in the mind you should  remove the unnecessary schema and you should create the best schema possible 
example :- in my another repo there is bad of storing the inivites in the org schema it is storing it in the array of invites format that is so bad every time update a invite it need to find org and then it should find the array of elements and then it should update it and it should again put into it so it is so bad instead maintain the invites in another seperate schema update is easy just fetch and update it 

so keep this in mind and you should make the schema the best and it is easy to scale, maintainable, readable, resuable and future proof, efficient, optimized, clean and all the other best coding pratices 

2. api level, you should use the efficient, optimzied, what are all needed data and with proper files and limits and skips you should fetch them only and make sure every db call is super efficient and db should be less CPU usage and less cost and also best way of using the caching, invalidating the caching, faster the api responses and throwing the proper responses with proper status code and making the promise.all usage and other points you should follow and make sure it should be the best and also list down all the best coding pratices and you should follow it for every api call and proper zod validation and also dto which is best it should follow that one and it should also follow the proper role validations as well  

3. use the proper names for the functions and variables etc and also for example last time i give a task like add only theme to authencated page but you did like adding a checks for the app routes instead you can only wrap the theme to the authecation page layout level so it will fix right so similarly you should always follow the best way to do everything and every standard principle and rules of nestjs and nextjs you should follow every line and you should act as expert in doing it 

4. in the frontend you should make the seperate files for the hooks and best way of grouping them instead of writing it in page level only, and maintain the single api client for making the api calls and you should use the tanstack queries to make the api calls and also make the every api call refetch on window focus ( you can call the api do it global level by adding the default to the tanstack providers) and also for the fetch api and all you should use the debouncing api and this should not for the state which handled the UI maintain the seperate states if needed but make sure UI and UX should be top notch instead of typing three it is showing one not like this and everything should be well typed output without forcing the typing and also if possible of cache update you should update after successful of api call instead of triggering the api's by invalidating them 

5. while you are doing code review all the files and you should find a pattern of repeating if anything is repeated then you should make the reusable component example there is a multiple page using the memeber select instead of writing everytime the same code you should use the single component and use it everywhere and note if that component is not existed then only you should do it else use the exising components always 

6. make sure every screen is responsive follow the same this page http://localhost:1000/users when if the page having the filtes too many, if that is already handling the mobile responsive then you should ignore it and don't break the existing UI and also for the popovers, and also sheet, and dialogs in the mobile should be a drawer component for this you can make a resuable component and use it that's good and make the rule as well and every proper over should maintain the minimum width and also when the fields are bigger then the popover width then you should increase the width of the popovers as well same width of the trigger

7. every page weather it is empty state, for error state or whatever it is it should use the full width and full height properly

8. remove the unnecessary page like some page having only the redirect routes only you can remove those page and you should update the routes properly and make sure everything should be well typed and zod valdation and showing the proper error messages 

9. don't force the types and make sure everything is well typed and for a page maximum of 600 lines of code only allowed you should follow the proper file structure, folder structures, everything and you should fix everything and repeative components across the multiple features you should move them to the common folder but make sure everyting should be properly followed and also skeletons should be exact resable of the pages with data 

10. remove the unnecessary files, types, zod validations, components, and reduce the bundle sizes and make sure everything should be loaded properly and skeletons everything 

11. test all the api's before fixing saying it is completed and feel free to ask the questions 

12. Add AI features inline and also don't break anything because most of the code it is working also 

13. i want to migrate the rag model or else any vectordb instead of SQL and make sure it should be scalable and best coding pratices and let me know what are all needed so that like RAG and vector db and quadrant and keep this in mind this app will need to serve for Millions of users so keep this in mind and you should write the code and follow the @claude.md and @ui-ux-system.md file as well and i find a final robust, bug free, reliable, super efficint, fastest and best animatied super cool UI and UX and performance as well it should scale as well 

14. mainly same card kanban card component across the module and same list and same table etc same kind of components across the modules http://localhost:1000/projects/2 and this should be your primary task 